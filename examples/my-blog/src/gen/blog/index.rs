//! Generated by runts

use runts_lib::runtime::prelude::*;
use serde::{Serialize, Deserialize};
use axum::{response::IntoResponse, body::Body};

#[derive(Clone, PartialEq, Serialize, Deserialize)]
pub struct BlogData {
    pub titles: Vec<String>,
    pub slugs: Vec<String>,
}

pub async fn handle_get(_req: Request, _ctx: HandlerContext) -> impl IntoResponse {
    { let data: BlogData = BlogData {titles: vec!["Introducing runts", "Islands Architecture"], slugs: vec!["introducing-runts", "islands-architecture"]};; return {
    let __body = serde_json::to_string(&data).unwrap();
    let mut __resp = Response::builder();
    __resp = __resp.header("Content-Type", "application/json");
    __resp.body(Body::from(__body)).unwrap()
}; } // Handler body
}



#[component]
pub fn blog_index(_props: PageProps<BlogData>) -> VNode {
    let data = _props.data;
    return html!(<div class_name = "blog-index"><div class_name = "posts-list">{data.titles.map(|title, i| { html!(<article key = {i} class_name = "post-card" style = {{margin_bottom: "2rem", padding: "1.5rem", border: "1px solid #e0e0e0", border_radius: "12px"}}><h2 style = {{margin: 0}}><a href = {/blog/{data.slugs[i]}} style = {{text_decoration: "none", color: "#1a1a2e"}}>{title}</a></h2></article>) })}</div></div>);
}


