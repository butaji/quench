# Debug println/eprintln cleanup — completed

## Changes made

All debug `println!` and `eprintln!` statements from the quench-runtime crate init/registration
process were removed from non-test source files. Two files needed changes:

### `crates/quench-runtime/src/context/mod.rs`
- Removed `println!("init_builtins: starting")` + flush
- Removed `println!("init_builtins: register_builtin_functions done")` + flush

### `crates/quench-runtime/src/builtins/mod.rs`
- Removed all 17 `println!`/`flush` pairs from `register_builtins()` (already cleaned by parent agent)

### `crates/quench-runtime/src/builtins/date.rs`
- Already clean (parent agent removed all debug statements)

### `crates/quench-runtime/src/builtins/function.rs`
- Already clean (parent agent removed the debug statement)

## Preserved (intentionally NOT removed)

- `builtins/console.rs` lines 15, 21, 27 — these are the **actual** `console.log`,
  `console.error`, `console.warn` implementations; removing them would break user output
- `swc_parse.rs` lines 222, 229, 251, 309 — these are all inside `#[test]` blocks;
  they only fire during `cargo test` and are useful for debugging test failures

## Verification

```
cargo test -p quench-runtime  →  189 passed, 0 failed
cargo test                    →  36 passed (main) + 5 passed (parity), 0 failed
cargo check                   →  Finished dev profile, no warnings
cargo run -- examples/simple.js →  "Hello, Quench!" / "All FFI tests passed!" (no register_* noise)
```

## Remaining print(e)lns in quench-runtime

| File | Lines | Type | Reason to keep |
|------|-------|------|---------------|
| `builtins/console.rs` | 15,21,27 | `println!`/`eprintln!` | Actual JS console API |
| `swc_parse.rs` | 222,229,251,309 | `eprintln!` | Inside `#[test]` blocks only |
