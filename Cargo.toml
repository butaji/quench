[workspace]
resolver = "2"
members = [
    "quench-bench",
    "crates/quench-js-check",
    "crates/quench-node",
    "crates/quench-node-test",
    "crates/quench-runtime",
    "crates/quench-test262",
]

[profile.dev]
opt-level = 3

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = "symbols"
panic = "abort"

[profile.release-thin]
inherits = "release"
lto = "thin"

[profile.production]
inherits = "release"
# Alias the release contract for explicit score invocations. The optimizer
# policy itself lives only in `profile.release`.

[profile.bench-throughput]
inherits = "production"
debug = 0
strip = "symbols"
