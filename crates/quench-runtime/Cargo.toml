[package]
name = "quench-runtime"
version = "0.1.0"
edition = "2021"
description = "Residual VM kernel for the Quench program reducer"
license = "MIT"

[dependencies]
num-bigint = "0.4"
oxc = { version = "0.47", features = ["semantic"] }
