# 412 — Three-way inherited-property PIC physical-cover experiment

Status: complete

Test the only unmeasured finite inline cover between Task 411's insufficient two ways and
Task 410's overlarge four ways. The semantic mechanism remains one bounded normalized way
sequence; only the shared `PROPERTY_IC_WAY_COUNT` changes the source and native projection.
No key, source, benchmark, or heat-based admission is permitted.

Acceptance: focused and full release tests pass; disassembly proves exactly three native
guard chains; an alternating screen retains a material Richards improvement without the
four-way suite regressions; then and only then run the randomized nine-pair exact gate. A
strictly positive aggregate confidence interval and no component-floor violation are
required. Otherwise restore the accepted Task 385 binary behavior and leave future
overflow ways to a shared immutable kernel design.

Baseline: `/tmp/deegen-task385-mixed-register-candidate`.

## Result: rejected and fully removed

The three-way cover passed the focused tests and the alternating three-pair screen in
`reports/task412-three-way-pic-screen-3/comparison.txt` showed Richards +8.11% and
aggregate +0.71%. The required randomized exact gate did not confirm a global win. In
`reports/task412-three-way-pic-exact-9/comparison.md`, Richards gained 5.68% and DeltaBlue
3.19%, but RayTrace lost 2.53%; aggregate was +0.10% with a 95% paired interval of
[-0.57%, +0.72%]. It therefore failed the strictly-positive-confidence requirement.

Together Tasks 410–412 establish the boundary: two inline ways do not cover Richards'
receiver diversity; three and four ways recover Richards but duplicated guard code offsets
the gain across the suite. All PIC code and layout changes were removed, restoring the
accepted Task 385 monomorphic source and native representation exactly. Task 413 moves the
secondary-way cover to one shared immutable kernel.
