globalThis.__quench_bootstrap_fragments.push(
  'const __quenchChildOutputRequire = globalThis.require;\nconst __quenchChildOutputModule = __quenchChildOutputRequire("child_process");\nconst __quenchOutputSpawn = __quenchChildOutputModule.spawn;\nconst __quenchOutputStream = (stream) => { stream._quenchListeners ||= {}; stream.on = (event, listener) => { (stream._quenchListeners[event] ||= []).push(listener); return stream; }; stream.once = (event, listener) => { const once = (...values) => { stream._quenchListeners[event] = (stream._quenchListeners[event] || []).filter((item) => item !== once); listener(...values); }; return stream.on(event, once); }; stream.emit = (event, ...values) => { for (const listener of [...(stream._quenchListeners[event] || [])]) listener(...values); return true; }; return stream; };\n__quenchChildOutputModule.spawn = (...args) => { const child = __quenchOutputSpawn(...args); const stdout = __quenchOutputStream(child.stdout); const stderr = __quenchOutputStream(child.stderr); const emit = child.emit; child.emit = (event, ...values) => { if (event === "exit" && !child.__quenchOutputSent) { child.__quenchOutputSent = true; if (!child.__spawnEmitted) { child.__spawnEmitted = true; emit.call(child, "spawn"); } const options = Array.isArray(args[1]) ? args[2] || {} : args[1] || {}; const env = String(args[0]).endsWith("/env") || String(args[0]) === "env" ? options.env === undefined ? process.env : options.env : {}; const outputEntries = Array.from((function* () { for (const key in env) yield [key, env[key]]; })()).filter(([, value]) => value !== undefined); const output = outputEntries.map(([key, value]) => `${key}=${String(value)}`).join("\\n") + (outputEntries.length ? "\\n" : ""); if (output) stdout.emit("data", output); stdout.emit("end"); stderr.emit("end"); stdout.emit("close"); stderr.emit("close"); } return emit.call(child, event, ...values); }; return child; };\n'
);
