[package]
name = "quench-node"
version = "0.1.0"
edition = "2021"
description = "Node-compatible JavaScript runtime built on rquickjs"
license = "MIT"

[dependencies]
rquickjs = { version = "0.9", features = ["array-buffer", "classes", "futures", "loader", "macro", "properties", "parallel"] }
walkdir = "2"
sha2 = "0.10"
