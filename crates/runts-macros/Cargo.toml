[package]
name = "runts-macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for runts"

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "parsing"] }

[lib]
proc-macro = true
doctest = false
