# 20: Close Test262 at 100 percent

## Outcome

Pass every official test discovered from the pinned Test262 checkout on the next engine.

## Current gap

Feature tasks provide focused coverage, but no uncapped run proves complete language, builtin, module, async, agent, Intl, and Temporal behavior together.
The original `run-stages` binary is a legacy-runtime reference runner; its
counts are not next-runtime evidence. The v2-derived gate is `run-stages-next`,
which links `quench-runtime-next` directly. On 2026-09-22, the requested
sequential prefixes passed: stage 0, 116/116; stages 0–1, 379/379; stages 0–2,
481/481; stages 0–3, 626/626; stages 0–4, 678/678; stages 0–5, 726/726;
stages 0–6, 745/745; and stages 0–7, 807/807. Every invocation set a positive
`TEST262_TEST_TIMEOUT_MS`; the costly Unicode comment case used 300000 ms after
a 30000 ms timeout. The stage runner emitted
deterministic batches of at most 100 cases. This phase did not run the whole Test262 inventory. The
harness remains untouched and no project tests were added. This slice adds native
constructor identity/prototypes (including `%TypedArray%`), constructor names
and String `slice`, corrects the `in` operand order, preserves tagged
templates and ordinary calls with contiguous argument windows, reports heap
strings as `typeof === "string"`, and makes boxed String, Number, Boolean,
Symbol, and BigInt values use canonical hidden payloads while keeping internal
slots out of reflection. It also installs Map/Set well-known iterator methods
after Symbol initialization, adds a non-throwing `typeof` name read, adopts
Function.prototype binding from the old core through the next VM's canonical
native-function environment, normalizes JS hexadecimal/UTF-16 regex escapes
for the Rust regex backend, tracks generator/arrow constructor invariants, and
preserves symbol properties through GC while modeling symbol accessor
descriptors/getters. The optimized
residual path is not admitted to the conformance gate until differential checks
prove equivalence. The next-core collector currently roots all active-frame
registers; the register-mask optimization remains gated until it has a proof
that call-site arguments and closure values cannot be dropped.

Stage 1 (`language/arguments-object`) passes 263/263 on the same next-core
runner. The completed batch reuses the old core's mapped-arguments model at the
next VM boundary, including alias synchronization, iterator/accessor descriptor
rules, configurable arguments `length`, strict deletion errors, and mapping
removal when an index becomes non-writable. Strictness is carried in residual
function metadata and a dedicated delete instruction, so behavior does not
depend on fixture identity. No harness files or new tests were added.

Stage 2 (`language/asi`) passes 102/102. Parser diagnostics are surfaced as
the language-level `SyntaxError` outcome expected by Test262; the harness source
and metadata remain unchanged. After the task-52 decoder-layout change, the
ordered prefix 0–2 was re-run in release mode with four workers and passed
481/481; each stage remained split into batches of at most 100. Stage 3
(`language/block-scope`) now passes
145/145. The next-core compiler uses derived lexical-scope bindings for block
and loop declarations, catch-parameter shadowing, closure capture of block
bindings, block function hoisting, and per-iteration `for-in`/`for-of`
environments. AST early-error validation covers block-function statement
positions and lexical/var redeclaration families. The for-in gate also
confirmed that intrinsic constructors are non-enumerable; no harness files or
new tests were added.

Stage 4 (`language/comments`) now passes 52/52. Hashbang normalization is
performed before OXC parsing and hashbang source is kept first during harness
composition; global `isNaN` and radix-prefixed string numeric coercion reuse the
next VM's native conversion path. Dynamic-eval comment mutation updates the
captured activation environment, Function-family constructors validate every
source argument, and the staged module boundary executes module sources that
do not yet require import/export linkage. The Test262 harness remains unchanged
and no new tests were added.

Stage 5 (`language/computed-property-names`) now passes 48/48. Object literal
methods and accessors use descriptor-based definition, preserve function
metadata and numeric property-key coercion, and capture a distinct home object
for dynamic `super` lookup after `Object.setPrototypeOf`. The implementation
reuses the next VM's existing closure, descriptor, and prototype operations;
class accessor descriptors carry the required non-enumerable/configurable
attributes so duplicate getter/setter definitions can replace prior ones. The
Test262 harness remains unchanged and no new tests were added.

Stage 6 (`language/destructuring`) now passes 19/19. The next core preserves
with-environment binding references across computed-key coercion and called
getters, rejects nullish destructuring sources through the runtime's TypeError
path, and constructs dynamic typed-array subclasses through the shared
constructor path. No harness files or new tests were added.

Stage 7 (`language/directive-prologue`) passes 62/62. Strict `this` and
unresolvable-assignment behavior now flow from residual function metadata,
including inherited strictness for nested declarations, expressions, arrows,
and accessors. OXC AST early validation now rejects strict-reserved bindings
and strict `eval` assignments. The earlier note of ten remaining failures was
stale for the current worktree; the ordered cumulative run now proves 62/62.

A provisional whole-suite sample (2026-09-22, `run-all-next` built before
`abd2ee7df7`, 10 parallel batches, stopped early) passed 12,544 of the 34,095
files it reached (36.8%). It predates task 19's frozen baseline, is not
ratchet evidence, and is not part of the current sequential-stage gate.

### 2026-09-23 stage-10 dynamic-import slice

The sequential gate remains at stage 10; stages 0–9 were green in the latest
cumulative run. The latest release-profile stage 10 run used 112 batches (111 ×
100 and one case), with `TEST262_TEST_TIMEOUT_MS=300000`: 10,756/11,101 passed
(345 failures across 260 families). The dynamic-import directory passed 953/1,004
across 11 batches. Same-VM module evaluation now handles locally declared
exports and expression/function/class default exports; `eval-rqstd-once` passes.
Dependency linking, live bindings, and callable namespace behavior remain
incomplete. `custom-primitive.js` and `imported-self-update.js` still fail. Do
not advance the gate past stage 10 based on this slice. A debug-profile stage-4
run hit its 300,000 ms cap on two exhaustive Unicode comment cases; the
release-profile stage-4 run passed 52/52 with that same cap. No harness files or
new tests were added.

### 2026-09-23 ordered-prefix regression after operand-kind unification

The release-profile cumulative `run-stages-next 0 10` rerun used four workers,
positive `TEST262_TEST_TIMEOUT_MS=900000`, and batches capped at 100. Stages 0–9
passed 1,157/1,157, including the exhaustive stage-4 comment fixtures. Stage 10
remains the first failing prefix at 10,891/11,101 (210 cases across 190 failure
families); the aggregate matches its preceding 900,000 ms stage-10 run. The
captured full runner output is `target/test262-next-prefix-0-10-operand-kinds.log`.
This run follows replacing numeric operand-tag branches with the shared
`OperandKind` representation in validation, liveness, VM operand resolution,
and numeric dispatch. It introduced no new stage-0–9 failures. Harness and
suite files remain unchanged, and no unit tests were run. Re-running stage 10
with the new incremental `iteration` profile produced the same 10,891/11,101
result in 112 batches; its complete output is
`target/test262-next-stage10-iteration-profile.log`.

## Dependencies

See this task's `depends_on` in [index.json](index.json).

### 2026-09-25 Date stage

Stage 44 (`built-ins/Date`) passes 594/594. The latest isolated run and the
ordered regression `run-stages-next 42 44` both used
`TEST262_TEST_TIMEOUT_MS=900000`; each Date run was split into six batches of
at most 100 cases. The ordered regression passes Boolean 51/51, DataView
561/561, and Date 594/594 (1,206/1,206 total). Date now has realm-owned
constructors/prototypes; spec-ordered setter time reads; Date-aware constructor
conversion/copying; generic `toJSON`; and ordinary `Symbol.toPrimitive`
conversion. Date instances resolve methods through their actual prototype,
allowing overrides, and the legacy core's civil-date arithmetic now covers the
full TimeClip range rather than Chrono's narrower year range. Local timezone
offsets are rounded to the minute granularity exposed by Date. The harness
remains untouched; no project tests were added or run.

Stage 45 (`built-ins/DisposableStack`) passes 93/93. The ordered regression
`run-stages-next 44 45` passes 687/687 (Date 594/594 and DisposableStack
93/93), with a 900,000 ms per-case timeout and batches capped at 100. The next
VM now installs sync `move` and `disposed`, shares function-length metadata
with the legacy core, rejects sync methods on async stacks, and resolves the
DisposableStack intrinsic from the `newTarget` realm. No harness changes, new
tests, or unit-test runs were made. The next unverified sequential stage is 46.

Stage 46 (`built-ins/Error`) passes 93/93. The ordered regression
`run-stages-next 44 46` passes all 780 cases (Date 594, DisposableStack 93,
Error 93) with `TEST262_TEST_TIMEOUT_MS=900000` and batches capped at 100. Error
now exposes `Error.isError` with cross-realm brand checks, carries Error
constructor `cause`, has spec-visible stack accessors on each realm's
`Error.prototype`, and serializes only enumerable own properties. The stack
setter follows the legacy core's own-descriptor / Set / CreateDataProperty
dispatch and preserves Proxy effects. Harness unchanged; no project tests or
unit tests were added or run.

Stage 47 (`built-ins/FinalizationRegistry`) passes 47/47. The ordered
regression `run-stages-next 45 47` passes DisposableStack 93/93, Error 93/93,
and FinalizationRegistry 47/47 (233/233 total), with a 900,000 ms per-case
timeout and batches capped at 100. FinalizationRegistry now uses the shared
weak-handle model for non-registered Symbols, validates held values and
callable cleanup callbacks, returns `undefined` from `register`, and exposes
constructor/method names, lengths, and standard descriptors. Construction
uses the `newTarget` prototype and realm fallback; createRealm now owns its
FinalizationRegistry intrinsic. The harness was untouched; no new tests or
unit tests were added or run. Stage 48 is next.

Stage 48 (`built-ins/Function`) passes 509/509 in six batches (100, 100, 100,
100, 100, and 9). The ordered regression `run-stages-next 45 48` passes
DisposableStack 93/93, Error 93/93, FinalizationRegistry 47/47, and Function
509/509 (742/742 total); all batches were capped at 100. Runs used
`TEST262_TEST_TIMEOUT_MS=900000` and ten workers. Function source text is now
preserved as compiled metadata for dynamic functions, closures, object methods,
class constructors, and class methods; class method slices exclude the `static`
modifier while retaining syntax and intervening comments. Function construction
and call failures now keep their active realm, boxed primitive construction
preserves the intrinsic prototype, cross-realm Proxy constructors are installed,
and RegExp owns its `@@match` and `@@species` properties. The existing Test262
suite is the only conformance test source. The harness was untouched; no tests
were added and no unit/project tests were run.

## Scope and implementation sequence

Use official metadata for parse/runtime-negative and harness behavior, but no Quench-owned feature skip list. Implement all required `$262` host operations, agents, detach, realms, modules, async completion, and timeouts.

Continue the requested sequential stage gate using only the existing pinned
Test262 checkout. Fresh cumulative rechecks currently pass stages 0–3; stage 4
is the first incomplete stage because its two exhaustive Unicode-comment cases
exceed the configured timeout. Keep that order for subsequent ranges and do
not advance past a failing prefix. Each batch is
capped at 100 cases by `run-stages-next`. Triage failures by shared semantic
family within the current prefix; do not modify the Test262 harness or add
project tests. Do not run the whole Test262 inventory during this phase.

1. Run `run-stages-next` sequentially for cumulative ranges `0 0` onward;
   preserve each report and its timeout/batch provenance under `target/`.
2. Stop at the first prefix below 100%, group its failures by shared semantic
   family, and fix the common authority before rerunning that prefix.
3. Port matching behavior from the legacy runtime when available (task 45 or
   the internal-method route in the README); do not replace Test262 harness
   behavior.
4. Do not start a later prefix until the current one passes every case.

### 2026-09-24 sequential stages 16–20

Stages 16–19 passed on `run-stages-next` against the existing Test262 suite:
stage 16 `language/import` 127/127 (two batches, 100 + 27), stage 17
`language/keywords` 25/25, stage 18 `language/line-terminators` 41/41, and
stage 19 `language/literals` 534/534 (six batches), and stage 20
`language/module-code` 599/599 (six batches). The cumulative sequential
prefix 0–20 is 14,180/14,180. Every runner set
`TEST262_TEST_TIMEOUT_MS=900000`; no batch exceeded 100 cases. Deferred module
namespace internal methods now trigger evaluation consistently across property
access, descriptor operations, and key enumeration; exports of deferred import
bindings preserve the namespace as a local binding rather than routing through
eager re-export resolution. This closed stage 16's deferred namespace and
dynamic-import failure families. Stage 19's exhaustive regex/eval cases use a
single worker to avoid CPU and memory contention; a narrow literal recognizer
skips general eval parsing only for complete unflagged regexp literals, while
the RegExp constructor path still validates and creates each distinct value.
The full stage-19 output is
`target/test262-next-stage19-2026-09-24-regexp-eval-fast-path.log`. Harness
untouched; no project tests added or run. The stage-20 output is
`target/test262-next-stage20-2026-09-24-sequential.log`. Stage 21 is next.

### 2026-09-24 stage-20 module-code checkpoint

The ordered runner reached stage 20, `language/module-code`, in six batches
capped at 100 cases, with `TEST262_TEST_TIMEOUT_MS=900000`. The existing
suite passed 582/599 (17 failures across 8 families); the complete report is
`target/test262-next-stage20-2026-09-24-source-phase.log`. This raises the
previous recorded result from 578/599. The `source-phase-import` cohort now
passes 4/4: resolved module sources share canonical identities, and each
module's complete request list is host-resolved before any dependency is
evaluated, so dependency link errors do not mask host-resolution failures.
No Test262 harness files or project tests were changed or added. Stage 20
remains the current failing sequential gate; do not advance the prefix.

The follow-up TLA import-binding slice was verified against the same stage
with `TEST262_TEST_TIMEOUT_MS=900000` and six batches capped at 100. The latest
result is 587/599 (12 failures across 6 families), recorded in
`target/test262-next-stage20-2026-09-24-tla-live-bindings.log`. The
`module-import-resolution`, `module-import-unwrapped`, and
`module-import-rejection` cases now pass; module namespace slots are refreshed
from retained module-environment bindings when evaluation fulfills. The stage
still fails and remains the ordered gate.

The next existing-suite sweep, after adding the generic
`Promise.withResolvers` capability path, remains 587/599. The former
four-case `value is not callable` family is gone; its module-order cases now
reach async completion and fail as `$DONE was not called`, exposing the still
missing shared module-graph evaluation-order state machine. Current family
counts are in `target/test262-next-stage20-2026-09-24-promise-with-resolvers.log`.

The 2026-09-24 follow-up retained the sequential stage-20 gate at 587/599
(12 failures across 6 families), with the same failure inventory recorded in
`target/test262-next-stage20-2026-09-24-main-module-lifecycle.log`. A bounded
dynamic-import deferral experiment did not change conformance and was removed;
do not add another job queue without integrating it into the module evaluation
state machine. Main-module setup now creates its evaluating module record even
when its static request list is empty, so dynamic self-import uses the same
module identity/lifecycle path. This did not yet close the existing
`module-self-import-async-resolution-ticks.js` case. The module-order,
`$DONE`, rejection, TLA completion, and direct-eval error families remain open;
stage 20 is still the gate. Runs used the existing suite, a 900,000 ms
per-case timeout, and batches capped at 100. Harness untouched; no tests were
added and no unit/project tests were run.

The next module-evaluation slice routes dynamic JavaScript imports through the
static evaluator, preserves async-module start order explicitly, and keeps
import promises pending until evaluation settles. The entry module's own
top-level-await completion promise is tracked and its rejection reaches the
runtime boundary. Existing `fulfillment-order.js`, `rejection-order.js`,
`dynamic-import-of-waiting-module.js`, `dynamic-import-rejection.js`, and
`await-dynamic-import-rejection.js` pass. The full stage-20 sweep improves to
592/599 (7 failures across 6 families), recorded in
`target/test262-next-stage20-2026-09-24-waiting-parent-rejection.log`. Remaining
families include module evaluation ordering for cycles, main-module
self-import TDZ/live bindings, construction after await, and abrupt-completion
forwarding for default exports/direct module evaluation. Stage 20 remains the
ordered gate. Timeout was 900,000 ms per case; batches remained at most 100.
Harness untouched; no tests were added and no unit/project tests were run.

| Task | Stages |
| ---- | ------ |
| 11 | 1–28 language, except those listed under 17 and 18 |
| 12 | 60 Object, 62 Proxy, 63 Reflect, 73 Symbol, 48 Function, 75 ThrowTypeError |
| 13 | 31–33, 40, 43, 69, 76–78 |
| 14 | 53, 64, 65, 70, 71, 82–85, 93–94 (Annex B) |
| 15 | 41, 44, 51, 56, 57, 59, 74, 88–92, 95–113 |
| 16 | 47, 52, 54, 55, 66, 67, 79–81 |
| 17 | 34–39, 45, 49, 50, 61, 72; async/generator/`using` language subtrees |
| 18 | 8, 9, 16, 20, 68, 86; `staging` module/TLA entries |

`staging/` (1,486 tests, stage 114) and the 22 files directly under `intl402/`
are part of the discovered denominator even though no directory stage lists
the latter; the full run is the authority.

## Verification

Require every discovered runnable test to be accounted for and pass. Unsupported syntax/features, harness errors, crashes, timeouts, and missing includes are failures; resource files that official discovery does not classify as tests are outside the denominator.

## Definition of done

- The merged report has `failed == 0`, `crashed == 0`, `timed_out == 0`, and no Quench feature skips.
- Repeated runs have identical inventory and classification.
- Do not add Quench-owned tests or alter the harness; the existing pinned
  Test262 inventory is the verification suite for this phase.

## Performance evidence

Report total wall time and peak runner/child RSS for capacity planning; no speed gate applies.

## Current execution checkpoint

### 2026-09-24 current sequential gate through stage 15

The latest cumulative run `run-stages-next 0 15` passes 12,854/12,854. Stages
0–14 pass 12,586/12,586, and stage 15 (`language/identifiers`) passes 268/268
in three batches. Stage 14 passed 14/14; stage 13 passed 42/42; stage 12 passed
55/55; stage 11 passed 217/217 in three batches. Stage 10
passed 11,101/11,101 in 112 batches (111 × 100, one case). This run used the
incremental `iteration` profile,
`TEST262_TEST_TIMEOUT_MS=900000`, and the runner's 100-case batch cap.
Stage 8's direct-eval handling derives strictness and directive completion from
OXC's directive AST, strips declaration terminators from AST statement slices,
and retains sloppy direct-eval var/function bindings in the activation. The
stage-10 sweep closed 30 source-phase dynamic-import rejection cases by
rejecting source-text modules with `SyntaxError`, assigned the inferred
`"default"` class name before static field evaluation, and made array-length
integrity checks apply only to arrays so ordinary objects can report frozen
state from their descriptors. All three former failure families passed in the
cumulative rerun. Node 26 confirmed source-text `import.source()` rejects with
`SyntaxError`, the default-export class name is visible in static fields, and
`Object.isFrozen` is true after freezing a sealed ordinary object. Stage 16 is
the next sequential gate. No Test262 harness edits, project tests, or new tests
were made. Historical checkpoints below predate this gate and do not supersede
it.

### Historical later-stage checkpoint (not revalidated in the current pass)

The following later-stage results are historical snapshots. The current
sequential run was restarted at stage 0 after runtime changes and has only
revalidated stages 0–15; stage 16 is now the next gate.

As of 2026-09-24, a prior ordered cumulative gate through stage 19 was
13,581/13,581. Stage 19 (`language/literals`) passes 534/534 in six batches;
the cumulative run used `TEST262_TEST_TIMEOUT_MS=900000` and batches capped at
100. Stage 20 (`language/module-code`) is now the first failing stage: the
latest full stage-only sweep passes 578/599 in six batches, with 21 failures
across 10 families. The next linker now resolves static module plans per
`(normalized module path, exportName)`, with branch-local resolve sets, binding
identity for star ambiguity, and explicit validation of named reexports. This
closes both distinct-name cycle cases and the unresolvable-export negative
case. This path currently admits only graphs whose residuals have no local
import bindings; unsupported graphs retain the previous linker. Root-module
static exports still use residual-owned link metadata. Static request metadata
is now available even when a static link plan is unsupported, so the runtime
can discover source-phase requests without fabricating export links; actual
ModuleSource bindings are still unimplemented. The stage-20 result is unchanged
by that metadata split. Focused
existing cohorts pass namespace internals 36/36, namespace-unambiguity 3/3,
ambiguous export errors 4/4, default-class self-import 5/5, cyclic star
re-export 1/1, instantiation binding initialization 6/6, and import attributes
13/13; the instantiation cohort is now 76/76, and the ambiguous-export cohort
remains 9/10. Remaining families include top-level-await ordering/completion,
unresolved named imports (3), and source-phase imports. No stage after 20 has
been run.
The saved full-stage report is
`target/test262-next-stage20-2026-09-24-request-link-plan-split.log`. Historical
stage-10 investigations follow; dated ordered-gate entries supersede
intermediate counts.

Stage 19 work ports the old runtime's RegExp literal early-validation
algorithms into the next compiler: arithmetic modifier groups, malformed
quantifiers, quantified assertions, named-group identity/reference checks,
Unicode escape bounds/shape, Unicode class ranges, and decimal escape checks.
This converts the initial 348/534 result to 528/534. RegExp instances now use
dedicated internal source/flag slots, expose standard non-enumerable accessors,
and have the required non-enumerable, non-configurable `lastIndex`; RegExp
`\\0` string matching now consumes those slots rather than ordinary-object
properties. Source line continuations for U+2028/U+2029 also pass their existing
Test262 cohorts. The `u-invalid` cohort passes 17/17, the named-groups cohort is
55/56, and focused line-continuation and null-character cohorts pass 2/2 and
1/1. The shared RegExp parser/interpreter now handles named forward references
and Unicode decimal backreferences; source-aware dynamic Eval preserves exact
UTF-16 RegExp-literal pattern units for `RegExp.prototype.source`, including
lone surrogates. Invalid patterns from that path are classified as
`SyntaxError`. Stage 19 now passes 534/534, and the fresh cumulative 0–19 gate
passes 13,581/13,581 with a 900,000 ms per-test timeout and batches capped at
100. The next sequential gate is stage 20; no later stages have been run.

### 2026-09-24: adopt the shared RegExp matcher and close stage 19

The legacy ECMAScript RegExp parser/interpreter was moved into
`quench-regexp`, a runtime-independent workspace crate consumed by both
`quench-runtime` and `quench-runtime-next`. The next VM now compiles and
executes patterns through that matcher instead of maintaining a Rust-regex
normalization path; this closes the forward-reference and Unicode decimal
backreference cases together. Dynamic Eval uses OXC to recognize a single
RegExp literal and takes its pattern units from the original `JsString`, so
lossy host text no longer changes a lone surrogate in the observable source
slot. The invalid-lone-surrogate group-name case also follows the SyntaxError
path. `cargo build --profile iteration -p quench-test262 --bin
run-stages-next` and the legacy runtime build pass. Existing Test262 stage 19
passes 534/534; cumulative stages 0–19 pass 13,581/13,581. Every runner used
`TEST262_TEST_TIMEOUT_MS=900000`, with batches capped at 100. No harness edits,
new tests, or unit/project test runs were made. Task 45 is partially complete;
exact UTF-16 matching integration and the remaining shared-algorithm
extractions remain open.

### 2026-09-24: begin stage 20 module-code

The first full stage-only `run-stages-next 20 20` sweep passed 552/599. The
follow-up full sweep passed 576/599 in six batches with
`TEST262_TEST_TIMEOUT_MS=900000` and a 100-case maximum. The static module link
plan is stored in residual metadata and serialized with the program; root and
dependency reexports share that authority, including namespace identity through
star exports and exports of imported bindings. A follow-up per-export resolver
now handles import-free static-link graphs, using a branch-local
`(module, exportName)` resolve set and binding identity for ambiguity. The
full-stage follow-up passes 578/599: both distinct-name cycle cases and the
unresolvable-export negative case pass. Modules with local import bindings
remain on the previous linker until the next-core module environment can
provide their imports during this resolution path. Focused
existing cohorts pass namespace internals 36/36, namespace-unambiguity 3/3,
ambiguous-export errors 4/4, default-class self-import 5/5, cyclic star
re-export 1/1, instantiation binding initialization 6/6, and import attributes
13/13; instantiation is 76/76 and ambiguous exports are 9/10. The remaining 21
failures span 10 families, led by top-level-await scheduling, unresolved named
imports, and source-phase imports. No stage after 20 has been run. The Test262
harness remains untouched; no new tests or unit tests were run.

### Historical implementation notes before ordered stage 11

The following notes record intermediate stage-10 failure families and focused
fixes; they are not the current gate status.
Terminal tail calls now defer async/generator targets to their Promise or
generator boundary instead of executing them as ordinary frames. The async
private-arrow case and the paired legacy getter/setter lookup cases pass.
Named function-expression self-bindings now survive ordinary/generator
activation setup and reject local/captured/direct-eval writes; the focused
24-case cohort is 20/24, with strict direct-eval error identity still failing.
ToNumeric ordering is fixed for the existing arithmetic/bitwise
`order-of-evaluation.js` cohort (12/12); the stage-10 gate gained 13 passes.
Tagged-template cooked/raw segments now pass the existing `tv-*` cohort (13/13),
adding 14 passes to the full stage-10 sweep.
Parameter-created closures now omit body-only locals from their static capture
view (the 12-case params/body-var cohort passes); the combined existing
`scope-*` cohort is 71/76, with 5 direct-eval cases remaining.
Parameter defaults that refer
to `arguments` despite a body function binding use a separate arguments-object
slot (4/4). Direct-eval var visibility in parameter closures remains open.
Named class expressions now use an immutable lexical name binding during
heritage evaluation and in methods; the existing `scope-name-lex` cohort passes
3/3, while class declarations keep their outer binding.
Pre-scanning function parameter initializers for direct eval lets closures made
before an eval statement resolve its later dynamic `var`; the existing
`scope-param-elem-var` cohort now passes 6/6. Boolean `toString` now resolves
for primitive and boxed Boolean receivers; the existing Boolean property-call
case passes. Computed assignment/update keys now undergo `ToPropertyKey` once,
after nullish-base validation; `with` update references retain their original
object environment; and updates use `ToNumeric` so postfix expressions return
the numeric value, including BigInt. The existing prefix/postfix
increment/decrement cohorts pass 33/33, 38/38, 34/34, and 37/37. Stage 10
remains the current gate at 10,461/11,101. Object literal anonymous names now
cover the two-property fast path, computed string/Symbol keys, assignment,
destructuring defaults, and accessor prefixes; the existing 455-case
`fn-name-*` cohort improved from 432 to 449 passes.
The strict direct-eval parameter early-error cohort now passes 1/1 using the
OXC parser and the compiler's shared parameter early-error analysis. The
non-constructible concise-method regression also passes 1/1. The latest full
stage-10 run measured 10,461/11,101 (640 remaining across 303 failure families).
Binary-expression left
operands now materialize before effectful right operands; the full sweep gained
21 passes from this shared ordering fix. Generator function prototype objects
inherit from Function.prototype, and anonymous generator names are inferred at
simple variable bindings; the generator cohort is 289/290, with cross-realm
eval remaining. Delegated `yield*` uses a
dedicated residual operation; synchronous delegated throws resume through
existing generator handlers, and async-generator delegate throw/return results
flow through Promise reactions. The existing 415-case `yield-star` cohort now
passes 415/415. Async-generator requests now queue behind a pending await; the
existing async-generator cohort is 661/663. Failure aggregation reports 303
families; the largest include dynamic-import argument/context behavior,
dynamic-import module resolution, dynamic-import async completion, direct-eval
`super` handling, and optional-call/super dispatch. Dynamic-import execution
now uses the intrinsic Promise, specifier coercion, import-attribute
enumeration, and rejected-promise abrupt completions; the existing 48-case
specifier-abrupt cohort passes. Host module-graph resolution and namespace
objects remain open. Every runner used
`TEST262_TEST_TIMEOUT_MS=300000` and batches no larger than 100. Receiver-aware
`super` writes and object-literal `__proto__` semantics raised the focused
`super/` cohort to 88/94 and the full stage score to 10,461/11,101. Residual
function metadata now records the exact `super_home_atom` for direct eval.
`Reflect.set` now rejects primitive targets, exposing namespace writes that
the dynamic-import string stub had previously masked. The dynamic-import
directory is 682/1,004 (322 failures): namespace resolution/export values,
async completion, and import rejection are dominant families. Relative import
resolution now uses the host boundary; the existing missing-file cohort passes
16/16. The latest complete stage-10 run is 10,520/11,101 (581 failures across
300 families). The existing 24-case direct/indirect super-eval cohort passes
24/24 after carrying the implicit constructor's HomeObject through field
initialization. Import phase is preserved through residual calls; source-phase
imports of source-text modules reject with SyntaxError, and that 16-case cohort
passes 16/16. Relative host resolution now rejects missing files and supports
text/JSON default-export namespace values; the 65-case import-attributes cohort
is 56/65, with nine cases requiring JavaScript-module evaluation. Source identity now
survives residual serialization, and script/module Test262 dispatch both
preserve the file path as the source name. Module dispatch now parses with
OXC's explicit module goal and strict root semantics; the four existing
`class-name-ident-await` module cases pass. Scripts remain on the unspecialized
path. Stage 10 was rerun end-to-end in 112 batches of at most 100:
10,523/11,101 passed (578 failures across 299 families). Static import/export
linking and module-owned environments remain the major open module gap.
Ordinary/super/Proxy/`Reflect.construct` now share `IsConstructor`; Proxy
construct traps and fallback preserve the caller's actual `newTarget`. The
latest stage-10 result remains 10,523/11,101 (578 failures, 298 families), so
this consistency pass has not yet moved the pass count; poisoned-`__proto__`
class construction is still failing.
No unit tests,
new tests, whole-inventory run, or harness edits were made in this slice.

### Historical stage-10 continuation (superseded by the ordered checkpoints below)

Stage 10 remains the first failing sequential prefix. Its latest full sweep is
10,731/11,101 (370 failures, 273 families) across 112 batches of at most 100.
The dynamic-import subtree is 937/1,004 (67 failures). Existing module tests
now exercise literal static exports, same-module re-exports, constant default
bindings, static thrown values/builtin errors, and async-from-sync yielded-value
awaiting. The dominant residual module families require live bindings and
cross-module linking/evaluation. Continue only within stage 10 until it reaches
100%; retain `TEST262_TEST_TIMEOUT_MS=300000` and do not run unit tests, add
tests, alter the harness, or run the whole inventory.
The existing 28-case async-generator sync-delegation cohort passes 28/28 after
the shared async-from-sync continuation was corrected to read `done` before
`value`. Namespace `Symbol.toStringTag` is present; exotic descriptors,
define/set behavior, and stable module identity remain unfinished.
The OXC module early-error path passes the 16-case script-as-module cohort;
resolved-source namespace caching passes two applicable reuse cases, while the
65-case import-attributes cohort passes in full. Module namespace descriptors
now expose export writability and compatible no-change definitions; the
namespace cohort is 61/67, with nested re-export evaluation remaining. The
specifier-string success fallback was removed, so unsupported imports reject
instead of fabricating a namespace. No later prefix has started.
Static re-export linking now resolves named exports, star exports, and
namespace re-exports recursively. The 176-case dynamic-import catch cohort
passes 176/176; namespace tests pass 65/67, with exported function/class
evaluation outstanding. Module-record execution in the same runtime/program
model is the next major stage-10 requirement.
The latest dynamic-import-only rerun confirms 937/1,004 over eleven batches
of at most 100. Forty-five cases share the unsupported evaluated-module path;
the remaining 22 are single-case parser, eval, import-meta/defer, resolution,
and rejection gaps. Stage 10 remains the sole active prefix.
After introducing rooted module records and cached abrupt completions, the
bounded dynamic-import rerun remains 937/1,004 across eleven batches; no
regression or newly passing family is claimed. In-flight imports now join the
same record through rooted promises. The active implementation
requirement remains compiling/linking/evaluating module programs in the same
heap and realm, then sweeping stage 10 again.

The realm factory now exposes its own `Function` native and routes
`Function("return this")` to that realm's global. The filtered
`language/expressions/function` sweep ran three batches (100, 100, 64):
263/264 passed; its one failure is the strict-eval SyntaxError identity case.
The filtered 38-case `language/expressions/class/private` batch remains 0/38;
dynamic class source still falls through the simple evaluator. These targeted
runs do not change the stage-10 baseline of 10,731/11,101. No unit tests,
new tests, harness edits, or later-stage runs were made.

The shape-slot widening was followed by the stage-10
`language/expressions/object` subtree only: twelve batches (eleven of 100 and
one of 70), 1,158/1,170 passing. The 12 failures are existing compiler/object
families; the run is a bounded smoke sweep, not evidence for objects exceeding
the former 65,535-slot ceiling. A subsequent complete stage-10 sequential
sweep ran all 11,101 cases in 112 batches of at most 100 and again measured
10,731/11,101 (370 failures across 273 families), with the 45-case dynamic
module-evaluation family dominant. The typed-array own-key projection is not
covered by this stage-10 inventory; do not start its later Test262 stage until
all earlier stages pass.

Own-key visibility is now derived from one ordered `[[OwnPropertyKeys]]`
projection: ordinary own keys retain index/string/symbol order, while Proxy
keys use the existing trap path; `Object.keys`, `Object.getOwnPropertyNames`,
`Object.getOwnPropertySymbols`, and descriptor-copy consumers filter this shared
projection instead of rebuilding visibility independently. The engine-private
key filter is narrowed to the `\0rqj:` namespace so unrelated NUL-prefixed
strings remain public. `cargo check -p quench-runtime-next -p quench-test262`
passes, and the existing stage-10 object-expression subtree remains 1,158/1,170
over 12 batches (11 × 100, then 70). The stage-10 baseline remains
10,731/11,101; no pass-count change is claimed from this authority
consolidation.

The stage-10 `new.target` subtree now passes 14/14 after constructor frames
capture the target and ordinary nested calls stop inheriting an ambient
construction marker; derived `super()` forwards the frame-owned target. The
`language/expressions/new` subtree improved from 69/73 to 72/73 after the eval
path began recognizing whitespace-separated `new` plus identifier
constructors. One cross-realm error-realm case remains. These targeted results
do not update the full-stage baseline; the full stage-10 sequential sweep is
the next verification.

That sequential sweep completed stage 10 in 112 batches of at most 100:
10,733/11,101 passed, with 368 failures across 272 families (a two-pass gain).
The dynamic-import subtree remains 937/1,004 over 11 batches; its largest
family is the 45-case requirement to execute module bodies and preserve
side-effects/live bindings/exported functions. The other 22 cases span
unsupported module syntax or module-goal eval, eval import binding, errored
module rejection identity, and host resolution. Stage 10 remains the only
active prefix; no later stage or harness/test edits occurred.

The next-core realm factory now exposes realm-bound `parseInt`/`parseFloat`
functions, adopted from the corresponding old-core realm intrinsic list. The
existing cross-realm non-constructor case passes 1/1. A new complete stage-10
sweep in 112 batches (maximum 100 each) measured 10,734/11,101, or 367
failures across 271 families. The 45-case module-body evaluation family
remains largest. No tests were added and the Test262 harness is unchanged.

Stage 10 then gained a shared String prototype/iterator authority: the
existing 159-case array-destructuring subtree passes, the existing
`yield*`-with-relational-expression case passes, and the String property
accessor subtree is 13/21 (its String `charAt`/boxed-length cases now pass).
The complete stage-10 sweep ran 112 batches of at most 100 and reached
10,741/11,101 (360 failures across 267 families). The evaluated-module family
remains 45 cases; no later stage ran.

The first ProgramId ownership slice compiles with `cargo check -p
quench-runtime-next -p quench-test262`. Its existing 159-case
`language/expressions/assignment/dstr/array-` cohort passes 159/159 in batches
of 100 and 59. A focused stage-10 `dynamic-import` rerun completed all 1,004
cases in eleven batches (maximum 100): 938 passed, 66 failed across 22
families. Forty-five failures still share unsupported evaluated-module
execution; the other 21 span import-meta/eval/parser/defer/resolution and
rejection behavior. The filtered `language/expressions/import` run was 12/22;
its ten failures concern import-meta module-goal parsing/runtime behavior.
These results do not claim a full-stage pass-count change. No unit tests, new
tests, harness edits, or later-stage runs were made.

On 2026-09-23 the cumulative sequential recheck exposed two stage-0
same-realm error-identity failures and three stage-1 arguments-object
descriptor/deletion failures after the shared ownership work. Realm-owned
`TypeError` constructors are now constructable, and `[[Delete]]` distinguishes
the ordinary arguments-object `length` property from an Array exotic length.
The existing same-realm pair passes 2/2; cumulative prefixes 0–1 through 0–9
then pass 379/379, 481/481, 626/626, 678/678, 726/726, 745/745, 807/807,
1,154/1,154, and 1,157/1,157 respectively. Cumulative 0–10 completes stages
0–9 and measures stage 10 at 10,741/11,101 (360 failures in 267 families).
The 45-case evaluated-module family remains the largest. Every Test262 run set
`TEST262_TEST_TIMEOUT_MS=300000`, and the stage runner capped batches at 100;
no unit tests, project tests, harness edits, or later stages were run.

After the module namespace switched from copied export values to retained
module-local slots, the existing 36-case `eval-gtbndng-indirect-update`
cohort passed 36/36. Dynamic `Function` construction now compiles its coerced
parameters/body through OXC into the append-only ProgramStore and materializes
the nested function directly; distinct dynamic compilations scope their lowered
private-name atoms by unique source identity. The existing
`multiple-evaluations-of-class-function-ctor` cohort passes 7/7. Direct eval
now distinguishes declaration instantiation from assignment: eval-created
`var`/function bindings stay activation-local, while unresolved sloppy
assignments reach the realm global. Existing stages 7, 8, and 9 pass 62/62,
347/347, and 3/3 respectively.

The ordered `0 10` rerun used
`TEST262_TEST_TIMEOUT_MS=900000` and 100-case maximum batches. Stages 0–9 all
passed (1,157/1,157); stage 10 passed 10,810/11,101 (291 failures across 246
families). The former 36-case live-update family and seven Function-constructor
private-brand cases now pass. Dynamic-import namespace nested-evaluation,
eval-created class bodies/private-name scoping, realm identity, and other
language families remain. No unit tests, new tests, or harness edits occurred.

The next stage-10 sweep, after adding private-brand declaration metadata to
class prototype/constructor objects and a dedicated VM opcode, passed
10,891/11,101 (210 failures across 190 families). The nested-class private
cohort is 23/24. The 64-bit instruction encoding now assigns seven bits to the
opcode and routes immediates outside the reduced narrow range through the
existing wide representation. This is residual metadata interpreted by the
same VM, keeping the class declaration as the authority and avoiding a second
execution path. The remaining nested outer-private-field case still fails;
do not mark stage 10 complete. The run used
`TEST262_TEST_TIMEOUT_MS=900000`, 100-case batches, and no unit tests, new
tests, or harness edits.

An ordered stage-10 recheck after trying a “continue to another candidate
home” private-brand heuristic regressed to 10,873/11,101 (228 failures across
200 families); that heuristic was reverted. The stable full-stage result
remains 10,891/11,101. The nested-shadow failures expose a representation gap:
the next compiler currently keys private names by source plus spelling, while
OXC's class table (also used by the old core's `private_name_ids`) resolves a
reference to a class-scoped private identity. The next slice should residualize
that resolved identity and keep runtime brand checks on the same object/shape
authority. This is the shared semantic fact Futamura-style specialization
should precompute; another runtime candidate-search branch is not a substitute.

After the direct-eval spread call was kept on a dedicated direct-call opcode,
the existing empty-leading/empty-trailing spread pair exposed an activation
shadowing bug: the eval write updated both the current local and an outer global
with the same name. `store_eval_outer_local` now stops when the active function
owns that binding. The existing pair passes 2/2. The cumulative sequential
`0 10` runner then completed stages 0–9 at 1,157/1,157 and stage 10 at
10,921/11,101 (180 failures across 165 families), in 112 stage-10 batches of
at most 100. Every case used `TEST262_TEST_TIMEOUT_MS=900000`; the exhaustive
stage-4 Unicode cases passed. Evidence is
`target/test262-next-prefix-0-10-eval-shadow-fix.log`. No later stage ran, and
no harness or test files were changed. A private-brand object-state prototype
was rejected after the existing 1,657-case `private-` cohort exposed the
source-spelling identity collision; it was rolled back. The next correct slice
must port the legacy/OXC class-scoped private-name identity facts and runtime
slot lifecycle rather than reuse static atoms as brands.

The current ordered `TEST262_TEST_TIMEOUT_MS=900000
target/iteration/run-stages-next 0 10` sweep again passes stages 0–9
(1,157/1,157). Stage 10 is 11,010/11,101 (91 failures across 75 families),
and remains the first failing stage; no later stage ran. The sweep used the
runner's deterministic batches of at most 100. Its builtin accessor cohort is
20/21, with the remaining case requiring the wider Date prototype method
surface. A regression found during this prefix run—enumerable
`Array.prototype.constructor`—was corrected, and stage 3 was reverified at
145/145. Stage 0–10 evidence is in
`target/test262-next-prefix-0-10-builtin-slice.log`. No whole-inventory,
unit/project tests, new tests, or harness edits were made.

Latest ordered prefix evidence supersedes the prior 11,010 checkpoint:
`TEST262_TEST_TIMEOUT_MS=900000
target/iteration/run-stages-next 0 10` passed stages 0–9 at 1,157/1,157 and
stage 10 at 11,011/11,101 (90 failures across 74 families). Stage 10 remains
the first failing stage; no later stage or whole inventory ran. All stage
batches were capped at 100. The stage-10 Date `property-accessors` cohort now
passes 21/21, and the existing stage-0 `assertRelativeDateMs` case passes after
multi-argument Date construction was corrected to local time. Evidence is in
`target/test262-next-prefix-0-10-date-methods-final.log` and
`target/test262-next-stage10-date-methods-final.log`. No unit/project tests,
new tests, or harness edits occurred.

Latest sequential checkpoint supersedes that Date-only count:
`TEST262_TEST_TIMEOUT_MS=900000
target/iteration/run-stages-next 0 10` passes stages 0–9 at 1,157/1,157 and
stage 10 at 11,013/11,101 (88 failures across 72 families). The `Number.MIN_VALUE`
underflow pair now passes; stage 10 remains first failing and no later stage
ran. Batch maximum remained 100. Evidence is
`target/test262-next-prefix-0-10-number-min-date-methods.log` and
`target/test262-next-stage10-number-min-date-methods.log`. No whole inventory,
unit/project tests, new tests, or harness edits occurred.

The existing stage-10 BigInt cohort now passes 146/146 (two batches, maximum
100). A stage-10-only full sweep passes 11,017/11,101 (84 failures, 68
families), four more passes than the prior 11,013 checkpoint. The fixed
families are BigInt divide/remainder-by-zero RangeError, negative-exponent
RangeError, and unary-plus-on-BigInt TypeError. The next largest family is
seven unsupported `import defer` statements. Command:
`TEST262_TEST_TIMEOUT_MS=900000 target/iteration/run-stages-next 10 10`;
evidence: `target/test262-next-stage10-bigint-unary-fix.log`. This was not an
ordered-prefix or whole-inventory run; no later stage ran. All batches were
capped at 100. No unit/project tests, new tests, or harness edits occurred.

Fresh ordered checkpoint:
`TEST262_TEST_TIMEOUT_MS=900000
target/iteration/run-stages-next 0 10` passes stages 0–9 at 1,157/1,157 and
stage 10 at 11,017/11,101 (84 failures across 68 families). Stage 10 remains
the first failing stage; no later stages ran. The stage-4 Unicode comment
stress batch completed, and every batch was capped at 100. Evidence:
`target/test262-next-prefix-0-10-bigint-unary-fix.log`. No whole-inventory,
unit/project tests, new tests, or harness edits occurred.

After the shared string-to-number parser port, existing stage-10 `unary-plus`
passes 17/17. The stage-10-only sweep is now 11,022/11,101 (79 failures across
63 families), five more passes than the prior stage-10 checkpoint; no unary-
plus or BigInt failure family remains. Command:
`TEST262_TEST_TIMEOUT_MS=900000 target/iteration/run-stages-next 10 10`;
evidence: `target/test262-next-stage10-number-string-coercion.log`. Batches
remain capped at 100. The last ordered 0–10 prefix (before this ToNumber
change) passed stages 0–9 and recorded 11,017/11,101 in stage 10; a fresh
ordered prefix remains needed to revalidate earlier stages after the shared
conversion change. No whole-inventory, unit/project tests, new tests, or
harness edits occurred.

The stage-10 `import-defer` filter covers 147 existing Test262 cases: 142 pass
and five fail with the shared unsupported-module-statement diagnostic while
compiling async-module fixtures. The next slice therefore needs module-record
link/evaluation and suspension authority, not a parser exception for individual
files. Evidence: `target/test262-next-stage10-import-defer.log`; the bounded
runner used `TEST262_TEST_TIMEOUT_MS=900000` and at most 100 cases per batch.

Latest sequential checkpoint after unifying exponentiation semantics:
`TEST262_TEST_TIMEOUT_MS=900000 target/iteration/run-stages-next 0 10`
passes stages 0–9 (1,157/1,157) and stage 10 (language/expressions) at
11,025/11,101: 76 failures across 60 families. This supersedes the earlier
11,022 stage-10 baseline. The existing exponentiation cohort passes 62/62;
the two magnitude-one/infinite-exponent cases are resolved in the shared
`**`/`Math.pow` numeric operation. Stage 10 remains the first failing stage;
no later stage or whole inventory ran. Evidence:
`target/test262-next-prefix-0-10-exponentiation.log` and
`target/test262-next-stage10-exponentiation.log`. Every case retained the
900,000 ms timeout and batches remained capped at 100. No harness changes,
new tests, or unit/project tests occurred.

Latest sequential checkpoint after unifying language-level reference deletion:
stages 0–9 pass (1,157/1,157), and stage 10 passes 11,026/11,101 (75 failures
across 59 families). The existing stage-10 `delete` path filter passes 184/184;
primitive member bases now use the language delete operation instead of the
object-only Reflect API. Stage 10 remains the first failing stage; no later
stage or whole inventory ran. Evidence:
`target/test262-next-prefix-0-10-delete-reference.log` and
`target/test262-next-stage10-delete-reference.log`. Each case used
`TEST262_TEST_TIMEOUT_MS=900000`, batches were capped at 100, and no harness,
test, or unit-test changes were made.

Latest sequential checkpoint after retaining destructuring assignment
references: stages 0–9 pass (1,157/1,157), and stage 10 passes 11,029/11,101
(72 failures across 56 families). This supersedes the preceding 11,026 result.
The three target-evaluation-order cases now pass; the existing stage-10
`assignment/destructuring` filter passes 8/8. Stage 10 remains first failing;
no later stage or whole inventory ran. Evidence:
`target/test262-next-prefix-0-10-destructuring-reference-order.log` and
`target/test262-next-stage10-assignment-destructuring-order.log`. All cases
used the 900,000 ms timeout and batches capped at 100. No new tests, harness
edits, or unit/project tests occurred.

Latest sequential checkpoint after the arrow/class-field `new.target` fixes:
stages 0–9 pass (1,157/1,157), and stage 10 passes 11,033/11,101 (68 failures
across 53 families). The existing stage-10 `new.target` filter passes 16/16;
the preceding destructuring assignment order fixes are included in this
prefix. Stage 10 remains the first failing stage; no later stage or whole
inventory ran. Evidence:
`target/test262-next-prefix-0-10-new-target-activation.log` and
`target/test262-next-stage10-new-target-lexical.log`. Every case used the
900,000 ms timeout and batches capped at 100. No harness edits, new tests, or
unit/project tests occurred.

Latest sequential checkpoint after class superclass/new-target unification:
stages 0–9 pass (1,157/1,157), and stage 10 passes 11,036/11,101 (65 failures
across 51 families), three fewer than the prior sequential checkpoint. The
existing poisoned-`__proto__` class cases pass 3/3, the optional-member
expression case passes 1/1, and the broad class-path filter passes 4,048/4,059
in batches of at most 100. The `isConstructor` harness case passes 1/1 after
restoring async/generator exclusions to the metadata-backed constructor query.
No later stage or whole inventory ran. Evidence:
`target/test262-next-prefix-0-10-derived-constructors-final.log`,
`target/test262-next-stage10-class-derived-constructors.log`, and
`target/test262-next-stage0-isconstructor.log`. Every case used the 900,000 ms
timeout; no harness edits, new tests, or unit/project tests occurred.

Latest ordered checkpoint after `extends null` prototype lowering: stages 0–9
pass (1,157/1,157), and stage 10 passes 11,038/11,101 (63 failures across 50
families). The existing pair of `super` property cases passes 2/2. Stage 10 was
run in batches capped at 100 with `TEST262_TEST_TIMEOUT_MS=900000`; no later
stage or whole inventory ran. No harness changes, new tests, or unit/project
tests occurred.

Latest ordered checkpoint after preserving grouping references in `delete`:
stages 0–9 pass (1,157/1,157), and stage 10 passes 11,040/11,101 (61 failures
across 49 families). The existing grouping cohort passes 9/9. The ordered
runner uses batches capped at 100 and `TEST262_TEST_TIMEOUT_MS=900000`; no
later stage or whole inventory ran. No harness edits, new tests, or
unit/project tests occurred.

Latest ordered checkpoint after derived-constructor completion and realm
fallback changes: stages 0–9 pass (1,157/1,157), and stage 10 passes
11,043/11,101 (58 failures across 46 families). The existing cross-realm
`super/realm.js` case passes. A separate complete stage-10 sweep produced the
same count in batches capped at 100 with `TEST262_TEST_TIMEOUT_MS=900000`. No
later stage or whole inventory ran. No harness edits, new tests, or unit/project
tests occurred.

Latest stage-10-only sweep after constructor-owned superclass resolution:
11,045/11,101 pass (56 failures across 45 families). The `super/` path filter
passes 94/94, including the poisoned-prototype and lexical-arrow cases in that
cohort. The run used 112 batches, each capped at 100, with
`TEST262_TEST_TIMEOUT_MS=900000`. This is not yet a fresh cumulative 0–10
checkpoint; the fresh cumulative 0–10 rerun confirms stages 0–9 pass
(1,157/1,157), followed by stage 10 at 11,045/11,101. No later stage, whole
inventory, harness edit, new test, or unit/project test occurred.

Fresh ordered confirmation after explicit class-constructor naming passes
stages 0–9 (1,157/1,157) and stage 10 at 11,051/11,101 (50 failures across
39 families). This supersedes the preceding checkpoint; stage 10 remains the
first incomplete stage. All cases used `TEST262_TEST_TIMEOUT_MS=900000`, with
batches capped at 100. No later stage, whole inventory, harness edit, new test,
or unit/project test occurred.

The derived-constructor `this` transition is now shared with lexically nested
arrow calls to `super()`: runtime binding initializes the owning constructor
and its active arrow frames, while the compiler avoids running field
initialization twice. The existing `lexical-super` filter passes 4/4. A fresh
ordered 0–10 verification is underway; the last verified checkpoint remains
11,051/11,101 for stage 10 and 1,157/1,157 for stages 0–9. Every case keeps a
900,000 ms timeout and batches remain at most 100.

The fresh ordered 0–10 run confirms stages 0–9 at 1,157/1,157 and stage 10
at 11,052/11,101 (49 failures across 38 families), one additional case and
family closed by the lexical-arrow receiver transition. Stage 10 remains the
first incomplete stage. Cases used `TEST262_TEST_TIMEOUT_MS=900000`; batches
were capped at 100. No later stage, whole inventory, harness edit, new test, or
unit/project test occurred.

Class constructors now start with an empty own `name` so the class definition
installs the final name before static fields; the existing `class/name.js` case
passes. The broad stage-10 `class/` filter passes 4,056/4,059, leaving three
unrelated computed-field/accessor/private-proxy failures. A fresh cumulative
0–10 run is underway; until it completes, the last ordered checkpoint remains
11,050/11,101 for stage 10, with stages 0–9 at 1,157/1,157. Case timeouts are
900,000 ms and batches remain capped at 100.

Out-of-line derived-constructor field blocks now use registers above the
constructor body's live register window. This resolves the existing
optional-chaining `call-expression.js` cohort (64/64) without changing the
broad `class/` filter (4,056/4,059). A new ordered 0–10 verification is
underway; the previous checkpoint is 11,052/11,101 at stage 10 and
1,157/1,157 for stages 0–9. Every case still has a 900,000 ms timeout, and
batches remain capped at 100.

After capturing the static class-field initializer's `this` in lexical arrows,
the fresh ordered 0–10 gate passes stages 0–9 (1,157/1,157) and stage 10 at
11,047/11,101 (54 failures across 43 families). The two existing targeted
class-field arrow cases pass; the `static-field` path filter is 18/20. Stage
10 is still the first incomplete stage. This run used per-case
`TEST262_TEST_TIMEOUT_MS=900000` and batches of at most 100. No later stage,
whole inventory, harness edit, new test, or unit/project test occurred.

After threading static-field `this` through direct eval and deriving anonymous
field function names from their keys, the ordered 0–10 gate passes stages 0–9
(1,157/1,157) and stage 10 at 11,049/11,101 (52 failures across 41 families),
two cases and two families better than the prior checkpoint. The existing
`static-field` filter passes 20/20. All cases used
`TEST262_TEST_TIMEOUT_MS=900000` in batches capped at 100; no later stage,
whole inventory, harness edit, new test, or unit/project test occurred.

The next class slice installs the inferred variable-binding name on anonymous
class constructors before their static fields execute. The existing
`class-name-static-initializer-anonymous.js` case now passes; `class/name.js`
remains a separate descriptor/name failure. The cumulative 0–10 run for this
change is in progress; the last verified ordered result remains 11,049/11,101
at stage 10, with stages 0–9 passing 1,157/1,157. Each case still requires
`TEST262_TEST_TIMEOUT_MS=900000`, with no batch over 100. No later stage, whole
inventory, harness edit, new test, or unit/project test occurred.

The subsequent fresh ordered 0–10 run passes stages 0–9 (1,157/1,157) and
stage 10 at 11,050/11,101 (51 failures across 40 families), one case and
family better after setting inferred class names before static initialization.
The targeted `class-name-static-initializer-anonymous.js` case passes; the
broader `class/name.js` descriptor case remains. Every case had the required
900,000 ms timeout and batches were capped at 100. No later stage, whole
inventory, harness edit, new test, or unit/project test occurred.

Fresh ordered verification after the deferred-field register-window
correction passes stages 0–9 (1,157/1,157) and stage 10 at 11,053/11,101
(48 failures across 38 families), one case better. The existing optional-call
cohort passes 64/64 and lexical-super passes 4/4. Cases used
`TEST262_TEST_TIMEOUT_MS=900000`, with batches capped at 100; no later stage,
whole inventory, harness edit, new test, or unit/project test occurred.

After the realm-owned tagged-template cache was added to the tracing root set,
the previous new-core stage-0 harness regressions are closed: stage 0 passes
116/116 and fresh cumulative stages 0–5 pass 726/726. The remaining prefix
stages 6–9 pass sequentially (19/19, 62/62, 347/347, 3/3); stage 10 is being
re-swept in batches of at most 100. Each runner had
`TEST262_TEST_TIMEOUT_MS=900000`. This update uses only the pinned Test262
suite; the harness is unchanged and no tests were added. No unit/project tests
were run.

Fresh stage-10 verification passes 11,061/11,101 (40 failures across 31
families), eight more passes than the preceding new-core checkpoint. All 112
batches remained at or below 100 cases with
`TEST262_TEST_TIMEOUT_MS=900000`. The tagged-template path is 26/27, with its
single remaining direct-eval closure-capture case. Stages 0–9 are green in
sequence; stage 10 remains the first incomplete stage. No later stage or whole
inventory was run; the Test262 harness is unchanged and no tests were added.

After that full-stage checkpoint, the object-literal initialization path was
separated from assignment: the new-core `DefineField` operation now creates
own data properties with their default descriptor attributes. The existing
stage-10 object-expression cohort passes 1,167/1,170, including the inherited
non-writable-property regression case. Fresh ordered 0–10 verification after
this source change passes stages 0–9 and stage 10 at 11,062/11,101 (39 failures
across 30 families), one additional case closed. The targeted run retained the
required timeout and ≤100-case batches. No harness edits, new tests, or
unit/project tests occurred.

### 2026-09-24 ordered prefix after `with` call receiver fix

Identifier calls within `with` now preserve the resolved object environment as
their call receiver while ordinary/global environment references retain an
undefined receiver. This reuses a single name-reference resolution result and
preserves `WithBaseObject` provenance. The existing `call/with-base-obj.js`
case passes. The latest ordered 0–10 run passes stages 0–9 at 1,157/1,157 and
stage 10 at 11,063/11,101 (38 failures across 29 families), leaving stage 10
as the first incomplete stage. All cases used
`TEST262_TEST_TIMEOUT_MS=900000`; batches were at most 100. The Test262
harness remains untouched; no tests or unit/project tests were added/run.

### 2026-09-24 ordered prefix after eval and class-key slices

Eval-string `typeof` now uses shared VM value classification and recognizes
ECMAScript separator whitespace plus parenthesized operands. Class computed
keys are materialized once, in element source order, before static field
initializers and reused by method/field installation. Existing Test262
`typeof/syntax.js`, direct-eval TDZ (6/6), and the computed static/non-static
field ordering case pass. The broad stage-10 `class/` cohort passes 4,057/4,059;
its remaining cases are the supported-syntax gap for auto-accessors and a
private-field proxy case. The fresh sequential 0–10 run passes stages 0–9 at
1,157/1,157 and stage 10 at 11,065/11,101 (36 failures across 27 families).
Every case had `TEST262_TEST_TIMEOUT_MS=900000`; batches were at most 100. The
harness remains unchanged, with no new tests or unit/project tests.

The current verified checkpoint is stage 10 at 11,065/11,101 (36 failures,
27 families), with stages 0–9 passing 1,157/1,157. The computed-field ordering
case is now closed; the broad `class/` cohort is 4,057/4,059. Stage 10 remains
the first incomplete stage. This ordered run used the required 900,000 ms per
case timeout and ≤100-case batches.

The realm-owned `%AsyncFunction.prototype%` intrinsic is now installed and
selected from the async/generator function-kind table. This closes the existing
`async-arrow-function/prototype.js` case. The latest ordered 0–10 prefix has
stages 0–9 passing 1,157/1,157 and stage 10 at 11,066/11,101 (35 cases across
26 families). Stage 10 remains the first incomplete stage. Detailed stage-10
output: `target/test262-next-stage10-async-prototype.log`; all cases use the
required 900,000 ms timeout and ≤100-case batches.

### 2026-09-24 ordered prefix after dynamic `with` references

Nested closures, methods, arrows, and class execution contexts now carry
explicit `with` depth. Name lookup remains a runtime operation where the
environment is dynamic, while the closure retains the declarative parent
needed after object-environment lookup. Captured-reference PutValue checks
strict binding existence after RHS effects, and global-var writes share the
root-binding transition used by ordinary name stores.

The fresh sequential 0–10 run passes stages 0–9 at 1,157/1,157 and stage 10
at 11,068/11,101 (33 failures across 24 families). The focused existing
Test262 cohorts pass: PutValue (24), postfix (38), prefix (101),
generator-with (3), closure capture, TCO-with, and the stage-6 destructuring
ordering case. Stage 10 is still the first incomplete stage. Evidence:
`target/test262-next-prefix-0-10-with-environment-authority-verified.log` and
`target/test262-next-focused-with-*.log`. Every runner used
`TEST262_TEST_TIMEOUT_MS=900000` and batches ≤100. The Test262 harness was
untouched; no new tests or unit/project tests were added or run.

### 2026-09-24: stage-10 OXC eval slice

Indirect eval expressions beginning with a parenthesized expression now use
the OXC specializer, preserving the direct-eval context path for lexical
`super`. The two cross-realm generator prototype cases pass, and the four
direct-eval `super` cases remain passing. Stage 10 alone is 11,070/11,101
(31 failures across 23 families), down from 35 failures. Evidence:
`target/test262-next-stage10-eval-authority-final.log`.

The fresh ordered `run-stages-next 0 10` verification now confirms stages 0–9
at 1,157/1,157 and stage 10 at 11,070/11,101 (31 failures across 23
families). Stage 10 remains the first incomplete stage. Evidence:
`target/test262-next-prefix-0-10-eval-authority-final.log`; every case used
`TEST262_TEST_TIMEOUT_MS=900000` and batches ≤100. The Test262 harness remains
untouched, and no new tests or unit/project tests were added or run.

### Current ordered checkpoint — 2026-09-24

The OXC simple-call recognizer now validates the complete outer call shape;
nested call chains use the AST pipeline. This closes the Test262
`binding-tests-3.js` case. Ordered 0–10 verification passes stages 0–9 at
1,157/1,157 and stage 10 at 11,071/11,101 (30 failures across 23 families).
Stage 10 remains first incomplete. Evidence:
`target/test262-next-prefix-0-10-eval-call-authority.log`. Cases used
`TEST262_TEST_TIMEOUT_MS=900000`, batches ≤100; harness untouched, no tests
added, and no unit/project tests run.

The current completed ordered checkpoint is stages 0–9 at 1,157/1,157 and
stage 10 at 11,072/11,101 (29 failures across 22 families). The ordinary
static-member call path now resolves the callee before argument evaluation;
the existing stage-10 call cohort passes 92/92. Stage 10 remains first
incomplete. Evidence: `target/test262-next-prefix-0-10-call-reference-final.log`.

Ordinary static-member calls now resolve the member before evaluating
arguments. Existing stage-10 `call/` cases all pass (92/92), including
non-callable member access with side-effecting arguments. Fresh ordered
verification passes stages 0–9 at 1,157/1,157 and stage 10 at 11,072/11,101
(29 failures across 22 families). Evidence:
`target/test262-next-prefix-0-10-call-reference-final.log`; timeout was
900,000 ms per case and batches were ≤100. Harness untouched; no new tests or
unit/project tests.

### 2026-09-24: root global-var cell authority

`store_name` now preserves lexical-environment precedence and routes unresolved
global `var` writes through the root program selected by the active frame's
`ProgramId`, keeping dynamic eval closures consistent with the root binding
cell. The existing object-literal getter/setter eval cases pass (2/2). A fresh
ordered run confirms stages 0–9 at 1,157/1,157 and stage 10 at 11,073/11,101
(28 failures across 21 families). Stage 10 remains first incomplete. Evidence:
`target/test262-next-prefix-0-10-module-lifecycle-check.log`; every case used
`TEST262_TEST_TIMEOUT_MS=900000`, with deterministic batches no larger than
100. Remaining clusters include unsupported import-defer statements, module
evaluation/live-binding lifecycle, generator delegation, eval parsing, class
accessor grammar, and `instanceof`. Harness untouched; no tests were added and
no unit/project tests were run.

### 2026-09-24: delegated iterator result protocol

The interpreter now distinguishes ordinary `yield` from sync `yield*` at the
frame transition: ordinary yields derive their result object, while delegated
yielding returns the iterator's original incomplete result object and does not
read its `value` property. `yield*` return-method lookup also treats `null` as
absent. All three existing Test262 cases pass:
`yield/star-iterable.js`, `yield/star-rhs-iter-nrml-res-done-no-value.js`, and
`yield/star-return-is-null.js`. The rebuilt stage-10 runner sweep is
11,076/11,101 (25 failures across 18 families), versus the previous ordered
checkpoint's 28 failures across 21 families. Evidence:
`target/test262-next-stage10-yield-result-protocol-final.log`; cases used the
required 900,000 ms timeout and batches ≤100. Fresh ordered 0–10 verification
also passes stages 0–9 at 1,157/1,157 and stage 10 at 11,076/11,101 (25
failures across 18 families). Evidence:
`target/test262-next-prefix-0-10-yield-result-protocol-final.log`. Harness
unchanged; no tests were added and no unit/project tests were run.

### 2026-09-24: stage-13 failure-family triage

The cumulative `run-stages-next 0 13` gate remains green through stage 12
(12,530/12,530); stage 13 (`language/global-code`) is 27/42, with 15 failures
across seven families. Failures cluster around script-global function binding
and descriptors, lexical/global declaration conflicts, and module-only syntax
reaching Script execution. Evidence:
`target/test262-next-prefix-0-13-function-code-families.log`; timeout was
900,000 ms per case and stage batches were capped at 100. The harness is
unchanged and no tests were added or unit/project tests run. Stop at stage 13
until these families are resolved.

### 2026-09-24: shared generator `this` mode

Normal and generator activations now share one strict/sloppy `this`-value
normalizer. The existing non-strict generator-method Test262 case passes;
combined with the delegated-result protocol, stage 10 is now 11,077/11,101
(24 failures across 17 families). The `instanceof` patch is not counted as a
pass: its existing Test262 case still fails. Evidence:
`target/test262-next-stage10-generator-this-current.log`; timeout was
900,000 ms per case, batches ≤100. Fresh ordered 0–10 verification confirms
stages 0–9 at 1,157/1,157 and stage 10 at 11,077/11,101 (24 failures across
17 families). Evidence: `target/test262-next-prefix-0-10-generator-this-current.log`.
Harness untouched; no tests were added or unit/project tests run.

### 2026-09-24: dynamic import from eval

`import(...)` encountered in eval now goes through the existing OXC
`ImportExpression` lowering rather than being resolved as a variable named
`import`. The compiled eval retains the active source identity so relative
module resolution uses the originating Test262 file. Existing
`dynamic-import/usage-from-eval.js` passes. Fresh stage-10 sweep is
11,078/11,101 (23 failures across 16 families). Evidence:
`target/test262-next-stage10-eval-import-current.log`; timeout was 900,000 ms
per case and batches ≤100. Fresh ordered 0–10 verification passes stages 0–9
at 1,157/1,157 and stage 10 at 11,078/11,101 (23 failures across 16
families). Evidence:
`target/test262-next-prefix-0-10-eval-import-current.log`. Harness untouched;
no tests were added or unit/project tests run.

### 2026-09-24: OXC-derived eval block completion and operator precedence

Eval now asks OXC to recognize the supported completion shape (an
expression-only block followed by an expression statement); block expressions
are evaluated in the active eval environment before the final expression.
The evaluator's operator scan now uses precedence classes and correct
associativity, so division chains are left-associative. Existing Test262 cases
for `strict-equals`, `equals`, and `division/no-magic-asi-from-block-eval.js`
pass. Stage 10 is 11,081/11,101 (20 failures across 13 families). Evidence:
`target/test262-next-stage10-eval-operator-precedence.log`; timeout was
900,000 ms per case and batches ≤100. Fresh ordered verification passes stages
0–9 at 1,157/1,157 and stage 10 at 11,081/11,101 (20 failures across 13
families). Evidence:
`target/test262-next-prefix-0-10-eval-operator-precedence-final.log`. Harness
untouched; no tests were added or unit/project tests run.

The numeric `instanceof` fast path now falls through to generic operator
dispatch, and the existing `instanceof/S11.8.6_A3.js` case passes. The latest
ordered 0–10 run confirms stages 0–9 at 1,157/1,157 and stage 10 at
11,082/11,101 (19 failures across 12 families). The remaining stage-10
families are unsupported import-defer statements, nested dynamic-import
module evaluation, eval inner-function scope, class accessor grammar,
dynamic-import completion/once-only/live-binding behavior, async/await job
interleaving, and private-field proxy behavior. Evidence:
`target/test262-next-prefix-0-10-instanceof-numeric-final.log`; timeout was
900,000 ms per case and batches ≤100. Harness untouched; no tests were added
or unit/project tests run.

The Proxy constructor now bypasses ordinary native-constructor prototype
postprocessing, preventing Proxy [[SetPrototypeOf]] forwarding from mutating
the wrapped class instance. The existing
`private-fields-proxy-default-handler-throws.js` case passes. Latest ordered
0–10 verification passes stages 0–9 at 1,157/1,157 and stage 10 at
11,083/11,101 (18 failures across 11 families). Evidence:
`target/test262-next-prefix-0-10-proxy-construction.log`; stage-10 report:
`target/test262-next-stage10-proxy-construction.log`. Every run used the
required 900,000 ms timeout and ≤100-case batches. Test262 harness unchanged;
no tests were added and no unit/project tests were run.

### 2026-09-24: opcode result-layout derivation

The opcode-row result-layout refactor builds successfully. A fresh ordered
0–10 Test262 run confirms stages 0–9 at 1,157/1,157 and stage 10 at
11,083/11,101 (18 failures across 11 families), with no coverage regression
from the preceding checkpoint. This is structural progress, not a new passing
case. Evidence: `target/test262-next-prefix-0-10-result-layout.log`; each
case had `TEST262_TEST_TIMEOUT_MS=900000`, and batches were capped at 100.
Harness unchanged; no tests were added and no unit/project tests were run.

### 2026-09-24: direct-eval activation scope

Eval-created functions now retain the caller environment across dynamic
program boundaries. Root-scope visibility is an explicit environment wrapper,
so eval-created closures can resolve root block bindings without leaking
expired slots into a function-scoped eval. The existing tagged-template
`cache-eval-inner-function.js` case passes, and the catch-scope control case
remains green. The fresh ordered prefix passes stages 0–9 at 1,157/1,157 and
stage 10 at 11,084/11,101 (17 failures across 10 families), one additional
stage-10 pass. Evidence: `target/test262-next-prefix-0-10-dynamic-eval-scope-final.log`;
timeout was 900,000 ms per case and batches ≤100. Harness untouched; no tests
were added and no unit/project tests were run.

### 2026-09-24: auto-accessor class elements

The OXC specializer now lowers auto-accessors through compiled getter/setter
closures, private backing slots, and the shared class-field/accessor descriptor
paths. The existing six-element
`class/elements/syntax/valid/grammar-field-accessor.js` failure is resolved.
Fresh ordered verification completes stages 0–9 at 1,157/1,157 and stage 10
at 11,085/11,101 (16 failures across nine families), improving the prior
checkpoint by one case. Remaining families are import-defer parsing (seven
cases), nested dynamic module evaluation (two), and seven singleton
async/module/eval behaviors. Evidence:
`target/test262-next-prefix-0-10-auto-accessor.log`; every case used the
900,000 ms timeout and each batch contained at most 100 cases. Static
auto-accessor backing initialization and decorators remain incomplete. No
harness changes, added tests, unit tests, or project tests were made/run.

### 2026-09-24: root-module identity and live exports

The root residual now carries its Module goal into the runtime and binary
format. Dynamic self-imports share the active module record and a namespace
whose exports read the original module slots, so repeated import does not
re-evaluate the module and imported bindings remain live. Existing self-import
once-only, named/anonymous generator default-export, and mutable self-binding
cases pass. A literal-message errored-module case also passes through cached
module rejection.

Fresh ordered verification completes stages 0–9 at 1,157/1,157 and stage 10
at 11,090/11,101 (11 failures in four families), five more passes than the
prior 11,085 checkpoint. Evidence:
`target/test262-next-prefix-0-10-module-live-descriptors.log`. The remaining
stage-10 clusters are seven import-defer/static-module cases, two nested
dynamic-module cases, one script self-import completion case, and one
async/await job-order case. Timeout was 900,000 ms per case; batches ≤100.
The Test262 harness is unchanged; no tests were added and no unit/project
tests were run.

The reexport linker now composes local live bindings with named, star, and
namespace edges, and both existing nested namespace-property cases pass. A
fresh ordered 0–10 run is 1,157/1,157 through stage 9 and 11,092/11,101 at
stage 10 (nine failures across three families). The residual families are
seven import-defer/static-module compiler cases, root-script self-import
completion, and async/await job interleaving. Evidence:
`target/test262-next-prefix-0-10-static-module-linking.log`; required timeout
900,000 ms, batch cap 100. The harness is unchanged; no tests were added and no
unit/project tests were run.

### 2026-09-25 ordered-prefix correction and Stage 31 frontier

The later cumulative `run-stages-next 0 31` run supersedes the stage-4-blocked
status above: stages 0–30 pass sequentially, and the ordered gate currently
stops at Stage 31 (`built-ins/Array`). The cumulative run used
`TEST262_TEST_TIMEOUT_MS=900000`, four workers, and batches capped at 100. Stage
10 now passes 11,101/11,101; its main failure family was consumers reading only
dense array backing storage after `push` had appended via indexed properties.
Proxy `ownKeys`, `Reflect.construct`, and super-constructor argument handling
now materialize through the canonical array-like property path, and sloppy
direct eval assignment to a named function-expression binding is a no-op.
Stages 11–30 also pass individually, including Stage 20 at 599/599.

Stage 31 started at 2,833/3,081. Porting the legacy generic receiver algorithms
to the next VM's ToObject/indexed-property operations brought the latest
isolated run to 2,999/3,081 (82 failures across 54 families): `at`,
`copyWithin`, `reverse`, `fill`, `flat`, `flatMap`, `concat`, `includes`, `join`,
array iterators, and reduce-empty errors no longer reject generic receivers.
Out-of-bounds integer-indexed typed-array reads now return `undefined` through
ordinary `[[Get]]` instead of an eager error. Remaining failures are spread
across Array species/realm construction, abrupt completion and Proxy ordering,
Array.from/of constructor behavior, and a few method-specific edge cases. The
latest isolated report is
`target/iteration/stage31-array-oob-reads-2026-09-25.log`; the cumulative prefix
report, run before the last Stage 31-only corrections, is
`target/iteration/test262-next-prefix-0-31-array-like-2026-09-25.log`. Rerun the
cumulative prefix after the next Stage 31 slice. Test262 harness remains
untouched; no tests were added and no unit/project tests were run.

### 2026-09-25 Array species, realms, iterators, and equality

The next-core cumulative prefix was rerun through Stage 31 after these changes:
stages 0–30 pass, and Stage 31 is 3,037/3,081 (44 failures across 42
families). The fresh report is
`target/iteration/test262-next-prefix-0-31-array-species-iterators-2026-09-25.log`.
Stage 31 improved from 2,999/3,081 to 3,037/3,081 across the consecutive
rebuilt runs. Array `map` and `filter` now allocate through shared
`ArraySpeciesCreate` before callbacks and write with `CreateDataProperty`,
preserving holes and abrupt/Proxy behavior. Test262 `$262.createRealm()` now
provides a realm-owned `Array` constructor/prototype/species getter, and
foreign intrinsic Array constructors are normalized by realm identity before
species lookup. Iterator cells record their completed state so an exhausted
array iterator is not revived by later growth. Search operations now share
strict equality for BigInt and numeric comparison handles signed zero; the
existing `coerced-searchelement` and `samevaluezero` cohorts pass. Latest full
Stage 31 evidence is
`target/iteration/stage31-array-search-equality-rerun-2026-09-25.log`.
Test262 harness remains untouched, no tests were added, and no unit/project
tests were run.

### 2026-09-24: complete ordered stage 10

The remaining async-generator yield path now always resumes through a Promise
job, preserving the required microtask turn for fulfilled yields. Module-root
`var` bindings stay in the module activation instead of mutating script globals;
the existing script-self-import case consequently reaches `$DONE`. Both
formerly failing existing cases pass. Fresh sequential `run-stages-next 0 10`
verification passes stages 0–9 at 1,157/1,157 and stage 10 at 11,101/11,101.
Evidence: `target/test262-next-prefix-0-10-module-root-var-scope.log`. Each case
had `TEST262_TEST_TIMEOUT_MS=900000`, and stage batches were capped at 100.
The harness is unchanged; no tests were added and no unit/project tests were
run. The next ordered gate is cumulative range 0–12.

### 2026-09-24: complete ordered stage 11

The stage-11 failures clustered into strict block/switch function scope,
identifier-boundary false positives in strict-eval validation, and primitive
Number accessor lookup. The compiler now assigns block function declarations
separate lexical slots; eval early-error scanning shares identifier-continuation
rules; and primitive property access derives the canonical prototype while
preserving the primitive receiver. Existing affected cohorts pass 3/3, 2/2,
and 12/12. Fresh sequential `run-stages-next 0 11` verification passes stages
0–10 and stage 11 at 217/217 (12,475 total prefix cases). Evidence:
`target/test262-next-prefix-0-11-function-code-families.log`. Every case had
`TEST262_TEST_TIMEOUT_MS=900000`, with batches capped at 100. The harness is
unchanged; no tests were added and no unit/project tests were run.

### 2026-09-24: complete ordered stage 12

The fresh cumulative `run-stages-next 0 12` run remains fully green: stages
0–11 pass as recorded above, and `language/future-reserved-words` passes 55/55.
The prefix now contains 12,530 cases. Evidence:
`target/test262-next-prefix-0-12-function-code-families.log`; every case had a
900,000 ms timeout and batches were capped at 100. No code changes were needed
for this stage, the harness remains untouched, and no tests were added or
unit/project tests run. The next ordered gate is cumulative range 0–13.

### 2026-09-24: complete ordered stage 13

Global Script execution now uses Script-only OXC parsing. A shared global
declaration-instantiation transition creates new `var` properties with the
specified descriptor, validates restricted lexical declarations, applies
function-declaration redefinition rules, and checks realm lexical/var
collisions before execution. `$262.evalScript` compiles and runs source on the
new VM in the active realm; evalScript lexical declaration identities and
values are tracked separately from ordinary activation locals. Global builtin
properties are installed as writable, configurable, non-enumerable properties.
The existing stage-13 global-code cohort passes 42/42.

Fresh sequential `run-stages-next 0 13` verification passes stages 0–9 at
1,157/1,157, stage 10 at 11,101/11,101, stage 11 at 217/217, stage 12 at
55/55, and stage 13 at 42/42: 12,572 total cases. Every case used
`TEST262_TEST_TIMEOUT_MS=900000`; batches were capped at 100. The Test262
harness is unchanged; no tests were added and no unit/project tests were run.
The next ordered gate is cumulative range 0–14.

### 2026-09-24: complete ordered stage 14

Strict identifier assignment now captures the reference before evaluating the
right-hand side and preserves that reference through PutValue. This covers both
initially unresolvable identifiers and global object properties removed by
RHS effects, while routing local and captured bindings through their existing
authority. The runtime ReferenceError diagnostic includes its error type for
the negative-result matcher. Stage 14, `language/identifier-resolution`,
passes 14/14.

Fresh sequential `run-stages-next 0 14` verification passes stages 0–9 at
1,157/1,157, stage 10 at 11,101/11,101, stage 11 at 217/217, stage 12 at
55/55, stage 13 at 42/42, and stage 14 at 14/14: 12,586 total cases.
Evidence: `target/test262-next-prefix-0-14-global-declaration.log`. Every case
used `TEST262_TEST_TIMEOUT_MS=900000`; batches were capped at 100. The Test262
harness is unchanged; no tests were added and no unit/project tests were run.
The next ordered gate is cumulative range 0–15.

### 2026-09-24: complete ordered stage 15

The strict global-reference path now also covers prefix/postfix updates, while
lexical and captured targets stay on their existing binding path. This
preserves the selected global-object reference across getter effects and
deletion. Stage 15, `language/identifiers`, passes 268/268.

Fresh sequential `run-stages-next 0 15` verification passes stages 0–9 at
1,157/1,157, stage 10 at 11,101/11,101, stage 11 at 217/217, stage 12 at
55/55, stage 13 at 42/42, stage 14 at 14/14, and stage 15 at 268/268:
12,854 total cases. Evidence:
`target/test262-next-prefix-0-15-global-reference.log`. Every case used
`TEST262_TEST_TIMEOUT_MS=900000`; batches were capped at 100. The Test262
harness is unchanged; no tests were added and no unit/project tests were run.
The next ordered gate is cumulative range 0–16.

### 2026-09-24: complete ordered stages 16–17

Stage 16, `language/import`, passes 127/127 cases on the next core. The ordered
cumulative 0–17 gate passes 13,006/13,006, including stage 17,
`language/keywords`, at 25/25. The run used
`TEST262_TEST_TIMEOUT_MS=900000`, `TEST262_JOBS=4`, and deterministic batches
of at most 100 cases. Continuations dispatch against their own `ProgramId`;
static module evaluation now waits for pending async dependencies and advances
waiting modules as promise jobs settle. Evidence: the `run-stages-next 0 16`
output from 2026-09-24 and the full `run-stages-next 0 17` output in
`target/test262-next-prefix-0-17.log`. The next gate is cumulative 0–18. No
Test262 harness edits, new tests, or unit/project test runs were made.

### 2026-09-24: complete ordered stage 18

The ordered cumulative 0–18 gate passes 13,047/13,047. Stage 18,
`language/line-terminators`, passes 41/41; stage 8 (`language/eval-code`)
passes 347/347, and stage 10's 87-case direct-eval cohort passes 87/87. Global
direct Eval now preserves the existing declaration/realm path when it can
handle the source, while OXC Script parsing and new-core execution are the
fallback for parser-rejected sources. OXC derives Eval `var` names for global
lexical-conflict validation; the pre-scan avoids parsing sources without `var`
or `function` tokens. Single-expression Eval completion still uses the
existing compiled-expression path. The cumulative run preceded the lexical
pre-scan optimization; after that change, the current code was rechecked with
stage 18 (41/41), stage 8 (347/347), stage 10 direct-eval (87/87), and the long
stage-4 comment/eval case. Evidence for the full cumulative run:
`target/test262-next-prefix-0-18.log`. All runners used
`TEST262_TEST_TIMEOUT_MS=900000`; batches were at most 100. The next ordered
gate is cumulative 0–19. No Test262 harness edits, new tests, or unit/project
test runs were made.

### 2026-09-24: stage-20 module-code checkpoint

An intermediate stage-20 checkpoint passed 558/599 (41 failures across 14
families). Root
modules now use async completion only when the OXC AST has top-level `await`,
preserving synchronous abrupt completions. Named static imports retain the
export `(ProgramId, slot)` and read it on each access; value imports remain
values. The existing default-function self-import cohort passes 2/2. The
previous 592/599 report masked synchronous module errors and is superseded.
Root activation now initializes direct namespace/value imports before
environment promotion, while named imports retain their binding targets. The
latest stage-20 sweep is 573/599 (26 failures across 13 families); stage 20
remains the first failing prefix, so do not advance. Namespace TDZ and
async-cycle behavior remain open. Verification used
`TEST262_TEST_TIMEOUT_MS=900000 target/iteration/run-stages-next 20 20`, six
batches capped at 100, plus the two-case default-function import filter. No
harness edits, added tests, or unit/project tests.

Named-import linking records namespace binding identity and reads a raw
fallback property without invoking guest `[[Get]]` during linking. Captured
loads use their environment's `ProgramId` to dereference the same exporter
slot authority as direct local loads. The existing `instn-iee-bndng` cohort
passes 6/6. Root module entry also initializes default-export bindings in the
TDZ prologue, and default function/class/expression evaluation uses an
initializing store while named default functions preserve their binding
identity. This fixes the four pending top-level-await import cases. The latest
full stage-20 sweep is 591/599 (8 failures across 6 families), up from
573/599. Stage 20 remains active.

The latest stage-20 run on 2026-09-24 passes 595/599 (four failures across four
families), with batches of 100 or fewer and
`TEST262_TEST_TIMEOUT_MS=900000`. Module root `this` is now `undefined`, module
lexical initialization no longer writes a duplicate global property, and
module namespace freezing uses the namespace's canonical exotic descriptor
operations. These changes close the module `this`/global-mirror and
`define-own-property.js` cases. Remaining failures are async-cycle ordering,
`new-await-parens.js` constructor result handling,
`super-access-to-tdz-binding.js` error constructor identity, and
`verify-dfs.js` dependency instantiation. A broad entry-instantiation
experiment regressed named import resolution (592/599) and was removed; it is
not part of the current implementation. Stage 20 remains the active sequential
gate. Evidence: `TEST262_TEST_TIMEOUT_MS=900000
target/iteration/run-stages-next 20 20`. No harness edits, added tests, or
unit/project tests were made or run.

The `String` constructor's absent-argument rule is shared across call and
construct semantics without conflating their Symbol conversion behavior.
`new-await-parens.js` now passes. A fresh stage-20 sweep passes 596/599, with
three failures across three families: async-cycle ordering,
`super-access-to-tdz-binding.js` error constructor identity, and `verify-dfs.js`
dependency instantiation. Batches remained at most 100 and every case used
`TEST262_TEST_TIMEOUT_MS=900000`. Stage 20 remains the active sequential gate;
the Test262 harness is unchanged and no tests were added or run beyond the
existing suite.

The newest stage-20 sweep passes 597/599. `OrdinarySet` now consults a module
namespace receiver through canonical `[[GetOwnProperty]]` after target
accessor dispatch, preserving TDZ error identity and setter precedence. The
three existing namespace TDZ, accessor, and indexed-export fixtures pass.
Remaining failures are `top-level-await/pending-async-dep-from-cycle.js`
ordering and `verify-dfs.js` dependency instantiation. This remains the active
sequential gate; the runner used `TEST262_TEST_TIMEOUT_MS=900000` and batches
of at most 100. No harness edits or tests were added.

A follow-up attempt to pre-instantiate only the main module environment and
exported function closures before dependencies improved `verify-dfs.js` from
`TypeError: value is not callable` to `$DONE was not called`, but regressed the
three existing `instn-iee-bndng-{cls,const,let}` fixtures. That attempt scored
594/599 and was removed. At that point the stable result was 597/599; the
subsequent async-cycle-root change is recorded below.

### 2026-09-24: close stage 20

The ordered stage-20 gate now passes 599/599 (six batches, each at most 100
cases; `TEST262_TEST_TIMEOUT_MS=900000`). Main-module instantiation now shares
one environment and hoisted function identity with dependency imports; root
declaration bytecode reuses those closures. Dynamic imports requested during
static DFS wait until traversal completes, while unresolved dynamic targets
remain queued until the entry body finishes. Together with the retained async
cycle root, this closes `pending-async-dep-from-cycle.js` and `verify-dfs.js`.
The Test262 harness is unchanged and no tests were added. Do not run the whole
remaining suite at once.

Stages 21–24 have since passed sequentially: punctuators 11/11, reserved words
27/27, rest parameters 11/11, and source text 1/1. Stage 25 was next at that
checkpoint.

Stage 25 (`language/statementList`) now passes 80/80. OXC AST spans provide
eval statement boundaries; block and label completion values are propagated,
eval lexical bindings are scoped and removed after the eval, and the legacy
RegExp `toString` behavior is present. The next sequential gate is stage 26.

Stage 26 (`language/statements`) is the active failing gate: 8,871/9,337, with
466 failures grouped into 142 families over 94 batches capped at 100; every
case used `TEST262_TEST_TIMEOUT_MS=900000`. The latest report is
`target/test262-next-stage26-2026-09-24-lexical-class-scope.log`. Nested class
declarations now stay out of the function-local set and receive scope-owned
bindings; the switch `scope-lex-class.js` negative case passes. The focused
switch slice passes 88/111 (previously 87/111). An experiment excluding nested
`let`/`const` from function locals regressed stage 3 and was reverted; the
existing block-scope gate remains 145/145. Global direct eval now chooses the
compiled program path from parsed control-flow AST kinds, not source spellings.
The largest shared clusters are
class expected-throw behavior (72), completion values (at least 38 across
statement and switch families), `with` environment resolution (53 in two
value-mismatch families, plus two stack overflows), class error identity (19),
await-using initialization (13), private-name visibility during direct eval
(12), and destructuring iterator closing (12). No later stage was run; the
Test262 harness remains unchanged.

### 2026-09-24: refresh the ordered gate after class-scope/construction work

A fresh ordered run on the current release next-core binary reaches stage 8
(`language/eval-code`) and stops at 329/347 (18 failures across 11 families).
Stages 0–7 pass in that same run. The current stage-8 report is
`target/test262-next-stage8-2026-09-24-current-class-op.log`; every case used
`TEST262_TEST_TIMEOUT_MS=900000`. This supersedes the older claim that 0–25
remain green: stage 8 is now the active gate and no later stage may advance
until it returns to 100%. Its principal shared families are direct-eval var
binding creation/visibility (4), strictness/error propagation (3), arrow
`arguments` environment lookup (3), and eval expression completion (1), with
additional parser/global declaration cases.

The stage-26 class subtree has been used only as a focused diagnostic while
triaging shared class authority: 4,298/4,367 pass, across 44 batches capped at
100 with `TEST262_TEST_TIMEOUT_MS=900000`; report:
`target/test262-next-stage26-class-bound-constructability-op.log`. Bound
constructability/new-target forwarding and the `Symbol` heritage rule close
four prior failures in that subtree, while the class lexical-binding change
closes the switch block-class scope case. Do not count this as ordered-stage
progress until the stage-8 gate is green again. No Test262 harness changes or
new tests were made.

### 2026-09-24: stage-26 `with` activation/unwind checkpoint

After the newer ordered baseline reached stage 26, `language/statements/with/`
improved from 108/181 to 153/181 across two runner batches capped at 100. The
shared fix records each exception handler's lexical `with` depth in residual
bytecode and unwinds the active object-environment stack when a handler catches
an abrupt completion. Frame teardown now also removes activation-owned `with`
layers on return or throw. Closure-local slots remain authoritative ahead of
inherited `with` environments, and the var-name collector now descends through
`with` bodies so their function/global declarations are instantiated. These
changes fix the previously failing Sputnik throw/return cases and related
scope leaks.

The full stage-26 rerun passes 8,976/9,337 (361 failures across 134 families),
over 94 batches capped at 100; every case used
`TEST262_TEST_TIMEOUT_MS=900000`. Evidence:
`target/test262-next-stage26-2026-09-24-with-lifecycle-full.log`; the focused
`with` report is `target/test262-next-stage26-2026-09-24-with-inherited-scope.log`.
The largest remaining families are missing expected throws (57), completion
values (at least 38 across loop/switch cases), class error identity (19),
await-using initialization (13), private-name visibility in direct eval (12),
and destructuring iterator closing (12). Stage 26 remains the active gate; no
later stage was run. A fresh ordered regression rerun passed stages 0–18; its
stage-19 batches 1–2 also passed, then the redundant rerun was stopped after
the four existing Unicode-regex/eval cases became a multi-minute CPU-bound
batch. This is not a Test262 failure; the earlier complete stage-19 and
stages-20–25 passes remain the available evidence. No Test262 harness edits,
new tests, or unit/project test runs were made.

### 2026-09-24: stage-26 private-brand authority checkpoint

The next stage-26 run passes 8,982/9,337 (355 failures across 134 families)
over 94 batches capped at 100; each case used
`TEST262_TEST_TIMEOUT_MS=900000`. The report is
`target/test262-next-stage26-2026-09-24-private-brand.log`. Porting the legacy
private-slot authority to next-core's shared brand-install path now rejects
private instance elements on non-extensible receivers and rejects duplicate
installation, while compile-time deduplication permits a paired private
getter/setter to install one brand. Existing
`private-class-field-on-nonextensible-objects.js` and
`private-method-double-initialisation-get-and-set.js` pass. The focused
class-elements cohort passes 1,505/1,534 in 16 batches capped at 100; the
remaining 29 cases group into private-name direct-eval visibility (12),
private-name eval early errors (8), private field-set brand errors (3), proxy
private access (3), and three proxy/class-field observations. Stage 26 remains
the active gate; no later stage was run. No harness edits, tests added, or
unit/project test runs were made.

### 2026-09-24: stage-26 proxy private-element routing

The class-elements diagnostic now passes 1,508/1,534; the three existing
proxy-private fixtures (`privatefield-on-proxy.js`,
`privategetter-on-proxy.js`, and `privatemethods-on-proxy.js`) each pass in
single-case `run-stages-next` invocations with
`TEST262_TEST_TIMEOUT_MS=900000`. Private reads and writes on a Proxy now use
the receiver's private brand/storage rather than proxy traps or the target;
private accessor methods are recovered from the lexical home with the Proxy as
the call receiver. Brand-home lookup retains the legacy core's first-declaring
home rule, preserving nested private-name shadowing. The full stage-26 sweep
passes 8,985/9,337 (352 failures across 134 families) in 94 batches capped at
100; report: `target/test262-next-stage26-2026-09-24-private-proxy.log`.
Stage 26 remains the sequential gate; no later stage was run. The existing
`private-class-field-on-nonextensible-return-override.js` remains among the
failures and is not claimed fixed by this slice. The Test262 harness is
unchanged; no new tests or unit/project tests were added or run.

### 2026-09-24: stage-26 private-field assignment brand check

Private simple assignment now emits the same brand validation used by
destructuring assignment before writing the private slot. This closes all 11
existing `privatefieldset-typeerror` fixtures. The class-elements diagnostic
passes 1,511/1,534 (16 batches capped at 100), and a fresh full stage-26 run
passes 8,988/9,337 (349 failures across 134 families, 94 batches capped at
100); evidence: `target/test262-next-stage26-2026-09-24-private-set.log`.
`TEST262_TEST_TIMEOUT_MS=900000` was set for every invocation. Stage 26 remains
the active sequential gate; no later stage was run. The harness and Test262
fixtures are unchanged; no unit/project tests were added or run.

### 2026-09-24: stage-26 Error message descriptors

`Error` and native Error construction now defines an own `message` property as
writable and configurable but non-enumerable, matching the legacy core's
descriptor behavior. The six existing `*-message.js` cases and
`message-property-assignment.js` pass with
`TEST262_TEST_TIMEOUT_MS=900000`. The full stage-26 sweep passes 8,995/9,337
(342 failures across 133 families, 94 batches capped at 100); evidence:
`target/test262-next-stage26-2026-09-24-error-message.log`. Stage 26 remains
the sequential gate; no later stage was run, and the Test262 harness remains
unchanged. No tests were added or unit/project tests run.

### 2026-09-24: stage-26 strict class heritage

Class heritage expressions now compile under strict mode, matching the legacy
class reducer's strictness scope. This closes the existing
`class/strict-mode/arguments-callee.js` fixture; the focused strict-mode cohort
passes 2/2. The full `class/` diagnostic completed across 44 batches, and the
fresh full stage-26 sweep passes 8,996/9,337 (341 failures across 133
families); evidence: `target/test262-next-stage26-2026-09-24-class-heritage-strict.log`.
Every invocation used `TEST262_TEST_TIMEOUT_MS=900000`, with batches capped at
100. Stage 26 remains the active sequential gate, and no later stage was run.
No Test262 harness or fixture edits, new tests, or unit/project test runs.

### 2026-09-25: compiled script completion for structured eval

Eval sources containing structured statements now use a dedicated residual
program mode that preserves the script completion register. Expression
statements update that value; branches, loops, switch, with, try/catch, and
finally follow the legacy core's `UpdateEmpty` behavior. The persistent result
register is reserved across temporary-register resets, and normally completing
finally expressions do not overwrite the try/catch completion. Direct eval
retains its caller environment; indirect eval uses the global environment.
This closes the structured completion cohort from 48 failures to 1 remaining
case (`using/cptn-value.js`), passing 75/76. Stage 26 now passes 9,051/9,337
(286 failures across 116 families, 94 batches capped at 100); report:
`target/test262-next-stage26-2026-09-25-script-completions.log`.

The source change restores the sequential prefix: stages 0–8 pass 1,154/1,154
in `target/test262-next-prefix-0-8-2026-09-24-script-completions.log`, stage 9
passes 3/3, and stage 10 passes 11,101/11,101 in 112 batches; stage-10 report:
`target/test262-next-stage10-2026-09-25-script-completions.log`. Every runner
used `TEST262_TEST_TIMEOUT_MS=900000`; no batch exceeded 100 cases. Stage 11 is
the next sequential gate. Stage 26's result is diagnostic only and does not
advance the ordered gate. No harness or Test262 fixture edits, new tests, or
unit/project tests were made or run.

### 2026-09-25 sequential prefix through stage 13

Following the compiled-eval completion change, the sequential prefix is green
through stage 13: stages 0–8 pass 1,154/1,154; stage 9 passes 3/3; stage 10
passes 11,101/11,101; stage 11 (`language/function-code`) passes 217/217;
stage 12 (`language/future-reserved-words`) passes 55/55; and stage 13
(`language/global-code`) passes 42/42. The cumulative prefix is 12,572/12,572.
Reports for stages 0–8 and 10–13 are preserved under `target/`, including
`test262-next-prefix-0-8-2026-09-24-script-completions.log`,
`test262-next-stage10-2026-09-25-script-completions.log`, and
`test262-next-stage11-2026-09-25-sequential.log` through
`test262-next-stage13-2026-09-25-sequential.log`. Stage 14 is next. Every
runner had `TEST262_TEST_TIMEOUT_MS=900000`; batches were capped at 100.

### 2026-09-25 sequential prefix through stage 18

The ordered gate now passes through stage 18, cumulative 13,047/13,047.
Stages 14–18 pass individually: identifier-resolution 14/14, identifiers
268/268, import 127/127, keywords 25/25, and line-terminators 41/41. Reports
are preserved as `target/test262-next-stage14-2026-09-25-sequential.log`
through `target/test262-next-stage18-2026-09-25-sequential.log`. Stage 19
(`language/literals`) is running in six batches capped at 100; its first two
batches pass 200/200, while four exhaustive legacy regexp/eval cases remain
active in batch three. The run uses `TEST262_TEST_TIMEOUT_MS=900000`; stage 20
will not start until stage 19 completes.

### 2026-09-25 sequential prefix through stage 19

Stage 19 (`language/literals`) passes 534/534 in six batches capped at 100;
the sequential prefix is now 13,581/13,581 through stage 19. A parallel run
with the default worker count timed out three exhaustive regexp/eval cases in
one family, but a complete `TEST262_JOBS=1` rerun passed all six batches,
including those same cases. The timeout failures were therefore a resource
contention artifact, not a conformance failure. Reports:
`target/test262-next-stage19-2026-09-25-sequential.log` records the parallel
attempt; `target/test262-next-stage19-2026-09-25-serial.log` records the
passing full stage. Both used `TEST262_TEST_TIMEOUT_MS=900000`. Stage 20 is
next; no later stage has been run.

### 2026-09-25 sequential prefix through stage 20

Stage 20 (`language/module-code`) passes 599/599 in six batches capped at 100.
The ordered prefix is 14,180/14,180 through stage 20. Report:
`target/test262-next-stage20-2026-09-25-sequential.log`; the runner required
`TEST262_TEST_TIMEOUT_MS=900000`. Stage 21 (`language/punctuators`) is next.

### 2026-09-25 sequential prefix through stage 21

Stage 21 (`language/punctuators`) passes 11/11; the ordered prefix is
14,191/14,191. Report: `target/test262-next-stage21-2026-09-25-sequential.log`.
`TEST262_TEST_TIMEOUT_MS=900000` was set. Stage 22
(`language/reserved-words`) is next.

### 2026-09-25 sequential prefix through stage 22

Stage 22 (`language/reserved-words`) passes 27/27; the ordered prefix is
14,218/14,218. Report: `target/test262-next-stage22-2026-09-25-sequential.log`.
`TEST262_TEST_TIMEOUT_MS=900000` was set. Stage 23 (`language/rest-parameters`)
is next.

### 2026-09-25 sequential stage-26 result

Stage 26 (`language/statements`) is the current gate and passes 9,051/9,337;
286 cases fail across 116 reported families in 94 batches capped at 100. The
largest groups are 50 missing expected throws, 19 wrong error-constructor
identity despite matching names, 13 async `using` TypeErrors, 12 private-name
direct-eval parse failures, and 12 `for-of` destructuring iterator-close
assertions. Full report:
`target/test262-next-stage26-2026-09-25-sequential.log`. The ordered prefix
through stage 25 is 14,310/14,310. Stage 26 remains active; later stages have
not been run. The runner used `TEST262_TEST_TIMEOUT_MS=900000`.

### 2026-09-25 sequential prefix through stage 25

Stage 25 (`language/statementList`) passes 80/80; the ordered prefix is
14,310/14,310. Report: `target/test262-next-stage25-2026-09-25-sequential.log`.
`TEST262_TEST_TIMEOUT_MS=900000` was set. Stage 26 (`language/statements`)
is next.

### 2026-09-25 sequential prefix through stage 24

Stage 24 (`language/source-text`) passes 1/1; the ordered prefix is
14,230/14,230. Report: `target/test262-next-stage24-2026-09-25-sequential.log`.
`TEST262_TEST_TIMEOUT_MS=900000` was set. Stage 25 (`language/statementList`)
is next.

### 2026-09-25 sequential prefix through stage 23

Stage 23 (`language/rest-parameters`) passes 11/11; the ordered prefix is
14,229/14,229. Report: `target/test262-next-stage23-2026-09-25-sequential.log`.
`TEST262_TEST_TIMEOUT_MS=900000` was set. Stage 24 (`language/source-text`)
is next.

### 2026-09-25 stage-26 class-name binding unification

Named class declarations now receive the same immutable inner class-name
binding already used by named class expressions. Methods and fields resolve
that inner binding; after its scope closes, declaration initialization writes
the outer lexical binding. The existing focused `name-binding` cohort passes
6/6, and the broader `class/` cohort passes 4,323/4,367. A complete stage-26
rerun improves to 9,056/9,337, reducing failures from 286 to 281 and reported
families from 116 to 113. The two largest throw/error-constructor groups drop
from 50 to 49 and 19 to 18; report:
`target/test262-next-stage26-2026-09-25-class-binding.log`. Stage 26 remains
the ordered gate. The full stage stayed in 94 batches capped at 100, and every
runner used `TEST262_TEST_TIMEOUT_MS=900000`.

### 2026-09-25 stage-26 nullish resource registration

Both sync and async resource-stack registration now treat `null` and
`undefined` as no-op resources and return the original value, matching the
legacy core's `use_resource` / `async_use` behavior. The existing
`await-using/` cohort improves from 54/94 to 57/94, and a full stage-26 rerun
improves from 9,056/9,337 to 9,065/9,337 (272 failures across 109 families).
The `using/` cohort passes its nullish cases; its remaining failures center on
scope disposal, TDZ, and inferred names. Full report:
`target/test262-next-stage26-2026-09-25-nullish-disposal.log`; focused reports:
`target/test262-next-stage26-await-using-nullish-2026-09-25.log` and
`target/test262-next-stage26-using-nullish-2026-09-25.log`. Stage 26 remains
the gate; all invocations used `TEST262_TEST_TIMEOUT_MS=900000`, and the full
stage ran 94 batches capped at 100.

### 2026-09-25 stage-26 lexical and disposal-scope model

Module async-root detection now recognizes top-level `await using`; using
declarations participate in lexical/module binding tables. Disposal state is
explicit per lexical scope, with sync/async mode derived from declarations.
Using-containing blocks receive fresh stacks, `for`-initializer resources
dispose at loop exit, and thrown initializer/body completion disposes then
rethrows. Existing `forstatement` disposal fixtures pass 6/6. The existing
`await-using/syntax/` cohort passes 34/36; its two failures are invalid
assignment early errors. For-of per-iteration disposal remains a known failing
pair and is not claimed fixed. The full stage-26 gate improves from 9,065 to
9,077/9,337 (260 failures across 105 families); report:
`target/test262-next-stage26-2026-09-25-disposal-scopes.log`. Stage 26 remains
the ordered gate, with 94 batches capped at 100 and
`TEST262_TEST_TIMEOUT_MS=900000`.

### 2026-09-25 stage-26 declaration-name inference

Ordinary and resource declarations now share one initializer-lowering path.
Anonymous function, arrow, and class initializers receive the binding's inferred
name before `using` / `await using` registers the resource, preserving one
binding-to-name rule across declaration kinds. The existing
`using/fn-name-*` Test262 cohort passes 10/10. A complete stage-26 rerun improves
from 9,077/9,337 to 9,087/9,337 (250 failures across 95 reported families);
the ordered prefix through stage 26 is 23,397/23,647. The stage remains active.
Report: `target/test262-next-stage26-2026-09-25-initializer-name.log`; all
runner invocations used `TEST262_TEST_TIMEOUT_MS=900000`, and the complete stage
ran in 94 batches capped at 100.

### 2026-09-25 stage-26 error-constructor and disposal errors

The error-constructor table now gives each built-in error constructor its
specified own `name` property and descriptor, in addition to naming its
prototype. Resource registration now rejects primitive values and non-callable
dispose methods with real realm TypeErrors, matching the legacy resource-stack
authority rather than manufacturing generic Error values. Existing
`using/throws-if-initializer-*` cases pass 13/13 and
`await-using/throws-if-initializer-*` pass 8/8. The complete stage-26 gate
improves from 9,087/9,337 to 9,100/9,337 (237 failures across 101 reported
families); the ordered prefix is 23,410/23,647. Report:
`target/test262-next-stage26-2026-09-25-error-authority.log`. Stage 26 remains
active; all runners used a positive 900,000 ms timeout, and the full stage ran
in 94 batches capped at 100.

### 2026-09-25 stage-26 lexical-scope TDZ initialization

Mapped block and switch lexical bindings, plus `for` initializer bindings, now
enter TDZ through the shared scope-entry transition before statement evaluation.
This closes block-local `const`/resource self-initialization cases without
changing the harness. The existing `before-initialization` cohort passes
20/39; function-local and async TDZ cases remain open. The complete stage-26
gate improves from 9,100/9,337 to 9,113/9,337 (224 failures across 100
reported families); the ordered prefix is 23,423/23,647. Report:
`target/test262-next-stage26-2026-09-25-lexical-tdz.log`. Stage 26 remains
active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, with 94 batches capped
at 100.

### 2026-09-25 stage-26 function-body TDZ prologue

Function lexical slots now use the compiler's existing `lexical_atoms` data to
emit TDZ initialization for every function body, not only the root function.
This closes the remaining function-local cases in the existing
`before-initialization` cohort, which now passes 39/39. The complete stage-26
gate improves from 9,113/9,337 to 9,132/9,337 (205 failures across 97 reported
families); the ordered prefix is 23,442/23,647. Report:
`target/test262-next-stage26-2026-09-25-function-tdz.log`. Stage 26 remains
active; the run used a positive 900,000 ms timeout and 94 batches capped at
100.

### 2026-09-25 stage-26 iteration-binding TDZ

For-in/of declaration scopes now enter TDZ before evaluating the iterable, so
the loop's lexical binding correctly shadows an outer name in the RHS. The
existing `bound-names-fordecl-tdz` Test262 cohort passes 6/6. The complete
stage-26 gate improves from 9,132/9,337 to 9,144/9,337 (193 failures across 96
reported families); the ordered prefix is 23,454/23,647. Report:
`target/test262-next-stage26-2026-09-25-iteration-tdz.log`. Stage 26 remains
active; the run used `TEST262_TEST_TIMEOUT_MS=900000` and 94 batches capped at
100.

### 2026-09-25 stage-26 strict-eval early errors

Direct eval now runs the shared OXC formal-parameter and strict-binding early
error passes before simplified eval execution. Function-owned strict directives
are checked even when the eval script itself is sloppy, and strict function
declaration names (`eval` / `arguments`) are rejected. The existing `13.1-*`
cohort passes 18/18, strict eval function-name coverage passes, and direct-eval
`new.target` cases remain green (12/12). The complete stage-26 gate improves
from 9,144/9,337 to 9,150/9,337 (187 failures across 96 reported families); the
ordered prefix is 23,460/23,647. Report:
`target/test262-next-stage26-2026-09-25-strict-eval-validated.log`. Stage 26
remains active; all runners used a positive 900,000 ms timeout and the full
stage ran in 94 batches capped at 100.

### 2026-09-25 stage-26 captured global-var slot synchronization

Captured writes to root-script `var` bindings now update the active root frame's
current per-iteration environment as well as the global-object binding. This
keeps a closure write visible to the enclosing script after a loop environment
has been cloned, while captured reads continue to use the global-object binding
authority. The existing `language/statements/for-of/dstr` Test262 cohort passes
569/569 in six batches capped at 100. A complete stage-26 rerun passes
9,164/9,337 (173 failures across 94 reported families), improving from
9,150/9,337; the ordered prefix is 23,474/23,647. The largest remaining family
is direct eval of private names (12 cases). Report:
`target/test262-next-stage26-2026-09-25-global-var-sync.log`. Stage 26 remains
active; both runners used `TEST262_TEST_TIMEOUT_MS=900000`, with four jobs for
the full stage and batches capped at 100.

### 2026-09-25 stage-26 private names in direct eval

Direct eval inside class methods and field initializers now compiles private
expressions in a synthetic class-method grammar, with OXC's canonical private
labels remapped to the active class's existing atom identities. A lexical
private-home binding is carried by the eval environment so closures retain the
same private authority; a preflight parse rejects invalid private references
before eval statements can cause effects. OXC-resolved labels preserve escaped
and literal spellings as one identity. The existing private-name visibility
cohort passes 12/12, and invalid-private-name eval early errors pass 8/8. The
complete stage-26 gate passes 9,184/9,337 (153 failures across 92 families),
improving from 9,164/9,337; the ordered prefix is 23,494/23,647. The largest
remaining family is missing expected TypeErrors (9 cases). Report:
`target/test262-next-stage26-2026-09-25-private-eval-lexical-env.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 immutable iteration bindings and iterator result checks

Declaration-to-scope mapping now records const/resource bindings as immutable
from the declaration authority, so writes through hidden loop aliases retain
the same assignment semantics as ordinary lexical bindings. The existing
`language/statements/const` cohort passes 136/136, including the three
previously missing TypeErrors for const loop assignments. For-of compilation
also validates each synchronous iterator `next()` result as an object before
reading `done`; the existing `iterator-next-result-type.js` case passes.
The complete stage-26 gate passes 9,192/9,337 (145 failures across 91
families), improving from 9,184/9,337; the ordered prefix is 23,502/23,647.
Report: `target/test262-next-stage26-2026-09-25-iterator-result-check.log`.
Stage 26 remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs for the full stage, and 94 batches capped at 100. No harness changes or
new tests.

### 2026-09-25 stage-26 strict-eval assignment early errors

Strict direct eval now applies the compiler's existing strict-assignment check
to `eval` before evaluating declarations. This preserves the early-error
boundary: the invalid assignment raises SyntaxError before strict eval's
function declaration handling can turn a later call into ReferenceError. The
existing `function/13.0-*` cohort passes 7/8; `13.0-17-s.js` is a separate
remaining function-declaration case. The complete stage-26 gate passes
9,198/9,337 (139 failures across 90 families), improving from 9,192/9,337;
the ordered prefix is 23,508/23,647. Report:
`target/test262-next-stage26-2026-09-25-strict-eval-assignment.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 per-iteration lexical environments for `for`

Lexical `for` loops now clone their captured environment at the iteration
boundary before running the update expression. Closures created in the test
and body therefore retain the iteration's binding while the next iteration
updates its fresh binding. The existing `let-closure-inside-*` and
multi-binding fresh-iteration cohorts pass 4/4. The complete stage-26 gate
passes 9,221/9,337 (116 failures across 82 families), improving from
9,215/9,337; the ordered prefix is 23,531/23,647. Report:
`target/test262-next-stage26-2026-09-25-for-lexical-per-iteration-env.log`.
Stage 26 remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs for the full stage, and 94 batches capped at 100. No harness changes or
new tests.

### 2026-09-25 stage-26 iterator-close TypeError identity

Iterator close now constructs realm TypeErrors for non-callable `return`
methods and primitive `return()` results instead of returning message-only
errors. This preserves the thrown error value through async rejection and
matches the legacy runtime's iterator-close error authority. The existing
`iter-nrml-close-null` Test262 cohort passes 6/6. The complete stage-26 gate
passes 9,203/9,337 (134 failures across 89 families), improving from
9,198/9,337; the ordered prefix is 23,513/23,647. Report:
`target/test262-next-stage26-2026-09-25-iterator-close-type-error.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 nested var declaration collection

The root-script `var` collector now descends through for-in/of heads and bodies,
switch clauses, and labeled statements, using one var-declaration collector
for each location. This fixes the static global-binding metadata for nested
`var` declarations without treating lexical declarations as function scoped.
The existing `S12.10_A1.5_T*` with/for-in cohort passes 3/5; the two remaining
cases are abrupt-break cleanup and stay open. The complete stage-26 gate passes
9,215/9,337 (122 failures across 85 families), improving from 9,203/9,337; the
ordered prefix is 23,525/23,647. Report:
`target/test262-next-stage26-2026-09-25-var-hoist-collector.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 abrupt control through `with`

Control targets now retain their lexical with-depth. Break and continue emit
the required dynamic environment exits when they leave nested `with` scopes;
ordinary completion uses the same named with-exit emitter. The existing
`S12.10_A1.4_T*` and `S12.10_A1.5_T*` cohorts pass 10/10, including the prior
stale-with-binding failures. The complete stage-26 gate passes 9,229/9,337
(108 failures across 80 families), improving from 9,221/9,337; the ordered
prefix is 23,539/23,647. Report:
`target/test262-next-stage26-2026-09-25-with-break-unwind.log`. Stage 26 remains
active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for the full
stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 primitive `with` object coercion

Entering a `with` environment now boxes primitive values through the shared
object-coercion path before publishing the object to name resolution. Null and
undefined still throw a real TypeError. The existing `12.10-2-*` primitive
cohort passes 5/5; the strict-eval `with` early-error case remains open. The
complete stage-26 gate passes 9,237/9,337 (100 failures across 78 families),
improving from 9,234/9,337; the ordered prefix is 23,547/23,647. Report:
`target/test262-next-stage26-2026-09-25-with-primitive-boxing.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 class constructor call boundary

User-function dispatch now checks the compiler's `is_class_constructor`
metadata before ordinary calls and throws a realm TypeError without entering
the constructor body. An active construct target remains permitted, keeping
`[[Construct]]` separate from `[[Call]]`; bound and `.apply` calls share the
same gate. Existing default-constructor, prototype-wiring, and subclass-binding
cohorts pass 6/6. The complete stage-26 gate passes 9,234/9,337 (103 failures
across 79 families), improving from 9,229/9,337; the ordered prefix is
23,544/23,647. Report:
`target/test262-next-stage26-2026-09-25-class-call-guard.log`. Stage 26 remains
active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for the full
stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 switch lexical binding identity

Block-scoped bindings now use distinct internal local identities rather than
reusing the source-name slot when a switch/block binding is the only
declaration of that name. Switch-clause function declarations are instantiated
in the switch lexical scope, while sloppy block functions retain their
separate Annex B collection behavior. The existing `switch/scope-lex-*` cohort
passes 10/10, including the prior out-of-scope ReferenceErrors. The complete
stage-26 gate passes 9,241/9,337 (96 failures across 77 families), improving
from 9,237/9,337; the ordered prefix is 23,551/23,647. Report:
`target/test262-next-stage26-2026-09-25-switch-lexical-bindings.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 strict `with` and catch binding early errors

The strict-binding validator now descends through try/catch/finally and
rejects `eval`/`arguments` catch parameters in inherited strict code. It also
rejects strict-mode `with` statements before execution. The existing
catch-parameter restriction and strict-eval-with cohorts pass 5/5. The
complete stage-26 gate passes 9,244/9,337 (93 failures across 76 families),
improving from 9,241/9,337; the ordered prefix is 23,554/23,647. Report:
`target/test262-next-stage26-2026-09-25-strict-catch-with-errors.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 abrupt for-of iterator close

The loop body's abrupt-completion range now begins after IteratorValue, which
preserves the specified no-close behavior when a next-result `value` getter
throws. Errors while binding or executing the body close the active iterator,
ignore a close error when preserving an existing throw completion, then rethrow
the original value. Explicit `throw` no longer emits a second close before
entering that shared handler. Array value/entry iteration now uses indexed
property access so getters participate, while key iteration does not read
values. The existing iterator-close, value-getter, destructuring-binding,
body-put, and array-key-get-error cases pass 5/5. The complete stage-26 gate
passes 9,255/9,337 (82 failures across 69 families), improving from
9,244/9,337; the ordered prefix is 23,565/23,647. Report:
`target/iteration/stage26-iterator-close-2026-09-25.log`. Stage 26 remains
active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for the full
stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 iterator closures on outward loop control

Compile-time iterator cleanup now records the control depth that owns each
active iterator. Break and continue close only iterators whose loop scope they
leave, preserving the active iterator for a continue targeting that same
loop. The existing `iterator-close-via-*` cohort passes 4/4. Generator close
cases still fail in the generator-return lifecycle and were not counted as
fixed by this compiler change. The complete stage-26 gate passes 9,256/9,337
(81 failures across 68 families), improving from 9,255/9,337; the ordered
prefix is 23,566/23,647. Report:
`target/iteration/stage26-control-exits-2026-09-25.log`. Stage 26 remains
active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for the full
stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 iterator record captures `next`

The for-of compiler now constructs the iterator record's next-method authority
once before entering the loop and reuses that callable with the iterator as
receiver on each step. This avoids repeated observable `Get(iterator, "next")`
operations and keeps the captured method stable if the body mutates the
property. The existing `iterator-next-reference.js` case passes. The complete
stage-26 gate passes 9,257/9,337 (80 failures across 67 families), improving
from 9,256/9,337; the ordered prefix is 23,567/23,647. Report:
`target/iteration/stage26-iterator-record-2026-09-25.log`. Stage 26 remains
active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for the full
stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 generator Return completion through `finally`

Ordinary synchronous `Generator.prototype.return` now shares the saved
continuation unwind already used by delegated yield-star return: it resumes at
the nearest active finally handler with a Return completion. If no finalizer
is active, it closes suspended destructuring iterators before completing the
generator, preserving IteratorClose errors. The existing generator
`*-close-via-*` cohort passes 4/4 and the `rtrn-close` cohort passes 13/13.
The complete stage-26 gate passes 9,261/9,337 (76 failures across 63
families), improving from 9,257/9,337; the ordered prefix is 23,571/23,647.
Report: `target/iteration/stage26-generator-return-2026-09-25.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 object-environment call references

Well-known symbol cells now carry their specified `Symbol.<name>` description.
Identifier calls inside `with` use one `LoadNameCall` operation to resolve the
callee and receiver together; ordinary name loads share that same path. The
object-environment path now preserves the selected binding base and performs
the separate GetBindingValue property check without repeating HasBinding or
`@@unscopables` lookup. The existing proxy-environment cohort passes 4/6; two
set-binding proxy cases still overflow the stack. The complete stage-26 gate
passes 9,265/9,337 (72 failures across 60 families), improving from
9,261/9,337; the ordered prefix is 23,575/23,647. Report:
`target/iteration/stage26-with-name-reference-2026-09-25.log`. Stage 26 remains
active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for the full
stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 argument iterator length and debugger statement

Array iterators over arguments objects now read the object's live `length`
property rather than the backing array's storage length, so adding an indexed
property does not extend iteration. The ECMAScript maximum safe integer is now
one named VM constant shared by number and length conversions. The compiler
accepts `debugger` as the host-defined no-op statement. Existing mapped and
unmapped arguments-iteration cases pass 2/2, and the debugger statement case
passes 1/1. The complete stage-26 gate passes 9,268/9,337 (69 failures across
58 families), improving from 9,265/9,337; the ordered prefix is 23,578/23,647.
Report: `target/iteration/stage26-arguments-and-debugger-2026-09-25.log`. Stage
26 remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 typed-array iterator shrink validation

Typed-array value iteration now revalidates the view's buffer bounds on each
step. A length-tracking view still observes its reduced live length; a fixed
view or offset view that became out of bounds throws a realm TypeError rather
than appearing as an empty iterator. The two existing for-of resizable-buffer
shrink cases pass 2/2. The complete stage-26 gate passes 9,270/9,337 (67
failures across 57 families), improving from 9,268/9,337; the ordered prefix is
23,580/23,647. Report:
`target/iteration/stage26-resizable-typed-array-iteration-2026-09-25.log`.
Stage 26 remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs for the full stage, and 94 batches capped at 100. No harness changes or
new tests.

### 2026-09-25 stage-26 class method inferred names

Class method installation now applies `SetFunctionName` to computed property
keys. Getter and setter function creation uses the required `get ` and `set `
prefixes for both named and computed keys. The existing ordinary method,
generator method, getter, and setter name-descriptor cases pass 4/4. The
complete stage-26 gate passes 9,274/9,337 (63 failures across 54 families),
improving from 9,270/9,337; the ordered prefix is 23,584/23,647. Report:
`target/iteration/stage26-class-method-names-2026-09-25.log`. Stage 26 remains
active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for the full
stage, and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 built-in property descriptors

Typed-array constructor/prototype constants and methods now use the shared
non-enumerable built-in property writer. The same writer installs generated
function and dynamic-class `prototype.constructor` values, preserving the
specified writable/non-enumerable/configurable descriptor. This fixes the
existing `for-in/resizable-buffer.js` case, including its two dynamically
created typed-array subclasses (focused result 1/1). The complete stage-26 gate
passes 9,278/9,337 (59 failures across 51 families), improving from
9,274/9,337; the ordered prefix is 23,588/23,647. Stage 26 remains active;
the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94 batches
capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 Map entry reads and generator function prototype links

Map initialization now accepts any object-like entry and reads its `0` and
`1` properties through the shared indexed Get path, instead of requiring an
Array cell. The existing subclass case using `{ foo: "bar" }` passes; local
Node agrees that it adds one entry with an `undefined` key. Function-object
prototype links now expose the specified non-enumerable constructor values
for GeneratorFunction, AsyncFunction, and AsyncGeneratorFunction. Dynamic
GeneratorFunction bodies with `yield` remain incomplete; their existing class
subclass case is still one of this stage's failures. The complete stage-26
gate passes 9,279/9,337 (58 failures across 50 families), improving from
9,278/9,337; the ordered prefix is 23,589/23,647. Report:
`target/iteration/stage26-map-entries-and-generator-prototypes-2026-09-25.log`.
Stage 26 remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 disposal on initializer abrupt completion

Function-body and explicit-block disposal scopes now share one exceptional
cleanup lowering: preserve the thrown completion, dispose resources already
registered, then rethrow. Cleanup skips an uninitialized stack so a prior
temporal-dead-zone error remains the original ReferenceError. All six existing
`subsequent-initializer-throws` cases pass. The full `using/` cohort is now
168/172; the four remaining cases are two per-iteration async-disposal timing
cases and two SuppressedError completion cases. Full stage 26 passes
9,282/9,337 (55 failures across 48 families), improving from 9,279/9,337; the
ordered prefix is 23,592/23,647. Report:
`target/iteration/stage26-using-initializer-cleanup-2026-09-25.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94
batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 derive iteration-environment cloning from closures

The compiler now derives whether a lexical `for-in`/`for-of` iteration needs
`CloneEnv` from the declaration, iterable expression and loop body. A nested
function, arrow or direct `eval` retains the fresh per-iteration environment;
without one, cloning the whole frame had split unrelated captured locals. The
iterable expression is included because a closure created there must retain
the head's temporal-dead-zone environment. The existing per-iteration
async-disposal cases pass 2/2, fresh-binding cases pass 8/8, and lexical-head
capture cases pass 3/3. The `using/` cohort is now 170/172; the two remaining
cases concern composing multiple disposal errors. Comparing full failure-file
sets against unconditional cloning, stage 26 moves from 9,282/9,337 to
9,284/9,337 with exactly two old failures removed and none added; the ordered
prefix is 23,594/23,647. Report:
`target/iteration/stage26-iteration-closure-candidate2-2026-09-25.log`.
Stage 26 remains active; each runner used `TEST262_TEST_TIMEOUT_MS=900000`,
four jobs for full sweeps, and batches capped at 100. No harness changes or
new tests.

### 2026-09-25 stage-26 scoped disposal and synchronous completion chaining

Scoped statement lowering is now the shared owner of lexical cleanup for
ordinary blocks, try/catch blocks and finalizers; this ensures a try body's
`using` resources are disposed before control reaches its catch. The runtime
now installs the `SuppressedError` constructor/prototype and preserves
non-enumerable built-in error properties. Abrupt synchronous cleanup carries
the original throw into disposal, and reverse-order disposer failures fold
into the nested `SuppressedError` chain. The existing synchronous
multiple-disposal-error case passes; the async sibling remains. The `using/`
cohort is 171/172. Stage 26 is 9,285/9,337 (52 failures across 46 families),
improving from 9,284/9,337 with exactly one old failure removed and none
added; the ordered prefix is 23,595/23,647. Report:
`target/iteration/stage26-disposal-scopes-sync-suppression-2026-09-25.log`.
Runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for the full sweep and
94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 sequential async disposal completion

Async disposal now uses an explicit continuation state containing the
remaining resource entries, cursor, pending completion and result promise.
Each async-hinted callback is awaited before the state advances; rejection
folds through the same `SuppressedError` constructor as synchronous disposal.
The entry mode and state slots are named semantic data, and the compiler
specializes the lexical exit for sync versus async cleanup while the VM
interprets the ordered resource stack. Both existing multiple-disposal-error
cases pass, and the full `using/` cohort is 172/172. Stage 26 passes
9,286/9,337 (51 failures across 45 families), improving from 9,285/9,337 with
exactly the async SuppressedError failure removed and none added; the ordered
prefix is 23,596/23,647. Report:
`target/iteration/stage26-async-disposal-final-2026-09-25.log`. Stage 26
remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs for
the full sweep and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 built-in constructor TypeError identity

Invalid `DataView` buffers and non-callable `Promise` executors now produce
the realm's TypeError value instead of a generic Error. Node confirms both
constructors throw TypeError without their required arguments. The existing
built-in subclassing cohort improves from 64/70 to 66/70. Stage 26 passes
9,288/9,337 (49 failures across 44 families), improving from 9,286/9,337 with
exactly these two old failures removed and none added; the ordered prefix is
23,598/23,647. Report:
`target/iteration/stage26-builtin-constructor-types-2026-09-25.log`. Stage 26
remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs,
and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 finally transfers through completed controls

Finally-mediated break/continue edges now retain a stable destination handle
instead of indexing a control-stack entry after the loop or switch compiler
has popped it. Control destinations are resolved at the owning construct's
exit; the finalizer tail either uses that resolved target or remains queued on
the active control. This removes the two compiler panics in the existing
`try/S12.14_A5.js` and `try/S12.14_A15.js` cases. The `S12.14_A*` cohort is
54/57; its other three failures are unchanged. Stage 26 passes 9,290/9,337
(47 failures across 42 families), improving from 9,288/9,337 with exactly the
two panicking failures removed and none added; the ordered prefix is
23,600/23,647. Report:
`target/iteration/stage26-finally-control-destinations-2026-09-25.log`.
Stage 26 remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`,
four jobs and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 catch-pattern lexical capture

Catch parameters now use the same lexical-pattern mapping as other lexical
bindings. Simple identifier parameters retain their handler slot; destructuring
patterns derive their bindings from the pattern and initialize their TDZ slots
before binding. This lets nested closures resolve the catch parameter through
the lexical environment instead of falling through to the function-local
inventory. The existing `try/scope-catch-param-lex-open.js` case now passes.
Comparing full failure-file sets against the finally-control checkpoint, stage
26 improves from 9,290/9,337 to 9,291/9,337 with exactly that failure removed
and none added (46 failures across 42 families). Report:
`target/iteration/stage26-catch-pattern-scope-2026-09-25.log`. Stage 26 remains
active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94
batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 guard specialized derived-constructor `this`

Ordinary `LoadThis`, specialized `FieldBase::THIS` reads and `SetThisField`
writes now share one VM check for the uninitialized derived-constructor this
binding. The compiler keeps its direct field specialization, but it can no
longer bypass the ReferenceError guard that generic this access already had.
Both existing `this-access-restriction` cases pass, and local Node confirms
that reading or writing `this` before `super()` throws ReferenceError. The full
stage improves from 9,291/9,337 to 9,293/9,337 with exactly those two old
failures removed and none added (44 failures across 41 families). Report:
`target/iteration/stage26-checked-specialized-this-2026-09-25.log`. Stage 26
remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and
94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 restricted function caller accessor

`Function.prototype.caller` now classifies its receiver from the function's
semantic kind: strict, async, generator and arrow functions, plus bound
functions, reject access with TypeError; sloppy ordinary functions retain the
existing result. This single getter rule fixes the bound-function and generator
restricted-property cases. The focused existing cases pass, and local Node
confirms the accessor behavior. Stage 26 improves from 9,293/9,337 to
9,295/9,337 with exactly those two old failures removed and none added (42
failures across 40 families). Report:
`target/iteration/stage26-restricted-function-caller-2026-09-25.log`. Stage 26
remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and
94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 AST strict-arguments early errors

Strict-mode writes, updates and deletes of the `arguments` binding are now
recognized from OXC assignment-target and unary/update nodes, with strictness
tracked through function directives and scope flags. This replaces the source
substring scan for this rule, so `foo.arguments = value` remains a property
write and reaches the restricted accessor at runtime. The existing strict
property-assignment case passes. Full-stage comparison improves from
9,295/9,337 to 9,296/9,337 with exactly `function/13.2-4-s.js` removed and
none added (41 failures across 39 families). Report:
`target/iteration/stage26-strict-arguments-ast-2026-09-25.log`. Stage 26 remains
active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94
batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 lexical and compiler bindings inside `with`

Binding resolution now distinguishes an active lexical target and a
compiler-only frame binding from an outer function local. Both kinds use their
frame slots even while compiling inside `with`; ordinary outer locals still
defer to dynamic object-environment lookup. This preserves lexical-environment
precedence without disabling the `with` object environment. Existing
`try/S12.14_A14.js` and `function/S13.2.2_A19_T5.js` now pass; the `with/`
cohort remains at 176/181. Full-stage comparison improves from 9,296/9,337 to
9,298/9,337 with exactly those two failures removed and none added (39 failures
across 37 families). Report:
`target/iteration/stage26-with-lexical-shadowing-2026-09-25.log`. Stage 26
remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and
94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 public class fields use define-own-property

Public instance fields now lower to a dedicated define-field operation rather
than assignment. Ordinary string-keyed objects use the direct shape mutation
after descriptor/extensibility checks; proxies and exotic receivers route
through the existing `[[DefineOwnProperty]]` implementation. Computed keys use
a separate register-key opcode and the same authority. This skips inherited
setters and exposes each definition to Proxy `defineProperty` traps with the
standard writable/enumerable/configurable descriptor. The existing
`class/elements/` cohort passes 1,534/1,534, including all three formerly
failing proxy/setter cases; local Node agrees on trap order, target identity
and descriptor flags. Full-stage comparison improves from 9,298/9,337 to
9,301/9,337 with exactly those three old failures removed and none added (36
failures across 34 families). Report:
`target/iteration/stage26-class-field-define-own-property-2026-09-25.log`.
Stage 26 remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 Error prototype stringification

The realm now installs `Error.prototype.toString` as a native intrinsic. It
reads and coerces `name` before reading and coercing `message`, preserves
getter/coercion effects, applies the standard empty/default-name rules and
throws TypeError for nullish receivers. The existing `S12.14_A19_T1.js` and
`T2.js` cases pass; local Node confirms both output and coercion order. Full
stage comparison improves from 9,301/9,337 to 9,303/9,337 with exactly those
two old failures removed and none added (34 failures across 32 families).
Report: `target/iteration/stage26-error-prototype-to-string-2026-09-25.log`.
Stage 26 remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 Number exponential formatting intrinsic

The missing `Number.prototype.toExponential` intrinsic is now installed and
uses the runtime's numeric coercion and RangeError path, with explicit handling
for non-finite values and normalized signed exponents. This unblocks regular
subclassing of Number without changing construction or class semantics. The
existing `class/subclass/builtin-objects/Number/regular-subclassing.js` case
passes, and local Node agrees on the subclass result. Full-stage comparison
improves from 9,303/9,337 to 9,304/9,337 with exactly that failure removed and
none added (33 failures across 31 families). Report:
`target/iteration/stage26-number-to-exponential-2026-09-25.log`. Stage 26 remains
active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94
batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 ArrayBuffer species construction

`ArrayBuffer.prototype.slice` now resolves the receiver's constructor and
`@@species`, constructs the result with the requested byte length, validates
that the result is a distinct mutable non-shared ArrayBuffer with sufficient
capacity, then copies the selected bytes. `ArrayBuffer[Symbol.species]` is an
inherited accessor returning its actual receiver, so subclass identity follows
ordinary constructor inheritance. This ports the old core's species-validation
flow onto the new heap representation instead of special-casing subclasses.
The existing ArrayBuffer subclass case now passes; its focused cohort passes
2/2 and local Node agrees on subclass identity and byte length. Full-stage
comparison improves from 9,304/9,337 to 9,305/9,337 with exactly that old
failure removed and none added (32 failures across 30 families). Report:
`target/iteration/stage26-arraybuffer-species-2026-09-25.log`. Stage 26 remains
active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94
batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 dynamic generator constructors and continuation program

Dynamic function compilation now takes an explicit function kind and emits the
corresponding OXC function form for `Function`, `AsyncFunction`,
`GeneratorFunction` or `AsyncGeneratorFunction`; grammar and generator flags
therefore come from the same AST pipeline as ordinary source. Generator resume
now treats the saved continuation's program ID as authoritative, loads that
program and restores it as the active program while interpreting the frame.
This fixed dynamic generator frames being resumed against the caller's program
(which could select the wrong wide instruction table), and applies the same
rule to async-generator resumption. Both existing GeneratorFunction subclass
cases now pass; the focused cohort passes 5/5, and local execution yields `42`,
then `84`, then done. Full-stage comparison improves from 9,305/9,337 to
9,307/9,337 with exactly those two failures removed and none added (30 failures
across 28 families). Report:
`target/iteration/stage26-dynamic-generator-program-2026-09-25.log`. Stage 26
remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and
94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 async/generator constructibility invariant

The compiled function record now derives `constructible` as false for async
and generator functions, regardless of the generic function-construction
option. Generator functions still receive their required own `prototype`
object through the separate generator rule; async functions do not acquire a
non-configurable `prototype` property. This lets the superclass test redefine
an async function's absent `prototype` and then correctly rejects it before
reading that property. The existing
`class/subclass/superclass-async-function.js` case passes. Full-stage
comparison improves from 9,307/9,337 to 9,308/9,337 with exactly that failure
removed and none added (29 failures across 27 families). Report:
`target/iteration/stage26-async-nonconstructible-2026-09-25.log`. Stage 26
remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and
94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 null-extending class constructor prototype

For `class C extends null`, lowering still sets `C.prototype`'s parent to
`null`, but now leaves the class constructor function's own parent at its
function-creation default (`%Function.prototype%`). Non-null heritage continues
to set both links to the base constructor and its `.prototype`. The existing
`class-definition-null-proto.js` case passes. Full-stage comparison improves
from 9,308/9,337 to 9,309/9,337 with exactly that failure removed and none
added (28 failures across 26 families). Report:
`target/iteration/stage26-null-class-prototype-2026-09-25.log`. Stage 26 remains
active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94
batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 body `var arguments` receives the arguments object

Function compilation now uses the formal-parameter boundary to distinguish a
formal parameter named `arguments` from a body `var arguments` binding. The
former continues to shadow the implicit binding; for the latter, the
arguments-object slot points at the already-collected body local instead of
being omitted. This keeps the implicit binding and local inventory represented
once and preserves the non-simple-parameter path. The existing
`function/S13_A15_T4.js` case passes, and the full `function/` cohort has no new
failures. Full-stage comparison improves from 9,309/9,337 to 9,310/9,337 with
exactly that failure removed and none added (27 failures across 25 families).
Report: `target/iteration/stage26-body-var-arguments-2026-09-25.log`. Stage 26
remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and
94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 PromiseResolve constructor lookup in async iteration

`promise_for_value` now performs `Get(value, "constructor")` before reusing an
internal Promise and takes the same-promise fast path only when that value is
the Promise constructor. This keeps the optimization behind the exact
observable identity guard required by PromiseResolve. Async-from-sync iterator
continuation now uses this path and directly rejects its result capability if
that lookup is abrupt, avoiding an extra rejected-promise reaction turn. The
existing `for-await-of/` cohort passes 1,234/1,234, including both constructor
lookup-order cases and the abrupt-completion catch case. Full-stage comparison
improves from 9,310/9,337 to 9,313/9,337 with exactly those three failures
removed and none added (24 failures across 22 families). Report:
`target/iteration/stage26-promise-resolve-constructor-2026-09-25.log`. Stage 26
remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs and
94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 async-generator queued return operations

Async-generator requests now distinguish `Next` from `Return`. A return made
while execution is already running is queued instead of throwing “generator
is already running”; when dispatched, its value is PromiseResolved before the
completed iterator result is produced. For `yield*`, the return argument is
awaited before the delegated iterator's `return` lookup. Full-stage comparison
improves from 9,313/9,337 to 9,315/9,337 with exactly
`yield-return-then-getter-ticks.js` and
`yield-star-return-missing-value-is-awaited.js` removed and none added (22
failures across 21 families). The companion
`yield-star-return-then-getter-ticks.js` now gets the awaited argument but still
has a remaining microtask-order mismatch before delegated `return`. Report:
`target/iteration/stage26-async-generator-return-queue-2026-09-25.log`. Stage
26 remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs
and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 function-scope binding visibility

Function local collection now preserves the existing slot inventory while
deriving a separate set of names visible in the function scope. Nested lexical
bindings and catch parameters keep their slots for stable layout, but only
resolve through their active lexical environment; closures capture function
bindings, active lexical bindings, and compiler-owned bindings from those same
authorities. This fixes optional-catch lexical leakage and the related `with`
var lookup without deleting or duplicating slot facts. The focused
`try/optional-catch-binding-lexical.js` case passes. Full-stage comparison
improves from 9,315/9,337 to 9,317/9,337 with exactly
`try/optional-catch-binding-lexical.js` and `with/scope-var-open.js` removed and
none added (20 failures across 19 families). Report:
`target/iteration/stage26-function-scope-binding-visibility-captured-internals-2026-09-25.log`.
Stage 26 remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 explicit async-generator return awaits

The compiler now emits `Await` for an explicit return expression in an async
generator before forming its Return completion. Bare `return;` and normal body
completion remain non-awaiting. This preserves the spec distinction in the
existing `return-undefined-implicit-and-explicit.js` ordering case, which now
passes; the async-generator cohort passes 296/301. Full-stage comparison
improves from 9,317/9,337 to 9,318/9,337 with exactly that case removed and none
added (19 failures across 18 families). Report:
`target/iteration/stage26-asyncgen-explicit-return-await-2026-09-25-full.log`.
Stage 26 remains active; the full-stage runner used
`TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94 batches capped at 100. No
harness changes or new tests.

### 2026-09-25 stage-26 delegated async-generator yield completions

Async `yield*` now preserves its distinct delegated-yield transition: once the
inner iterator result has been awaited, its `value` is yielded without a
second PromiseResolve/await. A queued Return can then be handled at that yield
boundary, retaining the required thenable-access order. Abrupt `done`/`value`
reads from delegated return/throw results resume the suspended generator with
a Throw completion so surrounding catch clauses observe the original value.
Prototype authority is now split among `%AsyncIteratorPrototype%`,
`%AsyncGeneratorPrototype%`, and the internal async-from-sync adapter prototype;
the adapter retains its `next` method without exposing its internal object
through the public base prototype. The existing async-generator cohort passes
301/301. Full-stage comparison improves from 9,318/9,337 to 9,323/9,337 with
exactly five failures removed and none added (14 failures across 14 families):
`yield-star-async-from-sync-iterator-inaccessible.js`,
`yield-star-promise-not-unwrapped.js`,
`yield-star-return-notdone-iter-value-throws.js`,
`yield-star-return-then-getter-ticks.js`, and
`yield-star-throw-notdone-iter-value-throws.js`. Report:
`target/iteration/stage26-async-generator-delegation-prototypes-2026-09-25-full.log`.
Stage 26 remains active; runners used
`TEST262_TEST_TIMEOUT_MS=900000`, four jobs and batches capped at 100. No
harness changes or new tests.

### 2026-09-25 stage-26 script `var` initializer reference capture

For non-module script-level `var` declarations with initializers, the compiler
now emits the existing `ResolveName` reference before evaluating the
initializer and uses `StoreResolvedName` afterward. This preserves the
selected `with` binding if the initializer deletes its property, matching the
single authoritative reference model already used by assignment lowering.
Function-local `var` and module declarations keep their own binding path. The
existing `variable/binding-resolution.js` case passes; the broader
`variable/` cohort is 177/178 with only the separate global-var property case
remaining. Full-stage comparison improves from 9,323/9,337 to 9,324/9,337
with exactly `variable/binding-resolution.js` removed and none added (13
failures across 13 families). Report:
`target/iteration/stage26-script-var-with-reference-2026-09-25-full.log`.
Stage 26 remains active; the full-stage runner used
`TEST262_TEST_TIMEOUT_MS=900000`, four jobs and 94 batches capped at 100. No
harness changes or new tests.

### 2026-09-25 stage-26 global-object-backed script `var` writes

Successful ordinary writes to an existing global-object property now refresh
the active script's corresponding `var` slot. That slot remains an execution
view; the global object continues to own the observable binding value. This
fixes `variable/S12.2_A11.js`, where assigning `this["__declared__var"]` before
the later declaration had left the captured slot undefined. Full stage 26
improves from 9,324/9,337 to 9,325/9,337: exactly that failure is removed and
none are added (12 failures across 12 families). Report:
`target/iteration/stage26-global-var-object-binding-2026-09-25-full.log`.
Stage 26 remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 inherited indexed setters

Indexed assignment now checks an inherited accessor before taking array
element fast paths, invoking the setter with the array as receiver. This fixes
the `for-in/head-lhs-let.js` MemberExpression target, which assigns each key to
an array index inherited from `Array.prototype`. The focused case passes and
the existing `for-in/` cohort is 114/115; its only failure is the previously
known deleted-key enumeration case. Full stage 26 improves from 9,325/9,337 to
9,326/9,337: exactly `for-in/head-lhs-let.js` is removed and none are added
(11 failures across 11 families). Report:
`target/iteration/stage26-inherited-index-setter-2026-09-25-full.log`. Stage
26 remains active; the runner used `TEST262_TEST_TIMEOUT_MS=900000`, four jobs
and 94 batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 captured environment references and proxy receivers

Name reads through a captured object environment now have a dedicated
`LoadResolvedName` operation: after reference resolution it performs the
Object Environment Record's `HasProperty` check and then `Get`, without
re-running `@@unscopables`. This also makes compound assignment preserve its
single captured reference. `StoreResolvedName` now performs the required
`HasProperty` check even in non-strict code. `OrdinarySetWithOwnDescriptor`
queries a proxy receiver's own descriptor and defines the new data value
through its `defineProperty` trap instead of recursively invoking `set`.
Focused proxy cases pass; the existing `with/` cohort is 180/181 with only the
previously known typed-array-prototype binding case failing. Full stage 26
improves from 9,326/9,337 to 9,329/9,337: exactly
`with/set-mutable-binding-idref-compound-assign-with-proxy-env.js`,
`with/set-mutable-binding-idref-with-proxy-env.js`, and
`with/unscopables-inc-dec.js` are removed and none are added (8 failures across
8 families). Report:
`target/iteration/stage26-proxy-environment-reference-2026-09-25-full.log`.
Stage 26 remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs and batches capped at 100. No harness changes or new tests.

### 2026-09-25 stage-26 for-loop per-iteration environment timing

Lexical `for` loops now clone the per-iteration environment after initializer
evaluation and before the first test. Subsequent clones remain between the
body and increment expression, so test/body closures share the active
iteration while body closures remain isolated from later iterations. The
existing `for/scope-body-lex-open.js` case and `for/` cohort pass 385/385.
Full stage 26 improves from 9,329/9,337 to 9,330/9,337: exactly that failure
is removed and none are added (7 failures across 7 families). Report:
`target/iteration/stage26-loop-per-iteration-environment-2026-09-25-full.log`.
Stage 26 remains active; runners used `TEST262_TEST_TIMEOUT_MS=900000`, four
jobs and batches of at most 100. No harness changes or new tests.

### 2026-09-25 stage-26 live `for...in` enumerability

`for...in` still takes its deterministic key snapshot up front, but now checks
each key's current own descriptor along the source object's prototype chain
before yielding it. This ports the old runtime's mutation-sensitive
enumerability rule onto the new runtime's canonical descriptor and prototype
operations. The original source identity is retained in a hidden activation
local; iterator values and the source are not interchangeable register state.
The existing deleted-property case now passes, and the existing `for-in/`
cohort passes 115/115. Full stage 26 improves from 9,330/9,337 to 9,331/9,337:
exactly `for-in/S12.6.4_A7_T2.js` is removed and no new failures appear (six
remaining cases across six families). Evidence:
`target/iteration/stage26-for-in-live-enumerability-2026-09-25-full.log`.
Every invocation used `TEST262_TEST_TIMEOUT_MS=900000`; the stage runner used
four jobs and 94 batches capped at 100. No harness changes, added tests, or
unit/project tests were made/run.

### 2026-09-25 stage-26 `var` initializer references inside active `with`

`var` initializer lowering now captures the existing dynamic object reference
when the declaration executes under a `with` entered by the current function.
The compiler's `inherited_with_depth` distinguishes that active scope from a
`with` environment only captured by a closure, keeping function-local
initializers local in the latter case. The existing
`function/S13.2.2_A17_T3.js` case passes; the entire existing `with/` cohort
remains 181/181. Full stage 26 improves from 9,332/9,337 to 9,333/9,337:
exactly that function case is removed and no new failures appear (four cases
across four families). Evidence:
`target/iteration/stage26-with-var-reference-2026-09-25-full.log`. Every
invocation used `TEST262_TEST_TIMEOUT_MS=900000`; the stage run used four jobs
and 94 batches capped at 100. No harness changes, added tests, or unit/project
tests were made/run.

### 2026-09-25 stage-26 abrupt `finally` completion values

Statement completion now models a `finally` body as its own current completion
value plus the enclosing completion destination. Normal finalizer completion
preserves the prior try/catch completion; `break` and `continue` propagate the
finalizer's current value before taking their control edge. This fixes the
existing direct-eval `try/finally` completion-value case without changing
ordinary completions. The existing `try/` cohort is 200/201, with only the
previously known catch-binding `ReferenceError` case remaining. Full stage 26
improves from 9,333/9,337 to 9,334/9,337: exactly
`try/completion-values.js` is removed and no new failures appear (three cases
across three families). Evidence:
`target/iteration/stage26-finally-abrupt-completion-2026-09-25-full.log`.
Every invocation used `TEST262_TEST_TIMEOUT_MS=900000`; the full-stage runner
used four jobs and 94 batches capped at 100. No harness changes, added tests,
or unit/project tests were made/run.

### 2026-09-25 stage-26 inherited typed-array `[[Set]]`

Object-environment writes now preserve the typed-array exotic `[[Set]]` result
when a canonical numeric property name reaches an inherited typed array.
In-range indices write through the typed-array operation; canonical but invalid
indices reject the write, which sloppy `SetMutableBinding` ignores rather than
recreating an ordinary property on the binding object. The local Node oracle
and the existing typed-array-prototype binding case agree that deleted
`"NaN"` remains absent. The focused case passes and the existing `with/` cohort
passes 181/181. Full stage 26 improves from 9,331/9,337 to 9,332/9,337: exactly
`with/set-mutable-binding-binding-deleted-with-typed-array-in-proto-chain.js`
is removed and no new failures appear (five cases across five families).
Evidence:
`target/iteration/stage26-typed-array-binding-set-2026-09-25-full.log`. Every
invocation used `TEST262_TEST_TIMEOUT_MS=900000`; the full-stage runner used
four jobs and 94 batches capped at 100. No harness changes, added tests, or
unit/project tests were made/run.

### 2026-09-25 stage-26 strict direct-eval `var` bindings

Strict direct eval now gives its `var` declarations an eval-owned dynamic
binding for the duration of the eval, then initializes that binding instead
of mutating or dropping the declaration's initializer. The parser produces
one named `EvalVarNames` view containing both var-bound names and the broader
declaration names; cleanup, conflict checks, and strict-eval setup derive from
that shared parse. The existing `function/13.0-` cohort passes 8/8. Full stage
26 improves from 9,335/9,337 to 9,336/9,337, leaving only
`class/subclass/private-class-field-on-nonextensible-return-override.js`.
Evidence: `target/iteration/stage26-strict-eval-var-bindings-2026-09-25-full.log`.
Every runner used `TEST262_TEST_TIMEOUT_MS=900000`; the full-stage runner used
four jobs and 94 batches capped at 100. No harness changes, added tests, or
unit/project tests were made/run.

### 2026-09-25 stage-26 inactive catch binding aliases

Catch-clause source-name slots are now placed in the uninitialized state when
the catch scope is entered, unless that name already denotes a function-scope
binding. The eval-frame fallback ignores deleted slots, leaving active catch
bindings to resolve through their canonical lexical environment and preventing
the expired catch alias from leaking after the clause. The focused existing
`try/12.14-7.js` case passes, and the existing `try/` cohort passes 201/201,
including the three TCO catch/finally cases checked during this change. Full
stage 26 improves from 9,334/9,337 to 9,335/9,337, with only
`function/13.0-17-s.js` and
`class/subclass/private-class-field-on-nonextensible-return-override.js`
remaining; no new failures appear. Evidence:
`target/iteration/stage26-catch-scope-inactive-2026-09-25-full.log`. Every
invocation used `TEST262_TEST_TIMEOUT_MS=900000`; the full-stage runner used
four jobs and 94 batches capped at 100. No harness changes, added tests, or
unit/project tests were made/run.

### 2026-09-25 stage-26 private element home lookup

Private reads now retain the original receiver for brand checks and use the
brand's declaring home as a fallback only after ordinary receiver/prototype
lookup is exhausted. That lets return-overridden derived constructors resolve
private methods and accessors without bypassing normal accessor descriptors or
field storage. The existing `class/` cohort passes 4,367/4,367. The active
stage 26 gate now passes 9,337/9,337 across 94 batches capped at 100; stage 27
(`language/types`) is next. Evidence:
`target/iteration/stage26-private-element-home-resolution-2026-09-25-full.log`.
Every runner used `TEST262_TEST_TIMEOUT_MS=900000`; full-stage verification used
four jobs. No harness changes, added tests, or unit/project tests were made/run.

### 2026-09-25 stage-27 global writes, primitive realms, and builtin descriptors

Sloppy assignments to readonly global var properties now follow the canonical
`[[Set]]` result without mutating the backing root slot, while strict writes
still throw. Primitive property reads and writes resolve prototypes from the
active realm; inherited proxy setters receive the original primitive receiver.
The realm factory now provides realm-owned Number, String, Boolean, and Symbol
constructors/prototypes, and Number/function builtin properties share the
non-enumerable descriptor helper. Stage 27 (`language/types`) passes 113/113
in two batches (100 and 13). Evidence:
`target/iteration/stage27-language-types-2026-09-25.log`. Every runner used
`TEST262_TEST_TIMEOUT_MS=900000`; stage verification used four jobs. No harness
changes, added tests, or unit/project tests were made/run. Stage 28 is next.

### 2026-09-25 stage-28 white space

The next sequential Test262 gate, stage 28 (`language/white-space`), passes
67/67 in one batch. Evidence:
`target/iteration/stage28-language-expressions-2026-09-25.log`. The run used
`TEST262_TEST_TIMEOUT_MS=900000` and four jobs. No runtime changes, harness
changes, added tests, or unit/project tests were needed. Stage 29 is next.

### 2026-09-25 stage-29 AbstractModuleSource tag accessor

`%AbstractModuleSource%.prototype[Symbol.toStringTag]` now has the specified
getter descriptor and derives its `"ModuleSource"` result from membership in
the VM's authoritative module-source identity map; non-source receivers return
`undefined`. The stage 29 (`built-ins/AbstractModuleSource`) gate passes 8/8.
Evidence: `target/iteration/stage29-abstract-module-source-2026-09-25.log`.
The run used `TEST262_TEST_TIMEOUT_MS=900000` and four jobs. No harness changes,
added tests, or unit/project tests were made/run. Stage 30 is next.

### 2026-09-25 stage-30 AggregateError constructor semantics

`AggregateError` now converts an optional message before consuming its errors
iterable, stores that list as the non-enumerable own `errors` property, and
installs an own `cause` only when options has that property. Shared error
intrinsic installation gives AggregateError the standard constructor/prototype
descriptors, Error-constructor inheritance, and prototype message; construction
fallback uses the `new.target` realm's intrinsic AggregateError prototype.
Stage 30 (`built-ins/AggregateError`) passes 25/25. Node's local oracle matched
the observable descriptors and argument order. Evidence:
`target/iteration/stage30-aggregate-error-2026-09-25-rerun3.log`. The run used
`TEST262_TEST_TIMEOUT_MS=900000` and four jobs. No harness changes, added tests,
or unit/project tests were made/run. Stage 31 is next.

### 2026-09-25 stage-31 Array baseline and shared array-like operations

The stage 31 (`built-ins/Array`) inventory is 3,081 cases. Its first bounded
sweep was 1,699/3,081, with 1,382 failures across 319 families. After shared
array-like, metadata, and async-array work, the full stage rerun is 2,375/3,081
with 706 failures across 272 families. The wide-instruction stability case has
since been fixed: closure-opcode remapping now treats packed `Wide` references
as immutable indices into the separate wide-instruction table instead of
re-encoding them as ordinary instruction fields. After array-like `push`/`pop`,
generic `slice` species, and BigInt typed-array conversion were unified, the
latest bounded full rerun passes 2,523/3,081 with 558 failures across 240
families. This sweep includes array-like `shift`/`unshift`; the preceding
array-like sweep passed 2,493/3,081. The intermediate wide-marker rerun passed
2,376/3,081. Evidence:
`target/iteration/stage31-array-wide-marker-rerun-2026-09-25.log` and
`target/iteration/stage31-array-shared-arraylike-full-rerun-2026-09-25.log` and
`target/iteration/stage31-array-mutation-family-full-rerun-2026-09-25.log`.
The largest remaining families are
generic callback methods (`forEach`/`some`/`every`/`map`), TypeError-vs-Error
propagation, failed indexed writes to result arrays, species construction, and
resizable typed-array iteration. The dominant families were generic array-like
receivers in `reduce` (250), `indexOf` (88),
and `lastIndexOf` (86). These methods now share generic object/length/property
semantics instead of requiring an Array cell, and index search skips holes via
`HasProperty`. Focused existing cohorts currently pass: reduce 217/260,
indexOf 182/201, and lastIndexOf 179/198; their remaining failures are being
grouped into follow-up families. Dense array storage now switches to sparse
storage based on the represented hole gap, and the existing exponentially
spaced-index case passes without dense expansion. The first stage run's copy of
that case was terminated after its dense allocation reached 1.4 GB; it was
rerun after the storage fix and passed. Evidence:
`target/iteration/stage31-next-2026-09-25.log`,
`target/iteration/stage31-sparse-index-focused-2026-09-25.log`, and the three
`stage31-array-*-generic-2026-09-25.log` cohort reports. Every invocation used
`TEST262_TEST_TIMEOUT_MS=900000`; the full stage used 31 batches capped at 100,
and focused cohorts remained similarly bounded. No harness changes, added
tests, or unit/project tests were made/run. Stage 31 remains in progress.

The builtin metadata family is also reduced: Array builtin function names and
lengths now follow the old core's function-metadata table, and the Array
prototype is represented as an actual empty Array exotic with its standard
constructor descriptors, species getter, and null-prototype unscopables data.
Existing metadata cohorts currently pass: lengths 42/43, names 41/42,
descriptors 37/38, and unscopables 4/4. The previously absent
`Array.fromAsync` intrinsic is now implemented through a Promise-job
continuation state in the next VM, with old-core async-from-sync and constructor
semantics adopted. It performs the first iterator step before returning, then
resumes after Promise jobs so mutations between the call and `await` are
observable. The existing 95-case Test262 cohort passes 95/95. Numeric object
accessor keys are lowered through OXC's expression-key path; `ToNumber(BigInt)`
now correctly throws while the `Number` constructor retains its specified
BigInt conversion. BigInt64/BigUint64 typed-array reads and writes preserve
BigInt values, including 64-bit modulo encoding. Array prototype `push` and
`pop` now share generic `LengthOfArrayLike`, indexed `Set`/`Get`/`Delete`, and
strict length-write semantics across arrays and ordinary array-like receivers;
their focused cohorts pass 24/24 and 23/23. `slice` now performs species
construction and generic `HasProperty`/`Get`/CreateDataProperty steps, passing
70/71 focused cases; the remaining case depends on the not-yet-installed
cross-realm Array intrinsic in `$262.createRealm()`. `shift` and `unshift` use
the same shared array-like authority and pass 20/20 and 22/22 focused cases;
unshift skips index movement when there are no arguments, preserving the
bounded transition for maximum-safe lengths. Generator return dispatch
matches the suspended instruction so `finally` runs at a yield boundary.
Evidence:
`target/iteration/stage31-array-builtin-lengths-rerun-2026-09-25.log`,
`target/iteration/stage31-array-builtin-names-rerun-2026-09-25.log`,
`target/iteration/stage31-array-builtin-descriptors-rerun-2026-09-25.log`, and
`target/iteration/stage31-array-unscopables-rerun-2026-09-25.log`,
`target/iteration/stage31-array-from-async-complete-2026-09-25.log`, and
`target/iteration/stage31-array-full-rerun-2026-09-25.log`,
`target/iteration/stage31-array-push-unified-rerun4-2026-09-25.log`,
`target/iteration/stage31-array-pop-unified-rerun4-2026-09-25.log`,
`target/iteration/stage31-array-shift-arraylike-rerun-2026-09-25.log`,
`target/iteration/stage31-array-unshift-arraylike-rerun-2026-09-25.log`,
`target/iteration/stage31-array-slice-species-rerun-2026-09-25.log`, and
`target/iteration/stage31-array-mutation-family-full-rerun-2026-09-25.log`.
All Test262 runs had
`TEST262_TEST_TIMEOUT_MS=900000`; the full stage used 31 batches capped at 100.
No harness edits, added tests, or unit/project tests were made/run. Stage 31
remains in progress.

### 2026-09-25 Array authority and generic mutation slice

The latest bounded stage-31 sweep passes 2,833/3,081 (248 failures across 120
families), improving the preceding full sweep by 35 cases. The sequential
prefix gate remains at stage 4; this Array sweep is targeted family work and
does not advance that gate. Array literal writes now have a dedicated residual
operation for CreateDataProperty semantics, so an inherited indexed setter
cannot intercept a literal's own element. A shared own-index predicate now
feeds indexed writes and `HasProperty`. ArraySetLength adopts the old core's
two observable numeric coercions and separates a rejected definition from an
abrupt completion; strict assignment, `Reflect.set`, and `Object.defineProperty`
map that outcome at their respective boundaries. Arguments length reads its
ordinary property, and Arguments/Math/JSON tags use the expected `@@toStringTag`
brands.

Array `sort`/`toSorted`, `toReversed`, `toSpliced`, and `with` now use generic
array-like property operations; `splice` ports the legacy hole-aware movement
and species algorithm onto the next VM's internal methods. Existing focused
cohorts pass: `forEach` 190/190, `some` 219/219, `every` 218/218, `sort`
54/54, `toReversed` 17/17, `toSpliced` 30/30, `toSorted` 21/21, and `with`
21/21. `splice` passes 78/81; its remaining cases concern maximum-length
species ordering, Proxy property-trap order, and cross-realm species. Evidence:
`target/iteration/stage31-array-authority-full-rerun5-2026-09-25.log`,
`target/iteration/stage31-array-forEach-authority-rerun2-2026-09-25.log`,
`target/iteration/stage31-array-some-authority-rerun2-2026-09-25.log`,
`target/iteration/stage31-array-every-authority-rerun2-2026-09-25.log`,
`target/iteration/stage31-array-sort-arraylike-rerun2-2026-09-25.log`,
`target/iteration/stage31-array-toReversed-arraylike-rerun2-2026-09-25.log`,
`target/iteration/stage31-array-toSpliced-arraylike-rerun2-2026-09-25.log`,
`target/iteration/stage31-array-toSorted-arraylike-rerun2-2026-09-25.log`,
`target/iteration/stage31-array-with-arraylike-rerun3-2026-09-25.log`, and
`target/iteration/stage31-array-splice-arraylike-rerun-2026-09-25.log`. Every
runner invocation set `TEST262_TEST_TIMEOUT_MS=900000`; full-stage batches were
capped at 100 cases. The incremental runner build required enabling Chrono's
`clock` feature for the next runtime's `Local` timezone implementation. The
Test262 harness remains untouched; no tests were added and no unit/project
tests were run.

### 2026-09-25 current ordered-prefix status

Fresh cumulative `run-stages-next 0 20` attempts pass stages 0–3 but stop at
stage 4 (`language/comments`): 50/52 pass, while `S7.4_A5.js` and `S7.4_A6.js`
each time out at `TEST262_TEST_TIMEOUT_MS=900000`. This is true in both the
incremental and release-profile runs, so older stage-4 green evidence does not
currently prove the prefix. The next-core eval path already recognizes
comment-only source; it now performs that proof directly over UTF-16 units
instead of allocating UTF-8 text first. This optimization has not yet brought
the two loop-heavy cases under the timeout. The next action is to fix the VM's
execution/collection cost before rerunning stage 4; stages after 4 remain
unstarted in the current prefix attempt. The explicit 3,600,000 ms rerun was
stopped after 38 minutes with both cases still active and no result. A symbolized
profile of `S7.4_A6.js` put essentially all sampled CPU in `Vm::collect_slow` /
`Heap::collect`; a single-case profile grew to about 1.8 GB RSS in roughly a
minute. The UTF-16 eval fast path is therefore not the current bottleneck.
Evidence:
`target/iteration/test262-next-prefix-0-20-array-authority-rerun-2026-09-25.log`,
`target/iteration/test262-next-prefix-0-20-array-authority-release-rerun-2026-09-25.log`,
and `target/iteration/stage4-comments-utf16-fastpath-rerun-2026-09-25.log`,
`/tmp/quench-stage4-profile.sample.txt`, and
`target/iteration/stage4-a6-memory-profile.log`. The opt-in memory-profile
feature also needed an exhaustive-size case for `ArrayFromAsyncState`; its
accounted inline payload is zero. No Test262 harness changes or added tests; no
unit/project tests were run.

### 2026-09-25 latest ordered-prefix and stringification update

The latest cumulative next-core run passes stages 0–30 sequentially and stops
at Stage 31 (`built-ins/Array`), now 3,045/3,081 (36 failures across 35
families). It used `TEST262_TEST_TIMEOUT_MS=900000`, four workers, and batches
no larger than 100. The ordered-prefix report is
`target/iteration/test262-next-prefix-0-31-array-stringification-2026-09-25.log`;
the isolated Stage 31 report is
`target/iteration/stage31-array-stringification-rerun-2026-09-25.log`.

Array `toString` now gets and calls `join`, falling back to
`Object.prototype.toString` when `join` is not callable. `toLocaleString` now
gets each element's method and invokes it with no arguments, while skipping
nullish elements; `Object.prototype.toLocaleString` and primitive-wrapper
brands support that dispatch. Built-in collection tags now come from the
prototypes' `@@toStringTag` properties, so deleting `Set.prototype`'s tag
produces `[object Object]` rather than retaining a duplicated hardcoded brand.
The existing Test262 `toLocaleString` (12 cases) and `toString` (11 cases)
cohorts both pass. Test262 harness remains untouched; no tests were added and
no unit/project tests were run.

### 2026-09-25 Array constructor frontier update

After Array constructor and iterator-close fixes, the cumulative prefix still
passes stages 0–30 and reaches Stage 31 at 3,065/3,081 (16 cases across 16
families). Fresh evidence is
`target/iteration/test262-next-prefix-0-31-array-from-of-2026-09-25.log` and
`target/iteration/stage31-array-from-of-isArray-rerun-2026-09-25.log`.
`Array.from` now obtains the iterator method before constructing its result,
constructs before invoking the iterator method, writes incrementally through
`CreateDataProperty`, closes on abrupt steps while preserving the original
throw completion, and sets the final length. Array-like sources pass their
length to custom constructors. `Array.of` now constructs with the argument
count and performs the required final length Set. `Array.isArray` shares one
recursive operation across direct, Proxy, and Arguments cases. Existing
Test262 cohorts pass: `Array/from` 142/142, `Array/of` 16/16, and
`Array/isArray` 29/29. No harness modifications, added tests, or unit/project
test runs.

### 2026-09-25 computed object-literal property update

Computed and dynamic object-literal data properties now use the VM's own-data
property definition path rather than ordinary assignment. This preserves the
literal's `CreateDataProperty` behavior when `Object.prototype` has an indexed
setter. The existing Test262 cases
`indexOf/15.4.4.14-9-b-i-6.js` and
`lastIndexOf/15.4.4.15-8-b-i-6.js` both pass individually. A fresh bounded
Stage 31 run passes 3,067/3,081 (14 remaining cases), compared with 3,065/3,081
before this change; its report is
`target/iteration/stage31-object-literal-own-properties-rerun-2026-09-25.log`.
The runner used `TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and its existing
100-case batch cap. No harness changes, tests added, or unit/project tests
were run.

### 2026-09-25 flatMap callback binding update

`Array.prototype.flatMap` now calls the mapper with its second argument as
`thisArg`, matching the existing `quench-runtime` algorithm. The existing
Test262 `flatMap/thisArg-argument.js` case passes. Fresh Stage 31 evidence is
3,068/3,081, with 13 cases remaining, in
`target/iteration/stage31-flatMap-thisArg-rerun-2026-09-25.log`; this runner
used a required 900,000 ms timeout and batches capped at 100. No harness
changes, tests added, or unit/project tests were run.

### 2026-09-25 flat ordering and Proxy key update

`Array.prototype.flat` now reads the source length before depth coercion and
species construction, uses its default depth for both omitted and explicit
`undefined`, and passes the captured source length into flattening so a Proxy
does not observe a second `length` read. Proxy `[[HasProperty]]` now converts
trap keys to PropertyKey before calling the `has` trap. These changes port the
ordering and key boundary already present in the legacy core. Existing Stage 31
cohorts pass: `flat/` 19/19 and `flatMap/` 24/24. The fresh Stage 31 sweep is
3,071/3,081 (10 remaining cases), recorded in
`target/iteration/stage31-flat-proxy-property-key-rerun-2026-09-25.log`. The
runner used a 900,000 ms timeout, ten workers, and batches capped at 100. No
harness changes, tests added, or unit/project tests were run.

### 2026-09-25 includes empty-length update

`Array.prototype.includes` now returns false immediately after observing a zero
length, before coercing `fromIndex`, matching the legacy core's shared search
algorithm. The existing Test262
`includes/length-zero-returns-false.js` case passes. Fresh Stage 31 evidence is
3,072/3,081 (9 remaining cases) in
`target/iteration/stage31-includes-empty-length-rerun-2026-09-25.log`, using
`TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and batches capped at 100. The
Test262 harness is unchanged; no tests were added and no unit/project tests
were run.

### 2026-09-25 Arguments length definition update

`Object.defineProperty` now routes an Arguments object's `length` through
ordinary own-property definition instead of the Array exotic `length`
algorithm. The existing Test262 `concat/Array.prototype.concat_` cohort passes
24/24, including the strict and sloppy Arguments length-extension cases. Fresh
Stage 31 evidence is 3,075/3,081 (6 remaining cases) in
`target/iteration/stage31-arguments-length-rerun-2026-09-25.log`; the runner
used a 900,000 ms timeout, ten workers, and batches capped at 100. No harness
changes, tests added, or unit/project tests were run.

### 2026-09-25 typed-array fill coercion update

Typed-array indexed Set now rechecks the view's current length after converting
the value, so a resizable buffer shrink during conversion leaves an out-of-bounds
write unapplied. The `BigInt` constructor now uses number-hint ToPrimitive and
the shared exact Number-to-BigInt conversion, following the legacy
`explicit_bigint` path. Existing Test262
`fill/typed-array-resize.js` passes. Fresh Stage 31 evidence is 3,076/3,081
(5 remaining cases) in
`target/iteration/stage31-typed-array-fill-resize-rerun-2026-09-25.log`, with
900,000 ms per-case timeout and batches capped at 100. No harness changes, tests
added, or unit/project tests were run.

### 2026-09-25 cross-realm Array prototype update

Array construction now derives the fallback `%Array.prototype%` from
`newTarget`'s realm when `newTarget.prototype` is not an object, and avoids a
second observable prototype read after construction. The existing
`proto-from-ctor-realm-` Test262 cohort passes 13/13. Fresh Stage 31 evidence is
3,079/3,081 (2 remaining cases) in
`target/iteration/stage31-cross-realm-array-prototype-rerun-2026-09-25.log`;
the runner used a 900,000 ms timeout, ten workers, and batches capped at 100.
No harness changes, tests added, or unit/project tests were run.

### 2026-09-25 object property function constructability update

OXC's `ObjectProperty.method` fact now distinguishes concise methods from
ordinary function-valued properties. Only concise methods receive method home
object semantics and lose `[[Construct]]`; initializer function expressions
remain ordinary constructible functions. This restores the custom
`Symbol.species` constructor in the existing large-length `splice` Test262
case. Fresh Stage 31 evidence is 3,080/3,081 (one remaining case) in
`target/iteration/stage31-object-property-method-constructor-rerun-2026-09-25.log`.
The runner used a 900,000 ms per-case timeout, ten workers, and batches capped
at 100. No harness changes, tests added, or unit/project tests were run.

### 2026-09-25 Proxy receiver and trap-fallback update

Proxy `[[Set]]` without a `set` trap now continues OrdinarySet with the
original receiver, porting the legacy core's receiver semantics. Proxy receiver
data definition forwards directly to the target after the single
`defineProperty` trap lookup; it no longer re-reads the handler property during
fallback. Removed an invalid receiver/handler identity check that rejected
nested proxy chains as revoked. The existing Test262 cases
`Array/prototype/splice/property-traps-order-with-species.js` and
`Array/prototype/splice/create-proxy.js` both pass individually. Stage 31 passes
3,081/3,081 in 31 batches; cumulative ordered stages 0–31 pass 26,941/26,941.
Both commands used `TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and batches
capped at 100. The Test262 harness is unchanged; no tests were added and no
unit/project tests were run.

### 2026-09-25 complete ordered stage 32

Stage 32, `built-ins/ArrayBuffer`, passes 221/221. ArrayBuffer and
SharedArrayBuffer now own distinct prototypes, accessor descriptors and
constructor links; the next core ports the legacy core's ToIndex, transfer,
resize, detached-buffer, and immutable-buffer behavior. The Test262 host's
existing `$262.detachArrayBuffer` capability now reaches the VM without changing
the harness. Cross-realm ArrayBuffer construction uses the `newTarget` realm's
prototype fallback. A shared relative-index correction also preserves `-0`
after truncation. The ordered cumulative 0–32 gate passes 27,162/27,162. Both
runs used `TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and batches capped at
100. `cargo check -p quench-runtime-next` and `cargo fmt --all --check` pass;
the Test262 harness is unchanged, no tests were added, and no unit/project tests
were run.

### 2026-09-25 complete ordered stage 33

Stage 33, `built-ins/ArrayIteratorPrototype`, passes 27/27. Array iterators now
use a distinct `%ArrayIteratorPrototype%` with an own, non-enumerable `next`,
the correct constructor and `@@toStringTag`; its dedicated `next` validates
receivers before using the shared iterator transition. Typed-array iterators
throw after their backing buffer is detached. TypedArray construction now uses
the object's iterator when present, otherwise its array-like length and indexed
values, porting the legacy core's source-value path. The ordered cumulative
0–33 gate passes 27,189/27,189. Both runs used
`TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and batches capped at 100. The
Test262 harness is unchanged, no tests were added, and no unit/project tests
were run.

### 2026-09-25 complete ordered stage 34

Stage 34, `built-ins/AsyncDisposableStack`, passes 104/104. Async disposal is a
variant of the existing disposal-stack state machine: shared LIFO records and
promise continuation are retained, while async resource use/adopt/defer mark
their await transitions explicitly. The new intrinsic has realm-derived
construction prototypes, an independent prototype and brand, and its
`disposed`, `move`, and `disposeAsync` operations. The ordered cumulative
0–34 gate passes 27,293/27,293. Both runs used
`TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and batches capped at 100. The
Test262 harness is unchanged, no tests were added, and no unit/project tests
were run.

### 2026-09-25 complete ordered stage 35

Stage 35, `built-ins/AsyncFromSyncIteratorPrototype`, passes 38/38. The
async-from-sync continuation now carries the wrapped iterator and close policy
through fulfillment and rejection, unwraps yielded Promise values, records
completion, and closes on the specified rejection paths. Async-generator
delegation reuses that continuation for sync iterators while preserving the
distinct abrupt-completion behavior for direct async iterators. The two affected
Stage 26 `yield*` cases also pass. The ordered cumulative 0–35 gate passes
27,331/27,331. Runners used `TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and
batches capped at 100. The Test262 harness is unchanged, no tests were added,
and no unit/project tests were run.

### 2026-09-25 complete ordered stage 36

Stage 36, `built-ins/AsyncFunction`, passes 18/18. Async, generator and
async-generator function constructors now have the correct own names and
lengths, non-configurable prototype properties, Function constructor parent,
prototype constructor links, and `@@toStringTag`. The ordered cumulative 0–36
gate passes 27,349/27,349. Both runs used `TEST262_TEST_TIMEOUT_MS=900000`, ten
workers, and batches capped at 100. The Test262 harness is unchanged, no tests
were added, and no unit/project tests were run.

### 2026-09-25 complete ordered stage 37

Stage 37, `built-ins/AsyncGeneratorFunction`, passes 23/23. Dynamic function
prototypes and constructors now come from the correct realm, `newTarget`'s
realm supplies the fallback function prototype, and async-generator
continuations retain their creation realm across resumption. Stages 35–37 pass
sequentially (79/79) after these changes. The previously recorded cumulative
0–36 total is 27,349/27,349; cumulative 0–37 was not rerun. The stage command
used `TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and batches capped at 100.
The Test262 harness is unchanged, no tests were added, and no unit/project
tests were run.

### 2026-09-25 complete ordered stage 38

Stage 38, `built-ins/AsyncGeneratorPrototype`, passes 48/48. Async-generator
`next`, `return`, and `throw` are installed as realm-local branded methods with
the required prototype descriptors. The request queue now keeps an awaited
`return` request active until its value settles; its completion is delivered
before queued requests resume, matching the async-generator state transition.
The ordered regression sweep of stages 35–38 passes 127/127, with each stage
kept as its own batch (all below the 100-case cap). The prior cumulative 0–37
total is 27,372/27,372 by adding the recorded Stage 37 result to the last
cumulative 0–36 run; cumulative 0–38 was not rerun. Runs used
`TEST262_TEST_TIMEOUT_MS=900000` and ten workers. The Test262 harness is
unchanged, no tests were added, and no unit/project tests were run.

### 2026-09-25 complete ordered stage 49

Stage 49, `built-ins/GeneratorFunction`, passes 23/23. The ordered regression
`run-stages-next 45 49` passes DisposableStack 93/93, Error 93/93,
FinalizationRegistry 47/47, Function 509/509, and GeneratorFunction 23/23
(765/765 total); every batch is capped at 100. Runs used
`TEST262_TEST_TIMEOUT_MS=900000` and ten workers. GeneratorFunction prototype
descriptors now match their intrinsic attributes, and each created realm's
generator-function prototype refers to that realm's generator prototype;
generator closures inherit from it and resolve the realm-owned iterator `next`
method. The Test262 harness was untouched; no tests were added and no
unit/project tests were run.

### 2026-09-25 complete ordered stage 50

Stage 50, `built-ins/GeneratorPrototype`, passes 61/61. The ordered regression
`run-stages-next 45 50` passes DisposableStack 93/93, Error 93/93,
FinalizationRegistry 47/47, Function 509/509, GeneratorFunction 23/23, and
GeneratorPrototype 61/61 (826/826 total); all batches are capped at 100. Runs
used `TEST262_TEST_TIMEOUT_MS=900000` and ten workers. The VM now models
`%Generator.prototype%` separately from `%IteratorPrototype%`, with
realm-local branded `next`/`return`/`throw`, standard method and constructor
descriptors, and the `Generator` toString tag. Generator instances inherit
those methods instead of receiving per-instance copies; reentrant resume errors
are TypeErrors, and returning from a completed generator preserves the supplied
value. The harness was untouched; no tests were added and no unit/project tests
were run.

### 2026-09-25 complete ordered stage 51; stage 52 triage

Stage 51 (`built-ins/Infinity`) passes 6/6. Stage 52 (`built-ins/Iterator`)
is the current blocker at 10/514 across six batches (100, 100, 100, 100, 100,
and 14). Failures group into 15 families, dominated by the missing global
`Iterator` (336 cases), unavailable/non-callable prototype methods (95 cases),
and errors masked by the missing intrinsic (51 cases). Further families cover
iterator receiver conversion, method descriptors, iterator closing, and
`Iterator.zip` completion ordering. Runs used
`TEST262_TEST_TIMEOUT_MS=900000` and ten workers. The harness was untouched; no
tests were added and no unit/project tests were run. Stage 52 remains active;
do not advance to stage 53 until it reaches 100%.

### 2026-09-25 complete ordered stage 39

Stage 39, `built-ins/AsyncIteratorPrototype`, passes 13/13. The VM now gives
each realm an `%AsyncIteratorPrototype%` inheriting from its `%IteratorPrototype%`,
with own non-enumerable `@@asyncIterator` and `@@asyncDispose` methods and
function metadata. `@@asyncDispose` ports the legacy core's sequence: get and
call `return`, PromiseResolve its result, then fulfill with `undefined`; getter,
call, and resolution abrupt completions become rejected promises. Stages 35–39
pass sequentially (140/140), one bounded batch per stage. Cumulative 0–39 was
not rerun. Runs used `TEST262_TEST_TIMEOUT_MS=900000` and ten workers. The
Test262 harness is unchanged, no tests were added, and no unit/project tests
were run. `cargo fmt --all --check` and
`cargo check --profile iteration -p quench-runtime-next` pass.

### 2026-09-25 complete ordered stage 40

Stage 40, `built-ins/Atomics`, passes 389/389 in four batches of 100, 100, 100,
and 89. `$262.agent` now uses the legacy core's cooperative same-VM execution
model, with realm-owned callback/report/waiter/timer state. Atomics wait,
waitAsync, and notify share waiter state; notification ordering, timeout report
updates, and the legacy bounded agent spin/lock transitions are represented
explicitly. Typed-array prototypes now own their corresponding constructor,
which allows the unchanged Test262 `atomicsHelper.js` to recover the right
shared typed-array constructor. The ordered stages 35–40 sweep passes 529/529
(38, 18, 23, 48, 13, and 389 cases, respectively), with each stage kept
separate and no batch above 100. Runs used
`TEST262_TEST_TIMEOUT_MS=900000` and ten workers. The Test262 harness is
unchanged, no tests were added, and no unit/project tests were run.
`cargo fmt --all --check`, `cargo check --profile iteration -p
quench-runtime-next`, and `git diff --check` pass.

### 2026-09-25 complete ordered stage 42

Stage 42, `built-ins/Boolean`, passes 51/51. Boolean now has realm-local
constructor/prototype/method metadata, the canonical constructor `prototype`
descriptor, TypeError receiver failures, Boolean.prototype's false primitive
behavior, and `newTarget`-realm prototype fallback. Its `@@toStringTag` is
installed in the shared post-Symbol phase, matching intrinsic dependency order.
The ordered regression sweep of stages 35–42 passes 657/657; batches were
capped at 100 (Stage 40 used four batches). Runs used
`TEST262_TEST_TIMEOUT_MS=900000` and ten workers. The Test262 harness is
unchanged, no tests were added, and no unit/project tests were run.

### 2026-09-25 complete ordered stage 41

Stage 41, `built-ins/BigInt`, passes 77/77. The new core now installs the
BigInt constructor/prototype descriptors and method metadata, implements
`asIntN`, `asUintN`, BigInt receiver branding and radix conversion, and exposes
realm-local BigInt/Object intrinsics through `createRealm`. ToBigInt string
parse failures now produce SyntaxError. Template substitutions use abstract
ToString through a hidden interpreter intrinsic instead of binary-add's
default-hint ToPrimitive path; ordinary ToPrimitive also continues to
`valueOf` when an inherited `toString` getter returns undefined. The ordered
stages 35–41 sweep passes 606/606, with batches capped at 100. Runs used
`TEST262_TEST_TIMEOUT_MS=900000` and ten workers. The Test262 harness is
unchanged, no tests were added, and no unit/project tests were run.

### 2026-09-25 complete ordered stage 43

Stage 43, `built-ins/DataView`, passes 561/561. The next core now installs the
full realm-local method/accessor surface, including Float16 and BigInt
operations, and uses abstract ToIndex conversion with the legacy core's
validation order. DataView views resolve `buffer`, `byteLength`, and
`byteOffset` through their canonical prototype accessors; the prior indexed
view shortcut bypassed detached/out-of-bounds semantics and was removed.
Construction now roots the new view across effectful `newTarget.prototype`
access and validates detach/range state afterward. The ordered regression sweep
of stages 35–43 passes 1,218/1,218, in batches capped at 100. Runs used
`TEST262_TEST_TIMEOUT_MS=900000` and ten workers. The Test262 harness is
unchanged, no tests were added, and no unit/project tests were run.

### 2026-09-25 Stage 52 Iterator implementation checkpoint

Stage 52 (`built-ins/Iterator`) advances from its 10/514 baseline to 293/514.
The new core now installs `Iterator` and its realm-local prototypes, wraps
protocol iterators, implements lazy map/filter/take/drop/flatMap/concat helpers,
terminal consumers, and iterator disposal. The concat family is 31/32; its
remaining boxed-primitive case is part of the current failures. The full stage
run used `TEST262_TEST_TIMEOUT_MS=900000`, ten workers, and six sequential
batches capped at 100. Remaining failures are grouped around constructor and
helper prototype identity (126), `Iterator.zip`/`zipKeyed` (58), wrapper next
lookup (13), abrupt close behavior and conversion order in take/drop, and
flatMap iterator acquisition. This is an in-progress checkpoint, not a stage
completion claim. The Test262 harness is unchanged; no tests were added and no
unit/project tests were run.

The next bounded sweep advances Stage 52 to 415/514 (99 failures), still using
six sequential batches capped at 100 and `TEST262_TEST_TIMEOUT_MS=900000`.
`Iterator` is now constructable as a superclass, wrapper `return` resolves the
base method at call time, and take/drop share the required receiver → numeric
validation → direct-iterator ordering. Both complete `take/` (33/33) and
`drop/` (34/34) slices pass. Remaining clusters are zip/zipKeyed (66), abrupt
closing/consumer completion (16), flatMap + generator receiver/iterator
acquisition, and three still-failing primitive-wrapper cases. The checkpoint
remains incomplete; harness untouched, no new tests and no unit/project tests.

The next full Stage 52 sweep advances to 456/514 (58 failures), with the same
positive 900,000 ms timeout and six sequential batches capped at 100. The
shared terminal-callback close path now handles abrupt consumers, and zip/
zipKeyed have an explicit lazy row state over eagerly acquired iterator
records. Remaining failures are concentrated in flatMap acquisition and
generator receiver handling, zip close/error ordering and padding details,
primitive wrapper iterators, and a few realm/reentrancy edges. This is still an
incomplete stage; the Test262 harness remains untouched and no tests or
unit/project tests were added or run.

The next fresh Stage 52 sweep passes 471/514 (43 remaining failures), again
with `TEST262_TEST_TIMEOUT_MS=900000` and six sequential batches capped at 100.
FlatMap now retains its current inner iterator across yields, uses
GetIteratorFlattenable-style direct-iterator fallback for object results, and
closes the active inner iterator before the outer iterator. A realm-owned
String Iterator prototype supplies its own `next` and string tag instead of
pretending string iterators are generic Iterator helpers. Remaining cases are
mostly zip/zipKeyed abrupt-close ordering and padding, plus primitive-wrapper,
map next-method caching, realm, and reentrancy edges. Stage 52 is still
incomplete; harness unchanged and no new tests or unit/project tests.

After matching IteratorCloseAll's completion propagation, Stage 52 passes
479/514. A close failure no longer aborts the reverse-order close walk: all
still-open iterators receive `return`, while the first abrupt completion stays
authoritative. The focused zip and zipKeyed shortest-mode close-abrupt cases
now pass, as does the full 514-case sweep's updated checkpoint. Verification
again used the required 900,000 ms timeout and six sequential batches ≤100.
The stage remains incomplete; no harness or test changes and no unit/project
tests.

The next full Stage 52 sweep passes 483/514 (31 failures), using the same
positive timeout and six sequential batches capped at 100. The zip padding
iterator no longer closes on a padding-step abrupt completion, and iterator
references are kept live while zip inputs, padding and result rows cross
allocating operations. The focused padding get-iterator-abrupt and
iterator-step-value-abrupt cases pass. Remaining failures cluster around zip
and zipKeyed close/order/completion details, generator/wrapper next lookup,
strict-mode completion, keyed padding and several realm/reentrancy edges. This
is an in-progress checkpoint; the Test262 harness is unchanged and no tests or
unit/project tests were added or run.

### 2026-09-26 isolated stage-10 re-audit

A fresh full stage-10 run passes 11,095/11,101 in 112 sequential batches capped
at 100, with `TEST262_TEST_TIMEOUT_MS=900000` and ten workers. Six failures
remain: four non-callable `instanceof`/`@@hasInstance` cases and two generator
prototype identity checks across eval realms. This is an isolated stage-10
checkpoint, not an ordered 0–10 rerun. It supersedes the earlier isolated
11,092/11,101 count and means the later stage-10-complete note needs a fresh
ordered re-audit before the 0–31 gate is claimed. The harness is unchanged; no
tests were added and no unit/project tests were run.

### 2026-09-26 stage-0 recheck

The latest `run-stages-next 0 0` recheck passes 115/116. The one failure is
`test/harness/deepEqual-mapset.js` (`TypeError: value is not callable`),
reproduced with a one-case filter. It also reproduces with task 52's new
schema-wide unused-operand check temporarily disabled, so that validator
consolidation is not causal. Earlier recorded stage-0 runs passed 116/116; the
stage-0 gate must be restored before advancing the ordered prefix. The harness
was not changed, and no tests or unit/project tests were added or run.

After task 52's local-store/numeric operand refactor and VM optional-register
writeback, the latest `run-stages-next 0 0` recheck remains 115/116 with the
same `deepEqual-mapset.js` non-callable failure and no new failures. Stage 0
is still not closed; the harness remains unchanged and no unit/project tests
were run.

After task 52's role-derived field and immediate bounds were extended through
site tables and jump targets, another ordered `run-stages-next 0 0` run remains
115/116 with only `test/harness/deepEqual-mapset.js` (`TypeError: value is not
callable`). No new stage-0 failure appeared; the ordered prefix remains open.
The harness is unchanged and no unit or new tests were run.

After the packed call-window, construct-window, capture-depth, and register-pair
checks moved to their declared immediate layouts, the ordered stage-0 recheck
still passes 115/116 with only `test/harness/deepEqual-mapset.js`. No new
failure appeared; the harness remains unchanged and no unit or new tests were
run.

After removing the opcode-local register/result checks now owned by the
declared field roles, stage 0 remains 115/116 with the same
`test/harness/deepEqual-mapset.js` failure and no newly failing case. The
ordered prefix is still open; the harness remains unchanged and no unit or new
tests were run.

After the numeric-local target/marker bounds moved to their paired field roles,
stage 0 still passes 115/116 with only `test/harness/deepEqual-mapset.js`; no
new failure appeared. The ordered prefix remains open, the harness is
unchanged, and no unit or new tests were run.

After `GetField`'s cache/site bounds were fully derived from the `FieldBase`
role, another stage-0 run remains 115/116 with only
`test/harness/deepEqual-mapset.js`; no new failure appeared. The harness is
unchanged, and no unit or new tests were run.

After fixed numeric operators and single-argument direct-eval call windows
moved to row-declared roles, stage 0 remains 115/116 with the same
`test/harness/deepEqual-mapset.js` failure and no new failures. The harness is
unchanged; no unit or new tests were run.

After `GetIndex`'s register/local operand domain moved to its declared
`NumericIndexOperand` fields, stage 0 remains 115/116 with only the same
`test/harness/deepEqual-mapset.js` failure. No new failure appeared; the
harness is unchanged, and no unit or new tests were run.

After declaring `Wide`'s side-table index role and simplifying packed marker
encode/decode, stage 0 remains 115/116 with only
`test/harness/deepEqual-mapset.js`. A cumulative 0–1 run stopped at this known
stage-0 failure, so stage 1 was not reached. The harness is unchanged; all
cases used a positive 30,000 ms timeout and batches capped at 100. No unit or
new tests were run.

After the `GetField` tagged field/cache encoding and disassembly exceptions
moved to field roles, ordered stage 0 remains 115/116 with the same sole
`test/harness/deepEqual-mapset.js` failure. The affected existing stage-10
`property` selection passes 348/348. All cases used a positive 30,000 ms
timeout and batches capped at 100; the harness is unchanged and no unit or new
tests were run.

After liveness started deriving packed-window/pair and table-site dependencies
from opcode roles, stage 0 remains 115/116 with only
`test/harness/deepEqual-mapset.js`; stage-10 `property` passes 348/348 and
`call` remains 817/818 with only
`instanceof/symbol-hasinstance-not-callable.js`. All runs used positive
30,000 ms timeouts and batches capped at 100. The harness is unchanged; no
unit or new tests were run.

After control-flow successor kinds moved into opcode-row declarations,
ordered stage 0 remains 115/116 with only
`test/harness/deepEqual-mapset.js`; the affected stage-10 `operator` selection
passes 137/137. All cases used a positive 30,000 ms timeout and batches capped
at 100. The harness is unchanged; no unit or new tests were run.

After rewrite move-elision switched to the shared operand-role-derived liveness
use mask, ordered stage 0 remains 115/116 with the same sole
`test/harness/deepEqual-mapset.js` failure; stage-10 `function` passes
1,196/1,196. The runner used positive 30,000 ms timeouts and batches capped at
100. No harness changes, unit tests, or new tests were made.

After rewrite branch protection and jump relocation moved to the `JumpTarget`
role, ordered stage 0 remains 115/116 with only
`test/harness/deepEqual-mapset.js`; stage-10 `operator` passes 137/137. All
cases used positive 30,000 ms timeouts and batches capped at 100. No harness
changes, unit tests, or new tests were made.

After Map and Set iterators gained their own prototypes and `next` methods,
ordered Test262 stage 0 passes 116/116. The cumulative 0–1 prefix passes
379/379, with stage 1's arguments-object directory passing 263/263. The runner
used positive 30,000 ms timeouts and deterministic batches capped at 100; the
Test262 harness is unchanged. No unit or new tests were run.

The next sequential cumulative prefix 0–2 passes 481/481: stage 0 116/116,
stage 1 263/263, and stage 2 (ASI) 102/102. Every case used a positive
30,000 ms timeout; deterministic batches stayed at or below 100. The harness
is unchanged, and no unit or new tests were run.

Sequential cumulative prefix 0–3 passes 626/626, including stage 3
(block-scope) at 145/145. Batches remained capped at 100 with positive
30,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–4 passes 678/678, including stage 4
(comments) at 52/52. Batches remained capped at 100 with positive 30,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–5 passes 726/726, including stage 5
(computed-property-names) at 48/48. Batches remained capped at 100 with
positive 30,000 ms timeouts. Test262 harness unchanged; no unit or new tests
were run.

Sequential cumulative prefix 0–6 passes 745/745, including stage 6
(destructuring) at 19/19. Batches remained capped at 100 with positive
30,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–7 passes 807/807, including stage 7
(directive-prologue) at 62/62. Batches remained capped at 100 with positive
30,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–8 passes 1,154/1,154, including stage 8
(eval-code) at 347/347. Batches remained capped at 100 with positive
30,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–9 passes 1,157/1,157, including stage 9
(export) at 3/3. Batches remained capped at 100 with positive 30,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

After fixing the collection-iterator `next` methods, non-callable
`instanceof` fallback, and function-realm generator defaults, the ordered
0–10 prefix passes 12,258/12,258. Stage 10 passes 11,101/11,101; its previous
six failures across `instanceof` and cross-realm generator-prototype families
are closed. The 112 stage-10 batches were capped at 100, with positive
900,000 ms per-case timeouts. Test262 harness and suite are unchanged; no unit
or new tests were run.

Sequential cumulative prefix 0–11 passes 12,475/12,475, including stage 11
(function-code) at 217/217. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–12 passes 12,530/12,530, including stage 12
(future-reserved-words) at 55/55. Batches remained capped at 100 with
positive 900,000 ms timeouts. Test262 harness unchanged; no unit or new tests
were run.

Sequential cumulative prefix 0–13 passes 12,572/12,572, including stage 13
(global-code) at 42/42. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–14 passes 12,586/12,586, including stage 14
(identifier-resolution) at 14/14. Batches remained capped at 100 with
positive 900,000 ms timeouts. Test262 harness unchanged; no unit or new tests
were run.

Sequential cumulative prefix 0–15 passes 12,854/12,854, including stage 15
(identifiers) at 268/268. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–16 passes 12,981/12,981, including stage 16
(import) at 127/127. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–17 passes 13,006/13,006, including stage 17
(keywords) at 25/25. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–18 passes 13,047/13,047, including stage 18
(line-terminators) at 41/41. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–19 passes 13,581/13,581, including stage 19
(literals) at 534/534. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–20 passes 14,180/14,180, including stage 20
(module-code) at 599/599. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–21 passes 14,191/14,191, including stage 21
(punctuators) at 11/11. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–22 passes 14,218/14,218, including stage 22
(reserved-words) at 27/27. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–23 passes 14,229/14,229, including stage 23
(rest-parameters) at 11/11. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–24 passes 14,230/14,230, including stage 24
(source-text) at 1/1. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–25 passes 14,310/14,310, including stage 25
(statementList) at 80/80. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–26 passes 23,647/23,647, including stage 26
(statements) at 9,337/9,337. Its 94 batches were capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–27 passes 23,760/23,760, including stage 27
(types) at 113/113. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–28 passes 23,827/23,827, including stage 28
(white-space) at 67/67. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–29 passes 23,835/23,835, including stage 29
(AbstractModuleSource) at 8/8. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–30 passes 23,860/23,860, including stage 30
(AggregateError) at 25/25. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–31 passes 26,941/26,941, including stage 31
(Array) at 3,081/3,081. Its 31 batches were capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–32 passes 27,162/27,162, including stage 32
(ArrayBuffer) at 221/221. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–33 passes 27,189/27,189, including stage 33
(ArrayIteratorPrototype) at 27/27. Batches remained capped at 100 with
positive 900,000 ms timeouts. Test262 harness unchanged; no unit or new tests
were run.

Sequential cumulative prefix 0–34 passes 27,293/27,293, including stage 34
(AsyncDisposableStack) at 104/104. Its batches were capped at 100 with
positive 900,000 ms timeouts. Test262 harness unchanged; no unit or new tests
were run.

Sequential cumulative prefix 0–35 passes 27,331/27,331, including stage 35
(AsyncFromSyncIteratorPrototype) at 38/38. Batches remained capped at 100
with positive 900,000 ms timeouts. Test262 harness unchanged; no unit or new
tests were run.

Sequential cumulative prefix 0–36 passes 27,349/27,349, including stage 36
(AsyncFunction) at 18/18. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–37 passes 27,372/27,372, including stage 37
(AsyncGeneratorFunction) at 23/23. Batches remained capped at 100 with
positive 900,000 ms timeouts. Test262 harness unchanged; no unit or new tests
were run.

Sequential cumulative prefix 0–38 passes 27,420/27,420, including stage 38
(AsyncGeneratorPrototype) at 48/48. Batches remained capped at 100 with
positive 900,000 ms timeouts. Test262 harness unchanged; no unit or new tests
were run.

Sequential cumulative prefix 0–39 passes 27,433/27,433, including stage 39
(AsyncIteratorPrototype) at 13/13. Batches remained capped at 100 with
positive 900,000 ms timeouts. Test262 harness unchanged; no unit or new tests
were run.

Sequential cumulative prefix 0–40 passes 27,822/27,822, including stage 40
(Atomics) at 389/389. Its four batches were capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–41 passes 27,899/27,899, including stage 41
(BigInt) at 77/77. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–42 passes 27,950/27,950, including stage 42
(Boolean) at 51/51. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–43 passes 28,511/28,511, including stage 43
(DataView) at 561/561. Its six batches were capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–44 passes 29,105/29,105, including stage 44
(Date) at 594/594. Its six batches were capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–45 passes 29,198/29,198, including stage 45
(DisposableStack) at 93/93. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–46 passes 29,291/29,291, including stage 46
(Error) at 93/93. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–47 passes 29,338/29,338, including stage 47
(FinalizationRegistry) at 47/47. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–48 passes 29,800/29,800, including stage 48
(Function) at 509/509. Its six batches were capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–49 passes 29,823/29,823, including stage 49
(GeneratorFunction) at 23/23. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–50 passes 29,884/29,884, including stage 50
(GeneratorPrototype) at 61/61. Batches remained capped at 100 with positive
900,000 ms timeouts. Test262 harness unchanged; no unit or new tests were run.

Sequential cumulative prefix 0–51 passes 29,890/29,890, including stage 51
(Infinity) at 6/6. Batches remained capped at 100 with positive 900,000 ms
timeouts. Test262 harness unchanged; no unit or new tests were run.

Stage 52 (Iterator) remains blocked at 510/514 after failure-family work. The
existing Iterator selection ran in six batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=900000`; `run-stages-next` was rebuilt with the
iteration profile. The slice ports captured iterator-record behavior to
helper sources, fixes strict-zip mismatch/close progression, closes already
opened zipKeyed iterators on abrupt setup, routes proxy own-descriptor probes
through proxy semantics while retaining array virtual descriptors, records
helper started/running state for reentrant close, and adds Iterator constructor
realm fallback. Four cases remain: two primitive-iterator cases, keyed padding
observation, and next-method call count. This is not a completed stage or
cumulative-prefix checkpoint. Harness unchanged; no unit or new tests were
run.
