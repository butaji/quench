# 076: Remove orphaned `js_runtime` host tree

The active `quench-node` crate does not declare `js_runtime`; the 58-file
`js_runtime*.rs` tree is an unreachable duplicate host architecture (~17.6k
lines). Confirm that no build script, include, test, or generated path depends
on it. Retain the fragment pulled by active `esm_imports.rs` if needed.

## Plan

- Record the reachability audit and exact retained files.
- Delete or move the unreachable tree to repository history/archive.
- Run `cargo check -p quench-node --lib` and relevant Node/oracle tests.
- Verify no duplicate `QuenchNodeHost` or capability model remains active.

## Acceptance

No active reference or compile path is lost; the active host builds and its
behavioral tests pass. Do not replace this with `allow(dead_code)`.
