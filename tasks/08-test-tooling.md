# Node test and developer tooling

Status: partial.

Implemented CLI eval mode and help text; reporters, mocks, snapshots, REPL, WASI, and full CLI semantics remain.
Current work scopes `node:test` hooks to their describe suite. Expand in-process test behavior, REPL, WASI, reporters, mocks, snapshots, and CLI semantics through focused observable contracts.
