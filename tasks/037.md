# 037: Full differential and neutral-corpus verification of the complete two-tier system

- **Theme:** deegen_full_jit
- **Executor:** codex
- **Depends on:** 036

## Context

Closing task for this backlog: with tier-up, OSR-entry, any closed
named-technique gaps (029), and the verified Optimizing extra layer (030) all
landed, record one consolidated evidence snapshot the way
`docs/architecture-evidence.md` already does for prior work.

## Scope
- docs/architecture-evidence.md

## Deliverables
- Run the full correctness gate: `cargo test -p quench-runtime --lib
  --no-default-features`, `--features execution-trace`, `cargo check -p
  quench-node`
- Run the neutral micro-corpus three ways: interpreter-only (tier-up
  disabled), tier-up+OSR enabled, tier-up+OSR+Optimizing enabled — record all
  three in `docs/architecture-evidence.md` alongside the existing snapshot
  format
- Confirm 100/100 observable matches in every configuration

## Acceptance
- `docs/architecture-evidence.md` updated with the new snapshot
- No configuration regresses vs. the interpreter-only baseline
- `docs/deegen-alignment.md`'s status table reflects the final state (nothing
  left as "unverified")

## Non-goals
- New feature work — this is verification and evidence recording only
