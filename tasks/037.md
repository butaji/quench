# 037: Full differential and neutral-corpus verification of the complete two-tier system

- **Theme:** deegen_full_jit
- **Executor:** codex
- **Depends on:** 036

## Context

Closing task for this backlog: with tier-up, OSR-entry, any closed
named-technique gaps (029), and the verified Optimizing extra layer (030) all
landed, record one consolidated evidence snapshot the way
`docs/architecture-evidence.md` already does for prior work.

## Scope
- docs/architecture-evidence.md

## Deliverables
- Run the full correctness gate: `cargo test -p quench-runtime --lib
  --no-default-features`, `--features execution-trace`, `cargo check -p
  quench-node`
- Run the neutral micro-corpus three ways: interpreter-only (tier-up
  disabled), tier-up+OSR enabled, tier-up+OSR+Optimizing enabled — record all
  three in `docs/architecture-evidence.md` alongside the existing snapshot
  format
- Confirm 100/100 observable matches in every configuration

## Validation

1. `cargo test -p quench-runtime --lib --no-default-features` — pass
2. `cargo test -p quench-runtime --lib --features execution-trace` — pass
3. `cargo check -p quench-node` — clean
4. Run `quench-bench/micros/verify.mjs --from 1 --to 100 --runs 3` in each of
   the three configurations (interpreter-only / tier-up+OSR / +Optimizing);
   each must independently report 100/100 exact matches
5. Compute wall-time/RSS/instruction/cycle ratios for all three; confirm a
   monotonic-or-equal improvement as tiers are enabled (interpreter-only ≤
   tier-up+OSR ≤ +Optimizing on wall time) — a configuration that's *slower*
   than a lower tier is a regression to investigate, not to record as-is
6. Update `docs/architecture-evidence.md` with all three snapshots in the
   existing format (see the current single-config snapshot as the template)
7. Re-read `docs/deegen-alignment.md`'s status table end to end: every row
   from tasks 027-036 must now read Present/Done — if any row still says
   Absent/Partial/blocked, this task is not complete; go finish or
   accurately report that unfinished item rather than closing this task early

**Fail condition**: closing this task while any status-table row is
non-Present, or without all three configurations' 100/100 results actually
recorded (not just asserted), is not acceptable.

## Non-goals
- New feature work — this is verification and evidence recording only

## Non-goals
- New feature work — this is verification and evidence recording only
