# 007: assert: deepEqual/deepStrictEqual edge cases

- **Theme:** correctness_edges
- **Executor:** codex
- **Depends on:** (none)

## Context

Partial sweep: `test-assert.js`, `test-assert-class.js`, `test-assert-deep.js`,
`test-assert-typedarray-deepequal.js`, `test-assert-async.js` failed;
`test-assert-esm-cjs-message-verify.js` timed out. `assert.rs` and
`deep_equal.rs` already exist — these are likely comparison-algorithm edge
cases (class/prototype identity, typed-array byte-for-byte vs value
comparison, Symbol-keyed properties, circular references, error-message
formatting) rather than missing APIs.

## Scope
- `crates/quench-node/src/modules/assert.rs`,
  `crates/quench-node/src/modules/assert_validate.rs`,
  `crates/quench-node/src/modules/deep_equal.rs`

## Deliverables
- Read each failing fixture to find the exact assertion that diverges
  (error message text, comparison result, or thrown-error shape) before
  changing comparison logic
- Fixtures below passing

## Validation

1. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-assert.js --timeout-secs 10`
2. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-assert-class.js --timeout-secs 10`
3. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-assert-deep.js --timeout-secs 10`
4. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-assert-typedarray-deepequal.js --timeout-secs 10`
5. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-assert-async.js --timeout-secs 10`
6. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter assert --timeout-secs 10` — no regressions vs whatever else in the assert cluster currently passes
7. `cargo test -p quench-node --lib` — no regressions

**Fail condition:** any of steps 1-5 not PASS, a regression in step 6 or 7.

## Non-goals
- Rewriting the comparison algorithm from scratch — these are targeted
  edge-case fixes against an already-mostly-working implementation
- `test-fs-watch-recursive-assert-leaks.js` exercises recursive Darwin
  filesystem watcher delivery and handle shutdown, not assertion behavior.
  It remains the sole assert-filter failure because the Rust fs watcher does
  not currently deliver this recursive event; it is tracked outside task 007
  rather than hidden by a manifest skip.

## Factual progress (2026-09-04)

- Assert-filter baseline: 22 pass, 6 fail, 0 timeout, 0 crash (28 total).
- Current assert-filter sweep: 22 pass, 6 fail, 0 timeout, 0 crash (28 total).
- The five required fixture validations remain failing; this task is still
  open. Root-cause work so far includes WebCrypto key metadata in deep
  comparison, insertion-order/circular collection diagnostics, and async
  assertion error shape/message handling. Remaining failures are assertion
  formatting/validation and typed-array/arguments edge cases, not skipped
  fixtures.
- Fresh current sweep (2026-09-04): `--all --filter assert --timeout-secs 10`
  is 21 pass, 7 fail, 0 timeout, 0 crash (28 total). The five required
  fixtures remain failing (`test-assert.js`, `test-assert-class.js`,
  `test-assert-deep.js`, `test-assert-typedarray-deepequal.js`, and
  `test-assert-async.js`); no fixture was skipped.
- After the async rejection lifecycle and source-call formatting fixes,
  refreshed validation is 23 pass, 5 fail, 0 timeout, 0 crash (28 total).
  `test-assert-async.js` and `test-assert-first-line.js` now pass; the four
  required comparison fixtures (`test-assert.js`, `test-assert-class.js`,
  `test-assert-deep.js`, and `test-assert-typedarray-deepequal.js`) remain
  failing, so task 007 is still open.
- After distinguishing shared from unshared ArrayBuffers, the refreshed
  assert-cluster sweep is 24 pass, 4 fail, 0 timeout, 0 crash (28 total).
  `test-assert-typedarray-deepequal.js` now passes; the remaining required
  failures are `test-assert.js`, `test-assert-class.js`, and
  `test-assert-deep.js` (plus the unrelated fs-watch assertion-leak fixture).
- The current refreshed sweep is 25 pass, 3 fail, 0 timeout, 0 crash (28
  total). Required `test-assert-deep.js`, `test-assert-typedarray-deepequal.js`,
  and `test-assert-async.js` pass; only `test-assert.js` and
  `test-assert-class.js` remain required failures (the third failure is the
  unrelated fs-watch assertion-leak fixture). Task 007 remains open.
- Latest isolated checks (2026-09-04): `test-assert-deep.js` PASS,
  `test-assert-typedarray-deepequal.js` PASS, and `test-assert-async.js` PASS;
  `test-assert.js` and `test-assert-class.js` still FAIL. The refreshed
  assert-cluster run is 24 pass, 3 fail, 1 timeout, 0 crash (28 total): the
  timeout is the typed-array fixture under the parallel sweep (it passes when
  run alone with a 30-second allowance), failures are `test-assert.js`,
  `test-assert-class.js`, and the unrelated fs-watch assertion-leak fixture.
- After correcting the shared simple-diff line budgets, isolated
  `test-assert-class.js` now PASSes. The latest cluster run is 26 pass, 2
  fail, 0 timeout, 0 crash (28 total): remaining failures are
  `test-assert.js` and the unrelated fs-watch assertion-leak fixture. The
  other four required fixtures pass in isolation; task 007 remains open.
- Final required-fixture validation (2026-09-04): all five required fixtures
  PASS. The assert-filter sweep is 26 pass, 2 fail, 0 timeout, 0 crash (28
  total); the two failures are `test-assert-first-line.js` and
  `test-fs-watch-recursive-assert-leaks.js`. `cargo test -q -p quench-node
  --lib` passes (2 tests).
- After isolating nested CommonJS source attribution and guarding stale source
  offsets, `test-assert-first-line.js` also PASSes. The refreshed assert-filter
  sweep is 27 pass, 1 fail, 0 timeout, 0 crash (28 total); the sole remaining
  failure is `test-fs-watch-recursive-assert-leaks.js`, outside this task's
  assertion semantics. All five required fixtures remain PASS and the Node
  library tests remain green.
- Final task-007 validation rerun (2026-09-04): each of the five `--one`
  fixtures PASSed, the assert-filter remained 27 pass, 1 fail, 0 timeout,
  0 crash (28 total), with only `test-fs-watch-recursive-assert-leaks.js`
  failing, and `cargo test -q -p quench-node --lib` passed (2 tests).
- Fresh current check (2026-09-04): `test-assert.js` now PASSes in isolation,
  while `test-assert-class.js` returns exit code 1 without a surfaced harness
  diagnostic. The full assert filter is 26 pass, 2 fail, 0 timeout, 0 crash
  (28 total); the other failure remains the documented recursive fs-watch
  fixture. This is recorded as current evidence, not treated as task closure.
- UTF-16 assertion-render follow-up (2026-09-04): large runtime strings are
  now rendered from `StringUnits` without the bounded inspect truncation used
  for diagnostic objects; encoded symbols remain on the symbol renderer rather
  than being coerced as strings. The five required `--one` validations all
  PASS, and the fresh assert sweep is 27 pass, 1 fail, 0 timeout, 0 crash (28
  total; inventory hash `d374befcb1dc0254`). The sole residual is
  `test-fs-watch-recursive-assert-leaks.js`, the documented filesystem-watch
  non-goal. `cargo test -q -p quench-node --lib` passes (2 tests).
- Fresh current closure check (2026-09-04): `--all --filter assert
  --timeout-secs 10` is 27 pass / 1 fail / 0 timeout / 0 crash (28 total;
  inventory hash `d374befcb1dc0254`), and all five required `--one` fixtures
  pass. The sole failure remains the explicitly documented recursive
  filesystem-watch non-goal; no manifest entry was changed.

- Gate rerun (2026-09-04): all five required fixtures PASSed with the exact
  10-second commands; the full assert filter reproduced 27/1/0/0 (28 total,
  hash `d374befcb1dc0254`), and `cargo test -p quench-node --lib` passed.

- Fresh baseline and closure (2026-09-04): the mandated assert sweep started
  at 26 pass / 1 fail / 1 timeout / 0 crash (28 total; hash
  `d374befcb1dc0254`). Large float typed-array comparisons were optimized in
  Rust using backing-byte equality with element-level fallback for signed-zero
  and NaN semantics; partial equality uses the same exact-length fast path.
  After rebuilding, all five required validators pass within 10 seconds and
  the sweep is 27 pass / 1 fail / 0 timeout / 0 crash. The sole failure is
  `test-fs-watch-recursive-assert-leaks.js`, the documented recursive watcher
  non-goal; `cargo test -q -p quench-node --lib` passes.

- Current gate recheck (2026-09-05): the fresh assert sweep is pass=27,
  fail=1, timeout=0, crash=0 (28 total; inventory hash
  `d374befcb1dc0254`), with only the documented recursive fs-watch
  non-goal failing. All five required one-fixture validators pass with the
  10-second limit, and `cargo test -q -p quench-node --lib` passes. No
  assertion fixture or manifest entry was changed.

- Post-006 baseline/validation recheck (2026-09-05): the mandated assert
  sweep records pass=27, fail=1, timeout=0, crash=0, total=28 (inventory hash
  `d374befcb1dc0254`). All five required one-fixture validators pass with the
  10-second gate, and `cargo test -q -p quench-node --lib` remains green. The
  sole residual is the documented recursive fs-watch non-goal; no fixture or
  manifest entry was edited.

- Fresh baseline recheck (2026-09-05): the assert sweep records 27 pass / 1
  fail / 0 timeout / 0 crash / 28 total (inventory hash
  `d374befcb1dc0254`). All five required fixtures pass with the 10-second
  gate and `cargo test -q -p quench-node --lib` is green. The sole residual
  remains `test-fs-watch-recursive-assert-leaks.js`, the documented recursive
  watcher non-goal; no fixture or manifest change was made.

- Dependency audit replay (2026-09-05): fresh assert-filter validation is
  27 pass / 1 fail / 0 timeout / 0 crash (28 total; inventory hash
  `d374befcb1dc0254`). All five required fixtures pass with the exact
  10-second one-fixture commands, and `cargo test -q -p quench-node --lib`
  passes. The sole residual remains the documented recursive fs-watch
  assertion-leak non-goal; no fixture or manifest entry was edited.
