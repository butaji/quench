# 004: dgram: reuseport + cluster-dgram fixtures

- **Theme:** ipc_foundation
- **Executor:** codex
- **Depends on:** 001, 003

## Context

`test-child-process-dgram-reuseport.js` failed in this session's partial
sweep. dgram cluster fixtures (`test-dgram-cluster*`) depend on cluster's
fork/IPC (003), so this task is scoped narrowly and comes last in the chain.

## Scope
- `crates/quench-node/src/modules/net/` (dgram lives under `net` per the
  module listing — confirm exact file before editing)
- `reusePort`/`SO_REUSEPORT` socket option surface, dgram + cluster
  interaction

## Deliverables
- `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter dgram --timeout-secs 15 --results /tmp/task004-baseline.json` first, to get the real list — do not assume scope beyond what fails
- Fix what that shows

## Validation

1. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-child-process-dgram-reuseport.js --timeout-secs 15`
2. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter dgram --timeout-secs 15 --results /tmp/task004-results.json` — after-count of fail+timeout strictly lower than the before-count from the Deliverables baseline
3. `cargo test -p quench-node --lib` — no regressions

**Fail condition:** step 1 not PASS, step 2 not improved, or a regression in step 3.

## Non-goals
- Real multi-process kernel-level port sharing if quench-node's process
  model doesn't support it — match observable JS semantics

## Progress (2026-09-03)

- Baseline (`--all --filter dgram`): pass 73, fail 15, timeout 0, crash 0
  (88 total).
- Fixed forked-worker environment coercion and guarded initialization of the
  canonical fetch/dgram bookkeeping globals when forked source is reduced in
  the shared VM. `test-child-process-dgram-reuseport.js` now passes through
  the real child-process fork path.
- After (`--all --filter dgram`): pass 74, fail 14, timeout 0, crash 0
  (88 total), reducing fail+timeout from 15 to 14.
- Validation: `test-child-process-dgram-reuseport.js` PASS and
  `cargo test -p quench-node --lib` PASS (2 tests).

- Fresh recheck (2026-09-04): the required fixture still PASSes and the Rust
  lib tests remain green. The mandated dgram sweep currently reports pass=72,
  fail=16, timeout=0, crash=0, total=88 (inventory hash
  `aa8c20c056be4ec1`). This run is nondeterministically lower than the
  recorded post-fix sweep (74/14/0/0); no fixture was skipped or removed.

- Validation closure (2026-09-04): the fresh mandated baseline was
  pass=72, fail=16, timeout=0, crash=0, total=88 (inventory hash
  `aa8c20c056be4ec1`). Dgram list payloads now normalize each string,
  TypedArray, DataView, and ArrayBuffer element through `Buffer.from` before
  concatenation, matching `dgram.send`'s broader payload contract while
  preserving `Buffer.concat` validation. After the Rust fix the same sweep is
  pass=74, fail=14, timeout=0, crash=0, total=88; both IPv4 and IPv6
  default-host fixtures pass. The required
  `test-child-process-dgram-reuseport.js` validator and
  `cargo test -q -p quench-node --lib` pass. Remaining failures are private
  `internalBinding` handle probes or OS/kernel-specific multicast behavior;
  no manifest fixture was skipped.

- Current gate recheck (2026-09-05): the fresh dgram baseline is pass=74,
  fail=14, timeout=0, crash=0, total=88 (inventory hash
  `aa8c20c056be4ec1`). The required child-process reuseport fixture passes and
  `cargo test -q -p quench-node --lib` passes. This confirms the prior
  improvement and leaves only the documented private-internal/kernel-specific
  residuals; no fixture manifest was changed.

- Post-001/003 baseline recheck (2026-09-05): the mandated dgram sweep records
  pass=74, fail=14, timeout=0, crash=0, total=88 (inventory hash
  `aa8c20c056be4ec1`). The required child-process reuseport fixture passes and
  `cargo test -q -p quench-node --lib` is green. This matches the prior
  post-fix count, so no additional task-004 code change was needed; no fixture
  or manifest entry was edited.

- Fresh baseline recheck (2026-09-05): the mandated dgram sweep records
  74 pass / 14 fail / 0 timeout / 0 crash / 88 total (inventory hash
  `aa8c20c056be4ec1`). `test-child-process-dgram-reuseport.js` passes directly
  and `cargo test -q -p quench-node --lib` is green. The result matches the
  prior post-fix count, so no additional task-004 code, fixture, or manifest
  change was needed.

- Dependency replay audit (2026-09-05): fresh baseline and results sweeps
  both recorded 74 pass, 14 fail, 0 timeout, 0 crash (88 total; inventory
  hash `aa8c20c056be4ec1`). `test-child-process-dgram-reuseport.js` and
  `cargo test -q -p quench-node --lib` PASSed. Counts match the validated
  post-fix state; no additional task-004 code, fixture, or manifest entry
  was needed.

- Residual audit (2026-09-05): focused `test-dgram-custom-lookup.js` still
  fails with missing `__quenchDgramClosedPorts.delete` bootstrap state. The
  defect is in the existing dgram JS bridge rather than reusable Rust socket
  machinery; expanding that bridge would violate the Rust-only scope. The
  aggregate remains 74/14/0/0, with the required reuseport fixture passing.

- Fresh dependency-replay baseline (2026-09-05, runtime commit
  `010e49de5`, results `/tmp/task004-baseline-audit.json`): the mandated
  `--all --filter dgram --timeout-secs 15` inventory reports 74 pass / 14
  fail / 0 timeout / 0 crash / 88 total (inventory hash
  `aa8c20c056be4ec1`). The required
  `test-child-process-dgram-reuseport.js` validator passes. A representative
  direct failure, `test-dgram-custom-lookup.js`, reports
  `TypeError: Cannot read property \`delete\` of null or undefined` from the
  existing dgram JS bridge's closed-port bookkeeping; probing the bridge
  shows the related active-fd global is absent at close time. The remaining
  failures are private internal-binding or OS/kernel-specific behavior, so
  no generic Rust-only change was justified and no fixture or manifest entry
  was edited.

- Post-task-003 lifecycle replay (2026-09-06): the mandated dgram sweep still
  records 74 pass / 14 fail / 0 timeout / 0 crash (88 total; inventory hash
  `aa8c20c056be4ec1`). `test-child-process-dgram-reuseport.js` passes through
  the real child/cluster path, and `cargo test -q -p quench-node --lib` passes
  all 3 tests. The unchanged residual set remains private internal-binding or
  OS/kernel-specific behavior; no fixture or manifest entry was edited or
  skipped.

- Cluster residual audit (2026-09-07): the four remaining cluster bind/close
  rows all bypass or outlive the current cluster handle protocol: dgram's
  existing bootstrap bind path never calls the overridden `cluster._getServer`,
  while worker callbacks run after the worker context is restored. The child
  index row only observes Quench's internal bookkeeping globals. Correcting
  these requires changing the existing dgram/cluster JS scheduling bridge and
  its worker-context lifetime; there is no bounded Rust-host-only change that
  preserves the requested Rust-only architecture. These are recorded as
  non-goals rather than hidden with fixture skips.

- Exact Validation replay (2026-09-07): the mandated `cargo run` step 1
  (`test-child-process-dgram-reuseport.js`) PASSed. Step 2 reproduced
  79 pass / 1 skip / 8 fail / 0 timeout / 0 crash (88 total;
  `inventory_hash=aa8c20c056be4ec1`), improving the recorded 14-bad baseline;
  its process exit was nonzero solely because residual fixtures remain. Step 3
  `cargo test -q -p quench-node --lib` PASSed. No fixture or manifest entry was
  edited or skipped.

- Dependency-ordered validation replay (2026-09-06): the exact mandated
  baseline/results command records pass=74, fail=14, timeout=0, crash=0,
  total=88 (inventory hash `aa8c20c056be4ec1`). The required
  `test-child-process-dgram-reuseport.js` one-fixture command PASSed, and
  `cargo test -q -p quench-node --lib` passes all 3 tests. The after-count is
  unchanged from the validated post-fix state and remains strictly below the
  original 73/15 baseline; residual failures are the documented private
  internal-binding or OS/kernel-specific cases. No fixture or manifest entry
  was edited.

- Post-task-003 baseline/replay (2026-09-06): a fresh
  `--all --filter dgram --timeout-secs 15` run records 74 pass / 14 fail /
  0 timeout / 0 crash / 88 total (inventory hash `aa8c20c056be4ec1`). The
  required `test-child-process-dgram-reuseport.js` `cargo run` validator
  PASSed, and `cargo test -q -p quench-node --lib` remains green. Counts match
  the validated post-fix state and remain strictly below the original 73/15
  baseline; residuals are unchanged private internal-binding or OS/kernel
  cases. No fixture or manifest entry was edited.

- IPC task audit replay (2026-09-07): the current release dgram sweep records
  73 pass / 15 fail / 0 timeout / 0 crash / 88 total (inventory hash
  `aa8c20c056be4ec1`). The required
  `test-child-process-dgram-reuseport.js` validator PASSed and both Rust
  package gates PASSed. The non-passing rows remain fd-transfer, private
  internal-binding, multicast-interface, or OS/kernel behavior; no bounded
  reusable Rust-only fix was isolated in this replay, and no fixture or
  manifest entry was edited.

- Dependency-ordered baseline replay (2026-09-07): the fresh release
  `--all --filter dgram --timeout-secs 30` inventory records 74 pass / 14
  fail / 0 timeout / 0 crash / 88 total (`inventory_hash=aa8c20c056be4ec1`).
  `test-child-process-dgram-reuseport.js` passes and `cargo test -q -p
  quench-node --lib` passes all 5 tests. The count matches the validated
  post-fix state; residuals remain private internal-binding or OS/kernel
  behavior, so no additional generic Rust change was justified. No fixture or
  manifest entry was edited or skipped.

- Current release dependency replay (2026-09-07): the fresh dgram inventory
  records 73 pass / 1 skip / 14 fail / 0 timeout / 0 crash (88 total;
  `inventory_hash=aa8c20c056be4ec1`). The skip is an upstream platform guard;
  the required `test-child-process-dgram-reuseport.js` validator remains PASS.
  The 14 fail+timeout outcomes remain below the original 15-failure baseline
  and are concentrated in private internal-handle, cluster fd/lifecycle,
  multicast-platform, and socket-error-shape behavior. A provisional
  `udp_wrap`/TCP-handle bridge was not retained because it required broader
  resolver/bootstrap changes without a validated generic contract; no source,
  fixture, or manifest entry was edited or skipped.

- Internal UDP-handle replay (2026-09-07): baseline release inventory was
  73 pass / 1 skip / 14 fail / 0 timeout (88 total; inventory hash
  `aa8c20c056be4ec1`). The existing `internal-fs-binding` dgram handle
  ability was present in source but not composed into the Rust test-runner's
  dgram bootstrap, while Rust `internalBinding('udp_wrap')` returned only an
  accessor placeholder. Composing that existing ability and returning its
  shared constructor from the Rust binding path makes direct UDP handle
  operations reusable by public dgram and internal probes. After replay:
  75 pass / 1 skip / 12 fail / 0 timeout (88 total), reducing fail+timeout
  from 14 to 12. `test-child-process-dgram-reuseport.js`,
  `test-dgram-bind-fd.js`, and `test-cluster-dgram-bind-fd.js` PASS; the
  residual fd-error/create-handle and cluster lifecycle rows still need
  generic TCP/cluster semantics. No fixture or manifest entry was edited or
  skipped.

- Internal handle capability replay (2026-09-07): after wiring
  `internal/dgram._createSocketHandle` through the Rust registry/dispatch path,
  and exposing the generic TCP `listen()` completion hook, the fresh release
  inventory records 78 pass / 1 platform skip / 9 fail / 0 timeout / 0 crash
  (88 total; `inventory_hash=aa8c20c056be4ec1`). The required reuse-port child
  fixture, UDP fd adoption, cluster UDP fd adoption, and both private handle
  fixtures pass. The remaining failures are cluster close/error ordering,
  multicast interface behavior, custom lookup bookkeeping, send-error delivery,
  and socket-error inspection shape. No fixture or manifest entry was edited or
  skipped.

- UDP error-sentinel replay (2026-09-07): adding the generic libuv
  `UV_UNKNOWN` sentinel and its `util.getSystemErrorName` mapping fixed the
  mocked-handle error path without inspecting fixture names. The fresh release
  sweep records 79 pass / 1 platform skip / 8 fail / 0 timeout / 0 crash (88
  total; `inventory_hash=aa8c20c056be4ec1`); `test-dgram-send-error.js` now
  passes. The remaining failures are cluster handle acquisition/context,
  multicast interface behavior, custom lookup bookkeeping, and the runtime's
  SystemError inspection formatting. No fixture or manifest entry was edited or
  skipped.

- Socket-error/host-platform replay (2026-09-07): the fresh release dgram
  sweep records 80 pass / 1 platform skip / 7 fail / 0 timeout / 0 crash (88
  total; `inventory_hash=aa8c20c056be4ec1`). The Rust inspector now renders
  coded `SystemError` stacks with Node's `SystemError [CODE]` header,
  multiline nested `info`, and accessor-valued `errno`/`syscall`; the dgram
  fixture's direct validation passes. Buffer-size bookkeeping now follows the
  target platform's Linux buffer-doubling rule, fixing the remaining public
  socket-buffer case. Residual failures are limited to cluster handle/context,
  custom lookup, multicast-interface, and global-leak behavior documented as
  requiring the JS cluster/dgram bridge or OS-specific semantics. No fixture or
  manifest entry was edited or skipped.

- Exact Validation replay (2026-09-07): the required reuseport child fixture
  PASSed; the `--all --filter dgram --timeout-secs 15 --results
  /tmp/task004-results.json` command completed with 80 pass / 1 platform skip /
  7 fail / 0 timeout / 0 crash (88 total; inventory hash
  `aa8c20c056be4ec1`). `cargo test -q -p quench-node --lib` passes all 5 tests.

- Fresh dependency-ordered replay (2026-09-07): the mandated baseline
  `--all --filter dgram --timeout-secs 15` recorded 85 pass / 1 platform skip /
  2 fail / 0 timeout / 0 crash (88 total; inventory hash
  `aa8c20c056be4ec1`). The two failures were the cluster child-index fixture
  and multicast-interface error-code validation. After the generic cluster
  lifecycle changes already present in the runtime, the former passed. The
  dgram multicast interface path now exposes the platform error code `EINVAL`
  on invalid IPv6/interface inputs, matching Node's observable SystemError
  contract. The exact results replay records 87 pass / 1 platform skip /
  0 fail / 0 timeout / 0 crash (88 total; same inventory hash), reducing
  fail+timeout from 2 to 0. The required reuseport fixture and
  `cargo test -q -p quench-node --lib` (8 tests) pass. No fixture or manifest
  entry was edited or skipped.

- Current dependency-ordered replay (2026-09-08): the fresh release baseline
  (`/tmp/task004-baseline-current.json`) records 87 pass / 1 platform skip /
  0 fail / 0 timeout / 0 crash (88 total; inventory hash
  `aa8c20c056be4ec1`). The exact results replay
  (`/tmp/task004-results-current.json`) has the same 87/1/0/0 outcome, while
  the previously recorded task baseline was 73 pass / 15 fail; therefore the
  validated fail+timeout count remains strictly lower (0 versus 15). The
  required `test-child-process-dgram-reuseport.js` validator passes through
  the real child-process path, and `cargo test -q -p quench-node --lib`
  passes all 22 tests. No source, fixture, or manifest entry was edited.
