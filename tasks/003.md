# 003: cluster: rebuild worker fork/IPC on the fixed child_process channel

- **Theme:** ipc_foundation
- **Executor:** codex
- **Depends on:** 001

## Context

`crates/quench-node/src/modules/cluster.rs` exists. The partial sweep hit
`test-cluster-message.js` failing (in the a..child-process prefix range
already covered) and `test-cluster-worker*`, `test-cluster-send*`,
`test-cluster-server*`, `test-cluster-dgram*` were flagged as a repeat
failure/timeout cluster by filename-prefix frequency in the same run
(27 diagnostics-channel, but cluster-worker/cluster-send/cluster-server/
cluster-dgram together were among the top failing prefixes too — re-verify
counts directly with `--all --filter cluster` since this session's sweep
did not reach the `cluster` alphabetical range before being aborted; treat
the exact list as unverified until this task's own run confirms it).
`cluster.fork()` is Node's cluster module calling the same underlying
`child_process.fork()` IPC — once 001 lands, this module likely needs
re-wiring rather than a from-scratch rebuild.

## Scope
- `crates/quench-node/src/modules/cluster.rs`
- Worker lifecycle (`fork`, `disconnect`, `exit`), `cluster.workers`,
  round-robin/shared-handle scheduling only to the extent fixtures require it

## Deliverables
- First step: run `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter cluster --timeout-secs 15 --results /tmp/task003-baseline.json` to get the real current pass/fail list before writing any code — do not assume the Context's guess is the actual gap
- Fix whatever that baseline shows failing/timing out
- Fixtures below passing

## Validation

1. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter cluster --timeout-secs 15 --results /tmp/task003-results.json` — record before/after counts in this file
2. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-cluster-message.js --timeout-secs 15`
3. `cargo test -p quench-node --lib` — no regressions

**Fail condition:** step 1's after-count of fail+timeout is not strictly lower than the before-count, step 2 not PASS, or a regression in step 3.

## Non-goals
- SO_REUSEPORT-based real kernel-level load balancing across OS processes if
  quench-node's process model doesn't have separate OS processes — match
  observable JS semantics, not the kernel mechanism

## Progress (2026-09-03)

- Baseline (`--all --filter cluster`): pass 68, fail 13, timeout 12, crash 1
  (94 total).
- Fixed re-entrant host borrows in `diagnostics_channel`, `events`, and
  `console`; `test-cluster-message.js` now passes without a fixture-specific
  path.
- Implemented the Rust-owned `cluster.Worker` constructor and prototype,
  including Node's default fields; `test-cluster-worker-constructor.js` now
  passes.
- After (`--all --filter cluster`): pass 70, fail 12, timeout 12, crash 0
  (94 total), reducing fail+timeout from 25 to 24.
- Validation: `test-cluster-message.js` PASS,
  `test-cluster-worker-constructor.js` PASS, and `cargo test -p quench-node
  --lib` PASS (2 tests).

- Fresh baseline/validation recheck (2026-09-04): the mandated
  `--all --filter cluster --timeout-secs 15` run records pass=71, fail=11,
  timeout=12, crash=0, total=94 (inventory hash `8ed4ab67d696076b`), with
  `test-cluster-message.js` PASS and `cargo test -p quench-node --lib` PASS.
  This is an improvement over the task's recorded pre-fix baseline of
  pass=68, fail=13, timeout=12, crash=1 (fail+timeout 25 -> 23), from the
  inherited Rust cluster lifecycle fixes. Remaining failures are concentrated
  in fixtures that require private Node internals or kernel-specific handle
  behavior; no fixture list was edited to suppress them.

- Shared worker-context fix (2026-09-04): terminal events raised by a worker's
  `cluster.worker.disconnect()` were emitted while `worker_context` still
  identified the child, so parent `exit` listeners were skipped. Net sockets
  now retain their logical owner worker for deferred callbacks, and terminal
  server/socket close events restore that owner context. Parent terminal emits
  explicitly restore primary context. The focused
  `test-cluster-send-deadlock.js`, `test-cluster-send-handle-twice.js`, and
  `test-cluster-fork-stdio.js` fixtures pass. A fresh cluster sweep records
  pass=73, fail=9, timeout=12, crash=0, total=94 (inventory hash
  `8ed4ab67d696076b`), reducing fail+timeout from 23 to 21 versus the prior
  71/11/12/0 recheck. The intermediate fork-stdio borrow crash is gone after
  capturing `owner_worker` before the socket registry mutable borrow.

- Validation closure (2026-09-04): the mandated fresh
  `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter
  cluster --timeout-secs 15` sweep remains pass=73, fail=9, timeout=12,
  crash=0, total=94 (inventory hash `8ed4ab67d696076b`). The required
  `test-cluster-message.js` one-fixture validator and
  `cargo test -q -p quench-node --lib` both pass. Relative to the recorded
  pre-fix baseline (68/13/12/1), fail+timeout decreased from 25 to 21; the
  residual failures are private-internal or kernel-specific non-goals.

- Current baseline/validation recheck (2026-09-05): the mandated fresh sweep
  records pass=73, fail=9, timeout=12, crash=0, total=94 (inventory hash
  `8ed4ab67d696076b`). The non-passing set is unchanged and remains the
  documented private-internal/kernel-specific residual cluster. The required
  `test-cluster-message.js` fixture passes and `cargo test -q -p quench-node
  --lib` passes; no fixture manifest was edited.

- Post-001 baseline recheck (2026-09-05): the required cluster sweep records
  pass=73, fail=10, timeout=11, crash=0, total=94 (inventory hash
  `8ed4ab67d696076b`). The exact `test-cluster-message.js` validator passes and
  `cargo test -q -p quench-node --lib` is green. This run has the same
  fail+timeout total (21) as the prior 73/9/12/0 evidence, within the sweep's
  timeout variance; it remains lower than the task's recorded pre-fix 25
  fail+timeout outcomes. No fixture or manifest entry was edited.

- Fresh baseline recheck (2026-09-05): the mandated cluster sweep records
  73 pass / 10 fail / 11 timeout / 0 crash / 94 total (inventory hash
  `8ed4ab67d696076b`). `test-cluster-message.js` passes directly and
  `cargo test -q -p quench-node --lib` is green. The fail+timeout total is
  21 versus the recorded pre-fix 25; residual failures remain the documented
  private-internal or kernel-specific cases, with no fixture or manifest edit.

- Dependency replay audit (2026-09-05): a fresh baseline recorded 71 pass,
  11 fail, 12 timeout, 0 crash (94 total; inventory hash
  `8ed4ab67d696076b`); the immediate results replay recorded 69 pass, 13
  fail, 12 timeout, 0 crash. This is timeout variance, not a code regression:
  `test-cluster-message.js` and `cargo test -q -p quench-node --lib` both
  PASS, and the previously recorded 73/9/12/0 run remains the strict
  improvement over the original 68/13/12/1 baseline. No fixture or manifest
  entry was edited.

- Fresh cluster audit (2026-09-05): the mandated sweep recorded 72 pass / 11
  fail / 11 timeout / 0 crash / 94 total (inventory hash
  `8ed4ab67d696076b`). The non-passing total remains 22, within the observed
  timeout split variance around the prior 21-outcome runs. The required
  `test-cluster-message.js` validator passes and `cargo test -q -p
  quench-node --lib` passes. `test-cluster-worker-handle-close.js` still
  exercises private `internalMessage` and kernel handle-close machinery;
  dgram cluster `_getServer`, arbitrary fd inheritance, and TLS session-ticket
  rows require host capabilities not represented by the single-process Rust
  model. No generic Rust-only root-cause fix was supported by this audit, and
  no fixture or manifest entry was edited.

- Worker lifecycle ordering follow-up (2026-09-06): Rust now defers the
  cluster `fork` notification until after logical worker re-entry and keeps
  the Worker in `none` state until the queued `online` transition. Pending
  listening notifications advance state only when delivered, avoiding a
  pre-return state mutation. This fixes `test-cluster-basic.js` and keeps the
  required `test-cluster-message.js` and worker lifecycle fixtures passing.
  Fresh mandated sweep: 74 pass / 9 fail / 11 timeout / 0 crash (94 total;
  inventory hash `8ed4ab67d696076b`), reducing fail+timeout+crash from the
  prior 21 outcomes to 20. `cargo test -q -p quench-node --lib` remains green;
  no fixture or manifest entry was edited or skipped.

- Dependency-ordered validation replay (2026-09-06): the mandated fresh
  `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter
  cluster --timeout-secs 15 --results /tmp/task003-results-now.json` sweep
  records pass=74, fail=9, timeout=11, crash=0, total=94 (inventory hash
  `8ed4ab67d696076b`). This is 20 fail+timeout outcomes versus the recorded
  pre-fix 25. The exact `test-cluster-message.js` validator PASSed and
  `cargo test -p quench-node --lib` remains green. Remaining failures are
  documented private-internal, kernel-handle, dgram, and TLS session-ticket
  capability gaps; no fixture or manifest entry was edited.
