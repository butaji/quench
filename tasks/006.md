# 006: async_hooks: resource destroy tracking, worker-termination hooks, stack-overflow guard

- **Theme:** event_loop_ordering
- **Executor:** codex
- **Depends on:** 005

## Context

Partial sweep evidence: `test-async-hooks-execution-async-resource.js`,
`test-async-hooks-execution-async-resource-await.js`,
`test-async-hooks-http-agent.js`, `test-async-hooks-http-agent-destroy.js`
failed; `test-async-hooks-fatal-error.js`,
`test-async-hooks-http-parser-destroy.js`,
`test-async-hooks-stack-overflow.js` (+ `-nested-async`/`-try-catch`
variants), and all four `test-async-hooks-worker-asyncfn-terminate-{1,2,3,4}.js`
timed out. Also `test-async-wrap-destroyid.js`,
`test-async-wrap-uncaughtexception.js`,
`test-async-wrap-tlssocket-asyncreset.js` failed, and
`test-async-wrap-pop-id-during-load.js` timed out.

The `-worker-asyncfn-terminate-*` timeouts likely depend on worker_threads
termination correctly running through the same resource-destroy path this
task covers — verify whether they need `worker_threads.rs` changes too or
whether they're purely an async_hooks issue once destroy tracking is fixed.

## Scope
- `crates/quench-node/src/modules/async_hooks.rs`
- The resource `init`/`before`/`after`/`destroy` callback lifecycle, its
  interaction with the event loop from 005 (destroy hooks fire on a
  specific tick relative to GC/task completion — do not guess, check Node's
  actual behavior)
- Stack-overflow handling inside an async_hooks callback (must surface as a
  catchable/reportable error path, not hang or crash the host)

## Deliverables
- Fixtures below passing

## Validation

1. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter async-hooks --timeout-secs 10 --results /tmp/task006-baseline.json`
2. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter async-wrap --timeout-secs 10` baseline too
3. Fix the destroy-tracking/stack-overflow-guard gaps
4. Re-run both filters — after-counts of fail+timeout strictly lower than baseline for each
5. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-async-hooks-worker-asyncfn-terminate-1.js --timeout-secs 15` — PASS (report explicitly in this file if it turns out to require worker_threads changes out of this task's scope; do not silently skip it)
6. `cargo test -p quench-node --lib` — no regressions

**Fail condition:** step 4 not improved for either filter, or a regression in step 6.

## Non-goals
- Full async_hooks `AsyncLocalStorage` snapshot/propagation correctness
  beyond what these fixtures assert (that's `test-als-*`, separate from
  this task unless a fixture above happens to require it)

## Progress (2026-09-04)

- Fresh baseline: `async-hooks` pass=29, fail=7, timeout=4, crash=0 (40 total; 11 bad).
- Fresh baseline: `async-wrap` pass=3, fail=4, timeout=0, crash=0 (7 total; 4 bad).
- Implemented the Rust `async_wrap.queueDestroyAsyncId` capability and queued destroy delivery; `async-wrap` now pass=6, fail=1, timeout=0, crash=0 (7 total; 1 bad).
- Added `RANDOMBYTESREQUEST` resource lifecycle, HTTPS Agent TLS-option inheritance, and async-id symbol exposure for network sockets. The required worker termination fixture passes.
- Added a bounded continuation-depth guard (2048 frames) and preserved caught errors when nested loop continuations unwind. The nested async stack-overflow fixture and recursive `runInAsyncScope` fixture now pass.
- Current `async-hooks` sweep: pass=32, fail=7, timeout=1, crash=0 (8 bad; baseline 11).
- Current `async-wrap` sweep: pass=6, fail=1, timeout=0, crash=0 (1 bad; baseline 4).
- Required worker-termination fixture passes and `cargo test -q -p quench-node --lib` passes. Validation step 4 is now satisfied; remaining async-hooks/trace-events failures are outside the task's listed required fixture set, so task remains open for those root causes.
- Resource-identity follow-up (2026-09-04): timer async-local metadata is now
  written in place after `attach_resource`, preserving the exact object passed
  to `init`. A focused repro confirms `executionAsyncResource() === timeout`
  and symbol state survives into the callback. The full async-hooks sweep
  remains 31 pass, 8 fail, 1 timeout (40 total; inventory hash
  `b4bff354631a16a7`): the two HTTP execution-resource fixtures still hit the
  existing ten-request HTTP lifecycle hang, so this does not close the task.
- Validation refresh (2026-09-04): from the current rebuilt tree,
  `async-hooks` is 31 pass / 7 fail / 2 timeout / 0 crash (40 total; inventory
  hash `b4bff354631a16a7`) and `async-wrap` is 6 pass / 1 fail / 0 timeout /
  0 crash (7 total; inventory hash `95f4aaf5e628e366`). Relative to the
  recorded baselines (11 and 4 bad respectively), both fail+timeout totals
  are strictly lower. The required worker-termination fixture 1 passes with
  a 15-second limit, and `cargo test -q -p quench-node --lib` passes (2 tests).
  The remaining HTTP execution-resource/parser and trace-events failures are
  residual fixtures outside the task's single required fixture gate; they are
  retained in the sweep rather than skipped.

- Mandated baseline refresh (2026-09-04): exact reruns reproduce
  `async-hooks` at 31/7/2/0 (40; hash `b4bff354631a16a7`) and `async-wrap` at
  6/1/0/0 (7; hash `95f4aaf5e628e366`). The required worker-termination-1
  fixture PASSes with the 15-second command. `cargo test -p quench-node
  --lib` (2 passed) and `cargo test -p quench-runtime --lib` (528 passed,
  1 ignored) remain green; counts match the recorded post-fix improvements
  and no fixture was suppressed.

- Validation closure recheck (2026-09-04): fresh sweeps reproduce
  `async-hooks` 31 pass / 7 fail / 2 timeout / 0 crash (40; hash
  `b4bff354631a16a7`) and `async-wrap` 6 pass / 1 fail / 0 timeout / 0 crash
  (7; hash `95f4aaf5e628e366`). The required
  `test-async-hooks-worker-asyncfn-terminate-1.js` fixture passes with the
  15-second validator. `cargo test -q -p quench-node --lib` (2 passed) and
  `cargo test -q -p quench-runtime --lib` (528 passed, 1 ignored) pass; both
  bad-counts remain strictly below their recorded baselines (11 and 4).

- Async tail-call/stack-overflow follow-up (2026-09-04): promoted tail calls
  now route async and generator callees through the ordinary target path so
  their Promise/generator wrappers are preserved. Async stack-guard overflow
  rejections carry a Rust-owned semantic marker and emit the corresponding
  RangeError diagnostic even with `--unhandled-rejections=none`. The exact
  `test-async-wrap-pop-id-during-load.js` validator now PASSes, and the fresh
  `async-wrap` sweep is 7 pass / 0 fail / 0 timeout / 0 crash (7 total; hash
  `95f4aaf5e628e366`), improving the prior 6/1/0/0 result. The fresh
  `async-hooks` sweep remains 31/7/2/0 (40; hash `b4bff354631a16a7`); the
  required worker-termination-1 fixture passes, and `cargo test -q -p
  quench-node --lib` plus `cargo test -q -p quench-runtime --lib` pass (2 and
  529 passed, 1 ignored). No fixture or manifest entry was edited.

- Current gate recheck (2026-09-05): fresh `async-hooks` remains pass=31,
  fail=7, timeout=2, crash=0 (40; inventory hash `b4bff354631a16a7`), while
  `async-wrap` remains fully green at pass=7, fail=0, timeout=0, crash=0 (7;
  hash `95f4aaf5e628e366`). The required worker-termination-1 fixture passes
  with the 15-second validator and `cargo test -q -p quench-node --lib`
  passes. Async-wrap has no remaining bad fixtures to reduce; the existing
  async-hooks improvement and documented HTTP/trace residuals are unchanged.

- Validation refresh (2026-09-05): after preserving Promise trigger context
  while running `PROMISE` init hooks, allocating timer-promises through the
  async-hook resource path, and unref'ing an HTTP server connection only when
  its keep-alive request becomes idle, the exact validation sweeps report
  `async-hooks` pass=33, fail=6, timeout=1, crash=0 (40 total; inventory hash
  `b4bff354631a16a7`) and `async-wrap` pass=7, fail=0, timeout=0, crash=0 (7
  total; inventory hash `95f4aaf5e628e366`). The prior recorded after-counts
  were 31/7/2/0 and 7/0/0/0 respectively, so the async-hooks fail+timeout
  count decreased from 9 to 7 and async-wrap remains green. Both execution-
  async-resource fixtures now pass directly; the HTTP Agent identity and
  parser-destroy fixtures plus three trace-events fixtures remain documented
  residuals. The required worker-termination-1 fixture passes with the
  15-second validator, and `cargo test -q -p quench-node --lib` passes (2
  tests). No fixture list was edited.

- Post-005 baseline recheck (2026-09-05): the mandated sweeps record
  `async-hooks` pass=33, fail=6, timeout=1, crash=0, total=40 (inventory hash
  `b4bff354631a16a7`) and `async-wrap` pass=7, fail=0, timeout=0, crash=0,
  total=7 (hash `95f4aaf5e628e366`). The required
  `test-async-hooks-worker-asyncfn-terminate-1.js` validator passes with the
  15-second gate, and `cargo test -q -p quench-node --lib` is green. Counts
  match the existing resource-lifecycle improvements; no new task-006 code or
  fixture suppression was needed.

- Fresh baseline recheck (2026-09-05): `async-hooks` records 33 pass / 6
  fail / 1 timeout / 0 crash / 40 total (inventory hash
  `b4bff354631a16a7`) and `async-wrap` remains 7 pass / 0 fail / 0 timeout /
  0 crash / 7 total (hash `95f4aaf5e628e366`). The required worker-termination
  fixture passes with the 15-second gate and `cargo test -q -p quench-node
  --lib` is green. No new task-006 code, fixture, or manifest change was
  needed.
