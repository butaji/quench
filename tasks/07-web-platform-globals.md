# Web-platform globals and streams

Status: partial.

Implemented Request option validation/cloning, common Web-platform globals, Web Streams global registration, and ReadableStream locking/getReader behavior; BYOB, transfer, and complete stream semantics remain.
Complete independently testable Web Streams, BYOB, encoder/decoder streams, message transfer, Request options, and remaining WebCrypto/global behavior using existing runtime primitives.
