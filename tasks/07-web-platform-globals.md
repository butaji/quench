# Web-platform globals and streams

Status: partial.

Implemented Request option validation/cloning, common Web-platform globals, Web Streams registration for the stream fixture path, and ReadableStream locking/getReader behavior; stage-2238 still exposes a separate global-registration gap, while BYOB, transfer, and complete stream semantics remain.
Complete independently testable Web Streams, BYOB, encoder/decoder streams, message transfer, Request options, and remaining WebCrypto/global behavior using existing runtime primitives.
