# Web-platform globals and streams

Status: partial.

Implemented Request option validation/cloning, common Web-platform globals, and ReadableStream locking/getReader behavior; BYOB, transfer, complete stream semantics, and remaining WebCrypto/global registration gaps remain.
Complete independently testable Web Streams, BYOB, encoder/decoder streams, message transfer, Request options, and remaining WebCrypto/global behavior using existing runtime primitives.
