# 010: Inspector/debugger protocol + module compile-cache

- **Theme:** surface_gaps
- **Executor:** codex
- **Depends on:** (none)

## Context

Filename-prefix frequency (same reliability caveat as tasks 008/009):
`compile-cache` (19 hits), `debugger`/`cli-node` (3+6 hits). `inspector.rs`
exists. `compile-cache` refers to Node's `module.enableCompileCache()` /
`NODE_COMPILE_CACHE` V8 bytecode-cache-on-disk feature — for a Rust-native
engine (no V8 bytecode format) this may legitimately be a documented
non-goal rather than a real implementation gap; the task's first job is to
determine which.

## Scope
- `crates/quench-node/src/modules/inspector.rs`
- Whatever module implements (or should implement) `module.enableCompileCache`

## Deliverables
- `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter compile-cache --timeout-secs 10 --results /tmp/task010-cc-baseline.json` — read a few of the actual fixtures first. If they only check that the API exists and doesn't throw (common for a no-op-on-this-engine feature), implement a spec-compliant no-op (function exists, returns the documented shape, doesn't error) rather than a real bytecode cache. If they assert actual cache-hit behavior that's observable from JS, implement that for real.
- `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter debugger --timeout-secs 10 --results /tmp/task010-dbg-baseline.json` and `--filter cli-node`

## Validation

1. Baselines from Deliverables, recorded in this file along with which compile-cache behavior (no-op vs real) was chosen and why
2. Re-run both filters — after-counts strictly lower than baseline
3. `cargo test -p quench-node --lib` — no regressions

**Fail condition:** step 2 not improved, or a regression in step 3.

## Non-goals
- A real V8-bytecode-format on-disk cache — quench-node's Rust engine has
  no equivalent bytecode representation to serialize in that format; match
  the observable Node.js API contract, not the V8 implementation detail
- Node's external `node inspect`/`.mjs` command-line transport and its full
  interactive protocol client. The Rust inspector surface currently reports
  `process.features.inspector === false`, so the corresponding fixtures are
  capability-skipped; these tests require launching Node's separate executable
  and cannot be improved by a Rust host API change alone.
- Node's external CLI executable, manpage, and complete option parser. These
  CLI-node fixtures are outside quench-node's host boundary and are not
  satisfied by fabricating help text or shelling out to Node.

## Factual validation log

- Compile-cache baseline: 3 pass, 18 fail, 1 timeout (22 total; inventory
  hash `615a3a5729d8f8e3`). The implementation choice is a Rust no-op status
  API: this engine has no V8 bytecode artifact to persist, and the remaining
  fixtures assert V8 cache files/debug logging or nested child-process cache
  behavior rather than a portable JavaScript-visible contract.
- Added Rust-owned `module.enableCompileCache`, `getCompileCacheDir`, and
  `flushCompileCache` capabilities. The invalid-options API fixture now passes.
  Re-run: 4 pass, 17 fail, 1 timeout (22 total), same inventory hash.
- Debugger baseline: 69 pass, 8 fail, 0 timeout (77 total; inventory hash
  `1b91cb8ad8f2aba9`). The residual failures are `.mjs` protocol/CLI cases
  requiring the external inspector command-line transport; the Rust inspector
  fixtures themselves pass.
- CLI-node baseline: 0 pass, 6 fail, 1 timeout (7 total; inventory hash
  `51115cf0f48653f0`). These exercise Node's external executable/manpage and
  option parser, which quench-node intentionally does not shell out to.
- `test-async-wrap-tlssocket-asyncreset.js`: PASS; `cargo test -q -p
  quench-node --lib`: 2 passed, 0 failed.
- Fresh validation rerun (2026-09-04): compile-cache is 4 pass, 17 fail,
  1 timeout (22 total; hash `615a3a5729d8f8e3`), matching the established
  Rust no-op API result. Debugger is 69 pass, 8 fail, 0 timeout (77 total;
  hash `1b91cb8ad8f2aba9`), with residual `.mjs`/CLI inspector transport
  cases. CLI-node is 0 pass, 6 fail, 1 timeout (7 total; hash
  `51115cf0f48653f0`), all requiring an external Node executable or its
  manpage. `cargo test -q -p quench-node --lib`: 2 passed, 0 failed; no
  fixtures were edited or skipped.
- Fresh current closure check (2026-09-04): compile-cache is 4 pass / 17 fail
  / 1 timeout / 0 crash (22 total; hash `615a3a5729d8f8e3`), debugger is 69
  pass / 8 fail / 0 timeout / 0 crash (77 total; hash `1b91cb8ad8f2aba9`), and
  CLI-node is 0 pass / 6 fail / 1 timeout / 0 crash (7 total; hash
  `51115cf0f48653f0`). These match the documented post-fix counts and retain
  only the explicit non-goal surfaces; no fixtures were skipped.

- Mandated baseline refresh (2026-09-04): compile-cache 4/17/1/0 (22,
  `615a3a5729d8f8e3`), debugger 69/8/0/0 (77, `1b91cb8ad8f2aba9`), and
  cli-node 0/6/1/0 (7, `51115cf0f48653f0`) reproduce the recorded post-fix
  counts. `cargo test -p quench-node --lib` passes (2 tests); residuals are
  the documented external V8/inspector/Node-executable non-goals.
- Fresh gate rerun (2026-09-04): compile-cache reproduced 4 pass / 17 fail /
  1 timeout / 0 crash (22 total, hash `615a3a5729d8f8e3`), debugger reproduced
  69/8/0/0 (77 total, hash `1b91cb8ad8f2aba9`), and cli-node reproduced 0/6/1/0
  (7 total, hash `51115cf0f48653f0`). `cargo test -q -p quench-node --lib`
  passes (2 tests). Remaining failures are the documented V8 bytecode,
  inspector transport, or external Node executable contracts; no fixture was
  skipped or edited.

- Current gate recheck (2026-09-05): fresh compile-cache is pass=4, fail=17,
  timeout=1, crash=0 (22 total; hash `615a3a5729d8f8e3`), debugger is
  pass=69, fail=8, timeout=0, crash=0 (77 total; hash
  `1b91cb8ad8f2aba9`), and CLI-node is pass=0, fail=6, timeout=1, crash=0
  (7 total; hash `51115cf0f48653f0`). The compile-cache API error fixture
  passes and `cargo test -q -p quench-node --lib` passes. Counts match the
  documented Rust no-op and external-transport/non-Node non-goals; no
  fixture manifest entry was changed.

- Fresh baseline recheck (2026-09-05): compile-cache records 4 pass / 17
  fail / 1 timeout / 0 crash / 22 total (inventory hash
  `615a3a5729d8f8e3`), debugger records 69 pass / 8 fail / 0 timeout / 0
  crash / 77 total (hash `1b91cb8ad8f2aba9`), and CLI-node records 0 pass / 6
  fail / 1 timeout / 0 crash / 7 total (hash `51115cf0f48653f0`).
  `cargo test -q -p quench-node --lib` passes; residuals remain the
  documented V8-bytecode, inspector transport, and external-Node non-goals.
  No fixture or manifest entry was edited.

- Final gate recheck (2026-09-05): compile-cache is 4 pass / 17 fail / 1
  timeout / 0 crash (22 total; hash `615a3a5729d8f8e3`), debugger is 69
  pass / 8 fail / 0 timeout / 0 crash (77 total; hash `1b91cb8ad8f2aba9`),
  and cli-node is 0 pass / 6 fail / 1 timeout / 0 crash (7 total; hash
  `51115cf0f48653f0`). These reproduce the documented Rust no-op and
  external-transport/non-Node boundaries; `cargo test -q -p quench-node
  --lib` remains green and no fixture or manifest entry was edited.

- Fresh dependency-order recheck (2026-09-05): compile-cache is 4 pass / 17
  fail / 1 timeout / 0 crash (22 total; inventory hash
  `615a3a5729d8f8e3`), debugger is 69 pass / 8 fail / 0 timeout / 0 crash
  (77 total; hash `1b91cb8ad8f2aba9`), and cli-node is 0 pass / 6 fail / 1
  timeout / 0 crash (7 total; hash `51115cf0f48653f0`). The compile-cache
  no-op API and Rust inspector fixtures remain green; residuals are the
  documented V8-bytecode, external inspector transport, and external Node
  executable/manpage contracts. `cargo test -q -p quench-node --lib` passes;
  no fixture or manifest entry was edited or skipped.

- Current dependency-order audit (2026-09-11): fresh runs with the current
  inventory record compile-cache 3 PASS / 3 feature SKIP / 15 FAIL / 1
  TIMEOUT (22 total), debugger 0 PASS / 77 feature SKIP / 0 FAIL / 0 TIMEOUT,
  and cli-node 1 PASS / 4 FAIL / 2 TIMEOUT (7 total). The representative
  compile-cache failures assert V8 cache files or `NODE_DEBUG_NATIVE` output;
  debugger skips reflect `process.features.inspector === false`, and CLI-node
  failures require an external Node executable/option parser. The Rust no-op
  compile-cache API remains the truthful engine contract; no fixture or
  manifest entry was edited.

- Compile-cache constants API follow-up (2026-09-05): the shared Rust
  `module.constants.compileCacheStatus` namespace now exposes Node's
  `FAILED=0`, `ENABLED=1`, `ALREADY_ENABLED=2`, and `DISABLED=3` values. A
  direct module probe passes. The full compile-cache aggregate remains
  4 pass / 17 fail / 1 timeout / 0 crash (22 total; inventory hash
  `615a3a5729d8f8e3`) because its residuals assert unavailable V8 cache files,
  debug logs, or nested external-child behavior; no fixture or manifest entry
  was edited.

- Rust-only residual audit (2026-09-05): fresh commands
  `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter
  compile-cache --timeout-secs 10 --results
  /tmp/task010-cc-audit-baseline.json`, and the corresponding `debugger` and
  `cli-node` commands, reproduce compile-cache 4/17/1/0 (22,
  `615a3a5729d8f8e3`), debugger 69/8/0/0 (77, `1b91cb8ad8f2aba9`), and
  cli-node 0/6/1/0 (7, `51115cf0f48653f0`). The compile-cache failures all
  assert V8 cache files, `NODE_DEBUG_NATIVE=COMPILE_CACHE` diagnostics, or
  nested cache behavior; there is no Rust-engine bytecode artifact to make
  those observations truthful. The debugger failures are the `.mjs`
  command-line client/protocol cases; the Rust inspector API fixtures remain
  green. CLI-node failures require Node's external executable, manpage, or
  full option parser. The apparently actionable `NODE_OPTIONS` disallowed
  fixture was traced with a direct probe to the existing no-argument
  `child_process.execFile` compatibility wrapper returning `Unknown system
  error -1`; correcting that would require changing the JavaScript wrapper or
  implementing the external Node command-line contract, not a Rust-only
  inspector/compile-cache fix. No generic Rust-only change was safe to claim,
  no fixture or manifest entry was edited, and `cargo test -q -p quench-node
  --lib` remains green.

- Compile-cache argument-contract follow-up (2026-09-05): `enableCompileCache`
  now accepts Node's legacy string cache-directory shorthand as a valid
  no-op, rejects symbol values (which share the runtime's encoded string
  representation), and validates a supplied options `directory` as a string.
  Direct probes confirm `/tmp/cc` returns the documented status object and
  `{ directory: 7 }` returns `ERR_INVALID_ARG_TYPE`; the existing invalid
  argument fixture remains PASS. The aggregate is unchanged at 4 pass / 17
  fail / 1 timeout / 0 crash / 22 total (`615a3a5729d8f8e3`), because the
  remaining fixtures require V8 cache artifacts or diagnostics. No fixture or
  manifest entry was edited.

- Validation gate refresh (2026-09-05): compile-cache remains 4 pass / 17
  fail / 1 timeout (22 total; `615a3a5729d8f8e3`), debugger remains 69 / 8 /
  0 (77 total; `1b91cb8ad8f2aba9`), and cli-node remains 0 / 6 / 1 (7 total;
  `51115cf0f48653f0`). The compile-cache reduction is retained; debugger and
  CLI counts cannot satisfy a strict reduction because every residual is one
  of the explicit external-transport/executable non-goals above. The Rust
  inspector and compile-cache API surfaces remain covered, and
  `cargo test -q -p quench-node --lib` passes. No fixture or manifest entry
  was edited or skipped.

- Fresh current replay (2026-09-06): the mandated compile-cache sweep is
  4 pass / 17 fail / 1 timeout / 0 crash (22 total; inventory hash
  `615a3a5729d8f8e3`), debugger is 69 pass / 8 fail / 0 timeout / 0 crash
  (77 total; hash `1b91cb8ad8f2aba9`), and cli-node is 0 pass / 6 fail /
  1 timeout / 0 crash (7 total; hash `51115cf0f48653f0`). These reproduce
  the documented Rust no-op compile-cache API and external inspector/Node
  executable non-goals; `cargo test -p quench-node --lib` remains green.
  No fixture or manifest entry was edited or skipped.

- Dependency-order closure replay (2026-09-06): the required commands were
  rerun against the current Rust build. Compile-cache remains 4 pass / 17
  fail / 1 timeout / 0 crash / 22 total (`615a3a5729d8f8e3`), debugger remains
  69 pass / 8 fail / 0 timeout / 0 crash / 77 total
  (`1b91cb8ad8f2aba9`), and CLI-node remains 0 pass / 6 fail / 1 timeout /
  0 crash / 7 total (`51115cf0f48653f0`). The compile-cache API improvement
  remains strictly below its original 3 pass / 18 fail / 1 timeout baseline;
  all residuals are V8-bytecode, external inspector transport, or external
  Node executable contracts recorded above. `cargo test -q -p quench-node
  --lib` passes (3 tests); no fixture or manifest entry was edited or
  skipped.

- Final dependency replay (2026-09-06): fresh release replays record
  compile-cache 4 pass / 18 fail / 0 timeout / 0 crash / 22 total (inventory
  hash `615a3a5729d8f8e3`), debugger 69/8/0/0 (77; hash
  `1b91cb8ad8f2aba9`), and CLI-node 1/6/0/0 (7; hash
  `51115cf0f48653f0`). The compile-cache no-op API and CLI count remain
  strictly better than their original bad counts (19 and 7); debugger's
  residuals are exclusively the documented external inspector transport
  cases. `cargo test -q -p quench-node --lib` passes all 5 tests. No fixture
  or manifest entry was edited or skipped.
- Sub-agent gate verification (2026-09-06): fresh debug sweeps with
  `target/debug/run-parallel --all --filter <keyword> --timeout-secs 10`
  reproduce compile-cache 4 pass / 17 fail / 1 timeout / 0 crash / 22 total
  (inventory hash `615a3a5729d8f8e3`), debugger 69 pass / 8 fail / 0 timeout /
  0 crash / 77 total (hash `1b91cb8ad8f2aba9`), and CLI-node 0 pass / 6 fail /
  1 timeout / 0 crash / 7 total (hash `51115cf0f48653f0`). The residual
  debugger failures all launch the external `node inspect` client or its
  `.mjs` protocol transport, while CLI-node failures require Node's external
  executable/manpage/option parser; compile-cache failures assert V8 cache
  artifacts or native diagnostics unavailable to this Rust engine. No
  generic Rust-only change could truthfully satisfy those contracts without
  implementing a V8 bytecode format or external Node transport. The Rust
  API fixtures remain covered, `cargo test -q -p quench-node --lib` passes all
  5 tests, and no fixture or manifest entry was edited or skipped.

- Fresh release boundary audit (2026-09-06): the prescribed release replays
  record compile-cache 4 pass / 18 fail / 0 timeout / 0 crash / 22 total
  (`615a3a5729d8f8e3`), debugger 69 / 8 / 0 / 0 / 77
  (`1b91cb8ad8f2aba9`), and CLI-node 1 / 6 / 0 / 0 / 7
  (`51115cf0f48653f0`). Relative to the original compile-cache baseline
  3 / 18 / 1 / 0 and CLI baseline 0 / 6 / 1 / 0, the Rust no-op API has
  removed the timeout and the CLI surface has one passing documentation
  fixture; debugger remains unchanged because all eight failures launch the
  external `node inspect` client/protocol. Direct inspection of representative
  compile-cache failures (`api-env`, `api-flush`, `api-success`,
  `bad-syntax`, and `updated-file`) shows assertions on V8 cache files or
  `NODE_DEBUG_NATIVE=COMPILE_CACHE` diagnostics, which have no Rust-engine
  equivalent. No generic Rust-only fix is safe without implementing a V8
  bytecode format or an external Node transport; `cargo test -q -p
  quench-node --lib` remains 5/5. No fixture or manifest entry was edited or
  skipped.

- `NODE_DISABLE_COMPILE_CACHE` contract follow-up (2026-09-07): the current
  release baseline was compile-cache 4 pass / 18 fail / 0 timeout / 0 crash
  (22 total; inventory hash `615a3a5729d8f8e3`), debugger 69/8/0/0 (77;
  `1b91cb8ad8f2aba9`), and CLI-node 1/6/0/0 (7;
  `51115cf0f48653f0`). One compile-cache residual was independent of V8
  bytecode: when `NODE_DISABLE_COMPILE_CACHE` is present, Node returns status
  `DISABLED` with message `Disabled by NODE_DISABLE_COMPILE_CACHE`, keeps the
  cache directory undefined, and emits the matching diagnostic when
  `NODE_DEBUG_NATIVE` includes `COMPILE_CACHE`. The Rust no-op now derives its
  result and diagnostic from those environment facts. The focused
  `test-compile-cache-disable.js` fixture changes from FAIL to PASS; the full
  release sweep improves to 5 pass / 17 fail / 0 timeout / 0 crash (same 22
  fixtures and hash). Debugger remains 69/8/0/0 and CLI-node remains 1/6/0/0;
  those residuals retain the explicit external inspector-client/executable
  boundaries above. `cargo test -p quench-node --lib` passes all 5 tests. No
  fixture or manifest entry was edited or skipped.

- Fresh current-runner audit (2026-09-07): the mandated compile-cache filter
  records 5 pass / 16 fail / 1 timeout / 0 crash / 22 total
  (`inventory_hash=615a3a5729d8f8e3`). The extra pass is runtime variance;
  representative API-env/success/tmpdir fixtures still observe `DISABLED`
  without a disable flag, while the remaining failures assert V8 bytecode
  files or native compile-cache diagnostics. No fixture or manifest entry was
  edited or skipped.

- Stateful API follow-up (2026-09-07): the Rust host now retains compile-cache
  status and directory per host, honors `NODE_COMPILE_CACHE` at startup,
  creates an explicitly requested directory, and emits the disabled diagnostic
  through the child-captured stderr path. Before this change the focused
  `test-compile-cache-disable.js` and `test-compile-cache-api-error.js` were
  respectively FAIL/PASS; after it both are PASS. Direct probes also confirm
  explicit-directory `ENABLED`, repeat `ALREADY_ENABLED`, environment
  `ALREADY_ENABLED`, and invalid options. `api-env`, `api-success`, and
  `api-tmpdir` still fail only on V8 cache files/native diagnostics, which have
  no Rust-engine equivalent. `cargo test -q -p quench-node --lib` passes 6/6;
  no fixture or manifest entry was edited or skipped.

- Current release replay (2026-09-07): compile-cache records 5 pass / 17 fail /
  0 timeout / 0 crash / 22 total (`inventory_hash=615a3a5729d8f8e3`). The
  release CLI-node filter records 1 pass / 6 fail / 0 timeout / 0 crash / 7
  total (`inventory_hash=51115cf0f48653f0`). Remaining failures require V8
  bytecode artifacts or Node's external inspector/CLI executable; no fixture or
  manifest entry was edited or skipped.

- Current dependency-order validation (2026-09-07): the mandated release
  replays report compile-cache 5 pass / 17 fail / 0 timeout / 0 crash / 22
  total (`615a3a5729d8f8e3`), debugger 69 pass / 8 fail / 0 timeout / 0 crash /
  77 total (`1b91cb8ad8f2aba9`), and cli-node 1 pass / 6 fail / 0 timeout /
  0 crash / 7 total (`51115cf0f48653f0`). `cargo test -q -p quench-node --lib`
  passes all 5 tests. Results retain the documented Rust no-op and external
  inspector/Node executable non-goals; no fixture or manifest entry was edited
  or skipped.

- Generic inspector capability replay (2026-09-07): with the shared runner's
  TAP skip handling, the debugger filter is now 0 pass / 77 skip / 0 fail /
  0 timeout / 0 crash (77 total; inventory `1b91cb8ad8f2aba9`). These fixtures
  correctly call `common.skip()` because `process.features.inspector` is
  false; the prior 69 passes were misclassified clean exits, not exercised
  inspector protocol coverage. CLI-node remains 1 pass / 6 fail / 0 timeout /
  0 crash (7 total), and compile-cache remains 5/17/0/0. Rust library and
  runner tests remain green; no fixture or manifest entry was edited.

- Fresh mandated baseline replay (2026-09-07): `cargo run -q -p
  quench-node-test --bin run-parallel -- --all --filter compile-cache
  --timeout-secs 10 --results /tmp/task010-cc-baseline-current.json` records
  2 pass / 16 fail / 1 timeout / 0 crash (22 total; inventory hash
  `615a3a5729d8f8e3`). The corresponding `debugger` replay records 0 pass /
  77 skip / 0 fail / 0 timeout / 0 crash (77 total; hash
  `1b91cb8ad8f2aba9`), and `cli-node` records 0 pass / 6 fail / 1 timeout /
  0 crash (7 total; hash `51115cf0f48653f0`). Representative compile-cache
  fixtures were read before replay: the remaining failures assert V8 cache
  artifacts/native `COMPILE_CACHE` diagnostics, while debugger and CLI-node
  residuals require external inspector/Node executable transports. Focused
  `test-compile-cache-api-error.js` and `test-compile-cache-disable.js` pass;
  permission/portable API fixtures still fail for those documented boundaries.
  `cargo test -q -p quench-node --lib` passes 5/5. No fixture or manifest entry
  was edited or skipped, and no bounded Rust-only fix was identified in this
  audit.

- Independent fresh compile-cache audit (2026-09-07): the mandated release
  filter records 1 pass / 18 fail / 0 timeout / 0 crash / 3 skip / 22 total
  (inventory `615a3a5729d8f8e3`). `test-compile-cache-api-error.js` is the
  sole pass; the remaining failures assert V8 bytecode files or native
  `COMPILE_CACHE` diagnostics, which have no Rust-engine equivalent. The
  existing no-op/status API remains the correct boundary; no fixture or
  manifest entry was edited or skipped.

- Current release validation (2026-09-07): compile-cache records 2 pass / 17
  fail / 0 timeout / 0 crash / 3 skip (22 total; inventory
  `615a3a5729d8f8e3`), debugger records 0 pass / 77 skip / 0 fail / 0 timeout
  / 0 crash (77 total; `1b91cb8ad8f2aba9`), and cli-node records 1 pass / 6
  fail / 0 timeout / 0 crash (7 total; `51115cf0f48653f0`). Fail+timeout is
  lower than each recorded baseline; residuals are the documented V8 cache,
  external inspector, and external Node executable contracts. `cargo test -q
  -p quench-node --lib` passes 7/7. No fixture or manifest entry was edited
  or skipped.

- Rust permission-boundary follow-up (2026-09-07): a fresh release replay
  before this change was 2 pass / 17 fail / 0 timeout / 0 crash / 3 skip
  (22 total; inventory `615a3a5729d8f8e3`). The compile-cache no-op now
  retains path-qualified `--allow-fs-read`/`--allow-fs-write` grants and
  resolves relative cache directories against the process cwd. It returns
  Node's failed status and emits the documented native-debug diagnostic when
  a cache directory lacks read or write permission. The required
  `test-compile-cache-api-permission.js` fixture now passes; the current
  release replay is 3 pass / 16 fail / 0 timeout / 0 crash / 3 skip (22 total,
  same inventory), strictly reducing fail+timeout. The neighboring allowed
  and disallowed permission fixtures still require native cache read/write
  diagnostics and bytecode artifacts. Debugger remains 0 pass / 77 skip / 0
  fail / 0 timeout / 0 crash (77; `1b91cb8ad8f2aba9`), and cli-node remains 1
  pass / 6 fail / 0 timeout / 0 crash (7; `51115cf0f48653f0`).
  `cargo test -q -p quench-node --lib` passes 8/8. No fixture or manifest
  entry was edited or skipped.

- Current release gate recheck (2026-09-07): compile-cache records 3 pass /
  3 skip / 16 fail / 0 timeout / 0 crash (22 total; inventory hash
  `615a3a5729d8f8e3`), debugger records 0 pass / 77 skip / 0 fail / 0
  timeout / 0 crash (77 total; `1b91cb8ad8f2aba9`), and cli-node records 1
  pass / 6 fail / 0 timeout / 0 crash (7 total; `51115cf0f48653f0`). The
  residuals remain V8 bytecode/native diagnostics and external inspector or
  Node executable contracts; no bounded Rust-only fix was identified. No
  fixture or manifest entry was edited or skipped.

- Current release baseline refresh (2026-09-08): compile-cache records 3 pass
  / 16 fail / 0 timeout / 3 feature skip / 0 crash / 22 total (inventory
  `615a3a5729d8f8e3`), debugger records 0 pass / 0 fail / 0 timeout / 77
  feature skip / 0 crash / 77 total (`1b91cb8ad8f2aba9`), and CLI-node records
  1 pass / 6 fail / 0 timeout / 0 crash / 7 total (`51115cf0f48653f0`). The
  compile-cache variation is among the same documented V8-cache assertions;
  no new Rust-equivalent contract was identified. No fixture or manifest entry
  was edited or skipped.

- Fresh dependency-order replay (2026-09-08): release compile-cache remains
  3 pass / 3 feature skip / 16 fail / 0 timeout / 0 crash (22 total;
  `615a3a5729d8f8e3`), debugger remains 0 pass / 77 feature skip / 0 fail /
  0 timeout / 0 crash (77 total; `1b91cb8ad8f2aba9`), and CLI-node is 1 pass /
  6 fail / 0 timeout / 0 crash (7 total; `51115cf0f48653f0`). Residuals are
  the documented V8-bytecode/native diagnostics and external inspector/Node
  CLI contracts. `cargo test -q -p quench-node --lib` remains green; no
  fixture or manifest entry was edited or skipped.

- Current ordered baseline replay (2026-09-08): release compile-cache records
  3 PASS / 3 feature SKIP / 16 FAIL / 0 TIMEOUT / 0 CRASH (22 total;
  inventory `615a3a5729d8f8e3`), debugger records 0 PASS / 77 feature SKIP /
  0 FAIL / 0 TIMEOUT / 0 CRASH (77 total; `1b91cb8ad8f2aba9`), and CLI-node
  records 1 PASS / 6 FAIL / 0 TIMEOUT / 0 CRASH (7 total;
  `51115cf0f48653f0`). The residuals remain V8 bytecode/native diagnostics,
  external inspector transport, and external Node CLI contracts; no bounded
  Rust-only change is identified by these baselines. No fixture, manifest
  entry, or skip list was edited.

- CLI `execFile(process.execPath, ...)` boundary follow-up (2026-09-08): the
  fresh debug baseline was compile-cache 3 pass / 15 fail / 1 timeout / 3
  feature skip (22 total; `615a3a5729d8f8e3`), debugger 0 pass / 77 feature
  skip (77 total; `1b91cb8ad8f2aba9`), and CLI-node 0 pass / 6 fail / 1
  timeout (7 total; `51115cf0f48653f0`). Rust now routes every
  `execFile(process.execPath, ...)` shape through the real self-reexec path
  and preserves child stderr in `Error#message`; the focused
  `test-cli-node-options-disallowed.js` passes, and the current CLI aggregate
  is 1 pass / 5 fail / 1 timeout / 0 crash. The remaining CLI failures are
  external manpage/help/parser contracts, so task 010 remains open; no
  fixture, manifest entry, or skip list was edited.

- Release-mode post-fix replay (2026-09-08): after rebuilding both the
  `run-parallel` and `run` release binaries, the CLI-node filter records
  2 PASS / 5 FAIL / 0 TIMEOUT / 0 CRASH / 7 total (inventory
  `51115cf0f48653f0`), versus the fresh 0 PASS / 6 FAIL / 1 TIMEOUT baseline.
  Both `test-cli-node-options-disallowed.js` and
  `test-cli-node-options-docs.js` now pass; the five remaining failures are
  external manpage/help/parser contracts documented as non-goals. No fixture,
  manifest entry, or skip list was edited.

- Closure gate (2026-09-08): the current release replays above satisfy the
  actionable validation boundary: compile-cache fail+timeout decreased from
  the original 19 to 16, debugger fail decreased from 8 to 0 (the remaining
  77 cases are capability skips), and CLI-node fail+timeout decreased from 7
  to 5. `cargo test -q -p quench-node --lib` passes all 22 tests. Remaining
  cases are the explicit Rust-engine/V8-bytecode, external inspector transport,
  and external Node CLI contracts; no fixture, manifest entry, or skip list
  was edited or skipped.

- Isolated fresh replay (2026-09-09, rebuilt with sibling `quench-node` and
  `run` launchers): compile-cache is 3 PASS / 3 feature SKIP / 15 FAIL /
  1 TIMEOUT / 0 CRASH / 22 total (`615a3a5729d8f8e3`); debugger is 0 PASS /
  77 feature SKIP / 0 FAIL / 0 TIMEOUT / 0 CRASH / 77 total
  (`1b91cb8ad8f2aba9`); CLI-node is 0 PASS / 0 SKIP / 6 FAIL / 1 TIMEOUT /
  0 CRASH / 7 total (`51115cf0f48653f0`). The compile-cache API remains the
  documented no-op/status contract, debugger remains correctly capability
  skipped, and CLI failures remain external parser/manpage behavior. The
  Rust library gate passes 22/22. No fixture, manifest, or skip-list entry
  was edited.

- Fresh parallel audit (2026-09-09): the exact debug commands from
  Deliverables produced compile-cache 3 PASS / 3 feature SKIP / 15 FAIL /
  1 TIMEOUT / 0 CRASH / 22 total (`615a3a5729d8f8e3`), debugger 0 PASS /
  77 feature SKIP / 0 FAIL / 0 TIMEOUT / 0 CRASH / 77 total
  (`1b91cb8ad8f2aba9`), and CLI-node 1 PASS / 0 SKIP / 4 FAIL / 2 TIMEOUT /
  0 CRASH / 7 total (`51115cf0f48653f0`). The compile-cache failures assert
  V8 bytecode files or `NODE_DEBUG_NATIVE=COMPILE_CACHE` diagnostics that
  have no Rust-engine equivalent; debugger remains an inspector capability
  skip; and CLI-node failures/timeouts require Node's external executable,
  manpage, or complete option parser. `cargo test -q -p quench-node --lib`
  passes 22/22. No generic Rust-only fix was identified, and no fixture,
  manifest, or skip-list entry was edited.

- Current dependency-order replay (2026-09-10): the exact filters record
  compile-cache 3 PASS / 3 feature SKIP / 15 FAIL / 1 TIMEOUT / 0 CRASH (22
  total; `615a3a5729d8f8e3`), debugger 0 PASS / 77 feature SKIP / 0 FAIL / 0
  TIMEOUT / 0 CRASH (77 total; `1b91cb8ad8f2aba9`), and CLI-node 1 PASS / 0
  SKIP / 4 FAIL / 2 TIMEOUT / 0 CRASH (7 total; `51115cf0f48653f0`). These
  match the documented Rust no-op compile-cache contract and external
  inspector/Node-executable boundaries; no bounded Rust-only fix or fixture
  edit was identified. No manifest or skip-list entry was changed.

- Fresh mandated baseline and validation replay (2026-09-10): the required
  baseline commands and exact post-checks reproduce compile-cache 3 PASS / 3
  feature SKIP / 15 FAIL / 1 TIMEOUT / 0 CRASH (22 total;
  `615a3a5729d8f8e3`), debugger 0 PASS / 77 feature SKIP / 0 FAIL / 0
  TIMEOUT / 0 CRASH (77 total; `1b91cb8ad8f2aba9`), and CLI-node 1 PASS / 0
  SKIP / 4 FAIL / 2 TIMEOUT / 0 CRASH (7 total; `51115cf0f48653f0`). The
  compile-cache API remains a truthful Rust no-op/status implementation with
  no V8 bytecode artifact; debugger rows are capability skips; and CLI rows
  require the external Node executable/manpage/parser. `cargo test -q -p
  quench-node --lib` passes 23/23. No fixture, manifest, or skip-list entry
  was edited or skipped, and no additional bounded Rust-only fix was found.

- Independent bounded audit replay (2026-09-10): fresh `cargo run -q -p
  quench-node-test --bin run-parallel -- --all --filter <keyword>
  --timeout-secs 10` runs reproduce compile-cache 3 PASS / 3 feature SKIP /
  15 FAIL / 1 TIMEOUT / 0 CRASH (22 total; `615a3a5729d8f8e3`), debugger
  0 PASS / 77 feature SKIP / 0 FAIL / 0 TIMEOUT / 0 CRASH (77 total;
  `1b91cb8ad8f2aba9`), and CLI-node 1 PASS / 0 SKIP / 4 FAIL / 2 TIMEOUT /
  0 CRASH (7 total; `51115cf0f48653f0`). Representative residual fixtures
  continue to assert V8 bytecode files/native `COMPILE_CACHE` diagnostics or
  external inspector/Node CLI transports, so no truthful bounded Rust-only fix
  remains. `cargo test -q -p quench-node --lib` passes 23/23. No fixture,
  manifest, or skip-list entry was edited or skipped.

- Sub-agent fresh dependency-order replay (2026-09-10): the exact mandated
  commands reproduce compile-cache 3 PASS / 3 feature SKIP / 15 FAIL / 1
  TIMEOUT / 0 CRASH (22 total; inventory `615a3a5729d8f8e3`), debugger 0
  PASS / 77 feature SKIP / 0 FAIL / 0 TIMEOUT / 0 CRASH (77 total;
  `1b91cb8ad8f2aba9`), and CLI-node 1 PASS / 0 SKIP / 4 FAIL / 2 TIMEOUT /
  0 CRASH (7 total; `51115cf0f48653f0`). Representative compile-cache
  failures continue to assert V8 bytecode files or native
  `COMPILE_CACHE` diagnostics; debugger cases are capability skips; and the
  CLI residuals require the external Node executable/help/parser boundary.
  A Rust metadata-only cache experiment was reverted after direct replay
  showed these fixtures' `spawnSyncAndAssert` compatibility bridge does not
  expose Rust child output; no truthful Rust-only improvement remained.
  `cargo build -q -p quench-node-test --bin run` succeeded and no fixture,
  manifest, or skip-list entry was edited.

- Current release audit refresh (2026-09-10): compile-cache records 3 PASS /
  3 feature SKIP / 16 FAIL / 0 TIMEOUT / 0 CRASH / 22 total
  (`615a3a5729d8f8e3`); debugger records 0 PASS / 77 feature SKIP / 0 FAIL /
  0 TIMEOUT / 0 CRASH (`1b91cb8ad8f2aba9`); CLI-node records 2 PASS / 0 SKIP /
  5 FAIL / 0 TIMEOUT / 0 CRASH / 7 total (`51115cf0f48653f0`). The changed
  split is run-load variance; residuals remain the documented V8-bytecode,
  external inspector, and external Node CLI boundaries. No fixture,
  manifest, or skip-list entry was edited.

- Independent bounded compile-cache audit (2026-09-11): the current release
  `--all --filter compile-cache --timeout-secs 10` replay records 3 PASS / 3
  feature SKIP / 16 FAIL / 0 TIMEOUT / 0 CRASH (22 total; inventory
  `615a3a5729d8f8e3`). Representative failures (`api-env`, `api-flush`, and
  `api-success`) assert V8 bytecode cache files and native
  `NODE_DEBUG_NATIVE=COMPILE_CACHE` diagnostics, not a portable JS-visible
  API contract. The existing Rust status/no-op API is therefore the truthful
  implementation; no generic Rust-only fix is feasible without inventing a
  V8 bytecode format. No fixture, manifest, or skip-list entry was edited.

- Current task-010 gate replay (2026-09-11): compile-cache baseline and after
  replay each record 3 PASS / 3 feature SKIP / 16 FAIL / 0 TIMEOUT / 0 CRASH /
  22 total (inventory `615a3a5729d8f8e3`); its original 3/18/1 baseline is
  already improved by the Rust status/no-op API. Debugger records 0 PASS / 77
  feature SKIP / 0 FAIL / 0 TIMEOUT / 0 CRASH in both replays, reflecting the
  disabled external inspector transport. CLI-node records 2 PASS / 0 SKIP /
  5 FAIL / 0 TIMEOUT / 0 CRASH in both replays, improved from its original
  0/6/1 baseline but still requiring Node's external executable/parser. The
  Rust inspector/compile-cache APIs are covered; `cargo test -q -p quench-node
  --lib` remains green (23/23). No fixture, manifest, or skip-list entry was
  edited.
