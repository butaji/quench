# 077: Purge inactive polyfill history fragments

`polyfills/bootstrap.rs` documents that most fragment files are retained as
source history but are not compiled. Keep only the explicitly registered
bootstrap/post-bootstrap fragments (about 20 files); the inactive set is about
23.9k lines.

## Plan

- Derive the active file set from the bootstrap and post-bootstrap tables.
- Check tests, scripts, and documentation for direct fragment-path reads.
- Move inactive fragments to an archive or remove them from the source tree.
- Re-run polyfill registry/bootstrap tests and Node smoke/oracle tests.

## Acceptance

The generated bootstrap order and bytes are unchanged, inactive files are no
longer mistaken for implementation, and all active host tests pass.
