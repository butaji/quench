# 10: Extend the direct OXC specializer

## Outcome

Compile JavaScript directly from OXC ASTs into the verified residual program,
extending coverage in response to the full Test262 failure inventory.

## Current gap

The reference generating extension handles an ES5-oriented subset, while Quench's complete syntax currently flows through a separate reducer and operation model that must not survive cutover.

Regression against the reference subset (2026-09-23 core review,
`target/reviews/core-review-2026-09-23/`): the next core rejects pinned v2's
own bundled Richards, DeltaBlue, and Splay fixtures with `invalid residual
program: function N call is invalid` (`bytecode/validate.rs`, `Op::Call` /
`Op::CallKnown`). DeltaBlue and Splay also fail through the base.js harness.
The compiler emits call instructions that the validator's register-window or
known-target rules reject. Every program pinned v2 accepts must specialize and
validate; the task-26 gate cannot be measured until this is fixed.

## Dependencies

See this task's `depends_on` in [index.json](index.json).

## Scope and implementation sequence

1. Use one exact OXC version for parsing and semantic facts; distinguish Script, Module, and Eval goals and preserve source diagnostics.
2. Extend lexical-address, capture, declaration-instantiation, binding-time, effect, control-flow, liveness, and root-map analyses as data passes.
3. Lower all supported syntax through the macro-declared opcode vocabulary and canonical semantic operations; use side tables for rich metadata.
4. Reject only actual parse/early errors or explicit resource exhaustion; compact-domain overflow selects the wide representation.
5. Preserve v2's integrity rule: specialization may use syntax, constants, lexical facts, and bounded local patterns, never names, fixture identity, checksums, benchmark vocabulary, or whole-body matching.
6. Implement syntax and semantic lowering in failure-family slices. Start with
   the next unsupported constructs exposed by the Test262 sweep (currently
   destructuring/rest/spread as applicable); do not attempt a speculative
   "complete modern JavaScript" pass before the suite identifies demand.

## Verification

Use existing Test262 cases to drive OXC syntax-family coverage and early-error
behavior. Do not add tests or alter the Test262 harness. Inspect residual
verification and specialization decisions as part of implementation.

## Definition of done

- The next engine does not call the legacy reducer or execute legacy operations.
- Every emitted residual verifies before execution.
- Static facts have one analysis authority and exact dynamic fallback.

## Performance evidence

Report parse/specialization time, allocator capacity, residual size, and peak compile RSS on representative small and large sources.

## Implementation evidence

The OXC specializer now lowers both block and concise arrow functions through
the same residual closure path as ordinary functions. Arrow bodies receive the
same parameter/local analysis, capture conversion, and verifier checks; an API
regression executes `(x) => x + 1` through the next runtime.

Template literals now lower directly from OXC as ordered string-addition
operations, preserving interpolation evaluation order and cooked-template
diagnostics without introducing a second execution path. Lexical `let` and
`const` declarations use the existing analyzed local-slot representation.
Sequence expressions likewise compile as ordered residual expressions and
return their final value, preserving side effects without a legacy reducer.
Nullish coalescing is lowered with explicit null/undefined checks so falsy
non-nullish values remain observable and the right operand stays lazy.
Optional static and computed member chains now lower through a guarded
residual prefix that evaluates property keys only for non-nullish bases and
returns `undefined` otherwise; the subset suite covers both ordinary and
chained optional access.
Optional direct calls and static method calls share the guarded prefix, so
nullish receivers skip argument evaluation while non-nullish calls retain the
ordinary receiver and method-cache metadata.
Class getter/setter methods now lower through the same closure pipeline and
install accessor descriptors with `Object.defineProperty`; instance and static
accessors preserve receiver identity and merge paired getter/setter definitions.

Function constructibility is now explicit residual metadata: arrows, concise
object methods, and class methods are non-constructible, while ordinary
functions remain constructible. Prototype installation and `new` validation
derive from that single bit. Strict direct eval uses OXC parsing and the shared
parameter early-error pass for function-containing eval source; the existing
`param-eval-stricteval.js` case now passes. Full stage 10 is currently
10,461/11,101 (640 remaining across 303 failure families), so stage 10 remains
open. Binary-expression
left operands are materialized before right-side effects, fixing the shared
evaluation-order failures across the stage. Generator-function prototypes now
inherit from Function.prototype and variable-bound anonymous generators infer
their names; the existing 290-case generator cohort is 289/290, with
cross-realm eval remaining. Delegated yield preserves iterator
state across suspension; sync delegated throws and async-generator delegated
throw/return completions now use the iterator and Promise-reaction paths. The
existing 415-case `yield-star` cohort passes in five batches. Async-generator
requests are queued while the active request awaits, and the existing
`async-generator` cohort is 661/663. Dynamic import now uses the intrinsic
Promise, coerces specifiers, enumerates import attributes, and converts abrupt
completions into rejected promises; the existing 48-case specifier-abrupt
cohort passes. Host module-graph resolution and namespace objects remain
unimplemented in this slice. Remaining shared failure families include
dynamic-import/module execution, dynamic-eval compilation/context, and
optional-call/super dispatch. Super writes and `Reflect.set` now share
receiver-aware `OrdinarySet` behavior; object-literal `__proto__` data updates
the internal prototype, and the two-property literal specialization does not
bypass that rule. `super` obtains the HomeObject prototype through a hidden
intrinsic path rather than mutable `Object.getPrototypeOf`. Residual function
metadata now carries the captured
`super_home_atom`, allowing direct eval to resolve the exact HomeObject; the
four existing super `val-from-eval` cases pass and the full `super/` cohort is
88/94. Verification used stage 10 only, in at-most-100-case batches with a
300000 ms per-test timeout;
no harness edits, unit tests, or new tests were made.
`Reflect.set` now rejects primitive targets through its canonical TypeError
path; this exposes Test262 namespace-write cases previously masked by dynamic
import resolving to a primitive specifier string. Those cases remain failures
until module namespace execution is implemented. The staged
`dynamic-import/` cohort is 682/1,004 (322 failures across 11 batches of at most
100); the largest groups require namespace resolution and export values,
rejection propagation, and async completion. Residual programs now retain the
source unit name in their versioned binary representation, and the next-host
adapter preserves the Test262 path for both script and module dispatch so
future module resolution has an explicit referrer. This metadata plumbing does
not itself implement module loading. Relative dynamic-import resolution now
reads through the Test262 host boundary; the existing 16-case
`file-does-not-exist` cohort passes 16/16. The latest complete stage-10 run is
10,520/11,101 (581 failures across 300 families), up 44 passes from the
10,476/11,101 checkpoint. Direct-eval `super` now carries the implicit
constructor HomeObject through field initialization, and the existing
24-case direct/indirect `super` eval cohort passes. Dynamic import preserves
the OXC import phase; source-phase imports of source-text modules reject with
SyntaxError, and the existing 16-case cohort passes.
The Test262 host now resolves relative paths; missing files reject, while
`type: "text"` and `type: "json"` produce immutable default-export namespace
objects. The import-attributes cohort contains 65 cases: 56 pass and nine
fail, all expecting evaluation and exported bindings from JavaScript modules.
Test262 module dispatch now uses OXC's explicit `mjs` source goal and strict
root semantics instead of treating module files as scripts; the four existing
`class-name-ident-await` cases pass. Script conformance remains on the
unspecialized residual path. A bounded full stage-10 rerun completed all 112
batches (maximum 100 cases each) at 10,523/11,101 (578 failures, 299 families).
Static import/export linking and module-owned environments remain unimplemented.
Construction now shares the VM's `IsConstructor` predicate across ordinary,
super, Proxy, and `Reflect.construct` paths; Proxy construction preserves the
actual `newTarget` for trap and fallback. The latest full stage-10 sweep remains
10,523/11,101 (578 failures across 298 families), so this semantic cleanup has
not yet increased the stage pass count; poisoned-`__proto__` class construction
remains a focused failure family.

The next stage-10 sweep completed all 112 batches at 10,649/11,101 (452
failures across 298 families). Dynamic import now evaluates a conservative
static module subset: literal bindings, local and same-module re-exports,
constant default bindings, and constant/builtin-error throw completions. The
`dynamic-import/` cohort rose to 869/1,004 (135 failures). Async-from-sync
iteration now awaits yielded values through the Promise continuation, including
async-generator delegation. The remaining concentrated module failures require
live bindings and module-graph linking/evaluation; do not widen the static
specialization without preserving exact fallback semantics. Verification used
only existing stage-10 Test262 cases with a 300000 ms per-case timeout and
batches capped at 100; no unit tests, added tests, harness changes, or whole
inventory run.
After correcting AsyncFromSyncIteratorContinuation to read `done` before
`value`, the next full stage-10 sweep reached 10,677/11,101 (424 failures,
289 families); its existing 28-case sync-delegation cohort passes 28/28. The
namespace now also owns the `Symbol.toStringTag` value, but its exotic
descriptor/define semantics and stable module identity remain open.
OXC module parse/semantic early errors are now propagated as rejected
SyntaxErrors; the existing 16-case script-as-module import cohort passes
16/16. Module namespace identity is cached by resolved source and import type
with cache values included in VM roots; two reuse-namespace cases and all 65
import-attributes cases pass. The latest full stage-10 sweep is 10,694/11,101
(407 failures across 287 families), still the first failing sequential prefix.
Module namespaces now carry an explicit object internal slot; export
descriptors report `writable: true` while writes remain rejected, and
compatible no-change definitions are accepted. The existing namespace cohort
is 61/67, with nested namespace re-export evaluation remaining. The specifier
string success fallback has been removed: unresolved or unevaluated imports
reject instead of fabricating a namespace. The dynamic-import cohort is
895/1,004 (109 failures across 27 families), and a new full stage-10 sweep is
10,695/11,101 (406 failures across 274 families). Its dominant gap is now
module-record evaluation/linking, not the former string fallback.
Static re-export linking now recursively resolves named exports, `export *`,
and namespace re-exports through host-provided module sources; ambiguous and
unresolved named bindings reject with SyntaxError. The existing 176-case
dynamic-import `catch/` cohort now passes 176/176. The namespace cohort is
65/67; the remaining two cases require evaluating exported function/class
declarations. The latest full stage-10 sweep is 10,731/11,101 (370 failures,
273 families), and the dynamic-import subtree is 937/1,004 (67 failures across
23 families). The next required authority is dynamic module compilation and
execution in the same runtime/program model; no later stage has started.
The latest bounded dynamic-import rerun confirms 937/1,004 passes across
eleven batches of at most 100. Its largest family is 45 cases rejected as
`dynamic import module evaluation is unsupported`; the remaining 22 are
singletons, including unsupported eval/import-meta/defer syntax, one missing
`import` binding in eval, one host-resolution failure, and one errored-module
rejection mismatch. The two namespace failures specifically need evaluated
local declarations behind `export * as`. This evidence keeps the next slice on
the module-record/program-ownership boundary, rather than growing the static
re-export recognizer with more source-shape exceptions. `git diff --check` and
`cargo check -p quench-runtime-next -p quench-test262` pass.
A complete stage-10 sweep on the initial module-record implementation was
10,731/11,101 (370 failures, 273 families) over 112 batches of at most 100.
Its top family was the same 45-case evaluated-module gap; next were error
constructor identity, constructor checks, and iterator behavior. No later
stage was started.
Module namespace caching now uses rooted module records with an explicit
`Loading → Linking → Evaluating → Evaluated | Errored` lifecycle. Repeated
imports share either the completed namespace or the exact thrown value;
imports joining an in-progress evaluation attach rooted promises that the
record settles together.
The follow-up dynamic-import subtree rerun remains 937/1,004, so this is the state/identity
foundation, not a claim that executable module evaluation is complete.

Eval's existing simple-source path now recognizes `new` followed by lexical
whitespace and an identifier constructor, with an optional single argument,
and routes it through the VM's `Construct` authority. The existing stage-10
`language/expressions/new` subtree improved from 69/73 to 72/73; both
whitespace-separated `new Number` eval cases now pass. Constructor activations
also capture `new.target` per frame and `super()` forwards that captured value;
the existing `new.target` subtree passes 14/14. The remaining failure in the
`new` subtree is the cross-realm non-constructor error-realm case. These are
targeted subtree results only; a complete stage-10 rerun is required before
updating its baseline.

The complete sequential stage-10 sweep after those changes completed all
11,101 cases in 112 batches (each at most 100): 10,733 passed, 368 failed
across 272 families, improving the previous 10,731 baseline by two. No later
stage ran. The bounded dynamic-import subtree remains 937/1,004 in 11 batches;
its largest shared family is 45 cases requiring executable module evaluation
(including side effects, mutable bindings, and exported function behavior).
The next 22 failures split into unsupported module syntax / module-goal eval,
one eval import binding, one errored-module rejection identity mismatch, and
one host-resolution mismatch. Do not grow the static recognizer with
fixture-specific forms; executable module evaluation needs a same-VM
program/function ownership path. All runs retained
`TEST262_TEST_TIMEOUT_MS=300000`; no harness or tests were changed.

Ported the old-core Test262 realm's `parseInt` and `parseFloat` globals as
realm-bound native function objects. The existing
`new/non-ctor-err-realm.js` case passes 1/1. The next complete stage-10 sweep
ran 11,101 cases in 112 batches of at most 100 and reached 10,734/11,101
(367 failures across 271 families), one additional pass. The dynamic-import
45-case executable-module family remains the dominant gap; no later stage ran.

Direct calls to `eval` with spread arguments now retain direct-eval semantics
through the dedicated argument-array call opcode instead of lowering to
`eval.apply`. The existing empty-leading and empty-trailing spread cases pass
2/2. Their cumulative stage-0–10 run kept stages 0–9 green (1,157/1,157) and
measured stage 10 at 10,921/11,101 over batches capped at 100. Eval assignment
write-through also respects active-function shadowing: a local binding no
longer causes the same-named outer global to be updated. See task 20 for the
full run provenance and the still-open private-name identity family.

The builtin-property slice adds the missing ES5 Math trigonometric functions
through the common native registry and exposes Number `toFixed`/`toPrecision`
on the shared Number prototype. The existing 21-case stage-10
`property-accessors` cohort improved from 18/21 to 20/21; its remaining check
is the broader Date prototype surface, beginning with `getUTCFullYear`.
Adding `DateGetFullYear` to the native registry also removed a 21-case
installation panic. Rechecking the sequential prefix exposed that
`Array.prototype.constructor` had been installed enumerable; switching it to
the builtin descriptor installer restored the existing stage-3
`acquire-properties-from-array.js` case and the full stage 3 (145/145).
The ordered `0 10` run now passes stages 0–9 (1,157/1,157) and stage 10 is
11,010/11,101 (91 failures across 75 families). Stage 10 remains open; the
next stage-10 builtin slice is the Date prototype's shared method set, not
individual property-name patches. Verification used existing Test262 only,
`TEST262_TEST_TIMEOUT_MS=900000`, and batches capped at 100; no harness or
test files changed.

The Number builtin correction now uses the legacy core's smallest positive
subnormal bit pattern for `Number.MIN_VALUE`; both existing multiplication and
division underflow cases pass. Date native function lengths are derived from
the same prototype-method table and setter argument limits. The latest full
stage-10 sweep is 11,013/11,101 (88 failures across 72 families). The ordered
`0 10` prefix passes stages 0–9 at 1,157/1,157 and stage 10 at 11,013/11,101;
this remains the first failing stage. Evidence is in task 20. Verification
used only existing Test262, a 900,000 ms case timeout, and batches capped at
100; no new tests or harness edits were made.

Ported the Date local/UTC getter and setter families as shared Date-component
operations: the prototype and Date-instance fallback use the same method table;
setters preserve receiver validation, argument-conversion order/arity,
invalid-date recovery for full-year setters, component overflow normalization,
local-versus-UTC interpretation, and TimeClip. The constructor now interprets
multi-argument components in local time, matching the same authority; this
corrected the existing harness `assertRelativeDateMs` case after date-sensitive
`getTimezoneOffset` exposed the former UTC-construction mismatch. Date native
lengths derive from the setter arity metadata. The existing stage-10
`property-accessors` cohort passes 21/21; the current full stage-10 sweep is
11,011/11,101 (90 failures across 74 families). This is the first Date surface
slice, not a claim that Date formatting/Intl semantics are complete. The
ordered `0 10` run passes stages 0–9 at 1,157/1,157 and stage 10 at
11,011/11,101; see task 20. Every case used
`TEST262_TEST_TIMEOUT_MS=900000` and batches capped at 100. No harness or test
files changed.

BigInt exceptional completion now matches the legacy core: division/remainder
by zero, negative exponentiation, and excessive exponents produce RangeError;
invalid BigInt data remains TypeError. Unary plus on BigInt now raises TypeError
in the shared unary VM operation rather than coercing through `ToNumber`. The
existing stage-10 BigInt-named cohort passes 146/146 in two batches (100, 46).
A complete stage-10-only sweep passes 11,017/11,101 (84 failures across 68
families), improving the preceding 11,013 checkpoint by four; all batches
remain capped at 100 with `TEST262_TEST_TIMEOUT_MS=900000`. The next largest
family is seven unsupported `import defer` statements; module evaluation,
async/module lifetimes, tagged-template identity, and other language gaps remain.
Evidence: `target/test262-next-stage10-bigint-unary-fix.log`. No unit/project
tests, new tests, harness edits, or later stages were run.

The exponentiation edge cases exposed a divergence from the old core: Rust
`powf` returns `1` for magnitude-one bases with infinite exponents, while
ECMAScript requires `NaN`. Ported the old core's single `exponentiate`
authority—including NaN, signed zero, signed infinity, and odd-integer rules—
and routed `**`, numeric fast/slow paths, and `Math.pow` through it. The
existing stage-10 `exponentiation` cohort passes 62/62. The fresh sequential
0–10 prefix passes stages 0–9 (1,157/1,157) and stage 10 at 11,025/11,101
(76 failures across 60 families). Evidence:
`target/test262-next-stage10-exponentiation.log` and
`target/test262-next-prefix-0-10-exponentiation.log`. Verification used only
existing Test262, a 900,000 ms case timeout, and runner batches capped at 100;
no harness changes, new tests, unit/project tests, or later stages ran.

The stage-10 `import-defer` path filter covers 147 existing cases and passes
142/147; the five failures share `statement is outside the supported subset`
while compiling async-module fixtures with top-level await/static imports.
This is a module-record/link-evaluation lifetime gap, not an import-expression
parse gap. Evidence: `target/test262-next-stage10-import-defer.log`; all cases
used the mandatory timeout and the runner's batches remained capped at 100.

The Test262 `ToNumber` path now uses one parser ported from the legacy core's
string conversion authority, including ECMAScript whitespace trimming, empty
string-to-zero behavior, radix-prefixed strings, and rejection of nonstandard
case variants of `Infinity`; string objects reach the same primitive path.
The existing stage-10 `unary-plus` cohort passes 17/17. A stage-10-only full
sweep passes 11,022/11,101 (79 failures across 63 families), five more than
the prior 11,017 checkpoint, with no remaining unary-plus or BigInt failure
families. Batches remain capped at 100 and every case uses
`TEST262_TEST_TIMEOUT_MS=900000`. Evidence:
`target/test262-next-stage10-number-string-coercion.log`. No unit/project
tests, new tests, harness edits, or later stages were run.

The language `delete` operator no longer lowers sloppy member deletion through
`Reflect.deleteProperty`, whose object-only contract incorrectly rejected
primitive bases. Strict and sloppy member deletion now share the VM's
reference-delete operation; it boxes the base and uses strictness only to
select throw-versus-false after the canonical delete result. The existing
stage-10 `delete` cohort passes 184/184. A fresh ordered 0–10 prefix passes
stages 0–9 (1,157/1,157) and stage 10 at 11,026/11,101 (75 failures across 59
families). Evidence: `target/test262-next-stage10-delete-reference.log` and
`target/test262-next-prefix-0-10-delete-reference.log`. All batches were at
most 100 cases with `TEST262_TEST_TIMEOUT_MS=900000`; no harness edits, new
tests, or unit/project tests occurred.

Destructuring assignment now retains simple target references across the
source read/default phases. Object assignment evaluates its target reference
before `GetV`; array assignment evaluates the target before `IteratorStep` but
defers computed-key coercion until the reference is written. The three existing
target-evaluation-order cases pass, as does the 8-case stage-10
`assignment/destructuring` filter. A fresh ordered 0–10 prefix passes stages
0–9 (1,157/1,157) and stage 10 at 11,029/11,101 (72 failures across 56
families), closing three cases versus the prior 11,026 checkpoint. Evidence:
`target/test262-next-stage10-destructuring-reference-order.log`,
`target/test262-next-stage10-assignment-destructuring-order.log`, and
`target/test262-next-prefix-0-10-destructuring-reference-order.log`. Every
case used `TEST262_TEST_TIMEOUT_MS=900000`; batches remained at most 100. No
harness changes, new tests, or unit/project tests occurred.

The `new.target` stage-10 cohort now passes 16/16. Arrow activations no longer
shadow the enclosing function's captured dynamic binding with an artificial
`undefined`; direct eval in a class-field initializer also observes
`undefined` while still executing its surrounding eval statements. Together
with the preceding destructuring-reference slice, the fresh ordered 0–10
prefix passes stages 0–9 (1,157/1,157) and stage 10 at 11,033/11,101 (68
failures across 53 families). Evidence:
`target/test262-next-stage10-new-target-lexical.log` and
`target/test262-next-prefix-0-10-new-target-activation.log`. Every batch was
capped at 100 with `TEST262_TEST_TIMEOUT_MS=900000`; no new tests, harness
edits, or unit/project tests occurred.

Synthesized derived constructors now issue the VM's super-construct operation,
which derives the superclass from the active class function's internal
prototype rather than a captured `super` binding. Native results share the
`newTarget.prototype` adjustment in `construct_value_with_new_target`, restoring
built-in subclass semantics. The existing poisoned-`__proto__` class cohort
passes 3/3; optional-chain member-expression passes 1/1; the broad existing
stage-10 `class/` path filter passes 4,048/4,059 in batches capped at 100. The
stage-0 `isConstructor.js` case also passes 1/1 after `IsConstructor` was
returned to the authoritative constructibility metadata plus async/generator
kind facts. Fresh ordered 0–10 passes stages 0–9 (1,157/1,157) and stage 10 at
11,036/11,101 (65 failures across 51 families), three fewer failures than the
prior sequential result. Evidence:
`target/test262-next-stage10-constructor-poisoning.log`,
`target/test262-next-stage10-optional-member-expression.log`,
`target/test262-next-stage10-class-derived-constructors.log`,
`target/test262-next-stage0-isconstructor.log`, and
`target/test262-next-prefix-0-10-derived-constructors-final.log`. Every
runner had `TEST262_TEST_TIMEOUT_MS=900000`; batches were at most 100. No
harness edits, new tests, or unit/project tests occurred.

Class lowering now handles `extends null` as a distinct heritage case: the
derived prototype's `[[Prototype]]` is initialized directly to null rather than
reading a nonexistent `null.prototype`. Non-null heritage still reads the
constructor's `prototype` property and follows the same generic `Object` path.
The two existing `super` property cases for classes extending null pass 2/2.
Fresh ordered stages 0–10 pass stages 0–9 (1,157/1,157), and stage 10 passes
11,038/11,101 (63 failures across 50 families), two more cases than the
previous checkpoint. The entire stage ran in 112 batches of at most 100 with
`TEST262_TEST_TIMEOUT_MS=900000`; no later stage or whole inventory ran. No
Test262 harness changes, new tests, or unit/project tests were made.

Grouping expressions now preserve the unresolved identifier reference through
`delete`, so sloppy `delete (x)` lowers to the same `DeleteName` operation as
bare `delete x` instead of loading `x` first. The existing grouping cohort
passes 9/9. A fresh ordered 0–10 run passes stages 0–9 (1,157/1,157) and stage
10 at 11,040/11,101 (61 failures across 49 families), two more cases than the
previous checkpoint. All batches were at most 100 with
`TEST262_TEST_TIMEOUT_MS=900000`; no harness edits, new tests, or unit/project
tests occurred.

Static-field arrow creation now captures the initializer's explicit `this`
override into the arrow's lexical scope; `this` and super-property receivers
consume that same captured binding. The existing
`static-field-init-this-inside-arrow-function.js` and
`super-access-from-arrow-func-on-field.js` cases pass 2/2; the full existing
`static-field` filter passes 18/20. Direct-eval `this` and anonymous function
name inference remain in that filter's two failures. A fresh ordered 0–10 run
passes stages 0–9 (1,157/1,157) and stage 10 at 11,047/11,101 (54 failures
across 43 families). This keeps the receiver dynamic with respect to class
initialization while preserving the OXC specializer's shared lexical
representation. Every case used `TEST262_TEST_TIMEOUT_MS=900000`; batches were
capped at 100. No harness edits, new tests, or unit/project tests occurred.

The direct-eval receiver now follows the explicit call context during field
initialization, and anonymous field function definitions receive the
spec-derived key name (with private names rendered using `#`). The existing
`static-field` stage-10 filter now passes 20/20. A fresh cumulative 0–10
verification passes stages 0–9 (1,157/1,157) and stage 10 at 11,049/11,101
(52 failures across 41 families), improving the prior checkpoint by two cases
and two failure families. All cases used
`TEST262_TEST_TIMEOUT_MS=900000` in batches capped at 100. No harness edits,
new tests, or unit/project tests occurred.

Construction now distinguishes base and derived constructors. Derived
activations do not receive a provisional base object; completion carries the
activation's initialized `this` back to `[[Construct]]`, which applies the
object-return / `undefined` / primitive-return rules. Base constructors use
their allocated receiver unless they return an object. When `newTarget.prototype`
is not an object, the user-function fallback now comes from `newTarget`'s realm
`Object.prototype`. This removed a stale heap handle previously returned by
`Reflect.construct` and fixes existing `super/realm.js`. Full stage 10 now passes
11,043/11,101 (58 failures across 46 families). The ordered 0–10 prefix passes
stages 0–9 (1,157/1,157) and reports the same stage-10 result. Every case had
`TEST262_TEST_TIMEOUT_MS=900000`; stage batches were capped at 100. No harness
edits, new tests, or unit/project tests occurred.

Super-call lowering now captures the class constructor in one uniquely named
lexical slot, resolves its current `[[Prototype]]` through `[[GetPrototypeOf]]`
at call time, and uses the active constructor object for synthesized derived
constructors. This preserves mutations to a class constructor's prototype,
avoids consulting a poisoned `Object.prototype.__proto__` getter, and lets
arrows use the enclosing constructor's super authority. The existing stage-10
`super/` cohort passes 94/94. A complete stage-10 run in 112 batches of at most
100 passes 11,045/11,101 (56 failures across 45 families). Remaining failures
include async/module evaluation, parser gaps, and field-initializer lexical
`this`; the latter remains a concrete class-lowering gap. The cumulative 0–10
rerun confirms stages 0–9 still pass (1,157/1,157), followed by the same
stage-10 result. All cases used
`TEST262_TEST_TIMEOUT_MS=900000`; no harness edits, new tests, or unit/project
tests occurred.

Latest class-field and class-name lowering slice: static-field arrows capture
the initializer receiver, direct eval temporarily observes that call-context
receiver, anonymous field functions get an inferred property name (including
`#private`), and anonymous class variable initializers receive their inferred
name before static fields run. Existing `static-field` passes 20/20 and
`class-name-static-initializer-anonymous.js` passes. The fresh ordered 0–10
verification for the field changes passed stages 0–9 and stage 10 at
11,051/11,101 (50 failures across 39 families), one case and family better
than the preceding result. The remaining class-filter failures are computed
field order, class-accessor parsing, and private-proxy behavior. All runner
cases retain a positive 900,000 ms timeout and batches of at most 100; no
harness edits, new tests, or unit/project tests occurred.

Explicit class constructors no longer inherit the ordinary method name
`constructor`; ClassDefinitionEvaluation supplies the class name, including
the empty name, before static initialization. The existing `class/name.js`
case passes, and the broad stage-10 `class/` filter now passes 4,056/4,059.
The three remaining class-filter failures are computed-field order,
class-accessor parsing, and a proxy/private-field call. A fresh cumulative
0–10 run is underway; the last complete checkpoint is 11,050/11,101 at stage
10 with stages 0–9 passing.

Fresh ordered confirmation for the constructor-name change passes stages 0–9
(1,157/1,157) and stage 10 at 11,051/11,101 (50 failures across 39 families).
This is the current Test262 checkpoint. Every case used the required 900,000 ms
timeout; batches were capped at 100. No harness edits, new tests, or
unit/project tests occurred.

The `super()` path now carries a derived-constructor binding-time fact into
lexical arrows. `BindThisValue` updates the enclosing derived activation and
its arrow frames, while arrow lowering emits the binding transition without
duplicating instance-field initialization. The existing `lexical-super` filter
passes 4/4, including the immediate-arrow derived constructor. The fresh
cumulative 0–10 run for this activation change is in progress; the preceding
checkpoint remains 11,051/11,101 at stage 10.

Fresh ordered verification confirms stages 0–9 at 1,157/1,157 and stage 10 at
11,052/11,101 (49 failures across 38 families), one additional case and family
closed by lexical-arrow `BindThisValue`. The `lexical-super` filter remains
4/4. All cases used the required positive 900,000 ms timeout and batches were
capped at 100; no harness edits, new tests, or unit/project tests occurred.

Deferred instance-field blocks now allocate a fresh virtual-register window
above the constructor body's high-water mark. This prevents out-of-line field
initialization after `super()` from clobbering live user registers. The
existing stage-10 `call-expression.js` path filter passes 64/64, and
`lexical-super` remains 4/4; the broad `class/` filter remains 4,056/4,059.
A fresh ordered 0–10 run is underway; the last complete checkpoint remains
11,052/11,101 at stage 10.

Fresh ordered verification confirms stages 0–9 at 1,157/1,157 and stage 10 at
11,053/11,101 (48 failures across 38 families). The out-of-line register
window closes one more Test262 case; the 64-case optional-call filter and
4-case lexical-super filter are both green. The full run retained the
900,000 ms timeout and 100-case batch cap. No harness edits, new tests, or
unit/project tests occurred.

Tagged-template objects now use a compile-time site address and a realm-owned
identity cache. Cache hits bypass array construction; the first miss defines
the non-enumerable frozen `raw` property and freezes cooked/raw arrays using
engine-owned intrinsics. Member-expression tags retain their call receiver.
The existing `tagged-template/` filter passes 26/27, with only
`cache-eval-inner-function.js` still failing on direct-eval closure capture.
Cache identity, chaining, descriptors, freezing, and TCO cases in that path
pass. A new cumulative 0–10 check is next; the last complete checkpoint is
11,053/11,101 at stage 10.

The first post-tagged-template cumulative run exposed a GC-root omission: realm
template-object cache values were not in the tracing root set. Three existing
stage-0 `deepEqual` tests consequently reused collected template arrays while
formatting assertion messages. The realm cache values are now part of the
collector's root fold, preserving its existing cache key and ownership model.
The existing stage-0 cohort passes 116/116, and a fresh cumulative 0–5 prefix
passes stages 0–5 at 726/726. Stages 6–9 also pass individually in sequence
(19/19, 62/62, 347/347, and 3/3); stage 10 is now running in batches capped at
100. Every runner invocation set `TEST262_TEST_TIMEOUT_MS=900000`. No Test262
harness edits, new tests, or unit/project tests were made.

The full existing stage-10 sweep now passes 11,061/11,101 (40 failures in 31
families), closing eight cases versus the preceding checkpoint. The tagged
template path remains 26/27; its sole case is `cache-eval-inner-function.js`
and the remaining error is the direct-eval closure capture (`a is not
defined`). The stage-10 sweep used 112 batches of at most 100 cases, each with
the required 900,000 ms timeout. Next work should group the remaining compiler,
eval/module, activation, and object/class semantic families rather than chase
isolated cases.

One object-authority slice is now implemented: object-literal static data
properties emit `DefineField` (`[[DefineOwnProperty]]` behavior), distinct from
assignment `SetField` (`[[Set]]`). The existing stage-10
`expressions/object/` filter passes 1,167/1,170 and closes the inherited
read-only prototype property case. The three failures left in that filter are
the two direct-eval accessor-closure cases and one object-method receiver case.
The fresh ordered 0–10 re-sweep passes stages 0–9 and stage 10 at
11,062/11,101 (39 failures across 30 families), one more case closed. The
targeted object-expression cohort remains 1,167/1,170. Every runner used
batches capped at 100 and `TEST262_TEST_TIMEOUT_MS=900000`; no harness edits,
new tests, or unit/project tests were made.

### 2026-09-24: identifier calls through `with`

Identifier calls compiled inside a `with` body now obtain their receiver from
the same name-resolution result used for environment references. The resolver
retains whether the binding came from a `with` environment, so a binding on
`globalThis` is not confused with the global environment's absent
`WithBaseObject`. The existing `call/with-base-obj.js` Test262 case passes.
The fresh ordered 0–10 prefix preserves stages 0–9 at 1,157/1,157 and stage
10 improves to 11,063/11,101 (38 failures across 29 families). Every runner
used `TEST262_TEST_TIMEOUT_MS=900000` and batches of at most 100. No harness
edits, new tests, or unit/project tests occurred.

### 2026-09-24: eval `typeof` and computed class-field keys

Eval-string `typeof` now shares the VM's canonical value classification,
recognizes ECMAScript whitespace between the operator and operand, and accepts
the existing parenthesized operand form. The Test262 `typeof/syntax.js` case
passes; the direct-eval lexical pre-initialization cohort remains 6/6. Class
computed keys are now materialized once in class-element source order before
static field initializers, then reused by method and field definitions. The
existing 4,059-case `class/` cohort is 4,057/4,059, and the computed
static/non-static ordering case passes. Fresh ordered 0–10 verification passes
stages 0–9 at 1,157/1,157 and stage 10 at 11,065/11,101 (36 failures across
27 families). All runner invocations used `TEST262_TEST_TIMEOUT_MS=900000`
and batches of at most 100. No harness edits, new tests, or unit/project tests
occurred.

Computed class-key order closes one case in the broad class cohort; its current
result is 4,057/4,059, with only auto-accessor syntax and private-proxy behavior
remaining. Fresh ordered 0–10 verification is 1,157/1,157 for stages 0–9 and
11,065/11,101 for stage 10 (36 cases across 27 families). Stage 10 remains the
first incomplete stage. All case processes used the required 900,000 ms timeout
and batches of at most 100.

### 2026-09-24: async-function prototype authority

The realm now owns `%AsyncFunction.prototype%`, linked from the native
`AsyncFunction.prototype` property and inheriting from `Function.prototype`.
Closure object prototypes derive from the async/generator kind table, so async
arrows use this intrinsic. The existing stage-10
`async-arrow-function/prototype.js` case passes. The fresh ordered 0–10 prefix
passes stages 0–9 at 1,157/1,157 and stage 10 at 11,066/11,101 (35 failures
across 26 families). The stage-10-only summary is
`target/test262-next-stage10-async-prototype.log`; every batch is ≤100 and
uses `TEST262_TEST_TIMEOUT_MS=900000`.

### 2026-09-24: OXC owns indirect parenthesized eval expressions

Indirect eval expressions beginning with a parenthesized expression now route
to the OXC specializer instead of the shallow string evaluator. Direct eval
retains its context-aware path, including `super` lexical semantics. The
existing cross-realm generator and async-generator prototype cases now pass;
all four direct-eval `super` cases remain passing. The stage-10-only sweep is
11,070/11,101 (31 failures across 23 families), down from the prior 35-case
checkpoint. Existing direct-eval Test262 cases
`binding-tests-3.js` and `strict-equals/S11.9.4_A1.js` remain in the failure
inventory. Evidence: `target/test262-next-stage10-eval-authority-final.log`
and `target/test262-next-stage10-eval-authority-*.log`. The ordered-prefix
rerun passes stages 0–9 at 1,157/1,157 and stage 10 at 11,070/11,101 (31
failures across 23 families). Evidence:
`target/test262-next-prefix-0-10-eval-authority-final.log`. Every case used
`TEST262_TEST_TIMEOUT_MS=900000` with batches ≤100; the harness was untouched
and no new tests or unit/project tests were added or run.

### 2026-09-24: dynamic name/reference context across `with`

Nested compiler products now inherit the active `with` depth as explicit
specialization context. The captured environment retains declarative parents,
while runtime name resolution preserves the staged order: active object
environment, current activation, then captured outer bindings. A resolved
global `var` reference updates the root binding and its global-object view
through the shared binding transition. Strict PutValue checks the captured
object binding after right-hand-side effects before setting it.

Existing Test262 cohorts pass: 24 PutValue cases, 38 postfix, 101 prefix,
3 generator-with cases, closure capture, TCO-with, and the stage-6 keyed
destructuring evaluation-order case. The fresh ordered 0–10 prefix passes
stages 0–9 at 1,157/1,157 and stage 10 at 11,068/11,101 (33 failures across
24 families). Stage 10 remains first incomplete. Evidence:
`target/test262-next-prefix-0-10-with-environment-authority-verified.log` and
the `target/test262-next-focused-with-*.log` cohort outputs. The gate used
`TEST262_TEST_TIMEOUT_MS=900000`, batches ≤100, and no new tests, harness
edits, or unit/project tests.

### 2026-09-24: route nested call syntax through OXC

The simple eval-call recognizer now accepts only a complete identifier call
whose matching close parenthesis ends the expression. Nested call chains such
as `foo()()` fall through to OXC instead of being misread as a call to `foo`
with malformed argument text. This closes existing Test262
`arrow-function/arrow/binding-tests-3.js`. The fresh ordered 0–10 run passes
stages 0–9 at 1,157/1,157 and stage 10 at 11,071/11,101 (30 failures across
23 families). Stage 10 remains the first incomplete stage. Evidence:
`target/test262-next-prefix-0-10-eval-call-authority.log`; the focused case is
`target/test262-next-stage10-simple-call-binding-tests-3.log`. All runs used
the required 900,000 ms timeout and ≤100-case batches; no harness or tests were
changed or added, and no unit/project tests were run.

Current ordered checkpoint: stages 0–9 pass 1,157/1,157 and stage 10 passes
11,071/11,101 (30 failures across 23 families). Evidence:
`target/test262-next-prefix-0-10-eval-call-authority.log`; stage 10 remains
first incomplete. All cases used `TEST262_TEST_TIMEOUT_MS=900000` and batches
≤100, with no harness changes, new tests, or unit/project tests.

After moving ordinary static-member calls to resolve the property before
argument evaluation, the existing 92-case stage-10 `call/` cohort passes.
This fixes the case that previously evaluated arguments before a non-callable
member access failed. Fresh ordered verification remains stages 0–9 at
1,157/1,157 and stage 10 at 11,072/11,101 (29 failures across 22 families).
Evidence: `target/test262-next-prefix-0-10-call-reference-final.log` and
`target/test262-next-stage10-call-reference-language-expressions-call-.log`.

The root global-var store now resolves its authoritative cell from the
activation's root `ProgramId`, after lexical stores are considered. This
closes the existing getter/setter eval cases (2/2). Fresh ordered
`run-stages-next 0 10` verification passes stages 0–9 at 1,157/1,157 and
stage 10 at 11,073/11,101 (28 failures across 21 families). Stage 10 remains
first incomplete. Evidence:
`target/test262-next-prefix-0-10-module-lifecycle-check.log`; timeout was
900,000 ms per case and batches were ≤100. Harness untouched; no tests were
added and no unit/project tests were run.

Normal calls and generator calls now use the same strict/sloppy `this`
normalizer. The existing non-strict generator-method case passes. Combined
with the three passing `yield*` protocol cases, stage 10 is 11,077/11,101 (24
failures across 17 families). The `instanceof` TypeError change remains
unresolved by its focused existing case and is not counted as a pass.
Evidence: `target/test262-next-stage10-generator-this-current.log`; timeout
was 900,000 ms per case and batches were ≤100. Fresh ordered 0–10 verification
confirms stages 0–9 at 1,157/1,157 and stage 10 at 11,077/11,101 (24 failures
across 17 families). Evidence:
`target/test262-next-prefix-0-10-generator-this-current.log`. Harness
untouched; no tests were added or unit/project tests run.

Eval `import(...)` now uses OXC's existing import-expression lowering, and the
compiled dynamic program retains the active source identity as its module
referrer. The existing `dynamic-import/usage-from-eval.js` passes. Stage 10 is
11,078/11,101 (23 failures across 16 families). Evidence:
`target/test262-next-stage10-eval-import-current.log`; timeout was 900,000 ms
per case and batches ≤100. Fresh ordered 0–10 verification confirms stages
0–9 at 1,157/1,157 and stage 10 at 11,078/11,101 (23 failures across 16
families). Evidence: `target/test262-next-prefix-0-10-eval-import-current.log`.
Harness untouched; no tests were added or unit/project tests were run.

The generator frame transition now carries an optional delegated iterator
result. This preserves missing `done` properties and avoids reading `value`
while `done` is false, while ordinary `yield` continues deriving its result
object. `yield*` treats a null `return` method as absent. The three existing
Test262 `yield*` cases pass; stage 10 is 11,076/11,101 (25 failures across 18
families). Fresh ordered 0–10 verification confirms stages 0–9 at 1,157/1,157
and stage 10 at 11,076/11,101 (25 failures across 18 families). Evidence:
`target/test262-next-stage10-yield-result-protocol-final.log` and
`target/test262-next-prefix-0-10-yield-result-protocol-final.log`; timeout was
900,000 ms per case and batches were ≤100. Harness untouched; no tests were
added and no unit/project tests were run.

OXC now identifies the supported eval completion shape of an
expression-only block followed by an expression statement. The block body is
evaluated in the active eval environment, then the final expression is
evaluated. The eval operator scanner selects by precedence and associativity,
fixing division chains. Existing `strict-equals`, `equals`, and
`division/no-magic-asi-from-block-eval.js` cases pass. Stage 10 is
11,081/11,101 (20 failures across 13 families). Evidence:
`target/test262-next-stage10-eval-operator-precedence.log`; timeout was
900,000 ms per case and batches ≤100. Fresh ordered verification passes stages
0–9 at 1,157/1,157 and stage 10 at 11,081/11,101 (20 failures across 13
families). Evidence:
`target/test262-next-prefix-0-10-eval-operator-precedence-final.log`. Harness
untouched; no tests were added or unit/project tests were run.

The numeric `instanceof` operands now fall through the arithmetic fast path
into generic operator dispatch, where canonical `instanceof` semantics apply.
The existing `instanceof/S11.8.6_A3.js` case passes. A fresh ordered run
confirms stages 0–9 at 1,157/1,157 and stage 10 at 11,082/11,101 (19 failures
across 12 families). Stage 10 remains incomplete. Evidence:
`target/test262-next-prefix-0-10-instanceof-numeric-final.log`; the focused
case passed using `target/iteration/run-all-next --case`.
`TEST262_TEST_TIMEOUT_MS=900000` was set, and per-stage batches remained ≤100.
The run retained the existing harness; no tests were added and no
unit/project tests were run.

Proxy construction no longer applies ordinary native `newTarget.prototype`
postprocessing to its target. Previously this forwarded `Object.setPrototypeOf`
through the Proxy and replaced a class instance's prototype, so the test failed
before reaching its private-brand assertion. The existing
`private-fields-proxy-default-handler-throws.js` case now passes. A fresh stage
10 sweep is 11,083/11,101 (18 failures across 11 families); the ordered
0–10 run confirms stages 0–9 at 1,157/1,157 and the same stage-10 result.
Evidence: `target/test262-next-stage10-proxy-construction.log` and
`target/test262-next-prefix-0-10-proxy-construction.log`. Timeout was 900,000
ms per case and batches ≤100. Harness untouched; no tests were added or
unit/project tests run.

Compiled direct eval now receives the caller activation environment as its
parent scope. Environment cells preserve their owning program identity, and a
root-eval wrapper carries root lexical visibility through eval-created
closures without leaking it into unrelated function eval. The existing
`tagged-template/cache-eval-inner-function.js` case passes. A fresh ordered
0–10 run confirms stages 0–9 at 1,157/1,157 and stage 10 at 11,084/11,101
(17 failures across 10 families). Evidence:
`target/test262-next-prefix-0-10-dynamic-eval-scope-final.log`; all cases used
the required 900,000 ms timeout and batches ≤100. Harness unchanged; no tests
were added or unit/project tests were run.

Prior family triage identified valid auto-accessor syntax as an unsupported
`ClassElement::AccessorProperty`; this family is addressed by the implementation
evidence below.

Auto-accessors now lower through the shared class-field initialization,
private backing-slot, and accessor-descriptor paths. Synthetic getter/setter
methods are compiled by OXC and share one private-name identity; computed keys
and instance initializers use the existing field machinery. Decorated
auto-accessors still reject explicitly, and static backing-slot initialization
remains incomplete. The existing six-element
`class/elements/syntax/valid/grammar-field-accessor.js` case passes as part of
the full ordered run.

Fresh ordered 0–10 verification passes stages 0–9 at 1,157/1,157 and stage 10
at 11,085/11,101 (16 failures across nine families), one additional pass from
the previous 11,084 checkpoint. Evidence:
`target/test262-next-prefix-0-10-auto-accessor.log`. Every case had the
required 900,000 ms timeout; stage batches were capped at 100. The remaining
families are import-defer parsing (seven cases), nested dynamic-import module
evaluation (two), and seven singleton async/module/eval behaviors. Test262
harness untouched; no tests were added and no unit/project tests were run.

### 2026-09-24: module identity and errored module completion

The residual source goal now survives binary encoding; root module activations
own a module environment. Dynamic self-imports join the root module's pending
record, which is settled only after root execution. Its namespace points at
the live module slots, including descriptor reads, and normalized source names
share one identity authority with static graph resolution. Existing
self-import-once, generator default-export, and live-binding cases pass as
part of the full ordered gate. Static module throw classification also keeps a
statically known Error message instead of losing it.

The fresh ordered 0–10 run passes stages 0–9 at 1,157/1,157 and stage 10 at
11,090/11,101 (11 failures across four families). Evidence:
`target/test262-next-prefix-0-10-module-live-descriptors.log`. Remaining
families are import-defer/static-module compilation (seven), nested
dynamic-module evaluation (two), script self-import completion (one), and
async/await job interleaving (one). Every case had the 900,000 ms timeout and
batches were capped at 100. No harness edits, added tests, or unit/project
tests were made/run.

The module metadata path now keeps local export bindings and reexport edges in
one OXC-derived plan. The runtime composes local live bindings with named,
star, and namespace reexports, and exported reads come from the linked module's
retained environment rather than the caller activation. `ExportAllDeclaration`
is link metadata, not an execution statement. The two existing nested
namespace-property cases now pass. A fresh ordered 0–10 run completes stages
0–9 at 1,157/1,157 and stage 10 at 11,092/11,101 (nine failures across three
families). Evidence: `target/test262-next-prefix-0-10-static-module-linking.log`
and focused cases in `target/test262-next-stage10-module-reexports-focused.log`.
All cases had the required 900,000 ms timeout and batches ≤100. Remaining
families: seven import-defer/static-module compiler cases, root-script
self-import completion, and async/await job interleaving. No harness changes,
new tests, or unit/project tests were made/run.

The remaining stage-10 cohorts are closed: async-generator yielded values now
resume through the Promise job queue even when already fulfilled, and module
root `var` bindings no longer mirror into the realm global. The existing
async/await interleaving and script-self-import completion cases pass. Fresh
ordered verification now passes stages 0–9 at 1,157/1,157 and stage 10 at
11,101/11,101. Evidence:
`target/test262-next-prefix-0-10-module-root-var-scope.log`; every case had a
900,000 ms timeout and batches were capped at 100. The specializer/module
surface remains broader than this stage gate; task 10 is still in progress.

### 2026-09-24: stage-11 function-code families

Strict block function declarations now receive activation-local bindings that
do not leak outside their block, including try and switch scopes. Strict-name
early-error scanning treats `_`, `$`, and non-ASCII identifier bytes as
identifier continuations, so `my_eval = eval` is not misclassified as an
assignment to `eval`. Primitive Number property lookup now traverses the
canonical primitive prototype while preserving the original receiver for
accessor calls. The three existing failure cohorts pass: strict block/switch
function declarations (3/3), indirect eval `this` (2/2), and primitive-number
accessors (12/12). Fresh ordered verification passes stages 0–10 and stage 11
at 217/217. Evidence:
`target/test262-next-prefix-0-11-function-code-families.log`; timeout was
900,000 ms and batches were capped at 100. No tests were added, harness files
changed, or unit/project tests run.
