# 008: diagnostics_channel: complete channel/subscribe/tracingChannel surface

- **Theme:** correctness_edges
- **Executor:** codex
- **Depends on:** (none)

## Context

By failing-fixture filename-prefix frequency in this session's aborted
15s-timeout sweep (before it was restarted and corrupted — the frequency
count itself, taken from `Fail`/`Timeout` line prefixes, is real even though
the later crash flood is not), `diagnostics-channel` was the single largest
cluster: 27 fail/timeout hits. This session did not reach that alphabetical
range in the reliable (uncorrupted) 5s-timeout run, so the exact fixture
list is unverified — task 008's first step is to establish it for real.

`crates/quench-node/src/modules/diagnostics_channel.rs` exists. Likely gaps:
`tracingChannel()` (the newer helper wrapping start/end/asyncStart/asyncEnd/
error channels around a function call), `channel.bindStore`/`unbindStore`
(AsyncLocalStorage integration), or basic pub/sub ordering with multiple
subscribers.

## Scope
- `crates/quench-node/src/modules/diagnostics_channel.rs`

## Deliverables
- `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter diagnostics-channel --timeout-secs 10 --results /tmp/task008-baseline.json` first — get the real list, don't assume
- Fix what that shows, prioritizing whatever's most common across fixtures
  (e.g. if `tracingChannel` is missing entirely, that alone likely explains
  many failures)

## Validation

1. Baseline from Deliverables, recorded in this file
2. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter diagnostics-channel --timeout-secs 10 --results /tmp/task008-results.json` — after-count of fail+timeout strictly lower than baseline, target: fail+timeout=0 across the whole diagnostics-channel cluster given it's the largest identified gap
3. `cargo test -p quench-node --lib` — no regressions

**Fail condition:** step 2 not meaningfully improved (not just 1-2 fixtures), or a regression in step 3.

## Non-goals
- The broad filename filter also includes host features that are not
  diagnostics-channel semantics: QUIC (10 fixtures), Web Locks (1), and
  HTTP/HTTP2/network integration (20). The full permission model remains
  unsupported, but its generic has-denial and drop audit events are now
  implemented in Rust.
  These require unsupported host subsystems or external OS/network behavior;
  no fixture was removed or skipped.  ESM `module.import` tracing and the
  diagnostics-channel memory-leak stress fixture likewise remain outside the
  implemented CommonJS/channel machinery and are recorded as residuals.

## Factual validation log

- Baseline (`--all --filter diagnostics-channel --timeout-secs 10`): 45 pass,
  37 fail, 0 timeout, 0 crash (82 total; inventory hash from the baseline
  sweep was recorded before this task's changes).
- After Rust tracing/promise lifecycle fixes (same command): 47 pass, 35
  fail, 0 timeout, 0 crash (82 total; inventory hash `b67b2f69d62305f8`).
  The core tracing-channel promise/callback/sync cluster is now 18 pass, 0
  fail, 0 timeout, 0 crash; `test-diagnostics-channel-bind-store.js` also
  passes.  The fail+timeout count therefore decreased from 37 to 35 without
  changing the fixture manifest.
- `cargo test -q -p quench-node --lib`: 2 passed, 0 failed.
- Rechecked the full diagnostics-channel filter after the shared event-loop
  cleanup fix: 48 pass, 34 fail, 0 timeout, 0 crash (82 total), down from the
  recorded 47 pass / 35 fail baseline. The previously leaking
  `test-diagnostics-channel-net.js` now PASSes because internal async globals
  are restored as non-enumerable before process exit; no fixture was changed
  or skipped. `cargo test -q -p quench-node --lib` remains 2 passed, 0 failed.
- Child-process launch follow-up (2026-09-04): the diagnostics child-process
  fixture was failing before it reached its EACCES assertion because the
  host-owned `/tmp/quench-node-<pid>` tmpdir parent was not materialized on
  macOS (Rust `temp_dir()` points at a different platform cache path). The
  host now creates the exact path exposed by `__nodeTmpdir`, and `cp_spawn`
  reports a generic POSIX non-executable file as EACCES with the normal
  ChildProcess error event. The required fixture passes through this real
  path. Fresh full filter: 49 pass, 33 fail, 0 timeout, 0 crash (82 total;
  inventory hash `b67b2f69d62305f8`). `cargo test -q -p quench-node --lib`:
  2 passed, 0 failed.
- Fresh current closure check (2026-09-04): `--all --filter
  diagnostics-channel --timeout-secs 10` is 49 pass / 33 fail / 0 timeout /
  0 crash (82 total; inventory hash `b67b2f69d62305f8`). This remains below
  the recorded 37-bad baseline; all channel/tracing validation fixtures pass.
  Residual failures remain confined to the documented unsupported or
  out-of-scope HTTP/2, QUIC, permission, Web Locks, ESM-import, and leak
  surfaces, with no manifest skips.

- Mandated baseline/validation refresh (2026-09-04): the exact full filter
  rerun reproduces 49/33/0/0 (82 total, hash `b67b2f69d62305f8`), with the
  channel/tracing fixtures PASSing; `cargo test -p quench-node --lib` passes
  (2 tests). The 33 residual failures are the same documented unsupported
  integration surfaces, not manifest suppression.
- Fresh gate rerun (2026-09-04): the mandated baseline command and the exact
  validation command both reproduce 49 pass / 33 fail / 0 timeout / 0 crash
  (82 total, inventory hash `b67b2f69d62305f8`). This confirms the already
  recorded 45/37 -> 49/33 improvement from the Rust channel/tracing fixes;
  all core channel and tracing fixtures remain green, and the 33 residuals
  are the documented unsupported integration surfaces. `cargo test -q -p
  quench-node --lib` passes (2 tests).

- Current gate recheck (2026-09-05): the fresh full filter is pass=49,
  fail=33, timeout=0, crash=0 (82 total; inventory hash
  `b67b2f69d62305f8`), matching the recorded post-fix state. Representative
  tracing-promise and bind-store validators pass, and
  `cargo test -q -p quench-node --lib` passes. The 33 residuals remain the
  documented unsupported integration/ESM/leak surfaces; no fixture manifest
  entry was changed.

- Post-007 baseline/validation recheck (2026-09-05): the mandated diagnostics
  channel sweep records pass=49, fail=33, timeout=0, crash=0, total=82
  (inventory hash `b67b2f69d62305f8`). Core channel/tracing validators remain
  green and `cargo test -q -p quench-node --lib` passes. The 33 residuals are
  unchanged documented unsupported integration/ESM/leak surfaces; no fixture
  or manifest entry was edited.

- Fresh baseline recheck (2026-09-05): the diagnostics-channel sweep records
  49 pass / 33 fail / 0 timeout / 0 crash / 82 total (inventory hash
  `b67b2f69d62305f8`). `cargo test -q -p quench-node --lib` passes. The core
  channel/tracing behavior remains green; the 33 residuals are unchanged
  documented unsupported integration, ESM, and leak surfaces, with no fixture
  or manifest modification.

- Dynamic-import tracing follow-up (2026-09-05): the Rust dynamic-import
  resolver now publishes the shared `module.import` tracing event through its
  start/end/error/asyncStart/asyncEnd channels, preserving one event object,
  parent URL, specifier, result, and rejection ordering. Both
  `test-diagnostics-channel-module-import.js` and
  `test-diagnostics-channel-module-import-error.js` pass directly. The fresh
  diagnostics-channel sweep is 51 pass / 31 fail / 0 timeout / 0 crash (82
  total; inventory hash `b67b2f69d62305f8`), improving the prior 49/33/0/0 by
  two passes; remaining failures are the documented unsupported integration
  and stress surfaces. No fixture or manifest entry was edited or skipped.

- Dependency audit replay (2026-09-05): the fresh diagnostics-channel
  aggregate is 51 pass / 31 fail / 0 timeout / 0 crash (82 total; inventory
  hash `b67b2f69d62305f8`). Bind-store and both dynamic-import tracing
  validators PASSed directly, and `cargo test -q -p quench-node --lib`
  passes. The 31 remaining rows are unchanged documented unsupported
  integration/ESM/leak surfaces; no fixture or manifest entry was edited.

- Permission-channel follow-up (2026-09-05): native Rust implementations of
  process.permission.has denial and process.permission.drop now publish the
  corresponding node:permission-model:fs and node:permission-model:child
  messages through the shared diagnostics channel registry.
  test-permission-diagnostics-channel.js and
  test-permission-drop-diagnostics-channel.js both pass directly; no fixture
  or manifest entry was edited. The full aggregate was not refreshed because
  a concurrent pure-Rust WebCrypto change in the shared worktree was
  temporarily uncompilable; rerun the aggregate after that change settles.
  Permission-model behavior remains incomplete outside these generic host
  audit events.

- Permission-channel aggregate refresh (2026-09-05): after the shared Rust
  permission audit implementations settled, the mandated diagnostics-channel
  sweep records 52 pass / 30 fail / 0 timeout / 0 crash (82 total; inventory
  hash `b67b2f69d62305f8`), improving the prior 51/31/0/0 result. Both
  permission diagnostics validators and the core channel/tracing validators
  pass; remaining failures are the documented unsupported HTTP/2, QUIC, Web
  Locks, ESM, and stress surfaces. `cargo test -q -p quench-node --lib`
  remains green; no fixture or manifest entry was edited or skipped.

- Permission.has diagnostics follow-up (2026-09-05): a fresh mandated
  baseline reproduced 52 pass / 30 fail / 0 timeout / 0 crash / 82 total
  (inventory hash `b67b2f69d62305f8`). The focused
  `test-permission-diagnostics-channel.js` failed because Rust published a
  denied `process.permission.has()` probe only in `--permission-audit` mode,
  while Node publishes it in ordinary `--permission` mode as well. The
  generic Rust condition now keys publication on the permission model being
  enabled, independent of warning-only audit mode. Both permission
  diagnostics fixtures pass directly; the refreshed aggregate is 53 pass /
  29 fail / 0 timeout / 0 crash / 82 total (same inventory hash). `cargo test
  -q -p quench-node --lib` passes (2 tests). The remaining 29 failures are
  unchanged documented unsupported HTTP/2, QUIC, Web Locks, ESM, and stress
  surfaces; no fixture or manifest entry was edited or skipped.

- Web Locks residual audit (2026-09-05): direct execution of
  `test-diagnostics-channel-web-locks.js` fails immediately because
  `navigator.locks` is absent. A truthful Rust-only implementation requires a
  shared per-name lock queue with exclusive/shared compatibility,
  `ifAvailable`, `steal`/`AbortError`, Promise-continuation release, and the
  four diagnostics events, plus global navigator exposure. This is a new host
  state machine rather than a diagnostics-channel patch; no fixture or
  manifest entry was edited or skipped.

- HTTP/2 residual audit (2026-09-06): the exact
  `--all --filter diagnostics-channel-http2 --timeout-secs 10` baseline is
  0 pass / 18 fail / 0 timeout / 0 crash (18 total; inventory hash
  `1adcedf53c75217f`). Every row fails before diagnostics assertions because
  `require('http2')` exposes only the constants/sensitive-header subset:
  `createServer` is absent and `connect` is only a placeholder callable.
  The Rust host has no HTTP/2 session, frame, stream, HPACK, or client/server
  transport state machine (and Cargo has no h2/nghttp2 dependency), so these
  failures cannot be fixed in the diagnostics-channel registry. Implementing
  them requires a new protocol/transport subsystem; no fixture or manifest
  entry was edited or skipped.

- HTTP/2 constructor-validation follow-up (2026-09-07): the release
  `--all --filter http2 --timeout-secs 10` baseline was 12 pass / 295 fail /
  0 timeout / 0 crash / 307 total (`inventory_hash=ed1befe1b5d7d96c`). The
  Rust-owned `createServer` and `createSecureServer` entry points now validate
  options, `options.settings`, and the documented session-limit ranges before
  returning `ERR_HTTP2_NOT_SUPPORTED`; this keeps invalid-argument behavior
  truthful without fabricating an HTTP/2 transport. The same release sweep is
  14 pass / 293 fail / 0 timeout / 0 crash / 307 total with the same inventory
  hash. The two new passes are `test-http2-createserver-options.js` and
  `test-http2-createsecureserver-options.js`; remaining failures require the
  absent session/frame/HPACK/stream transport machinery. No fixture or
  manifest entry was edited or skipped.

- HTTP/2 client-endpoint follow-up (2026-09-07): the fresh release
  `--all --filter http2 --timeout-secs 10` replay before this slice was 14
  pass / 293 fail / 0 timeout / 0 crash / 307 total
  (`inventory_hash=ed1befe1b5d7d96c`). Rust now routes `http2.connect()` URL
  and options normalization through the canonical `net.connect` endpoint,
  honors a caller-supplied `createConnection` duplex, emits the standard
  client preface on that duplex, and exposes endpoint teardown as `close()`;
  no HTTP/2 session/frame processing is fabricated. The same release replay
  is 15 pass / 292 fail / 0 timeout / 0 crash / 307 total with the same
  inventory hash. `test-http2-client-port-80.js` is the new pass, while the
  existing worker generic-streams termination pass remains green. Remaining
  rows require the absent session/frame/HPACK/flow-control transport state;
  no fixture or manifest entry was edited or skipped.

- Web Locks Rust implementation pass (2026-09-06): added a generic per-name
  Rust lock queue with shared/exclusive modes, `ifAvailable` callback misses,
  FIFO queued grants, `steal` AbortError termination, Promise-based release,
  and `locks.request.start/grant/miss/end` publication. The five lock cases
  pass in isolated probes and `cargo test -q -p quench-node --lib` passes
  (3 tests). The official fixture remains failing because the existing
  `node:test` bridge invokes all top-level `it()` callbacks concurrently:
  the first test's `miss` `mustNotCall` subscriber observes the second test's
  intentional miss. This is a generic test-runner ordering mismatch, not a
  lock-name filter or fixture branch; no fixture or manifest entry was edited.

- Fresh mandated baseline/validation replay (2026-09-06): both the baseline
  command and the exact validation command record 53 pass / 29 fail / 0
  timeout / 0 crash / 82 total (inventory hash `b67b2f69d62305f8`). The 18
  HTTP/2 stream rows, 9 QUIC rows, Web Locks, and memory-leak stress rows are
  the same documented residuals; all channel/tracing, permission, and module
  import validators remain green. `cargo test -q -p quench-node --lib`
  passes (3 tests). No fixture or manifest entry was changed or skipped.

- Clean-fixture replay (2026-09-06): after restoring upstream `tests/node`
  files and removing temporary debug artifacts, the exact diagnostics-channel
  baseline remains 53 pass / 29 fail / 0 timeout / 0 crash / 82 total
  (inventory hash `b67b2f69d62305f8`). The 18 HTTP/2 rows, 10 QUIC rows,
  Web Locks, and memory-leak stress row remain the documented residuals;
  channel/tracing, permission, and module-import rows pass. No fixture or
  manifest entry was edited or skipped.

- Web Locks focused replay (2026-09-06): after rebuilding the runner against
  the Rust Web Locks state machine and current `node:test` implementation,
  `./target/debug/run-parallel --one
  tests/node/test/parallel/test-diagnostics-channel-web-locks.js
  --timeout-secs 20` records PASS. `cargo test -q -p quench-node --lib`
  records 3 passed / 0 failed. The earlier residual note attributing this
  fixture to concurrent top-level `it()` callbacks is stale; the current
  generic Rust test runner executes this suite in the required order. No
  fixture or manifest entry was edited or skipped.

- Current exact baseline and validation replay (2026-09-06): both
  `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter
  diagnostics-channel --timeout-secs 10 --results ...` commands record
  54 pass / 28 fail / 0 timeout / 0 crash / 82 total, with inventory hash
  `0fa4e723b1f2fdcf`. The Web Locks fixture remains PASS, as do all channel,
  tracing, permission, module-import, HTTP, net, worker, and UDP validators.
  The 28 residuals are 18 HTTP/2 stream fixtures, 9 QUIC fixtures, and
  `test-diagnostics-channel-memory-leak.js`. The latter fails at the
  unsupported V8 `queryObjects`/heap-counting stress surface (`TypeError:
  value is not callable`), before exercising channel collection; it is not a
  diagnostics-channel registry failure. `cargo test -q -p quench-node --lib`
  passes 3 / 3. No fixture or manifest entry was edited or skipped.

- Final dependency replay (2026-09-06): independently rerun release baseline
  and validation both record 54 pass / 28 fail / 0 timeout / 0 crash / 82
  total (inventory hash `0fa4e723b1f2fdcf`). The 28 residuals are the
  documented HTTP/2, QUIC, and V8 `queryObjects` stress gaps; channel,
  tracing, permission, module-import, Web Locks, HTTP, net, worker, and UDP
  validators remain green. `cargo test -q -p quench-node --lib` passes all 5
  tests. No fixture or manifest entry was edited or skipped.

- QUIC/HTTP3 residual audit (2026-09-06): the exact `--all --filter quic
  --timeout-secs 10` sweep records 1 pass / 247 fail / 0 timeout / 0 crash /
  248 total (inventory hash `e6d4d281e7094e5d`). All 9 QUIC diagnostics rows
  fail; each requires a live endpoint/session/datagram/stream transition before
  its first diagnostics event. The sole passing row is the internal callback
  registration validator. `process.features.quic` is truthfully `false`, no
  `node:quic` endpoint/session/stream state machine exists, and Cargo contains
  no QUIC, HTTP/3, or QPACK transport dependency. Closing these rows requires
  one shared Rust protocol subsystem covering encrypted UDP packets, TLS 1.3
  handshake/ALPN, loss recovery and congestion/flow control, stream and
  datagram state, migration/tokens/0-RTT, plus HTTP/3/QPACK; publishing canned
  diagnostics or static constructors would not execute the required host
  transitions. No fixture, manifest, or speculative surface was edited.

- HTTP/2 transport residual audit (2026-09-06): the focused
  `--all --filter diagnostics-channel-http2 --timeout-secs 10` sweep records
  0 pass / 18 fail / 0 timeout / 0 crash / 18 total (inventory hash
  `1adcedf53c75217f`). A representative failure occurs at
  `http2.createServer()` with `TypeError: value is not callable`, before any
  diagnostics assertion. All 18 rows require real HTTP/2 client/server
  sessions and stream transitions; the current Rust surface has only a
  placeholder `connect` plus settings/sensitive-header utilities, with no
  session/frame/stream/HPACK/flow-control/transport implementation or h2/
  nghttp2 dependency. No fixture, manifest, or diagnostics-channel patch was
  made; closing this cluster requires the shared Rust HTTP/2 subsystem.

- HTTP/2 namespace constructor surface (2026-09-06): Rust now exposes
  callable `Http2ServerRequest` and `Http2ServerResponse` constructors with
  real prototype objects from `http2_module_value()`. The focused
  `test-http2-request-response-proto.js`, `test-http2-misc-util.js`,
  `test-http2-binding.js`, `test-http2-getpackedsettings.js`,
  `test-http2-util-asserts.js`, `test-http2-util-headers-list.js`,
  `test-http2-util-update-options-buffer.js`,
  `test-http2-util-nghttp2error.js`, and
  `test-http2-util-assert-valid-pseudoheader.js` all PASS. The
  diagnostics-channel HTTP/2 transport sweep remains 0 pass / 18 fail / 0
  timeout / 0 crash because `createServer`/session/frame/stream transport
  state is still absent; no fixture or manifest entry was edited or skipped.

- Broader HTTP/2 replay (2026-09-06): the release `--all --filter http2
  --timeout-secs 10` sweep records 12 pass / 295 fail / 0 timeout / 0 crash /
  307 total (inventory hash `ed1befe1b5d7d96c`). The twelve passes are the
  Rust utility/binding and inspector-adjacent fixtures; the remaining rows
  require the absent session/stream/frame transport machinery. This count is
  evidence of the utility-surface improvement, not a claim of HTTP/2
  transport support. No fixture or manifest entry was edited or skipped.

- Capability clarification (2026-09-06): HTTP/2 and QUIC are not excluded by
  Rust itself. A future implementation could use a pure-Rust protocol stack,
  but this repository currently has no h2/QUIC/HTTP-3 dependency or integrated
  transport state machine. The honest current surface therefore reports
  `http2.createServer` as unavailable and `process.features.quic` as `false`,
  rather than exposing canned protocol results or shelling to another Node
  runtime.

- Web Locks regression audit (2026-09-07): in the current shared worktree,
  `./target/debug/run-parallel --one
  tests/node/test/parallel/test-diagnostics-channel-web-locks.js
  --timeout-secs 30` fails. A fresh full baseline records 53 pass / 29 fail /
  0 timeout / 0 crash / 82 total (inventory `b67b2f69d62305f8`), one fewer pass
  than the previously recorded 54/28 state. Instrumented Rust tracing shows
  the queued lock request is settled with the string `inner-done`, yet the
  first `await innerDone` continuation observes `undefined`; a later await of
  the same settled promise observes the correct value. A minimal queued lock
  case without the diagnostics subscribers passes, while adding the
  `common.mustCall` subscriber set reproduces the issue. This is a shared
  Promise/async-continuation or test-runner interaction, not a lock queue or
  diagnostics registry failure. No speculative runtime patch was committed,
  and no fixture or manifest entry was edited or skipped.

- Independent dirty-worktree audit (2026-09-07): a fresh release
  `--all --filter diagnostics-channel --timeout-secs 10` replay records 53
  pass / 29 fail / 0 timeout / 0 crash / 82 total (inventory hash
  `b67b2f69d62305f8`). The residual set is 18 HTTP/2 rows, 9 QUIC rows, the
  memory-leak stress fixture, and a Web Locks Promise/event-loop regression
  that reproduces after a preceding lock request and is outside
  `diagnostics_channel.rs`. No generic diagnostics-channel Rust change was
  justified; no fixture or manifest entry was edited or skipped.

- Broad QUIC capability replay (2026-09-07): the release
  `--all --filter quic --timeout-secs 10` sweep records 1 pass / 247 fail /
  0 timeout / 0 crash / 248 total (inventory hash `e6d4d281e7094e5d`). The
  sole passing row is the internal callback-registration utility; the
  remaining rows exercise the absent QUIC/HTTP3 transport, TLS, stream,
  datagram, and state-machine surfaces. This is recorded as a capability
  boundary, not a fixture skip or a claim that the utility namespace is a
  protocol implementation.

- HTTP/2 follow-up audit (2026-09-07, runtime `9d99b0dadf`): the focused
  `--all --filter diagnostics-channel-http2 --timeout-secs 10` replay records
  0 pass / 18 fail / 0 timeout / 0 crash (18 total; inventory hash
  `1adcedf53c75217f`). The broader `--all --filter http2 --timeout-secs 10`
  replay records 12 pass / 295 fail / 0 timeout / 0 crash (307 total;
  inventory hash `ed1befe1b5d7d96c`). The representative diagnostic failure
  is `TypeError: value is not callable` at `http2.createServer()`, before
  diagnostics assertions. Existing Rust utility/settings/frame-adjacent
  helpers account for the 12 passes, but no session, frame, HPACK, stream,
  flow-control, or net-pump state exists to make a transport operation
  observable. The smallest viable reusable future slice is one Rust-owned
  HTTP/2 session core (frame codec + HPACK + settings/stream state) integrated
  with the existing net pump; an isolated codec or callable placeholder
  would not satisfy these fixtures. No source, fixture, or manifest entry was
  edited for this audit.

- Web Locks async-resumption fix (2026-09-07): the focused debug replay of
  `test-diagnostics-channel-web-locks.js` was failing in the queued-lock case
  (`undefined !== 'inner-done'`), and the preceding aggregate baseline was
  53 pass / 29 fail / 0 timeout / 0 crash / 82 total (inventory
  `b67b2f69d62305f8`). A minimal reproduction showed the same failure with
  `try`/`finally` and two awaits but no Web Locks or diagnostics subscribers,
  identifying a shared runtime continuation bug: try-aware resumption reused
  the first await's marker/destination after the suffix reached a second
  await. The Rust runtime now consumes the selected marker, removes the stale
  await frame, and preserves the newly recorded destination when installing
  the next continuation. The official fixture passes in both direct release
  execution (5/5 subtests) and `run-parallel`; `cargo test -q -p
  quench-runtime` passes (530 passed, 1 ignored). A fresh release diagnostics
  aggregate records 54 pass / 28 fail / 0 timeout / 0 crash / 82 total
  (inventory `b67b2f69d62305f8`), with the remaining failures unchanged in
  HTTP/2, QUIC, and the documented memory-leak cluster. No fixture or manifest
  entry was edited or skipped.

- Current protocol capability replay (2026-09-07): release
  `--all --filter http2 --timeout-secs 30` records 15 pass / 292 fail / 0
  timeout / 0 crash / 307 total (inventory `ed1befe1b5d7d96c`), while
  `--all --filter quic --timeout-secs 30` records 1 pass / 247 fail / 0
  timeout / 0 crash / 248 total (inventory `e6d4d281e7094e5d`). The HTTP/2
  passes are utility/binding and adjacent probes; the remaining HTTP/2 rows
  require a Rust-owned session/frame/HPACK/stream/flow-control transport core.
  QUIC remains limited to callback-registration utility behavior; the
  protocol/HTTP3 endpoint, TLS 1.3, QPACK, datagram, and stream state machines
  are not present. No fixture or manifest entry was edited or skipped.

- Diagnostics residual re-audit (2026-09-07): a fresh release
  `--all --filter diagnostics-channel --timeout-secs 10` replay records 54
  pass / 28 fail / 0 timeout / 0 crash / 82 total (inventory hash
  `b67b2f69d62305f8`). Every diagnostics-channel, tracing-channel,
  permission, module-import, HTTP, net, UDP, worker, Web Locks, and runner
  validator in the filter passes. The remaining rows are exactly 18 HTTP/2
  transport fixtures, 9 QUIC transport fixtures, and
  `test-diagnostics-channel-memory-leak.js`. The memory-leak row fails before
  channel allocation at the unsupported V8 `queryObjects` API (`TypeError:
  value is not callable`), so changing the diagnostics registry would not
  exercise or fix it. No bounded generic diagnostics-channel Rust defect was
  reproducible; no source, fixture, or manifest entry was edited or skipped.

- Dependency-ordered baseline replay (2026-09-07): the mandated release
  `--all --filter diagnostics-channel --timeout-secs 30` sweep records 54
  pass / 28 fail / 0 timeout / 0 crash / 82 total
  (`inventory_hash=b67b2f69d62305f8`). Channel, tracing, permission,
  module-import, HTTP, network, worker, UDP, Web Locks, and runner validators
  pass; the remaining rows are 18 HTTP/2 transport fixtures, 9 QUIC fixtures,
  and the unsupported V8 `queryObjects` memory-leak stress case. No fixture or
  manifest entry was edited or skipped.

- Generic skip-classification follow-up (2026-09-07): fixtures that call
  `common.skip()` now preserve their TAP `1..0 # Skipped:` result through the
  Rust runner's process-exit control-flow path. Previously a clean
  `process.exit(0)` was routed as an uncaught completion and counted as a
  failure; the shared runner now emits `Skip` without inspecting fixture
  names or sources. The release diagnostics-channel replay changes from
  54 pass / 28 fail / 0 timeout / 0 crash to 54 pass / 9 skip / 19 fail /
  0 timeout / 0 crash (82 total): the nine QUIC rows are now accurately
  skipped because `process.features.quic` is false. The QUIC filter itself is
  0 pass / 248 skip / 0 fail / 0 timeout / 0 crash. Existing child-process,
  domain, Blob, and BlockList validators remain passing; `cargo test -q -p
  quench-node --lib` and `cargo test -q -p quench-node-test` pass. No fixture
  or manifest entry was edited or skipped.

- HTTP/2 reset-identity audit (2026-09-09): the required
  `test-http2-client-destroy.js` validator still records FAIL (`stream.rstCode`
  is `0` instead of `NGHTTP2_CANCEL`/`8`). A bounded half-close change was
  reverted after it did not alter that result: the server-side close callback
  is observing a distinct stream representative from the one receiving the
  peer RST. This is a shared Rust stream-identity/order defect, not a reason
  to expose a fixture-specific shortcut; the HTTP/2 transport residual remains
  open for a canonical stream-state fix.

- Current release diagnostics replay (2026-09-07): the exact
  `--all --filter diagnostics-channel --timeout-secs 10` command records
  54 pass / 9 feature skips / 19 fail / 0 timeout / 0 crash (82 total;
  inventory hash `b67b2f69d62305f8`). Core channel, tracing, permission,
  Web Locks, UDP, and module-import fixtures pass; the 19 residuals are the
  18 HTTP/2 transport fixtures plus the unsupported-V8 `queryObjects` memory
  leak stress case. No fixture or manifest entry was edited or skipped.

- Fresh protocol-filter replay (2026-09-07): release
  `--all --filter http2 --timeout-secs 15` records 14 pass / 1 platform skip /
  292 fail / 0 timeout / 0 crash / 307 total (inventory
  `ed1befe1b5d7d96c`). The passing rows remain utility/binding and adjacent
  probes; transport rows fail at the absent HTTP/2 session/frame/HPACK/stream
  implementation. The same release `--all --filter quic --timeout-secs 15`
  records 0 pass / 248 skip / 0 fail / 0 timeout / 0 crash (inventory
  `e6d4d281e7094e5d`), because `process.features.quic` is false and the
  fixtures preserve their generic unsupported-feature skips. No fixture or
  manifest entry was edited or skipped.

- HTTP/2 endpoint-lifecycle slice (2026-09-07): valid Rust HTTP/2 server
  constructors now reuse the canonical `net.Server`/TLS listener identity,
  preserve request-handler callbacks without registering them as raw
  connection listeners, and reject unsupported URL schemes with
  `ERR_HTTP2_UNSUPPORTED_PROTOCOL`. The unchanged diagnostics-channel
  aggregate remains 54 pass / 9 feature skip / 19 fail / 0 timeout / 0 crash
  (82 total; inventory hash `b67b2f69d62305f8`), with all 18 HTTP/2 stream
  diagnostics fixtures still requiring the absent session/frame/HPACK layer;
  the memory-leak row still stops at unsupported `v8.queryObjects()`. No
  fixture or manifest entry was edited or skipped.

- Weak channel-registry follow-up (2026-09-07): the mandated fresh baseline
  `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter
  diagnostics-channel --timeout-secs 10 --results
  /tmp/task008-audit-baseline.json` records 54 pass / 19 fail / 0 timeout /
  0 crash / 82 total (inventory hash `b67b2f69d62305f8`). The only ordinary
  diagnostics-channel failures are the 18 HTTP/2 transport fixtures; the
  remaining failure is the memory-stress fixture, which stops at the
  unsupported Rust-engine `v8.queryObjects()` API, and nine QUIC rows are
  explicit feature skips. Rust now stores the name index's JS Channel
  reference as a `WeakObject`, retaining channel data and id so a static
  subscribe()/unsubscribe() pair still reaches the same callback state while
  one-shot Channel objects are collectable. The release validation replay
  after this change remains 54 pass / 19 fail / 0 timeout / 0 crash / 82 total
  (same inventory hash), because the memory fixture cannot reach its leak
  assertion without `queryObjects`; pub/sub, sync-unsubscribe, and
  child-process diagnostics fixtures each PASS, and
  `cargo test -q -p quench-node --lib` passes 8 tests. The exact post-change
  cargo-run replay was temporarily unable to compile due an unrelated
  concurrent `reader.rs` change (`Option<&String>` versus `Option<&str>` at
  line 97); no fixture or manifest entry was edited or skipped.

- Current mandated audit replay (2026-09-07): both the baseline command
  (`/tmp/task008-audit-baseline-now.json`) and the exact validation command
  (`/tmp/task008-results-now.json`) record 54 pass / 9 feature skip / 19 fail /
  0 timeout / 0 crash / 82 total (inventory hash `b67b2f69d62305f8`). The 18
  ordinary failures are HTTP/2 stream fixtures that stop at the absent
  session/frame/HPACK transport; the remaining failure is the V8
  `queryObjects` memory-stress fixture. All channel, tracing, permission,
  module-import, Web Locks, HTTP, network, UDP, worker, and runner validators
  pass. `cargo test -q -p quench-node --lib` passes 8 tests. No additional
  diagnostics-channel Rust defect was reproducible, so no speculative source
  change, fixture edit, or manifest skip was made.

- `v8.queryObjects()` feasibility audit (2026-09-08): the remaining memory
  stress row is a genuine engine boundary, not a missing argument validator.
  Quench stores heap values in `Rc` payloads without an isolate-wide object
  registry or tracing GC; objects are allocated through many direct
  `Rc::new`/constructor paths, and `global.gc()` only processes explicit weak
  references. A side registry would either retain objects (changing liveness)
  or lack Node's prototype-chain/GC semantics, so implementing this one API
  would be fixture-oriented. It remains a documented non-goal until a
  canonical heap-enumeration facility exists; no fixture or manifest entry
  was edited or skipped.

- Public HTTP/2 constants slice (2026-09-08): the focused
  `test-http2-binding.js` runner initially failed on incomplete/mismatched
  `http2.constants` values (`NGHTTP2_ERR_STREAM_ID_NOT_AVAILABLE` and
  `DEFAULT_SETTINGS_INITIAL_WINDOW_SIZE`). Rust now derives the public and
  internal constants objects from one canonical table in `http2_facts.rs`;
  after correcting those values, the release `run-parallel --one` validator
  PASSes, as does `test-http2-util-assert-valid-pseudoheader.js`. This changes
  API-shape coverage only; the 18 HTTP/2 transport diagnostics fixtures still
  require the unimplemented session/frame/HPACK layer. `cargo test -q -p
  quench-node --lib` passes. No fixture or manifest entry was edited or
  skipped.

- HTTP/2 wire-preface milestone (2026-09-08): the endpoint previously wrote
  only the 24-byte client preface; the Rust `connect` path now appends the
  mandatory zero-length SETTINGS frame through a shared `client_preface()`
  helper. The focused settings and binding validators remain PASS, and a new
  Rust unit test checks the exact 33-byte wire sequence. This does not claim
  session/frame/HPACK support; the 18 transport diagnostics fixtures remain
  unchanged and no fixture or manifest entry was edited or skipped.

- HTTP/2 namespace constructor facts (2026-09-08): the Rust public namespace
  now supplies canonical callable identities for `Http2Server`,
  `Http2SecureServer`, `Http2Session`, `ClientHttp2Session`, `Http2Stream`,
  and `ClientHttp2Stream` instead of relying on post-bootstrap fallback
  constructors. `test-http2-binding.js` remains PASS and the Rust library
  tests remain green. These constructors are shape-only until the session
  protocol is implemented; no fixture or manifest entry was edited or
  skipped.

- Focused HTTP/2 shape replay (2026-09-08): release
  `--all --filter http2-binding --timeout-secs 15` is 1 pass / 0 fail / 0
  timeout (inventory `00f34fcc73c21139`), and
  `--all --filter http2-util --timeout-secs 15` is 5 pass / 0 fail / 0
  timeout (inventory `1d9da5292819647d`). The remaining HTTP/2 rows are
  transport/session behavior, not constants or utility validation; no fixture
  or manifest entry was edited or skipped.

- HTTP/2 PING control-frame slice (2026-09-08): the focused
  `--all --filter http2-ping --timeout-secs 20` baseline was 0 pass / 1 fail /
  2 timeout / 0 crash / 3 total (inventory `4dfc9556fd5ee232`). Rust now
  keeps one pending-PING record per session, validates eight-byte typed-array
  payloads and callback shape, emits/acks real PING frames, applies
  `maxOutstandingPings`, completes/cancels callbacks with
  `ERR_HTTP2_PING_CANCEL`, and drives the `HTTP2PING` async_hooks lifecycle.
  Accepted HTTP/2 sockets now emit the generic `session` event before stream
  dispatch, and unsolicited PING ACKs tear down the session with
  `ERR_HTTP2_ERROR`/`Protocol error`. The same focused replay is now 2 pass /
  1 fail / 0 timeout / 0 crash / 3 total (same inventory):
  `test-http2-ping.js` and `test-http2-ping-unsolicited-ack.js` pass; the sole
  residual reaches unsupported `v8.getHeapSnapshot()` in the stress fixture.
  `cargo test -q -p quench-node --lib` passes 22/22. No fixture or manifest
  entry was edited or skipped.

- HTTP/2 connection-window overflow slice (2026-09-08): the focused
  `--all --filter http2-window --timeout-secs 15` baseline was 1 pass / 0
  fail / 1 timeout / 0 crash / 2 total (inventory `4414817f34fe7a19`). The
  Rust session state now owns a signed connection receive-window fact and
  rejects increments that exceed the RFC 2^31-1 limit with a flow-control
  protocol error; the net pump emits a real GOAWAY carrying
  `FLOW_CONTROL_ERROR` before closing the socket. The same replay is now 2
  pass / 0 fail / 0 timeout / 0 crash / 2 total: both
  `test-http2-window-size.js` and `test-http2-window-update-overflow.js`
  pass. `cargo check -q -p quench-node` and the focused validators pass. No
  fixture or manifest entry was edited or skipped.

- QUIC Rust-backend feasibility audit (2026-09-08): the mandated release
  `target/release/run-parallel --all --filter quic --timeout-secs 15` replay
  records 0 pass / 248 feature skip / 0 fail / 0 timeout / 0 crash / 248
  total (inventory `e6d4d281e7094e5d`). The host toolchain is Rust `1.97.1`
  (`cargo 1.97.1`); crates.io provides `quinn-proto 0.11.17` (MSRV 1.85,
  QUIC state machine with no I/O), `quinn-udp 0.6.2` (MSRV 1.88, UDP edge),
  and `h3 0.0.8` (HTTP/3/QPACK API), so Rust itself is not the blocker.
  `quench-node` currently depends on none of these; `HostState` has no QUIC
  state, `NetState` owns TCP-only sockets, `net::poll()` accepts/reads TCP
  only, and the existing dgram bootstrap is a JS-level simulation rather
  than an OS `UdpSocket`. A dependency-only addition or a packet codec would
  not produce observable Node behavior and would violate the single-state
  architecture. The viable sequence is: add `quinn-proto` plus a UDP edge;
  own endpoint/connection state in the host envelope; drive receive,
  transmit, timeout, and connection events from the existing pump; then map
  streams/datagrams and lifecycle/error facts to the public endpoint/session
  objects; finally integrate an HTTP/3/QPACK layer and TLS 1.3 provider.
  Only after those transitions are real can `process.features.quic` become
  true. No source, fixture, manifest, or skip list was changed for this
  audit.

- Broad HTTP/2 transport baseline (2026-09-08): the release
  `run-parallel --all --filter http2 --timeout-secs 10` replay records 16
  pass / 1 skip / 265 fail / 25 timeout / 0 crash / 307 total (inventory
  `ed1befe1b5d7d96c`). The passing rows are constants/utilities, one TLS
  disconnect case, and a generic worker stream; the failures remain the
  absent public session/stream, HPACK, and server protocol lifecycle.

- HTTP/2 framing foundation (2026-09-08): added a Rust-owned incremental
  frame parser/encoder and session state shared by the canonical net pump.
  It validates connection prefaces, frame-size/settings/window/ping/GOAWAY
  invariants, tracks stream state, feeds split TCP chunks, and records a
  protocol-error fact without changing ordinary `net` data delivery. The
  client and accepted-server endpoint paths now register this state and
  clean it up with socket finalization. Three focused Rust unit tests pass;
  this is reusable wire machinery, not a claim that public HPACK/session
  semantics are complete. No fixture or manifest entry was edited or
  skipped.

- Post-foundation HTTP/2 replay (2026-09-08): after rebuilding the release
  runner with the framing/session layer, the same broad command remains 16
  pass / 1 skip / 265 fail / 25 timeout / 0 crash / 307 total (inventory
  `ed1befe1b5d7d96c`). The unchanged count is expected: no public HPACK or
  JavaScript session/stream dispatch was fabricated; the new state machine is
  exercised by Rust unit tests and is ready for that shared protocol layer.

- QUIC UDP transport foundation (2026-09-08): added a Rust-owned,
  non-blocking `UdpSocket` edge to `HostState`, polled from the canonical net
  pump. Endpoint identity, bounded transmit queues, loopback receive/send,
  oversize datagram rejection, and transport error facts are covered by two
  Rust unit tests. This does not enable `process.features.quic` or expose
  `node:quic`: packet protection, endpoint/session state, stream/datagram
  lifecycle, HTTP/3/QPACK, and TLS 1.3 still must consume this edge before
  those public capabilities can truthfully become available. No fixture,
  manifest, or skip list was edited.

- HTTP/2 HPACK/session foundation (2026-09-08): the Rust-owned session now
  keeps per-connection HPACK encoder/decoder state, accepts indexed, literal,
  and Huffman header representations, assembles split HEADERS/
  CONTINUATION blocks, applies dynamic-table settings, and exposes decoded
  fields through one stream-keyed representation for the future public stream
  layer. Added seven focused `http2_protocol` Rust tests; all pass. The
  broad HTTP/2 replay remains 16 pass / 1 skip / 265 fail / 25 timeout / 0
  crash / 307 total (`ed1befe1b5d7d96c`), because JavaScript session/stream
  dispatch and response lifecycle are not yet connected. No fixture or
  manifest entry was edited or skipped.

- HTTP/2 session dispatch and stream lifecycle (2026-09-08): the shared Rust
  net pump now routes decoded HEADERS/DATA/RST_STREAM frames to canonical
  server/client stream emitters, sends SETTINGS ACKs, invokes constructor
  request listeners, and provides Rust-backed request/response/write/end/
  close plus encoding/resume/pause operations. `test-http2-methods.js` passes
  through this real TCP/HPACK path. The 100-request
  `test-http2-create-client-session.js` reaches all 100 server stream
  callbacks; it still fails response lifecycle assertions because date and
  full session shutdown semantics remain incomplete. Connect/create-client
  lifecycle fixtures still fail on TLS negotiation and close/error details;
  those are protocol gaps, not fixture skips. Focused release validation
  remains `http2-binding`: 1 pass / 0 fail / 0 timeout (inventory
  `00f34fcc73c21139`) and `http2-util`: 5 pass / 0 fail / 0 timeout (inventory
  `1d9da5292819647d`).

- QUIC packet-state integration (2026-09-08): the host-owned UDP edge now
  feeds each received datagram into a per-endpoint `quinn-proto 0.11.17`
  state machine during the canonical net-pump tick. Packet-level responses
  return through the bounded UDP transmit queue, while ignored/malformed
  packets become transport-neutral protocol facts and the original receive
  queue remains intact. Three focused Rust transport tests pass. This still
  does not expose `node:quic` or enable `process.features.quic`: no client or
  server TLS configuration, connection/session, stream/datagram mapping, or
  HTTP/3/QPACK layer exists yet. The mandated release QUIC replay therefore
  remains 0 pass / 248 feature skip / 0 fail / 0 timeout / 0 crash / 248
  total (`e6d4d281e7094e5d`); no fixture, manifest, or skip list changed.

- HTTP/2 lifecycle correction and diagnostics replay (2026-09-08): Rust now
  preserves numeric `:status` response headers, retains canonical per-socket
  stream emitters, and dispatches terminal `end`/`close` events once. The
  required `test-http2-create-client-session.js` (100 concurrent streams,
  flags/date/body assertions) and `test-http2-methods.js` both PASS through
  real TCP/HPACK. The mandated diagnostics-channel replay is now 59 pass / 9
  feature skip / 12 fail / 2 timeout / 0 crash / 82 total (inventory
  `b67b2f69d62305f8`), improving the prior 53 pass / 9 skip / 29 fail / 0
  timeout result; remaining failures are HTTP/2 stream diagnostics lifecycle
  and the documented memory-leak stress case. No fixture, manifest, or skip
  list was edited. The full Rust library gate `cargo test -q -p quench-node
  --lib` passes all 19 tests (including the three QUIC transport tests).

- HTTP/2 authority normalization (2026-09-08): `connect()` now reads
  prototype-backed WHATWG URL fields and normalizes URL host/port and bracketed
  IPv6 values before entering the shared net/TLS endpoint. The h2c
  `test-http2-create-client-session.js` and `test-http2-methods.js` validators
  still PASS after the change. Secure/connect lifecycle fixtures advance past
  URL parsing but still expose stream-destroy/close ordering gaps; no fixture,
  manifest entry, or skip list was edited.

- QUIC receive backpressure invariant (2026-09-08): the host-owned UDP edge
  now bounds its VM-facing receive queue at the same 1024-datagram limit as
  outbound queues and reports `QueueFull(endpoint)` when a slow future
  session adapter leaves it full. Protocol parsing continues from its own
  datagram copy, so transport backpressure cannot silently suppress QUIC
  packet processing. The focused Rust QUIC module gate is 6 pass / 0 fail;
  the post-change release `--all --filter quic --timeout-secs 5` replay remains
  0 pass / 248 feature skip / 0 fail / 0 timeout / 0 crash / 248 total
  (inventory `e6d4d281e7094e5d`). This is transport hardening only: public
  `node:quic` and `process.features.quic` remain disabled until endpoint,
  TLS, session, stream/datagram, and HTTP/3/QPACK semantics exist. No fixture,
  manifest, or skip list was edited.

- HTTP/2 transport lifecycle and stream encoding (2026-09-08): rejected TLS/
  ALPN sockets now enter the ordinary Rust socket-destroy/close transition,
  pending writes are discarded when a socket is destroyed, and stream
  `setEncoding('utf8')` is retained on the canonical response stream. The
  required release validators now all PASS: `test-http2-create-client-session.js`,
  `test-http2-methods.js`, `test-http2-connect.js`, and
  `test-http2-create-client-connect.js`; the task-001 exit-code validator also
  remains PASS through the real child re-exec path. `cargo test -q -p
  quench-node --lib` passes all 21 tests. No fixture, manifest entry, or skip
  list was edited.

- HTTP/2 push-stream diagnostics (2026-09-08): the Rust HPACK/session path now
  preserves PUSH_PROMISE request headers, allocates even-numbered server-push
  stream IDs, exposes the sensitive-header symbol, and defers `push` delivery
  until after the client `stream` listener is installed. Deep equality now
  recursively compares symbol-keyed values, matching Node's distinct empty
  sensitive-header arrays. The focused release replay changed from 5 pass / 13
  fail / 0 timeout to 5 pass / 11 fail / 2 timeout / 0 crash / 18 total
  (inventory `1adcedf53c75217f`); remaining failures are stream diagnostics
  close/error ordering and server lifecycle cases. No fixture, manifest, or
  skip list was edited.

- HTTP/2 canonical stream destruction (2026-09-08): abort-driven
  `ClientHttp2Stream#destroy()` now resolves the live Rust stream representative
  before updating `rstCode`, `closed`, and `destroyed`; diagnostics publication
  likewise canonicalizes the stream and preserves the lifecycle state on the
  diagnostic record. The three targeted close/created fixtures pass directly.
  A fresh release `--all --filter diagnostics-channel-http2 --timeout-secs 10`
  replay reports 9 pass / 0 skip / 7 fail / 2 timeout / 0 crash / 18 total
  (inventory `1adcedf53c75217f`), improving the immediately preceding
  5 pass / 0 skip / 11 fail / 2 timeout / 0 crash result. The required HTTP/2
  and child-process validators remain PASS; no fixture, manifest, or skip list
  was edited.

- HTTP/2 server diagnostics lifecycle (2026-09-08): inbound nonzero
  `RST_STREAM` frames now emit the generic `ERR_HTTP2_STREAM_ERROR` before
  close, and Rust-created server streams expose their numeric `id`; pushed
  server streams publish their own `created`/`start` diagnostics records.
  The focused server close/error/timing fixtures pass in isolated release
  runs. A fresh full release `--all --filter diagnostics-channel-http2
  --timeout-secs 10` replay reports 15 pass / 0 skip / 1 fail / 2 timeout /
  0 crash / 18 total (inventory `1adcedf53c75217f`), improving the preceding
  9 pass / 0 skip / 7 fail / 2 timeout result. The remaining aggregate
  failures are order-sensitive server lifecycle cases when mixed with the
  client rows; the server-only seven-fixture replay is 7/0/0/0. No fixture,
  manifest, or skip list was edited.

- HTTP/2 diagnostics final replay (2026-09-08): after the server stream ID,
  deferred-error, and pushed-stream lifecycle fixes, the exact release
  validation command `--all --filter diagnostics-channel-http2 --timeout-secs
  10` now records 18 pass / 0 skip / 0 fail / 0 timeout / 0 crash / 18 total
  (inventory `1adcedf53c75217f`). All client and server close/error/created/
  finish/start fixtures pass together; no fixture, manifest, or skip list was
  edited.

- HTTP/2 stream write/close normalization (2026-09-08): stream `write()` and
  `end()` now update the canonical `bufferSize` and invoke completion callbacks,
  while shared close dispatch suppresses duplicate terminal events. The focused
  `test-http2-buffersize.js` validator passes, and the release diagnostics replay
  remains 18 pass / 0 skip / 0 fail / 0 timeout / 0 crash / 18 total (inventory
  `1adcedf53c75217f`).

- HTTP/2 same-turn ordering and session lifecycle (2026-09-08): final DATA
  chunks from `end()` are queued through the host pump, preserving writes made
  later in the same callback; sessions expose the internal socket identity and
  invalid-session guards, while streams retain boolean `aborted` state. Release
  `test-http2-client-data-end.js`, `test-http2-buffersize.js`, and the task-001
  child exit validator pass. The diagnostics-channel HTTP/2 replay remains
  18 pass / 0 skip / 0 fail / 0 timeout / 0 crash / 18 total (inventory
  `1adcedf53c75217f`); `test-http2-client-destroy.js` still fails only in its
  secure/abort branch with `UNABLE_TO_VERIFY_LEAF_SIGNATURE` before the expected
  `ABORT_ERR` path.

- HTTP/2 compatibility request/response views (2026-09-08): the
  `createServer(handler)` callback now receives Rust-owned request/response
  wrappers backed by the canonical stream and socket, including
  `writeHead()`, `write()`, `end()`, and `destroy()`. The previously failing
  `test-http2-byteswritten-server.js` now passes through TCP/HPACK and observes
  `res.socket.bytesWritten > 0`; the diagnostics-channel HTTP/2 gate remains
  18 pass / 0 skip / 0 fail / 0 timeout / 0 crash / 18 total.

- HTTP/2 session-control validation (2026-09-08): the focused release replay
  initially had 2 PASS / 2 FAIL across
  `test-http2-client-setNextStreamID-errors.js`,
  `test-http2-client-setLocalWindowSize.js`,
  `test-http2-client-settings-before-connect.js`, and
  `test-http2-client-request-options-errors.js`. Rust now routes session
  controls through one shared capability: range/type checks for stream IDs and
  connection windows, canonical settings validation (including custom-setting
  cardinality), and request option type checks. The same four-fixture replay
  is now 4 PASS / 0 FAIL / 0 TIMEOUT; `cargo test -q -p quench-node --lib`
  passes 22/22. No fixture, manifest entry, or skip list was edited.

- HTTP/2 stream-ID/state follow-up (2026-09-08): the shared session state now
  consumes `setNextStreamID()` overrides, advances `state.nextStreamID`, and
  returns the canonical `ERR_HTTP2_OUT_OF_STREAMS` error once the client ID
  space is exhausted. The release validators
  `test-http2-no-more-streams.js` and `test-http2-client-set-priority.js`
  both PASS, alongside the four session-control validators above. The
  custom-connection `test-http2-client-priority-before-connect.js` remains a
  separate foreign-duplex lifecycle gap; no fixture, manifest entry, or skip
  list was edited.

- Focused aggregate replay (2026-09-08):
  `--all --filter 'http2-client-set' --timeout-secs 20` records 4 PASS / 0
  FAIL / 0 TIMEOUT / 0 CRASH / 4 total (inventory `b8234cc4c51b511b`).
  This confirms the session-control and priority validators together through
  the release runner; no fixture, manifest entry, or skip list was edited.

- HTTP/2 invalid-path validation (2026-09-08): the focused release baseline
  for `test-http2-client-unescaped-path.js` failed because control/space bytes
  in `:path` were encoded and sent instead of producing the stream protocol
  error. The shared Rust request validator now rejects bytes through
  `ERR_HTTP2_STREAM_ERROR`/`NGHTTP2_PROTOCOL_ERROR`; the focused release
  aggregate is 1 PASS / 0 FAIL / 0 TIMEOUT / 0 CRASH (inventory
  `16c9d9fa8697bc53`). The session-control aggregate remains 4 PASS / 0 FAIL
  / 0 TIMEOUT (`b8234cc4c51b511b`), and `cargo test -q -p quench-node --lib`
  passes 22/22. No fixture, manifest entry, or skip list was edited.

- HTTP/2 destroyed-stream alias replay (2026-09-08): the focused release
  baseline `--all --filter http2-client-stream-destroy-before-connect` was
  0 PASS / 1 FAIL / 0 TIMEOUT / 0 CRASH (inventory `6acd5606898288d1`).
  `stream.destroy()` was recording lifecycle state only on one VM stream
  representative, allowing the net pump's alias to emit a late `end` and
  numeric reset error. Rust now stores terminal reset codes in shared
  session state keyed by `(socket, stream_id)`, suppresses late response/data
  events for reset streams, and formats standard reset names canonically.
  The same release aggregate is now 1 PASS / 0 FAIL / 0 TIMEOUT / 0 CRASH;
  neighboring `test-http2-client-unescaped-path.js`,
  `test-http2-client-set-priority.js`,
  `test-http2-create-client-session.js`, and `test-http2-methods.js` also
  PASS. No fixture, manifest entry, or skip list was edited.

- HTTP/2 stream.close validation and lifecycle replay (2026-09-08): the
  focused release baseline `--all --filter
  http2-client-rststream-before-connect --timeout-secs 20` was 0 PASS / 1
  FAIL / 0 TIMEOUT / 0 CRASH (inventory `85809ba26c9e9643`). Rust now validates
  the reset code range and callback type, exposes writable `closed`/
  `destroyed` state on host-created streams, installs the `_destroy` lifecycle
  hook, and makes repeated `close()` calls no-ops. The same aggregate is now
  1 PASS / 0 FAIL / 0 TIMEOUT / 0 CRASH. Neighboring release validators
  `http2-client-destroy`, `http2-client-stream-destroy-before-connect`,
  `http2-client-unescaped-path`, `http2-client-set-priority`,
  `http2-create-client-session`, and `http2-methods` were re-run; all except
  the pre-existing `http2-client-destroy` lifecycle race PASS. Rust library
  gates remain green (quench-node 22 tests; quench-runtime 531 passed, 1
  ignored). No fixture, manifest entry, or skip list was edited.

- Post-HTTP/2 diagnostics aggregate refresh (2026-09-08): the release
  `--all --filter diagnostics-channel --timeout-secs 15` replay records 72
  PASS / 9 feature SKIP / 1 FAIL / 0 TIMEOUT / 0 CRASH / 82 total (inventory
  `b67b2f69d62305f8`). Every channel, tracing, permission, UDP, Web Locks, and
  HTTP/2 diagnostics fixture passes; the sole residual is the documented
  `test-diagnostics-channel-memory-leak.js` stress case. No fixture, manifest
  entry, or skip list was edited.

- HTTP/2 request terminal-state ordering (2026-09-08): the focused release
  baseline for `test-http2-client-write-before-connect.js` was 0 PASS / 1
  FAIL (inventory `4938c85d1ddd9875`), because ordinary `request()` emitted
  HEADERS with END_STREAM before subsequent body writes. Rust now keeps client
  requests open until `.end()` or an explicit `{ endStream: true }`; stream
  error callbacks use a post-poll queue so buffered RST_STREAM control frames
  reach the peer before an error handler can close the session. The focused
  validator is now 1 PASS / 0 FAIL, and
  `test-http2-client-stream-destroy-before-connect.js`,
  `test-http2-client-rststream-before-connect.js`, and
  `test-http2-client-data-end.js` remain PASS. The required
  `test-http2-client-destroy.js` still exposes a separate cross-tick reset
  ordering race (`rstCode` 0 vs 8), so this is not task closure. The full
  diagnostics-channel replay remains 72 PASS / 9 feature SKIP / 1 FAIL / 0
  TIMEOUT / 0 CRASH / 82 total (inventory `b67b2f69d62305f8`), and
  `cargo test -q -p quench-node --lib` passes 22/22. No fixture, manifest
  entry, or skip list was edited.

- HTTP/2 same-turn AbortSignal ordering follow-up (2026-09-08): the focused
  `test-http2-client-destroy.js` baseline failed earlier with an unexpected
  server `stream` callback because `request({}, { signal })` emitted HEADERS
  before a synchronous `controller.abort()`. Rust now defers signal-bearing
  HEADERS through the host write queue and removes that frame when
  `stream_destroy()` reaches its terminal RST_STREAM state; already-aborted
  signals skip submission entirely. An isolated release replay no longer
  trips the `mustNotCall` stream branch and reaches the remaining assertion,
  `rstCode` 0 versus expected 8, which is a separate stale stream-alias
  lifecycle gap. Neighboring `test-http2-methods.js`,
  `test-http2-connect.js`, and `test-http2-create-client-connect.js` remain
  PASS. No fixture, manifest entry, or skip list was edited.

- HTTP/2 reset-alias investigation (2026-09-08): an isolated Rust probe for
  the final server `rstCode` assertion passes when the lifecycle slot is
  shared across COW representatives, but applying that slot to either all
  streams or only net-pump streams makes the complete
  `test-http2-client-destroy.js` fixture hang past a 30-second hard timeout.
  The experiments were reverted. This rules out scalar property storage as a
  standalone fix; the remaining gap is the event-loop teardown/ownership
  sequence after the reset. No fixture, manifest entry, or skip list was
  edited.

- HTTP/2 reset write trace (2026-09-08): the required fixture's peer-RST path
  was traced without fixture changes. `set_property_in_place` successfully
  wrote `rstCode=8` to the stream index's stale object, while the subsequent
  canonical read still observed `0`; canonicalizing only at RST dispatch fixed
  that assertion but left the fixture hanging during teardown. The partial
  experiment was reverted, so the release baseline remains 0 PASS / 1 FAIL /
  0 TIMEOUT for this validator; the evidence narrows the root cause to
  COW-representative ownership plus socket teardown, not reset-code parsing.

- HTTP/2 auto-discard lifecycle follow-up (2026-09-08): the focused release
  baseline for `test-http2-client-auto-discard-no-response-listener.js` was
  0 PASS / 1 FAIL / 0 TIMEOUT. Rust now marks a client stream destroyed at
  terminal close when no `response` listener exists, matching Node's
  fire-and-forget response-body discard contract without changing ordinary
  response streams. The focused validator is now 1 PASS / 0 FAIL / 0 TIMEOUT;
  `test-http2-client-stream-destroy-before-connect.js` and
  `test-http2-client-rststream-before-connect.js` remain PASS. The required
  `test-http2-client-destroy.js` remains the separate `rstCode` 0-vs-8
  teardown race, and no fixture, manifest entry, or skip list was edited.

- HTTP/2 request END_STREAM defaults (2026-09-08): the focused release
  baseline for `test-http2-create-client-session.js` was 0 PASS / 1 FAIL
  (server observed HEADERS flags `4` instead of Node's `5`). Rust now derives
  the default header END_STREAM bit from the method—GET/HEAD/DELETE close
  header-only requests, while POST/PUT/PATCH remain open for body writes—with
  explicit `endStream` still authoritative. The validator is now 1 PASS / 0
  FAIL; `test-http2-client-write-before-connect.js`, methods, data-end, and
  reset-before-connect validators remain PASS. The required
  `test-http2-client-destroy.js` remains the independent COW teardown race.
  No fixture, manifest entry, or skip list was edited.

- HTTP/2 reset-aware close experiment (2026-09-08): a bounded Rust-only
  candidate deferred a server-side `END_STREAM` close only when the shared
  write queue already contained a peer `RST_STREAM` for that stream, leaving
  ordinary server closes unchanged. An isolated release replay still failed
  `test-http2-client-destroy.js` with `rstCode` 0 versus 8, while the neighboring
  stream-destroy validator passed; the candidate was reverted and no debug
  traces, fixture edits, or skip-list changes remain.

- HTTP/2 staged-error experiment (2026-09-08): staging `pending_http2_events`
  generated during `poll_sockets()` until the following pump turn preserved
  the 18/18 diagnostics-channel HTTP/2 aggregate and all targeted stream
  neighbors, but `test-http2-client-destroy.js` still failed with
  `rstCode` 0 versus 8. The event staging was reverted; the remaining issue
  is therefore not solved by callback delay alone, and no fixture or skip-list
  change was made.

- HTTP/2 combined close/error staging experiment (2026-09-08): combining a
  one-turn delay for server `END_STREAM` close notifications with one-turn
  staging for HTTP/2 error callbacks kept the diagnostics-channel transport
  gate at 18 PASS / 0 FAIL / 0 TIMEOUT and kept the targeted stream neighbors
  green, but `test-http2-client-destroy.js` still failed with `rstCode` 0
  instead of 8. The candidate was reverted; no debug traces or fixture/skip
  list changes remain.

- HTTP/2 reset-aware session-close experiment (2026-09-08): delaying
  `ClientHttp2Session.close()` for a stream with a recorded reset until the
  next socket-read pass preserved the stream-destroy, reset-before-connect,
  and write-before-connect neighbors, but the required destroy fixture still
  failed with `rstCode` 0 versus 8. The candidate was reverted and left no
  source, fixture, or skip-list changes.

- HTTP/2 transport teardown experiment (2026-09-08): a Rust-only candidate
  delayed HTTP/2 server END_STREAM closes and client TCP destruction for one
  pump turn, then added a second non-blocking HTTP/2 socket read before close
  publication. The required destroy fixture still failed `rstCode` 0 versus
  8, so the candidate was fully reverted; no fixture, manifest, or skip-list
  changes remain.

- HTTP/2 COW identity trace (2026-09-08): the server stream is first stored in
  `net.http2_streams` as one object representative, then repeated
  `decorate_http2_stream()` descriptor definitions publish a successor. RST
  handling writes `rstCode=8` to the indexed predecessor while close
  canonicalization reads the successor's unchanged `0`. Canonicalizing only
  the map entry or every decoration call was replayed and still failed (and
  one variant hung teardown), so the broader identity rebinding contract is
  unresolved; all traces and source experiments were removed.

- HTTP/2 dual-representative reset write (2026-09-08): mirroring RST_STREAM
  terminal fields onto both the indexed stream and its canonical replacement
  left `test-http2-client-destroy.js` at the same `rstCode` 0 versus 8 failure.
  The server close callback still runs before the peer RST is dispatched, so
  property mirroring alone is insufficient; the candidate was reverted.

- HTTP/2 deferred-close age experiment (2026-09-08): deferring every
  server-side END_STREAM close until up to two subsequent pump turns allowed
  a minimal AbortSignal repro to observe `rstCode=8`, but the required
  `test-http2-client-destroy.js` hung for 20 seconds during teardown. The
  generic delay was reverted; the focused release baseline remains 0 PASS / 1
  FAIL / 0 TIMEOUT, with no fixture, manifest, or skip-list changes.

- HTTP/2 post-error close queue experiment (2026-09-08): queueing only
  reset-marked server closes until pending HTTP/2 error callbacks completed,
  and mirroring an already-recorded `NGHTTP2_CANCEL` fact onto the canonical
  stream, changed the focused assertion first to `2` versus `8` and then to a
  teardown hang when narrowed to reset-marked streams. The candidate was
  reverted completely; no fixture, manifest, or skip-list changes remain.

- Current dependency-order diagnostics replay (2026-09-08): the full
  `--all --filter diagnostics-channel --timeout-secs 10` sweep records
  pass=72, skip=9, fail=1, timeout=0, crash=0 (82 total; inventory
  `b67b2f69d62305f8`). All channel/tracing and HTTP/2 diagnostics fixtures
  pass; the sole failure is `test-diagnostics-channel-memory-leak.js`, the
  documented engine/V8 leak-stress non-goal. QUIC rows remain capability
  skips, with no fixture or manifest edits.

- HTTP/2 GOAWAY lifecycle slice (2026-09-09): after the flow-control/PING
  changes, the focused `--all --filter http2-goaway --timeout-secs 15`
  baseline was 2 pass / 0 fail / 1 timeout / 0 crash / 3 total (inventory
  `7ccef6b1e2e20575`); `test-http2-goaway-opaquedata.js` timed out because
  `session.goaway()` was still a callable no-op. Rust now validates the
  numeric code/last-stream-id and opaque typed-array payload, derives the
  default last stream from the session's canonical stream map, and emits a
  real connection-level GOAWAY frame through the owning socket. The same
  replay is 3 pass / 0 fail / 0 timeout / 0 crash / 3 total. The existing
  delayed-request and internally-triggered GOAWAY cleanup fixtures remain
  green; `cargo test -q -p quench-node --lib` passes 22/22. No fixture or
  manifest entry was edited or skipped.

- QUIC capability audit (2026-09-09): the required release replay
  `target/release/run-parallel --all --filter quic --timeout-secs 15` records
  0 pass / 248 feature skips / 0 fail / 0 timeout / 0 crash / 248 total
  (inventory `e6d4d281e7094e5d`). A direct release probe confirms
  `process.features.quic === false`, `require('node:quic')` throws
  `ERR_UNKNOWN_BUILTIN_MODULE`, and `module.isBuiltin('node:quic')` is false.
  The six focused Rust `quic_transport` tests pass. The Rust implementation
  currently owns only a non-blocking UDP edge, bounded queues, and a
  transport-neutral `quinn-proto` packet transition; no public endpoint,
  connection/session, stream/datagram, TLS/ALPN, or HTTP/3/QPACK state is
  registered. Enabling the capability now would therefore expose constructors
  without their observable state machine. This remains a genuine capability
  boundary, not a fixture skip or a shell-out: the next reusable slice must
  add endpoint/session state and pump-driven handshake/timeout/transmit facts
  before public registration can be truthful. No fixture, manifest, or skip
  list was edited.

- HTTP/2 foreign-Duplex transport probe (2026-09-09): the debug baseline
  `target/debug/run-parallel --all --filter http2-client --timeout-secs 15`
  was 27 pass / 0 skip / 6 fail / 6 timeout (39 total). A Rust-only adapter
  for `http2.connect({createConnection})` and `server.emit('connection',
  duplex)` was replayed against the generic-streams and backpressure fixtures;
  it stalled and exposed an ordering defect where the 24-byte client preface
  was not observed before a 9-byte SETTINGS frame. The adapter and diagnostics
  were fully reverted. This confirms HTTP/2 is not merely a missing constructor:
  the remaining work is a reusable foreign-transport byte-order/session pump,
  not fixture-specific handling.

- Isolated mandated replay (2026-09-09, rebuilt with sibling `quench-node`):
  both baseline and validation commands
  (`--all --filter diagnostics-channel --timeout-secs 10`) record 72 PASS /
  9 feature SKIP / 1 FAIL / 0 TIMEOUT / 0 CRASH / 82 total (inventory
  `b67b2f69d62305f8`). The sole failure is the documented
  `v8.queryObjects()` memory-stress non-goal; all diagnostics-channel,
  tracing, permission, Web Locks, HTTP, UDP, worker, and HTTP/2 diagnostics
  validators pass, while QUIC rows are truthful feature skips. The isolated
  `cargo test -q -p quench-node --lib` gate passes 22/22. No fixture,
  manifest, or skip-list entry was edited.

- HTTP/2 close-edge audit (2026-09-09): the mandated
  `diagnostics-channel-http2` filter remains 18 PASS / 0 FAIL / 0 TIMEOUT /
  0 CRASH (inventory `1adcedf53c75217f`). The remaining general HTTP/2
  edges are `test-http2-client-destroy.js` (the client stream records reset
  code 8 locally, but the queued RST_STREAM can be lost when session teardown
  closes the socket first) and `test-http2-server-close-idle-connection.js`
  (timeout because the idle-close path only scans the HTTP/1 connection
  registry). No fixture, manifest, or skip-list entry was edited; these are
  shared session/transport ordering gaps rather than constructor gaps.

- QUIC transport-state follow-up (2026-09-09): the Rust-only transport edge
  now retains bounded incoming Initials, supports an explicit host-side
  `accept()` transition, stores accepted `quinn-proto` connections, routes
  connection events, and polls application events/transmits with timeout
  handling. `cargo test -q -p quench-node quic_transport --lib` is 7 pass / 0
  fail. The VM capability remains disabled (`process.features.quic === false`)
  because endpoint/session objects, stream/datagram APIs, TLS/ALPN, HTTP/3,
  and QPACK are still absent; no fixture or manifest entry was changed.

- HTTP/2 lifecycle follow-up (2026-09-09): after routing request streams to
  both the server and owning session, tracking `writableFinished`, and keeping
  AbortSignal-backed header requests open until cancellation, the rebuilt
  `test-http2-server-close-idle-connection.js` validator is PASS (previously
  TIMEOUT). The rebuilt `test-http2-client-destroy.js` now reaches the expected
  local/server `NGHTTP2_CANCEL` reset observations but still TIMEOUTs during
  broader session teardown. The focused
  `--all --filter diagnostics-channel-http2 --timeout-secs 15` remains 18 PASS /
  0 FAIL / 0 TIMEOUT / 0 CRASH; no fixture, manifest, or skip-list entry was
  edited.

- HTTP/2 session teardown completion (2026-09-10): after fixing stream
  cancellation/close delivery, graceful GOAWAY errors, post-destroy session
  errors, secure-session ALPN setup, and hiding the internal binding from the
  enumerable global surface, rebuilt validation passes
  `test-http2-client-destroy.js` and
  `test-http2-server-close-idle-connection.js`; the focused
  `diagnostics-channel-http2` filter is 18 PASS / 0 FAIL / 0 TIMEOUT / 0 CRASH.
  No fixture, manifest, or skip-list entry was edited.

- HTTP/2/TLS ALPN option parity (2026-09-10): the shared Rust TLS boundary
  now rejects simultaneous `ALPNCallback` and `ALPNProtocols` with
  `ERR_TLS_ALPN_CALLBACK_WITH_PROTOCOLS`, invokes a server ALPN callback with
  the client's ordered protocol list, and rejects an unselected callback
  negotiation when protocols were offered. The unchanged
  `test-http2-alpn.js` validator and the broader
  `test-tls-alpn-server-client.js` validator both PASS in isolated debug
  runs. No fixture, manifest, or skip-list entry was edited.

- HTTP/2 callback/promisifier parity (2026-09-10): `http2.connect` now carries
  its non-error-first, single-session callback fact into the shared Rust
  `util.promisify` path, while opening transport errors still reject the
  resulting promise. The unchanged `test-http2-client-promisify-connect.js`
  and `test-http2-client-promisify-connect-error.js` validators both PASS in
  isolated runs. No fixture, manifest, or skip-list entry was edited.

- HTTP/2 internal-binding error-routing audit (2026-09-10): the unchanged
  `test-http2-client-onconnect-errors.js` validator still fails in a direct
  15-second run with `Error: Unexpected call`. The fixture monkeypatches
  `internalBinding('http2').Http2Session.prototype.request`; the current Rust
  host's public session path does not observe that patched prototype because
  the binding/prototype representatives diverge under copy-on-write. A
  canonical-binding/cache experiment was reverted after its prototype keys
  remained empty, so no fixture-specific error fabrication or manifest change
  was committed. This residual remains open for a genuine shared constructor
  identity fix.

- HTTP/2 internal-binding error-routing fix (2026-09-10): the Rust host now
  keeps one shared internal `Http2Session` prototype across binding
  materializations, reuses the host-installed binding when `require('http2')` loads after
  `internalBinding('http2')`, and routes native request return codes through
  the stream/session error lifecycle. The unchanged
  `test-http2-client-onconnect-errors.js` validator moved from 0 PASS / 1 FAIL
  to 1 PASS / 0 FAIL in isolated 20-second runs. The existing ALPN,
  promisified-connect, destroy, and idle-close validators remain PASS; no
  fixture, manifest, or skip-list entry was edited.

- HTTP/2 HTTP/1 preface error follow-up (2026-09-10): the focused
  `test-http2-client-http1-server.js` baseline failed on the release runner
  before the client-error/protocol-error routing fix. Rust now recognizes the
  HTTP/2 connection preface incrementally in the HTTP/1 parser and reports
  Node's `HPE_PAUSED_H2_UPGRADE` plus `bytesParsed: 24`; HTTP/2 teardown routes
  the resulting nghttp protocol error to pending streams. The isolated rebuilt
  validator changed to PASS. `test-http2-allow-http1.js` remains a separate
  TLS-certificate-verification residual (`unable to verify the first
  certificate`), not a protocol-preface failure. No fixture or manifest entry
  was edited or skipped.

- HTTP/2 `allowHTTP1` follow-up (2026-09-10): the focused
  `test-http2-allow-http1.js` baseline was 0 PASS / 1 FAIL, reporting
  `unable to verify the first certificate`; the shared Rust URL-form HTTPS
  option path now forwards `rejectUnauthorized: false`, and accepted secure
  HTTP/2 listeners attach the ordinary HTTP/1 parser when ALPN does not select
  `h2`. The rebuilt validator is 1 PASS / 0 FAIL. Neighboring
  `test-http2-client-http1-server.js`, `test-http2-alpn.js`, and
  `test-http2-client-promisify-connect.js` remain PASS. No fixture, manifest,
  or skip-list entry was edited.

- Current task-008 gate replay (2026-09-10): the exact baseline and
  validation commands both record 72 PASS / 9 feature SKIP / 1 FAIL / 0
  TIMEOUT / 0 CRASH / 82 total (inventory `b67b2f69d62305f8`). All
  diagnostics-channel, tracing, permission, Web Locks, module-import,
  network, UDP, and HTTP/2 validators pass; the sole failure is the
  documented V8 `queryObjects()` memory-stress non-goal, while QUIC rows are
  truthful feature skips because `process.features.quic` remains false.
  `cargo test -p quench-node --lib` passes all 23 tests. No diagnostics Rust
  code, fixture, manifest, or skip-list entry changed during this replay.

- HTTP/2 session-control follow-up (2026-09-10): focused baselines were
  `test-http2-altsvc.js` 0 PASS / 1 FAIL and
  `test-http2-session-settings.js` 0 PASS / 1 FAIL; malformed-ALTSVC and ping
  were already PASS. Rust now validates and emits ALTSVC frames, carries
  server/client SETTINGS through canonical `NetState`, preserves
  `localSettings`/`remoteSettings` snapshots and events, and filters custom
  settings through `remoteCustomSettings`. The four focused validators now
  all PASS (4/4), with no fixture, manifest, or skip-list edits.
  `cargo test -q -p quench-node --lib` remains 23 PASS / 0 FAIL.

- Post-session-control task gate (2026-09-10): the exact
  `--all --filter diagnostics-channel --timeout-secs 10` replay remains
  72 PASS / 9 feature SKIP / 1 FAIL / 0 TIMEOUT / 0 CRASH / 82 total
  (inventory `b67b2f69d62305f8`). The single failure is still the documented
  V8 `queryObjects()` memory-stress non-goal; QUIC rows remain truthful
  feature skips. `cargo test -q -p quench-node --lib` passes all 23 tests.

- ALTSVC wire-type correction (2026-09-10): the focused ALTSVC validator
  exposed that the Rust frame enum lacked the RFC 7838 type-0xA mapping even
  though session validation/dispatch existed. Commit `8e0720630` adds the
  canonical encode/decode mapping; ALTSVC, PING, GOAWAY opaque-data, and
  session-method validators all PASS afterward. No fixture or skip-list entry
  changed.

- Fresh QUIC capability audit (2026-09-10): the required
  `--all --filter quic --timeout-secs 10` replay records 0 PASS / 248 feature
  SKIP / 0 FAIL / 0 TIMEOUT / 0 CRASH (248 total; inventory
  `e6d4d281e7094e5d`). A direct Rust-host probe reports
  `process.features.quic === false`, `require('node:quic')` throws
  `ERR_UNKNOWN_BUILTIN_MODULE`, and no public endpoint/session/stream or
  datagram API is registered. `quic_transport.rs` currently owns only the
  non-blocking UDP edge, bounded queues, and internal quinn-proto packet and
  connection transitions; the missing reusable state includes TLS 1.3/ALPN,
  endpoint/session publication, stream/datagram lifecycle, loss recovery and
  flow control, migration/tokens/0-RTT, and HTTP/3/QPACK. Enabling the public
  capability would therefore expose constructors without their protocol
  state machine. No bounded Rust-only fix was identified, and no fixture,
  manifest, or skip-list entry was edited.

- HTTP/2 diagnostic stream identity fix (2026-09-10): the diagnostics sweep
  baseline was 60 PASS / 9 feature SKIP / 13 FAIL / 0 TIMEOUT (82 total) when
  stream decoration discarded its concrete constructor through a COW property
  update. Rust now installs the `ClientHttp2Stream`/`ServerHttp2Stream`
  constructor slot in place. The exact replay is 72 PASS / 9 feature SKIP /
  1 FAIL / 0 TIMEOUT / 0 CRASH (82 total; inventory
  `b67b2f69d62305f8`), and all 18 HTTP/2 diagnostics fixtures pass. The sole
  remaining failure is the documented V8 `queryObjects()` memory-stress
  non-goal. `cargo test -q -p quench-node --lib` passes 23/23. No fixture,
  manifest, or skip-list entry was edited or skipped.

- Fresh mandated baseline/validation replay (2026-09-10): both the required
  baseline and exact validation command record 60 pass / 9 feature skip / 13
  fail / 0 timeout / 0 crash / 82 total (inventory `b67b2f69d62305f8`). All
  diagnostics-channel core, tracing, permission, Web Locks, module-import,
  HTTP, UDP, worker, and network validators pass. The 13 failures are current
  HTTP/2 diagnostics lifecycle rows plus the documented V8 queryObjects
  memory-stress row; no diagnostics-channel registry fix was identified in
  this replay, and no fixture or manifest entry was edited. `cargo test -q -p
  quench-node --lib` passes 23/23.

- Fresh HTTP/2 residual audit (2026-09-10): the bounded debug sweep was run
  with `--all --filter http2 --timeout-secs 1 --results
  /tmp/http2-fresh-bounded.json`; it was stopped after the runner reached the
  compatibility-response range because `run-parallel --all` executes rows
  sequentially and the one-second timeout produced load-sensitive timeouts.
  The reliable focused diagnostics baseline remains 18 pass / 0 fail / 0
  timeout / 0 crash (inventory `1adcedf53c75217f`). A direct baseline of
  `test-http2-compat-serverresponse-end.js` failed at
  `response.stream.on` (`null`/`undefined`). Rust now exposes the canonical
  underlying stream as `Http2ServerResponse.stream`; the same fixture advances
  to its later callback-count assertion (`Unexpected call`), while the
  neighboring `test-http2-compat-serverresponse-close.js` remains PASS. The
  response event now also carries the protocol flags as its second argument.
  These are reusable response/stream surface facts; no fixture, manifest, or
  skip-list entry was edited.

- HTTP/2 HEAD response framing follow-up (2026-09-10): the focused
  `test-http2-compat-serverresponse-end.js` baseline reached a flags assertion
  with `4 !== 5`. Rust now carries the request method into the canonical
  compatibility stream, suppresses HEAD response DATA frames, and emits
  `END_HEADERS|END_STREAM` flags (`5`) for HEAD responses. The same fixture now
  advances past that assertion but still fails on a later generic
  `mustNotCall`/finish-order residual; the neighboring
  `test-http2-compat-serverresponse-close.js` remains PASS. No fixture,
  manifest, or skip-list entry was edited.

- Diagnostics regression replay after HTTP/2 framing changes (2026-09-10):
  `--all --filter diagnostics-channel --timeout-secs 10` remains 72 PASS / 9
  feature SKIP / 1 FAIL / 0 TIMEOUT / 0 CRASH / 82 total (inventory
  `b67b2f69d62305f8`). The sole failure remains the documented V8
  `queryObjects()` memory-stress non-goal; all diagnostics HTTP/2 lifecycle
  rows pass. `cargo test -q -p quench-node --lib` remains 23/23.

- HTTP/2 compatibility send-date follow-up (2026-09-10): the focused
  baseline `test-http2-compat-serverresponse-headers-send-date.js` failed
  with `AssertionError: true !== false` because `stream_respond` always
  inserted a Date header after `response.sendDate = false`. Rust now carries
  the compatibility response's `sendDate` fact onto the canonical stream and
  gates the shared response-header insertion. The focused fixture now PASSes,
  as does neighboring `test-http2-compat-serverresponse-close.js`; no
  fixture, manifest, or skip-list entry was edited.

- HTTP/2 compatibility status-code follow-up (2026-09-10): a fresh focused
  baseline of `test-http2-compat-serverresponse-statuscode.js` timed out
  because `createServer()` did not route a later `server.once('request')`
  listener through compatibility request/response views. Rust now dispatches
  installed request listeners generically and exposes a validating
  `statusCode` accessor: valid 2xx-5xx values store normally, informational
  and out-of-range values raise the documented RangeError codes. The focused
  fixture now PASSes; send-date and compatibility-close neighbors also PASS,
  and `cargo test -q -p quench-node --lib` remains 23/23. No fixture,
  manifest, or skip-list entry was edited.

- HTTP/2 compatibility `end()` residual recheck (2026-09-10): after a fresh
  rebuild, `test-http2-compat-serverresponse-end.js` remains 0 PASS / 1 FAIL /
  0 TIMEOUT / 0 CRASH when run in isolation. The failure is the client-side
  `mustNotCall()` receiving the 4-byte DATA payload `"data"` on a HEAD
  response. Suppressing that DATA frame alone causes a lifecycle timeout in
  the later headerless-`end()` cases, so no partial wire suppression was kept;
  the committed status-code, send-date, and close validators remain 3/3 PASS.

- HTTP/2 compatibility status-message follow-up (2026-09-10): the focused
  `--all --filter http2-compat-serverresponse --timeout-secs 5` baseline was
  5 PASS / 16 FAIL / 1 TIMEOUT / 0 CRASH (22 total; inventory
  `3834806262b22473`). Rust now exposes `statusMessage` as a warning-only
  accessor (returning `''` and emitting one `UnsupportedWarning` per response)
  and handles the `writeHead(statusCode, statusMessage, headers)` overload.
  The exact after sweep is 8 PASS / 13 FAIL / 1 TIMEOUT / 0 CRASH (22 total;
  same inventory), reducing fail+timeout from 17 to 14. The three
  status-message fixtures plus status-code, send-date, and close validators
  all PASS, and `cargo test -q -p quench-node --lib` remains 23/23. No
  fixture, manifest, or skip-list entry was edited.

- HTTP/2 compatibility response-header follow-up (2026-09-10): continuing
  from the same bounded inventory (`3834806262b22473`), the baseline was
  8 PASS / 13 FAIL / 1 TIMEOUT / 0 CRASH (22 total). Rust now retains
  `setHeader()` values, applies `writeHead()` overload precedence, parses both
  nested and flat header arrays (including duplicate-value merging and
  `ERR_INVALID_ARG_VALUE` for odd arrays), and normalizes callback-only
  `response.end(cb)` so the completion callback is delivered. The exact
  after sweep is 10 PASS / 11 FAIL / 1 TIMEOUT / 0 CRASH (22 total); both
  writeHead fixtures now PASS, and the status-message, status-code, send-date,
  and close validators remain green. `cargo test -q -p quench-node --lib`
  remains 23/23. No fixture, manifest, or skip-list entry was edited.

- HTTP/2 compatibility response lifecycle/header follow-up (2026-09-10): the
  next exact bounded sweep on inventory `3834806262b22473` started at 10 PASS /
  11 FAIL / 1 TIMEOUT / 0 CRASH (22 total). Rust now implements the shared
  response header accessors (`hasHeader`, `getHeader`, `getHeaders`,
  `getHeaderNames`, `removeHeader`, `appendHeader`) and `flushHeaders`, keeps
  `_header` synchronized with `headersSent`, rejects pseudo/invalid headers,
  and suppresses body DATA for HEAD responses while preserving stream end
  ordering. The after sweep is 13 PASS / 8 FAIL / 1 TIMEOUT / 0 CRASH (22
  total). `test-http2-compat-serverresponse-headers.js`,
  `flushheaders.js`, `end.js`, both writeHead fixtures, and the status/sendDate/
  close validators PASS; `cargo test -q -p quench-node --lib` remains 23/23.
  No fixture, manifest, or skip-list entry was edited.

- Validation recheck after the response-surface changes (2026-09-10): the
  exact task gate `--all --filter diagnostics-channel --timeout-secs 10` is
  72 PASS / 9 feature SKIP / 1 FAIL / 0 TIMEOUT / 0 CRASH (82 total; inventory
  `b67b2f69d62305f8`). The sole failure is the documented queryObjects memory
  stress non-goal; all diagnostics HTTP/2 rows remain green. `cargo test -q
  -p quench-node --lib` passes 23/23.

- HTTP/2 compatibility response lifecycle follow-up (2026-09-10): the next
  bounded response sweep began at 13 PASS / 8 FAIL / 1 TIMEOUT / 0 CRASH
  (22 total). Rust now detaches `response.socket`/`connection` after finish,
  keeps `response.stream` available, schedules `setTimeout()` through the
  host timer/event machinery, and suppresses HEAD response body DATA while
  retaining writable completion semantics. The exact after sweep is 15 PASS /
  6 FAIL / 1 TIMEOUT / 0 CRASH (22 total); `finished.js`, `settimeout.js`, and
  `end.js` now PASS. `cargo test -q -p quench-node --lib` remains 23/23; no
  fixture, manifest, or skip-list entry was edited.

- HTTP/2 compatibility push/write follow-up (2026-09-10): the bounded
  `--all --filter http2-compat-serverresponse --timeout-secs 10` sweep began
  at 15 PASS / 6 FAIL / 1 TIMEOUT / 0 CRASH (22 total; inventory
  `3834806262b22473`). Rust now keeps aliased stream lifecycle state coherent
  for `write()`, exposes `createPushResponse()` through the compatibility
  response, and derives push `:authority` from request diagnostics or the
  server local port when an alias lacks those fields. The after sweep is
  17 PASS / 4 FAIL / 1 TIMEOUT / 0 CRASH (22 total). The write and
  create-push-response fixtures now PASS; no fixture, manifest, or skip-list
  entry was edited.

- HTTP/2 compatibility trailers follow-up (2026-09-10): the focused baseline
  `--all --filter http2-compat-serverresponse-trailers --timeout-secs 10` was
  0 PASS / 2 FAIL / 0 TIMEOUT / 0 CRASH (2 total). Rust now preserves each
  completed HEADERS block per stream, exposes generic `setTrailer()` and
  `addTrailers()` compatibility methods, emits trailing HEADERS after the
  terminal body DATA, and dispatches received trailers to raw and
  compatibility request/response views. The after-count is 2 PASS / 0 FAIL /
  0 TIMEOUT / 0 CRASH (2 total); both trailers fixtures pass. The HTTP/2
  library tests remain 23/23, with no fixture or manifest edits.

- HTTP/2 compatibility response validation closure (2026-09-10): after
  rebuilding `run-parallel` with the lifecycle, push, backpressure, and
  trailer fixes, the exact bounded `--all --filter
  http2-compat-serverresponse --timeout-secs 10` replay records 22 PASS / 0
  FAIL / 0 TIMEOUT / 0 CRASH (22 total; inventory `3834806262b22473`). All
  response compatibility fixtures pass without fixture, manifest, or skip-list
  edits; `cargo test -q -p quench-node --lib` remains 23/23.

- Raw HTTP/2 settings validation follow-up (2026-09-10): the focused baseline
  `--all --filter http2-client-settings-before-connect --timeout-secs 10` was
  0 PASS / 1 FAIL / 0 TIMEOUT / 0 CRASH (1 total; inventory
  `d5183e431a8ab990`). `session.settings()` was discarding the shared settings
  encoder's `TypeError`/`RangeError` and replacing it with a generic `Error`.
  Rust now propagates the canonical validation result before encoding the wire
  payload. The exact after replay is 1 PASS / 0 FAIL / 0 TIMEOUT / 0 CRASH
  (1 total; inventory `a51614cf86a77405`), confirmed by a second replay and
  direct `run --one`; no fixture, manifest, or skip-list entry was edited.

- Raw HTTP/2 stream-close ordering follow-up (2026-09-10): the focused
  `--all --filter http2-buffersize --timeout-secs 10` baseline was 0 PASS / 1
  FAIL / 0 TIMEOUT / 0 CRASH (1 total; inventory `d5183e431a8ab990`).
  `stream.close()` wrote `RST_STREAM` directly ahead of queued DATA, so a
  peer observed `ERR_HTTP2_STREAM_ABORTED` before the request body ended. Rust
  now queues the reset in the shared FIFO write queue; the exact after replay
  is 1 PASS / 0 FAIL / 0 TIMEOUT / 0 CRASH (1 total; inventory
  `d5183e431a8ab990`), confirmed by three replays. No fixture, manifest, or
  skip-list entry was edited.
