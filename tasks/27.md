# 27: Cut over to the v2-derived runtime

## Outcome

Atomically make the new interpreter the sole production `quench-runtime` and remove the legacy interpreter.

## Current gap

The feature-complete engine still has a temporary package/name and production hosts still target the legacy package.

## Dependencies

See this task's `depends_on` in [index.json](index.json).

## Scope and implementation sequence

Freeze the green/performance-qualified commits, move the next implementation into `crates/quench-runtime`, update all workspace dependencies and production binaries, and remove the legacy source tree and migration-only compatibility adapters. Rename next development targets to canonical production targets and remove backend selection plumbing.

## Verification

From a clean build directory, run workspace check/test/clippy, CLI smoke/error/exit tests, complete Test262, tracked Node compatibility, complete Wasm, residual phase tests, and the four-workload performance gate. Search for legacy crate names, reducers, value models, runtime flags, JIT/stencil/native code, and sibling paths.

## Definition of done

- One `quench-runtime` package and one engine implementation remain.
- All hosts and runners use it by default without flags.
- No legacy semantic fallback, guest JIT, executable-memory path, or sibling build dependency remains.

## Performance evidence

Reconfirm task 26 with final production names/binaries; the rename/cutover may not invalidate provenance.
