{
    "schema": 1,
    "title": "Fact-generated interpreter VM — full Deegen-paper alignment",
    "source_of_truth": "tasks/index.json",
    "constraints": [
        "quench-runtime owns JavaScript semantics; quench-node owns only the Node host boundary",
        "Benchmark fixtures and workload-specific kernels are not production VM semantics",
        "Every fact is Proven, Guarded, or Unknown; every fast path (interpreter, IC, JIT) has complete fallback semantics",
        "Generated code is derived from one declaration; handwritten code is limited to irreducible behavior",
        "Every task belongs to exactly one theme",
        "Performance measurements are evidence: any optimization admitted here must generalize to any JavaScript program with the same runtime shape, not be sized to one fixture's numbers (see docs/benchmark-integrity.md)",
        "See docs/deegen-alignment.md for the paper-vs-implementation gap this plan closes",
        "Speculative optimization (optimizing JIT, tier-up, OSR) is admitted only alongside its deoptimization safety net; no speculative tier ships without a working bailout path"
    ],
    "implementation_state": {
        "completed_slices": [
            "001-026 (prior queue, all themes deegen_codegen/quickjs_perf/benchmark_integrity/copy_patch_jit): single-source vm_op! catalog, ordinary interpreter, bounded quickening/inline-cache state, compact tagged values, hashed shape/atom lookup, copy-and-patch region-stencil baseline JIT tier, callee-directed continuation dispatch. See git history and docs/architecture-evidence.md for evidence; superseded task files removed once this plan closed the gap in docs/deegen-alignment.md."
        ],
        "remaining_slices": [
            "027", "028", "029", "030", "031"
        ]
    },
    "themes": {
        "deegen_full_jit": {
            "goal": "Close the remaining gap between quench and the Deegen paper's (arXiv:2411.11469) full two-tier JIT-capable VM design: profiling-driven tier-up, OSR entry, deoptimization, and a real optimizing JIT tier built from the same vm_op! fact schema. See docs/deegen-alignment.md.",
            "plan": ["027", "028", "029", "030", "031"]
        }
    },
    "items": [
        {
            "id": "027",
            "file": "027.md",
            "title": "Profiling counters and tier-up policy",
            "theme": "deegen_full_jit",
            "depends_on": []
        },
        {
            "id": "028",
            "file": "028.md",
            "title": "OSR entry into a lower-tier-compiled loop",
            "theme": "deegen_full_jit",
            "depends_on": ["027"]
        },
        {
            "id": "029",
            "file": "029.md",
            "title": "Deoptimization protocol and frame reconstruction",
            "theme": "deegen_full_jit",
            "depends_on": ["028"]
        },
        {
            "id": "030",
            "file": "030.md",
            "title": "Cross-tier generic inline cache (λi/λe) shared by baseline and optimizing JIT",
            "theme": "deegen_full_jit",
            "depends_on": ["029"]
        },
        {
            "id": "031",
            "file": "031.md",
            "title": "Optimizing JIT IR: instruction selection, register allocation, speculative type specialization",
            "theme": "deegen_full_jit",
            "depends_on": ["030"]
        }
    ]
}
