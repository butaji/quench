# 19: Build next-engine runners and differential oracles

## Outcome

Exercise the next engine across existing suites without adding a production backend selector.

## Current gap

The imported crate has a standalone ES5 CLI, while Quench runners and hosts are coupled to legacy runtime APIs.

## Dependencies

See this task's `depends_on` in [index.json](index.json).

## Scope and implementation sequence

Add separately named `quench-next`, `quench-node-next`, `run-all-next`, and Wasm-next development targets. Build reusable result records for value/output/error/descriptor/order/exit/effect comparisons against legacy Quench and local Node. Keep suite discovery shared so old/new runs have identical inputs.

Before any next-engine comparison is used as evidence, freeze per-test legacy
outcomes for all three suites: every discovered Test262 file, every Wasm
directive, and every tracked `tests/node-compat` fixture, with the task 01
provenance fields. Task 01 recorded inventories only; the gates below need
outcomes. For Test262 and Wasm, any legacy failure is listed explicitly as a
known input to fix, not a skip: those gates are 100% of the inventory. For
Node, the fixtures that pass on legacy against the local Node oracle define the
implemented set, and that set is the Node gate. These records are the ratchet
floor for tasks 20, 22, 23, and 43.

The frozen legacy outcomes are also the migration worklist (see the
[README](README.md#migrating-legacy-semantics)): the runner reports the set of
tests that pass on legacy and fail on the next core, grouped by failure
reason, so each cluster points at legacy code to port.

The full-run speed and timeout requirements live in task 48, which this task
depends on.

## Verification

Prove runner self-tests, nonzero discovery, deterministic ordering, timeouts/crash classification, residual round trips, and deliberate mismatch reporting. Verify production `quench-node` contains no runtime engine flag.

## Definition of done

- Every migration lane can target the next engine explicitly.
- Differential records preserve raw outcomes and provenance.
- Development selection cannot affect production behavior.

## Performance evidence

Measure runner overhead separately from guest execution and exclude it from parity binaries.

## Implementation evidence

`tools/diff-next.mjs` now runs legacy Quench, the separately named
`quench-next` binary, and local Node against the same source path. It emits
structured status, signal, timeout, stdout, stderr, spawn-error, duration, and
per-engine match records; production `quench-node` is not selected by a flag.
`--self-test` exercises deterministic observable mismatch handling, timeout
classification, and nonzero-exit preservation without requiring a fixture.
The comparator now preserves raw stderr in each result while excluding only
structured `rqj-*` measurement records from semantic equality, so next-engine
profiling cannot create a false differential mismatch.
The `quench-node-next` development binary and `tools/run-all-next.mjs` provide
the host-shaped entry point and deterministic recursive fixture discovery;
the runner smoke covers two fixtures with zero mismatches against Node. The
next runner now also executes `quench-next --generic` and records an explicit
optimized-vs-generic match for every fixture.
