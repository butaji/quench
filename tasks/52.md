# 52: Declare opcode operand layouts and derive every decoder

## Outcome

Each opcode row declares what its `a`, `b`, `c`, and `imm` operands mean. The
encoder, residual validator, liveness, rewrite legality, disassembler, and
interpreter accessors are generated from that row. A decoder that disagrees
with the encoder can no longer be written.

## Current gap

`opcodes!` (`bytecode.rs`) declares only an opcode's name and effect. The
meaning of each operand is decoded again by hand in `bytecode/validate.rs`,
`compile/liveness.rs` (77 opcode arms), `compile/rewrite.rs`,
`bytecode/disassemble.rs`, and `vm/dispatch.rs`. The same bitfield expression
`(imm & 0x3fff_ffff) >> 16` appears in five places across four files, and the
flag bits (`SUPER_CONSTRUCT_BIT`, `SUPER_CONSTRUCT_ARRAY_BIT`, the eval bits of
`Call`) are known only to the sites that test them.

These copies have drifted. On 2026-09-23 the residual validator rejects
programs that the compiler emits and the interpreter would run:

- `validate.rs` checks `Op::Call | Op::CallKnown` in one arm and requires
  `register(b)`. For `CallKnown`, `b` is a function index, not a register.
  Any known call whose target index is at least the caller's register count
  fails with `function N call is invalid`. Twenty top-level functions and a
  call to the last from inside another function reproduce it. This error
  stops Richards, DeltaBlue, and Splay
  (`target/reviews/core-review-2026-09-23/README.md`).
- `validate.rs` limits `CallKnown` to 8 arguments (`call has too many
arguments`). The compiler does not honor that limit.
- `function N construct is invalid` accounts for 3,195 failing Test262 files
  in `target/test262-next-task08-parallel.json` (for example
  `built-ins/Array/S15.4.2.1_A2.2_T1.js`). Its root cause is not yet
  isolated; the standalone body passes, so the harness prefix changes the
  emitted `Construct` window.

## Dependencies

See this task's `depends_on` in [index.json](index.json). It extends task 05's
residual contract and v2's `opcodes!` declaration. Task 47 derives more of the
generating extension from the same rows and depends on this task.

## Scope and implementation sequence

1. Extend each `opcodes!` row with an operand layout. Operand roles are data:
   destination register (including the `RETURN_REGISTER`/`SET_THIS_REGISTER`
   bits), source register, tagged `Operand`, register window (base and count,
   with its bitfield), site-table index (field, method, object, name, closure,
   constant), function index, jump target, and named flag bits.
2. Generate typed accessors per role from the rows. Remove every hand-written
   bitfield decode from the compiler, rewrite engine, liveness, validator,
   disassembler, and interpreter.
3. Generate residual validation from the roles. Each role checks its own
   domain once: register bounds, window bounds, table lengths, function count,
   and code length. Keep only the cross-instruction invariants hand-written.
   Remove any limit (such as `CallKnown`'s 8 arguments) that the encoder does
   not also enforce from the same row.
4. Generate liveness uses and definitions and the disassembler text from the
   roles. `Wide` instructions and superinstruction rows decode through the
   same layouts.
5. Diagnose the `Construct` rejection from step 3's output, then remove the
   manual checks it replaces.

This is structural work that removes duplicate authorities. It is not tuning:
the `Instr` encoding, the opcode set, and the interpreter's hot paths stay as
they are.

## Verification

Run Richards, DeltaBlue, and Splay on `quench-next` to completion. Run the
full Test262 ratchet (task 48). The 3,195 `construct is invalid` and every
`call is invalid` failure must leave that failure class, with no newly failing
case. Confirm by inspection that no operand bitfield or flag mask remains
outside the generated code. Do not add tests or change the Test262 harness.

## Definition of done

- Each opcode's operand meaning has one declaration.
- The encoder, validator, liveness, rewrite engine, disassembler, and
  interpreter read operands only through generated accessors.
- The validator accepts every program the compiler emits and rejects operands
  outside their declared domain.

## Performance evidence

Measurement only. Record Richards, DeltaBlue, and Splay Score and maximum RSS
once they run (task 26 owns the matched gate), and confirm `Instr` stays 8
bytes.

## Implementation evidence

The opcode rows now declare the immediate layout for capture depth/slot pairs,
call argument windows (including direct-eval flag variants), constructor count
and flags, and register pairs. Named layout-owned masks and typed accessors are
shared by packed and wide instructions. The compiler, validator, liveness,
rewrite legality, and VM dispatch now consume those views rather than
re-decoding call windows, capture pairs, yield-star register pairs, or
constructor flags. `CallKnown` validation treats operand `b` as a function
index, validates its argument window, and no longer has an independent
eight-argument cap. `cargo check -p quench-runtime-next -p quench-test262`
passes. The ordered next-core Test262 prefix 0–2 passes 481/481 in batches no
larger than 100. Full opcode operand-role generation and the Construct failure
family diagnosis remain open; this is partial task evidence, not completion.
Operand tags are now represented by `OperandKind` and shared by residual
validation, liveness, VM resolution, and numeric dispatch rather than duplicated
tag-number branches. A fresh cumulative stages 0–9 rerun passed 1,157/1,157;
stage 10 remains at 10,891/11,101 with the same 210-case/190-family failure
aggregate (`target/test262-next-prefix-0-10-operand-kinds.log`).

Result-register flags now have an opcode-row-owned `ResultLayout`, covering
plain destinations, returnable results, `GetField`'s optional current-`this`
write, and numeric local targets. Packed and wide instruction views derive
result registers, return/`this`/local behavior, and flag validation from that
layout. Compiler rewriting, control-flow/liveness, profile dependencies,
numeric dispatch, ordinary dispatch, and validation consume these accessors.
Both Test262 runners build in the iteration profile. A fresh ordered 0–10 run
confirms stages 0–9 at 1,157/1,157 and stage 10 at 11,083/11,101 (18 failures
across 11 families), unchanged from the preceding Proxy-construction
checkpoint. Evidence: `target/test262-next-prefix-0-10-result-layout.log`;
all cases used the required 900,000 ms timeout and batches ≤100. This
structural slice adds no Test262 pass yet; remaining operand roles and the
Construct validator failure diagnosis are still open.

`CallKnown` keeps destination flags in the row-owned `ResultLayout` and
declares `b=FunctionIndex`; its validator and VM dispatch consume the generated
`known_function_index()` view instead of treating `b` as a register. `Construct`
declares its constructor register and tagged argument source, while its
destination remains derived from `ResultLayout`; the shared decoder asserts
that `c` has the Construct-argument role before deriving its register window
or argument-array operand. Method-call opcodes now declare their immediate as
a method-site index, consumed through `method_site_index()` by validation and
dispatch. `LoadConst`, `MakeClosure`, `MakeObject2`, and
`SuperConstArrayObject2` declare constant, function, object-site, and
superinstruction immediate roles; validation and execution use their typed
accessors. The duplicate `MakeClosure` validator check is removed. `cargo check
-p quench-runtime-next -p quench-test262` passes. The existing Test262 Array
constructor fixture passes 1/1 at stage 31, and the stage-10 `method` slice
passes 1,601/1,601 in batches capped at 100, each with a positive 30,000 ms
timeout. A broader `call` path-filter run passed 817/818; its one failure is
`instanceof/symbol-hasinstance-not-callable`. This is a structural increment
only: most opcode fields and immediate roles remain undeclared, and task 52
remains in progress.

Local access opcodes now declare `LocalSlot` as their immediate role, and the
validator and VM dispatch consume `local_slot()` for local loads, stores, and
TDZ initialization. `cargo check -p quench-runtime-next -p quench-test262`
passes. The full existing Test262 Iterator stage remains 483/514, unchanged
from the previous checkpoint, in six sequential batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=900000`; this confirms no regression in that stage,
not completion. The 31 existing failures remain clustered around zip/zipKeyed
ordering and close behavior, generator/wrapper lookup, strict completion,
keyed padding, and realm/reentrancy. No unit or new tests were run.

Branch instructions now declare `JumpTarget` as their immediate role. The
validator, CFG reachability, liveness successors, rewrite target relocation,
and interpreter dispatch all read this role through `jump_target()`. The
existing Test262 stage-10 `logical` selection passes 190/190 in two batches
capped at 100 with a positive 30,000 ms timeout. `cargo check -p
quench-runtime-next -p quench-test262`, formatting, and diff checks pass. Task
52 remains partial: the opcode table still has many undeclared field and
immediate meanings, and generated role-based validation/disassembly/liveness
is not complete.

Name and private-property immediates now declare `AtomIndex`, with validator
and VM dispatch reads routed through `atom_index()`. Validation now also
checks the atom and register operands for `ResolveName`, `StoreResolvedName`,
and `MarkPrivateName`. Formatting and `cargo check -p quench-runtime-next -p
quench-test262` pass. The existing stage-10 `private` path-filter selection
passes 2,056/2,056 in 21 batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`; no unit or new tests were run. Task 52 remains
in progress because many opcode field/immediate roles and generated decoder
views remain undeclared.

The liveness interpreter now resolves `SuperConstArrayObject2` through its
declared `SuperinstructionIndex` and method-call liveness through
`MethodSiteIndex`, eliminating the remaining raw immediate decodes for those
roles outside the validator and VM. Formatting and `cargo check -p
quench-runtime-next -p quench-test262` pass. The existing stage-10 `object`
path-filter selection passes 1,257/1,257 in 13 batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run; task 52 remains
in progress.

`CacheTemplateObject` and `LoadCachedTemplateObject` now declare
`TemplateSiteIndex`, and VM cache identity reads the index through its typed
accessor. Validation checks each opcode's value/destination register; runtime
identity remains the existing program/function/site tuple. Formatting and
`cargo check -p quench-runtime-next -p quench-test262` pass. The existing
stage-10 `template` selection passes 101/101 in two batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress.

`Binary`, `NumericAdd`, and `NumericMultiply` now declare `BinaryOperator` as
their immediate role. Compile-time specialization, interpreter/numeric
dispatch, profiling, and fused-superinstruction execution consume the typed
accessor. Residual validation constrains generic codes to OXC's operator
domain and verifies specialized opcodes retain their matching addition or
multiplication code. Formatting and `cargo check -p quench-runtime-next -p
quench-test262` pass. The existing stage-10 `operator` selection passes
137/137 in two batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`; the `binary` filename selection had only
3/3 cases. No unit or new tests were run. Task 52 remains in progress.

`IncDec` and `SetIndex` now share the declared `BooleanFlag` immediate role
with `Delete`. Validation rejects non-boolean encodings, and VM dispatch uses
the decoded value for increment/decrement direction and indexed-write
strictness. The first increment run exposed an inverted integer fast-path
branch; that was corrected, and the final existing stage-10 `increment`
selection passes 74/74. The existing stage-10 `assignment` selection passes
1,527/1,527. All batches were capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`; formatting and `cargo check -p
quench-runtime-next -p quench-test262` pass. The `index` filename selection
matched zero cases and is not counted. No unit or new tests were run; task 52
remains in progress.

`GetField` now declares its conditional immediate as `FieldLookup`, decoded
once to either `Atom` or `Site` using the base operand. Validation, liveness,
rewrite legality, and VM dispatch consume that sum representation; nested
field resolution now takes a typed site index. Formatting and `cargo check -p
quench-runtime-next -p quench-test262` pass. The existing stage-10 `property`
path-filter selection passes 348/348 in four batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress.

`Delete` now declares `BooleanFlag`; its decoder accepts only the two boolean
encodings, residual validation rejects other immediate values, and VM strict
deletion behavior consumes the decoded flag. Formatting and `cargo check -p
quench-runtime-next -p quench-test262` pass. The existing stage-10 `delete`
path-filter selection passes 184/184 in two batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress.

Capture loads/stores now pass the declared `CaptureDepthAndSlot` components to
the environment operations as separate depth and slot values; the VM helper no
longer re-decodes a packed immediate. Formatting and `cargo check -p
quench-runtime-next -p quench-test262` pass. Existing stage-10 `closure` and
`scope` selections pass 3/3 and 76/76, with batches capped at 100 and
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress.

`Binary`, `NumericAdd`, and `NumericMultiply` now declare their `b`/`c` fields
as tagged operands, and `JumpBinaryFalse` declares `a` as a binary operator,
`b`/`c` as tagged operands, and no result register. Validation, liveness,
rewrite legality, both dispatch loops, and fused binary execution use these
typed field views. Formatting, `git diff --check`, and `cargo check -p
quench-runtime-next -p quench-test262` pass. The existing stage-10 `logical`
selection passes 190/190 and `operator` passes 137/137, sequentially in batches
capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were
run. Task 52 remains in progress; other opcode roles and generated decoding
remain incomplete.

`JumpFalse`, `Return`, and `Throw` now declare their register-valued `a` field
and `NoResult` layout; `Jump` also declares `NoResult`. Validation, liveness,
rewrite legality, and packed/numeric VM dispatch use the typed register view
where these instructions consume `a`. Formatting, `git diff --check`, and
`cargo check -p quench-runtime-next -p quench-test262` pass. The existing
stage-10 `logical` selection passes 190/190 in batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; broader field-role declaration and generated decoding are open.

`Move`, `Unary`, and `IncDec` now declare `b` as a source register; validation,
liveness, rewrite legality, binding-time analysis, the numeric increment path,
and VM dispatch consume the typed `register_b()` accessor. Their result writes
and liveness definitions use the shared `ResultLayout` destination view.
Formatting, `git diff --check`, and `cargo check -p quench-runtime-next -p
quench-test262` pass. Existing stage-10 `increment` and `operator` selections
pass 74/74 and 137/137, respectively, in batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; many field roles and generated decoding are still open.

`GetIndex` now declares `b`/`c` as tagged operands. Validation checks each
operand against register/local/constant/field bounds; numeric-dispatch indexed
loads additionally require the register/local subset supported by that path.
The generic VM resolves these operands through the shared operand resolver,
while numeric dispatch and its local-index compactor use typed operand views.
Liveness and rewrite legality also consume those views; rewrite analysis now
accounts for field operands' base registers. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. Existing stage-10
`property`, `array`, and `numeric` selections pass 348/348, 319/319, and 28/28
in batches capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new
tests were run. Task 52 remains partial; generated decoding and many other
opcode roles remain open.

`MakeArray` now declares `ArrayLength` as its immediate role. Residual
validation and VM allocation both consume `array_length()` rather than
decoding the raw immediate. Formatting and `cargo check -p
quench-runtime-next -p quench-test262` pass; the existing stage-10 `array`
path-filter selection passes 319/319 in four batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress.

`MakeConstArray` now declares its constant start index and element-count
operand, and both residual validation and VM execution consume those typed
views. This makes the constant-range invariant explicit in the opcode row.
Formatting and `cargo check -p quench-runtime-next -p quench-test262` pass;
the existing stage-10 `array` path-filter selection passes 319/319 in four
batches capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new
tests were run. Task 52 remains in progress.

The already-declared `ConstantIndex`, `ClosureFunctionIndex`, and `LocalSlot`
roles now also drive binding-time analysis, argument-slot discovery, and
residual local-load bounds checks. This removes additional raw immediate reads
from compile-time consumers. Formatting, `cargo check -p quench-runtime-next
-p quench-test262`, and diff checks pass. The existing stage-10 `arguments`
path-filter selection passes 133/133 in two batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`; no unit or new tests were run. Task 52 remains
in progress.

Function-name opcodes now distinguish `SetFunctionName`'s `AtomIndex` from
`SetFunctionNameKey`'s `FunctionNamePrefix`. The validator checks atom/prefix
bounds and required registers; VM dispatch uses the declared typed views.
Formatting and `cargo check -p quench-runtime-next -p quench-test262` pass.
Existing stage-10 `function-name` and `function` path-filter selections pass
1/1 and 1,196/1,196 respectively, in runner batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress.

`DefineArrayElement` now declares `ArrayIndex`; both ordinary and numeric VM
dispatch consume `array_index()`, and residual validation rejects the reserved
non-index sentinel. Formatting and `cargo check -p quench-runtime-next -p
quench-test262` pass. The existing stage-10 `array` path-filter selection
passes 319/319 in four batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress.

`Unary` now declares `UnaryOperator`; VM dispatch reads it through the typed
accessor, and residual validation rejects values outside OXC's unary operator
domain (excluding `delete`, which has its dedicated opcode). Formatting and
`cargo check -p quench-runtime-next -p quench-test262` pass. The existing
stage-10 `unary` selection passes 84/84 in one batch with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress.

The numeric interpreter now consumes declared `LocalSlot`, `BooleanFlag`, and
`JumpTarget` views instead of raw immediates, including numeric local fusion
and branch dispatch. Its `SetIndex` path now combines the instruction flag
with function strictness, matching ordinary dispatch. Formatting and `cargo
check -p quench-runtime-next -p quench-test262` pass. Existing stage-10
`increment` and `assignment` selections pass 74/74 and 1,527/1,527 in batches
capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were
run. Task 52 remains in progress.

The fused const-array/object-pair executor now reads its nested
`MakeConstArray` start/count and `MakeObject2` site through the declared typed
views instead of raw immediates/fields. Formatting and `cargo check -p
quench-runtime-next -p quench-test262` pass. Existing stage-10 `array` and
`object` selections pass 319/319 and 1,257/1,257 in runner batches capped at
100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task
52 remains in progress.

The iterator instruction family now declares source registers, explicit
result/no-result layouts, and cleanup-pair registers. Validation no longer
mistakes `IteratorClose` or `RequireObjectCoercible` for result-producing
opcodes and now checks `RequireIteratorResult`'s input. Liveness, rewrite
legality, and VM dispatch use the declared source/destination views. Formatting,
diff checks, and `cargo check -p quench-runtime-next -p quench-test262` pass.
The stage-10 lowercase `iterator` path filter passes 35/35. A fresh full stage-10
run passes 11,095/11,101 in 112 sequential batches capped at 100, with
`TEST262_TEST_TIMEOUT_MS=900000`; the six failures are non-callable
`instanceof`/`@@hasInstance` and generator-prototype identity across eval
realms, outside this iterator opcode slice. No unit or new tests were run.
Task 52 remains partial; other operand roles and generated decoding remain
open.

`SetIndex` and `DefineArrayElement` now declare consumed register fields and
`NoResult`; their validation, liveness, rewrite analysis, ordinary dispatch,
and numeric dispatch use typed register/result views. This prevents side-effect
instructions from being interpreted as producing a result destination.
Formatting, diff checks, and `cargo check -p quench-runtime-next -p
quench-test262` pass. Existing stage-10 `assignment`, `array`, and `property`
selections pass 1,527/1,527, 319/319, and 348/348, respectively, in batches
capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were
run. Task 52 remains in progress; the other opcode roles and generated
decoding are incomplete.

`SetField`, `DefineField`, `DefineComputedField`, and `SetThisField` now
declare their consumed registers, cache-site fields, and `NoResult` layouts.
Residual validation, liveness, rewrite analysis, and VM dispatch use the typed
views; cache operands are checked and read through `cache_site_index()`.
Formatting, diff checks, and `cargo check -p quench-runtime-next -p
quench-test262` pass. Existing stage-10 `property`, `assignment`, and `object`
selections pass 348/348, 1,527/1,527, and 1,257/1,257 in batches capped at 100
with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52
remains partial; other operand roles and generated decoders are incomplete.

`CopyDataProperties` now declares all three consumed fields as registers and
has a `NoResult` layout. Validation, liveness, rewrite analysis, and ordinary
VM dispatch use the typed views. Formatting, diff checks, and `cargo check -p
quench-runtime-next -p quench-test262` pass. Existing stage-10 `object` and
`spread` selections pass 1,257/1,257 and 233/233 in batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; other operand roles and generated decoders are incomplete.

`SuperConstArrayObject2` now declares its returnable result, superinstruction
index, and unused B/C fields. Residual validation checks the result and table
index and rejects nonzero unused fields. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. Existing stage-10
`object` passes 1,257/1,257 in 13 batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; other operand roles and generated decoders are incomplete.

`MakeObject2` now declares register B/C inputs alongside its returnable result
and existing object-site immediate. Residual validation, liveness, rewrite
analysis, and VM dispatch use these roles. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. The existing
stage-10 `object` selection passes 1,257/1,257 in 13 batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; other operand roles and generated decoders are incomplete.

Iterator setup, cleanup, close, coercion, and spread opcodes now declare their
unused fields and immediate payloads alongside the register/result roles.
Validation checks declared-unused fields through one schema-driven view and
requires the unused immediate to be zero. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. Existing stage-10
`iterator`, `spread`, and `destructuring` selections pass 35/35, 233/233, and
53/53 in batches capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit
or new tests were run. Task 52 remains partial; other operand roles and
generated decoders are incomplete.

`SuperCallCheck` and `IteratorCleanupPop` now declare no result, unused
register fields, and an unused immediate. Their residual validator rejects
nonzero fields and payloads through these roles. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. Existing stage-10
`iterator` and `super` selections pass 35/35 and 269/269 in batches capped at
100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task
52 remains partial; other operand roles and generated decoders are incomplete.

`ValidateClassHeritage` now declares register A as its input and `NoResult`.
Residual validation, liveness, rewrite analysis, and VM dispatch use the typed
register role. Formatting, diff checks, and `cargo check -p
quench-runtime-next -p quench-test262` pass. Existing stage-10 `heritage` and
`extends` selections pass 22/22 and 24/24 in single batches with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; other operand roles and generated decoders are incomplete.

`DeleteName` now declares its result and unused `b`/`c` fields; `StoreName`
declares a consumed register, unused `b`, and cache-site `c`, with `NoResult`.
The explicit unused-field check rejects nonzero reserved encodings. Validation,
liveness, rewrite analysis, and dispatch use the declared roles, including
`DeleteName`'s result definition. Formatting, diff checks, and `cargo check -p
quench-runtime-next -p quench-test262` pass. Existing stage-10 `delete` and
`assignment` selections pass 184/184 and 1,527/1,527 in batches capped at 100
with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52
remains partial; other operand roles and generated decoders are incomplete.

`LoadNameCall` now declares its callee result, receiver register, cache-site
field, and existing `AtomIndex` immediate. Residual validation, VM dispatch,
and liveness definitions use those roles. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. Existing stage-10
`function` passes 1,196/1,196. The `call` selection passes 817/818; its sole
failure remains `instanceof/symbol-hasinstance-not-callable.js`, the existing
non-callable `@@hasInstance` TypeError mismatch. Both selections used batches
capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were
run. Task 52 remains partial; other operand roles and generated decoders are
incomplete.

`Delete` now declares a result register, two input registers, and its existing
boolean strictness immediate. Validation, liveness, rewrite analysis, and VM
dispatch consume those views; rewrite analysis now treats the property and
base registers as live inputs. Formatting, diff checks, and `cargo check -p
quench-runtime-next -p quench-test262` pass. The existing stage-10 `delete`
selection passes 184/184 in two batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
in progress; remaining operand roles and generated decoders are incomplete.

`CheckPrivate` now declares its receiver register and `NoResult`; `PrivateIn`
declares its receiver register and boolean result. Their atom immediates remain
the shared `AtomIndex` role. Validation, liveness, rewrite analysis, and VM
dispatch consume the typed views. Formatting, diff checks, and `cargo check -p
quench-runtime-next -p quench-test262` pass. The existing stage-10 `private`
selection passes 2,056/2,056 in 21 batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; other operand roles and generated decoders are incomplete.

`ResolveName`, `LoadResolvedName`, and `StoreResolvedName` now declare their
result/input registers and boolean fields; `ResolveName` also declares its
cache-site field. The shared boolean-field decoder is used by residual
validation and VM dispatch, while liveness and rewrite analysis use the typed
register roles. Formatting, diff checks, and `cargo check -p
quench-runtime-next -p quench-test262` pass. Existing stage-10 `with`, `scope`,
and `assignment` selections pass 146/146, 76/76, and 1,527/1,527 in batches
capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were
run. Task 52 remains partial; other operand roles and generated decoders are
incomplete.

`SetFunctionName` and `SetFunctionNameKey` now declare their consumed
registers and `NoResult` layouts, while preserving their `AtomIndex` and
`FunctionNamePrefix` immediates. Validation, liveness, rewrite analysis, and VM
dispatch use the typed views. Formatting, diff checks, and `cargo check -p
quench-runtime-next -p quench-test262` pass. Existing stage-10 `function-name`
and `function` selections pass 1/1 and 1,196/1,196 in batches capped at 100
with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52
remains partial; other operand roles and generated decoders are incomplete.


`MarkPrivateName` now declares `b`/`c` as object/home registers and `NoResult`,
retaining its shared `AtomIndex` immediate. Validation, liveness, rewrite
analysis, and VM dispatch consume those roles. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. The existing
stage-10 `private` selection passes 2,056/2,056 in 21 batches capped at 100
with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52
remains partial; other operand roles and generated decoders are incomplete.

`ToPropertyKey` and `ToNumeric` now declare the shared result-register/source-
register shape. The residual validator checks both fields; liveness, rewrite
analysis, and VM dispatch consume the typed views. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. Existing stage-10
`property`, `numeric`, and `property-key` selections pass 348/348, 28/28, and
2/2 in batches capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or
new tests were run. Task 52 remains partial; other operand roles and generated
decoders are incomplete.

`InitializeThis` now declares register A as its consumed value and `NoResult`
as its result layout. Validation, liveness, rewrite analysis, and VM dispatch
read that role through `register_a()`. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. The existing
stage-10 `constructor` path selection passes 18/18 in one batch (under the
100-case cap) with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were
run. Task 52 remains partial; other operand roles and generated decoders are
incomplete.

`CacheTemplateObject` and `LoadCachedTemplateObject` now declare template-site
immediates and their register/result roles. Validation, liveness, rewrite
analysis, and VM dispatch use typed accessors; the cache operation explicitly
keeps its read/write register live. Formatting, diff checks, and
`cargo check -p quench-runtime-next -p quench-test262` pass. The existing
stage-10 `template` selection passes 101/101 in two batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; other operand roles and generated decoders are incomplete.

`LoadThis` and `LoadImportMeta` now declare a register result and no encoded
input fields. Residual validation and VM dispatch read the generated result
register view. Formatting, diff checks, and `cargo check -p quench-runtime-next
-p quench-test262` pass. Existing stage-10 `import.meta` and `constructor`
selections pass 24/24 and 18/18 in single batches (each under the 100-case
cap) with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task
52 remains partial; other operand roles and generated decoders are incomplete.

`MakeClosure`, `MakeArray`, and `MakeObject` now declare register results;
their existing function-index and array-length immediates remain in the opcode
rows. `MakeConstArray`'s already-declared result role is also used by validation
and dispatch. Formatting, diff checks, and `cargo check -p quench-runtime-next
-p quench-test262` pass. Existing stage-10 `array`, `object`, and `function`
selections pass 319/319, 1,257/1,257, and 1,196/1,196 in batches capped at 100
with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52
remains partial; other operand roles and generated decoders are incomplete.

`LoadConst`, `LoadLocal`, `LoadEnvLocal`, and `LoadCapture` now declare
register results alongside their existing constant/local/capture immediate
roles. Residual validation checks their result registers, and VM dispatch uses
the result accessor. Formatting, diff checks, and `cargo check -p
quench-runtime-next -p quench-test262` pass. Existing stage-10 `function`,
`closure`, and `scope` selections pass 1,196/1,196, 3/3, and 76/76 in batches
capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were
run. Task 52 remains partial; other operand roles and generated decoders are
incomplete.

`LoadName` and `LoadNameTypeof` now declare register results and cache-site
fields alongside their existing `AtomIndex` immediates. Residual validation
checks all three roles, and VM dispatch reads the declared cache and result
views. Formatting, diff checks, and `cargo check -p quench-runtime-next -p
quench-test262` pass. Existing stage-10 `scope` and `typeof` selections pass
76/76 and 103/103 in batches capped at 100 with
`TEST262_TEST_TIMEOUT_MS=30000`. No unit or new tests were run. Task 52 remains
partial; other operand roles and generated decoders are incomplete.

`MakeConstArray` now marks C unused, and validation enforces that declared
field while dispatch writes through the declared result register. Formatting,
diff checks, and `cargo check -p quench-runtime-next -p quench-test262` pass.
Existing stage-10 `array` and `object` selections pass 319/319 and 1,257/1,257
in batches capped at 100 with `TEST262_TEST_TIMEOUT_MS=30000`. No unit or new
tests were run. Task 52 remains partial; other operand roles and generated
decoders are incomplete.
