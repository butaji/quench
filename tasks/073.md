# 073: Architecture validation and performance after infrastructure

Prerequisite for implementation/tuning: [075](075.md) passes its evidence gate.
The user separately authorizes pre-gate diagnostic research, not bypassing that gate.
Researcher changes evidence/docs/tasks only; Codex owns VM implementation.
Latest user direction: do not message or steer Codex. Publish findings here and
in the [architecture gap map](../docs/architecture-gap-map.md).

## Current evidence

The current complete production baseline is `b00f1961b0`; its immutable binary is
`target/task073/bin/quench-node-b00f1961b0`, SHA-256
`88f06a8d46f029e2d825663ac960f19ec1234d5f09e80e3716be14f7158117a2`.
Its reserved/all-size/legacy smoke is complete at 844/844 correct scenarios
(`target/micros/task073-reserved-legacy-smoke-b00f1961b0.json`). Its three-run
V8_v7 median-index geomean is 73.83 (MAD 0.20), with medians Richards 47.4,
DeltaBlue 37.1, Crypto 16.8, RayTrace 97.6, EarleyBoyer 64.6, RegExp 61.0,
Splay 363 and NavierStokes 211
(`target/v8-task073-b00f1961b0-runs3.json`). Crypto consumes 53.5% of suite
wall time. This supersedes the preceding one-sample aggregate for variability.

`0dc7a205fe` replaces RegExp's public descriptor materialization for the
intrinsic `lastIndex` fast write with the existing internal descriptor fact.
Default runtime is 992/1 ignored and Node-oracle writable/non-writable controls
match. The complete 18-case RegExp micro matrix remains correct but has no
consistent timing/RSS improvement; V8 RegExp is 60.9 median (MAD 0.3) versus
61.0 (MAD 0) before. Keep this as a shared-mechanism simplification, not a
claimed performance correction. Artifacts are
`target/micros/task073-regexp-{before-b00f1961b0,after-0dc7a205fe}.json` and
`target/v8-task073-regexp-after-0dc7a205fe-runs3.json`.

`b3f5f99ae7` recognizes an observed intrinsic `Array` species only after the
ordinary constructor and `Symbol.species` reads, then uses the existing fresh
ordinary-array representation instead of treating it as an escaped custom
species result. Three alternating fixed-work runs of 64 slices over 1024 values
changed from 1.61--1.70 s and 87.4 MB peak RSS to 0.03 s and 43.2--43.5 MB,
with identical output (`target/task073/slice-intrinsic-paired-b3f5f99ae7.log`).
A custom species Proxy produces the same ordered getter, index-definition and
length effects as Node; focused identity/value/descriptor tests and the legacy
029 smoke pass. The current reserved/all-size/legacy rerun is 844/844 correct
(`target/micros/task073-reserved-legacy-smoke-b3f5f99ae7.json`). This does not
explain Crypto: its first after sample is 15.9
versus 16.0--16.9 before, so the suite's dominant array/property traffic remains
a separate open cause (`target/v8-task073-crypto-after-b3f5f99ae7-run1.json`).

`6918ba0b3d` replaces ordinary writable-own-property descriptor objects with a
name-first internal `PlainOwnProperty` fact. The first generic entry-projection
attempt regressed the controlled write lane by 8--9x because it decoded unrelated
property values; the retained key-first scan instead improves sequential
`objects/write` and `property-mutation/overwrite` ratios by roughly 16--21%
across all sizes. Runtime tests are 994/1 ignored. Crypto remains inside baseline
variation at 16.2, so this is credited only as a general property-write
improvement. Evidence:
`target/micros/task073-{objects-write-hot-proof,property-overwrite-key-first}-0579d938af-dirty.json`
and `target/v8-task073-crypto-after-6918ba0b3d-run1.json`.

`57e428e176` reuses the existing identity-preserving numeric backing store for a
current ordinary array's existing dense numeric slot. Representation-changing or
stale writes retain the structural replacement path. Runtime tests are 996/1
ignored. A `Reflect.set` named-index control improves consistently from
211--218 ms to 199--200 ms for 100,000 writes, while the dedicated indexed micro
is timing-neutral/mixed and Crypto is 16.1. The retained conclusion is narrower:
this removes an unnecessary representation transition on the affected ordinary
path, but does not explain Crypto. Evidence:
`target/task073/array-reflect-write-control.js`,
`target/micros/task073-arrays-write-{before,after}-57e428e176.json`, and
`target/v8-task073-crypto-after-57e428e176-run1.json`.

Rejected experiment `28de5c59bd` extended that own-data projection to present
dense array indices. On current Crypto it removed only 155,892 of 142,014,697
descriptor views (0.11%) and about 0.037% of owned-word reads; its 16.9 score is
inside baseline variation. `05fb840698` removes the unproductive specialization.
The paired diagnostic reports are
`target/instruction-category-ledger-crypto-{04ba281edc-valid,28de5c59bd}.json`.

The progressively refined Crypto trace now resolves the dominant write without
guessing from descriptor counts. Of 28,536,661 named writes, 28,186,835 (98.77%)
are Number-to-Number updates of an existing sparse own array property; only
57,369 create an absent physical hole. Objects, aliases, descriptors, deleted
indices and mapped arguments are negligible in this lane. `d69e6971ea` therefore
makes the canonical sparse property store interior-mutable, removes its unsafe
shared-to-mutable cast, and admits only current-array ordinary numeric own slots
before public descriptor materialization. Read-only descriptors, stale arrays,
type changes, holes and inherited setters retain complete ordinary behavior.
The default runtime is 1004 passed/1 ignored; 66 reserved/all-size array and
array-interaction cases and focused Node-oracle controls pass. Three matched
Crypto runs improve from 17.0 median (MAD 0.1, about 218 s/run) to 55.2 (MAD 0.1,
about 68 s/run), a 3.25x local score improvement. The complete three-run V8_v7
geomean is 91.72, up 24.2% from the 73.83 baseline; per-fixture medians are
Richards 58.2, DeltaBlue 45.3, Crypto 54.8, RayTrace 102, EarleyBoyer 65.6,
RegExp 62.2, Splay 393 and NavierStokes 212. Every fixture is valid. This remains
far below the goal and establishes neither an RSS improvement nor Bun parity.
Evidence:
`target/instruction-category-ledger-crypto-{3c7f93118a,220092a004}.json`,
`target/v8-task073-crypto-{before-sparse-220092a004,after-sparse-dirty}-runs3.json`,
`target/v8-task073-after-sparse-d69e6971ea-runs3.json` and
`target/micros/task073-sparse-array-*-smoke.json`.

The current post-sparse all-size matrix is 372/372 correct
(`target/micros/task073-full-measure-d69e6971ea.json`). Its median Quench/Bun
ratios remain 3,423x for RegExp, 2,370x for closures, 2,188x for strings, 955x
for calls and arrays, and 782x for control. A calls/inline trace showed the hot
counted loop retained a structured `Slow(Loop)` gateway with separately entered
test/body/update ranges. Extending the existing safe loop lowering predicate to
`InitializeLocal` and named property reads exposes the same ordinary loop as one
canonical CFG with a backedge. Two alternating large-inline measurements improve
from 1.470--1.490 ms/call to 1.206--1.216 ms/call (about 18--19%); all 36 reserved
call cases and 1005 runtime tests pass, and an effectful throwing `n` getter
matches Node exactly.

The first diagnostic build nevertheless retained the old shape because its
metadata-only `TraceSite` operations changed loop admission. Making those facts
transparent to the structural predicate restores matching CFG shapes. In the
same 32-call diagnostic the compact handler count changes from 1,492,099 to
1,762,371 while slow gateways fall from 2,306 to 2,273: explicit branches and
the backedge replace fragment orchestration, but dispatch remains per operation.
Trace instrumentation raises retired instructions 3.4% and slightly worsens its
timed window, so it cannot stand in for the uninstrumented 18--19% result. The
remaining ~323x production gap identifies region residency as the next mechanism;
the lowering change is not itself a stencil speedup. Evidence:
`target/micros/task073-calls-inline-diagnostic-{adbff3626a,trace-transparent}.json`.

`94586e4d01` applies the useful AsmJit `CodeHolder` boundary without importing
AsmJit: selected leaves and composed regions now become the same transactional
`VerifiedRegionImage`, including a checked callable-entry offset, before any
arena copy or publication. This removes a parallel leaf renderer, makes cache
reuse validate exact finalized bytes, and fixes the latent assumption that every
artifact entry begins at allocation offset zero. Actual-byte nonzero-entry,
layout and composition tests pass (12/12 and 8/8; generated composition 10/10),
and the full default runtime is 1000 passed/1 ignored. This is a correctness and
structural simplification, not a throughput claim.

`0e36f1432e` and `52178bbc90` finish the corresponding lifecycle reduction:
cache-aware/cache-free eviction share one owner transition, and typed F64,
Bool, I32, U32 and F64x3 entries share one render/publication/invocation/cache
transition. F64 fallthrough and fallback remain explicit data rather than being
erased by the common path. The first refactor attempt exposed this distinction
through three failing tests; after correction all three and the full 1000/1
runtime suite pass. No timing benefit is claimed.

The earlier complete all-size timing matrix used `be866df1f7`; its immutable binary is
`target/task073/bin/quench-node-be866df1f7`, SHA-256
`1d584403ebf8d86d7b99db3d043b801abeb163e1b66a730519020e6f17b46910`.
At `e8c0f674d0`, the all-size 372-scenario run produced correct verification
values everywhere, but `strings/unicode/large/17` exceeded the 30-second timing
budget. The preceding structured-resume repair made all 12 suspension scenarios
valid across all sizes. Reports are
`target/micros/task073-full-measure-e8c0f674d0.json` and
`target/micros/task073-suspension-measure-e8c0f674d0.json`.

`975fb5d04d` removes per-access UTF-16 materialization and selects one indexed
UTF-16 backing for long wide strings. All 18 string scenarios now complete with
matching results (`target/micros/task073-strings-all-975fb5d04d.json`). The large
Unicode scan changed from about 306 ms/call in verification to 10.5 ms/call in
matched timing, while fixed-work RSS remained about 57 MiB. This is a roughly
29x local improvement and linearizes the size sweep, but the current timing ratio
is still about 2263x Bun; it is not a general performance or memory win.

The earlier all-size matrix is complete: 372/372 scenarios are correct at
`be866df1f7`, including the formerly timing-out large Unicode case
(`target/micros/task073-full-measure-be866df1f7.json`). A one-sample V8_v7 pass
is also complete at the same binary (`target/v8-task073-be866df1f7-run1.json`):
Richards 54.5, DeltaBlue 46.5, Crypto 17.5, RayTrace 99.7, EarleyBoyer 66.2,
RegExp 62.7, Splay 374 and NavierStokes 217; geomean 78.82. Crypto accounts for
53.6% of measured suite wall time. The current three-run result above supersedes
this sample for variability; neither record establishes RSS, causality or a
production-policy decision. Lifecycle and a current full timing matrix remain
pending.

The first AArch64 policy ablation found that the former aggregate development
opt-in also exposed the optimizing driver, which corrupts a repeated ordinary
numeric loop and makes Crypto/RayTrace invalid. Isolating the physical families
shows leaves and composed regions are individually correct on the RayTrace and
NavierStokes checks, but regress this one-sample pair: default scores 99.7/217,
leaves 81.2/60.1 and composed 97.9/110. The policy now derives `leaves`,
`composed` and safe aggregate capability sets while rejecting the unverified
AArch64 optimizing view. The exact repeated-loop control and micros numeric
control pass with both aggregate opt-in spellings. This is a correctness repair
and negative profitability result, not authority to enable ARM by default.
Artifacts: `target/v8-task073-arm-*-isolated.json`,
`target/micros/task073-arm-safe-numeric-integer.json` and
`target/task073/optimizing-loop.trace`.

The older `06e71d3f4` profile survey remains under
`target/architecture-survey-current/`. It is historical evidence, not a current
binary substitute. Trace-off and trace-on diagnostics overlapped on a shared host,
so they establish neither an overhead ablation nor an idle-host qualification.

Historical measurements and rejected explanations are preserved in
`target/architecture-survey-current/task073-history.md`; original raw artifacts
remain in `target/stencil-review-1788646448/`. They identify older executables.
Do not treat historical bug narratives as current implementation instructions.

## Architecture and implementation constraints

Use Rust and Rust macros, build-time rustc/LLVM and validated extracted templates.
No C/Clang stencil sources or runtime optimizing backend. OXC owns syntax.
One canonical operation/effect/fact contract; generate mechanical consequences.
Facts are Proven, Guarded or Unknown. Complete ordinary semantics precede fast paths.
No fixture recognition, another semantic IR, separate runtime or benchmark quotas.
These recommendations use lisp-mindset: data first, derived static structure,
explicit effects, shared representations and shallow composition.

## Required measurement matrix

1. Run every [micro variant](../quench-bench/micros/README.md), legacy workload,
   all sizes, reserved inputs and composition controls. The all-size harness
   yields 672 rows but 472 distinct scenarios because legacy inputs repeat.
   Report semantic failures, timeouts and missing observations separately.
2. Run all eight [V8_v7 fixtures](../docs/v8_v7.md). Keep per-fixture results,
   raw failures and complete aggregates only when every fixture is valid.
3. Use matched source identities for separate production timing/fixed-work RSS
   and symbolized profiling. Gather counters and trace-disabled native samples;
   annotate startup, measured phase, thread population and sampling omissions.
   Physical footprint is not RSS; adaptive suite peaks are not equal-work memory.
4. Run fixed-work lifetime controls with retained/released graphs and phase markers.
   Distinguish active work, final reporting and teardown. A finite plateau is
   not low absolute RSS or proof of leak freedom; a rising slope is not its cause.
5. Compare current Node/Bun on equivalent inputs. Collect supported hardware
   counters with provenance; unavailable is not zero. No historical absolute
   score or one development run establishes portable superiority.
6. Keep default ARM policy separate from supported opt-in native execution.
   Zero native events with native disabled do not establish failed admission.
   After 075, verify normal-source native PCs, exits and correctness on every
   claimed target before enabling policies.

## Dependency-ordered scope

### A. Trustworthy evidence and shared static metadata

Apply [observability contracts](../docs/observability-architecture.md):
generated event IDs/units/populations; immutable code attribution outside hot
increments; bounded detail with explicit omissions and sampling policy.
Audit lifecycle constructor/clone/drop populations before deriving live gauges.
Count physical headers/backings, resize, Rc aliases and semantic creation distinctly.
Measure profiler perturbation; never rank diagnostic hashing as production cost.
Freeze function frame requirements from canonical operands and same-frame fragments,
not repeated call-time traversal. Preserve callee/frame boundaries and sentinel roles.

### B. Shared activations and heap identity

Follow [activation architecture](../docs/activation-architecture.md):
compare existing pooling with bounded slot storage and selective captures.
Share frame/exit/root/suspension contracts; keep specialized interiors unboxed.
Eliminate dead local state only from use/def, escape and effect facts. Preserve
TDZ, eval, with, mapped arguments, defaults, coercions, calls and throws.
Measure initialization work separately from frame construction and retention.

Follow [heap identity](../docs/heap-identity-architecture.md):
stable identity with mutable backing; distinct shape/kind/length/backing validity;
implicit default attributes and internal descriptor records rather than public
descriptor objects for internal queries. Share ordinary, IC and native mechanisms.
Historical slice/species controls implicate destination construction, not source
construction alone; Crypto stale-representative misses do not prove missing I32.
Validate stale aliases, species/accessors, sparse holes, mapped arguments and
callback/resize boundaries. Reclaim released latest representatives without
manual global reset while live aliases continue to resolve correctly.
Unify immutable layout metadata, not semantically different ID domains: cached
interned layouts and visible-name shape fingerprints must have explicit roles.
Derive dictionary flags/slot counts without repeated property-name scans; select
descriptor keys before decoding unrelated values. Test transitions, metadata
mutation, ordering and hash collisions; preserve reentrancy-safe borrow boundaries.

### C. Ownership, reclamation and code lifetime

Audit actual owning edges rather than recursively counting semantic reachability.
Cover arrays, objects, functions, shared cells/environments, host/native references,
active/suspended frames and escaping closures. Deduplicated traversal must not
erase owning-edge multiplicity. Derive root/edge diagnostics from one contract.
Measure code-store disposal, captures retaining unrelated values, replacement
storage, survival and pauses before selecting collector policy.
Fresh trace-off EarleyBoyer sampling contains cycle/environment-edge traversal;
older frame-dominated samples are not a current exclusion of collector costs.
Require phase-matched collection frequency, visited-edge and temporary-byte
evidence before choosing visitor changes, scheduling or collector migration.
Compare retaining RC with corrected cycle accounting against a scoped collector
migration; no blanket Rc removal, unsafe nursery arithmetic or threshold inflation.
Any moving/generational proposal includes every root/handle/weak-reference/barrier
and safepoint contract, byte costs and migration tests. Task060 remains separate.

### D. Bounded native regions and calls

Use [region composition](../docs/region-composition-architecture.md).
Existing CFG validation, liveness and region contracts are not absent.
Verify their consumers support useful bounded composition and loop residency.
Use effect-scoped facts, backedge-compatible representations, explicit live exits,
fixed-role transfers and planning/code budgets. Cost matches by guards, boxing,
moves, spills and bytes, not longest match or a fixed fusion/template quota.
Compare scalar, composed and fused forms on training and held-out patterns.
Measure the bounded selection pass by staged ablation: known-fact specialization;
folding/DCE/value numbering; fixed-role placement and strength reduction; fusion;
layout/peepholes. Record planning work, emitted bytes, guards, transfers, spills,
exits and native residency for each stage. A lower internal count without matched
throughput and memory evidence is not a performance win, and a peephole must not
be credited for work already removed by an offline Rust recipe.
Keep the AsmJit-inspired boundary narrow: one selected physical contract, one
transactional layout result, then lease-backed publication. Measure whether
reusing layout scratch or batching publication reduces cold render work before
adding either; do not introduce a runtime assembler/compiler or replace the
existing bounded slab allocator by analogy alone.
Treat cache fingerprints as lookup accelerators, never semantic authority: a
composed cache hit must still match the immutable finalized image exactly before
its entry is reused. Keep that check at publication/admission rather than adding
byte comparisons to the native execution hot path.
Measure baseline-plan construction separately from retained sparse admissions:
temporary per-family vectors, BTreeSet liveness and repeated passes may have cold
cost even when native constructors reject policy. Compare generated single-pass
admission, reusable static CFG facts and deferred native-only work. The slab is
lazy; do not count configured capacity as allocated executable pages.

Extend range proofs only where alias/effect and JS numeric semantics permit.
Do not reassociate floating arithmetic or infer signed I32 from unsigned shifts.
Existing unboxed f64 arrays precede any new element-kind proposal; measure copies,
transitions and access costs first, including -0, NaN, holes and inherited values.
Tiny-call fusion reuses callee/environment/dependency facts and exact this/argument
order. Bound expansion, recursion, guards and code bytes; reconstruct live state.
Pre-shaped allocation/scalar replacement requires initializer order, setter/proto,
escape, identity and exception proofs plus materialization at every required exit.
No benchmark-specific vector/list/node representation.

### E. Existing subsystems and remaining workload costs

Extend the existing RegExp backend/native/cache only when matching profiles justify
it. Historical cache hits and lastIndex descriptor stacks favor challenging wrapper
cost before replacing the matcher or enlarging caches. Preserve UTF-16, Unicode,
captures, backreferences, lookarounds, flags, custom exec, brands and lastIndex
coercion/nonwritable errors. Share internal descriptor queries with ordinary writes.
Reassess strings, typed memory, property ICs, calls and allocation against the full
fresh matrix; absence from the current priority list is not completed review.

## Acceptance for every architectural finding

Record evidence identity, confidence, competing explanations and counterexamples;
compare alternatives, then name the recommended shared mechanism and dependencies.
Define correctness controls and measurable throughput/RSS/code-size acceptance.
Validate before/after artifacts, reserved workloads and structural ablations.
Track cold/hot admission, region lengths, helpers/exits, ownership/boxing traffic,
rendering cost, live and retired code/cache bytes, guard stability and churn.
Profitability changes require amortized benefit, not arbitrary threshold tuning.

Preserve rejected hypotheses. Separate observed gaps from proposed improvements
and implemented-but-unverified capabilities. Benchmark failures inform coverage,
not automatic architectural diagnosis. Do not pursue individual bug fixes as the
research deliverable. Reconcile code, tasks and machine evidence before handoff;
report remaining unknowns without claiming universal optimality or production
readiness from benchmark results alone. Task state lives in [index.json](index.json).
