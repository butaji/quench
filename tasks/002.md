# 002: child_process: exec/execFile promisified + AbortController integration

- **Theme:** ipc_foundation
- **Executor:** codex
- **Depends on:** 001

## Context

From the same partial sweep: `test-child-process-exec-abortcontroller-promisified.js`,
`test-child-process-execFile-promisified-abortController.js`,
`test-child-process-execfile-maxbuf.js`, `test-child-process-execfile.js`,
`test-child-process-promisified.js`, `test-child-process-no-deprecation.js`
all fail or time out. These sit on top of the spawn/exit-code machinery from
001 but add: `util.promisify(child_process.exec)`, `AbortSignal` wired to
`child.kill()`, and `maxBuffer` enforcement (killing the child and rejecting
when stdout/stderr exceed the limit).

## Scope
- `child_process.exec`, `execFile`, their `util.promisify`-compatible custom
  promisify hook (Node attaches `util.promisify.custom` returning
  `{child, promise}`-shaped result with `.child`)
- `AbortSignal` support on `exec`/`execFile`/`spawn` options (`signal`)
- `maxBuffer` enforcement

## Deliverables
- Fixtures below passing

## Validation

1. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-child-process-exec-abortcontroller-promisified.js --timeout-secs 15`
2. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-child-process-execFile-promisified-abortController.js --timeout-secs 15`
3. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-child-process-execfile-maxbuf.js --timeout-secs 15`
4. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-child-process-execfile.js --timeout-secs 15`
5. `cargo run -q -p quench-node-test --bin run-parallel -- --one tests/node/test/parallel/test-child-process-promisified.js --timeout-secs 15`
6. `cargo test -p quench-node --lib` — no regressions

**Fail condition:** any of steps 1-5 not PASS, or a regression in step 6.

## Non-goals
- Shell-injection-safety hardening beyond what Node itself does — match
  Node's documented behavior, not a stricter policy

## Verified progress log

- Baseline before task-002 changes (2026-09-03): the five validation fixtures
  were `FAIL, PASS, PASS, FAIL, FAIL` respectively (the task-001 child-process
  sweep baseline was pass=92, fail=26, timeout=1, crash=3, total=122).
- After the Rust fixes (2026-09-03), all five validation fixtures PASS. The
  refreshed `--all --filter child-process --timeout-secs 15` sweep recorded
  pass=94, fail=24, timeout=1, crash=3, total=122 in `/tmp/task002-results.json`.
