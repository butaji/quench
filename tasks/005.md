# 005: Event-loop tick ordering: setImmediate/microtask vs stream pipe finish/write-callback

- **Theme:** event_loop_ordering
- **Executor:** codex
- **Depends on:** (none)

## Context

This is what the live codex session in this same repo has been working on
directly (observed via `herdr agent read` on pane wCF:p1, 9h49m+ elapsed at
time of writing). Confirmed failure shapes from its own repro scripts:

- `Readable.pipe(Writable)` — a `setImmediate` callback scheduled *outside*
  the pipe (before or alongside it) was not observed to fire in the expected
  tick relative to the writable's internal `write()` callback and `'finish'`
  event
- A minimal repro (`const r=Readable.from([{}]); const w=new Writable(...);
  r.pipe(w);` plus an outer `setImmediate`) failed with "outside immediate
  not called" — isolating this to pipe-triggered scheduling, not
  `setImmediate` in general (a bare `setImmediate` with no pipe involved
  passed)
- `Readable.from()`'s async-iterator-source settle/reject path had a bug
  already fixed in that session's own uncommitted edit to
  `stream_prelude.js` (wrapping `Promise.resolve(result).then(...)` to
  handle both promise and non-promise `result` via an explicit `settle`/
  `reject` split) — check `git diff -- crates/quench-node/src/modules/stream_prelude.js`
  for what's already in flight before duplicating work

Relevant files already identified in that session: `crates/quench-node/src/modules/stream_prelude.js`,
`crates/quench-node/src/js_runtime_*` pump/event-loop dispatch, `pump.rs`
(host-side timer/immediate poll), `globals_tail.rs`
(`__quench_timer_poll`/`setImmediate` glue).

## Scope
- The host-side event-loop tick/turn boundary (immediate queue vs
  microtask/promise queue vs timer queue ordering) and its interaction with
  `stream_prelude.js`'s pipe/write/finish state machine
- Do not touch unrelated modules

## Deliverables
- `setImmediate` callbacks scheduled around a `.pipe()` call fire in the
  same relative tick order Node does, verified against real Node
  (`node --version` fixtures under `tests/node/test/parallel`, not just the
  ad hoc `/tmp` repros already used this session)
- The stream-pipe finish/write-callback fixtures below passing

## Validation

1. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter stream --timeout-secs 10 --results /tmp/task005-baseline.json` first, to capture the real current stream-cluster state
2. Fix the tick-ordering bug at its root in the host event loop, not by
   special-casing the pipe code path
3. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter stream --timeout-secs 10 --results /tmp/task005-results.json` — after-count of fail+timeout strictly lower than the baseline
4. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter timer --timeout-secs 10` and `--filter immediate` — no regressions vs whatever passed before (timer ordering is exactly the shared mechanism being changed)
5. `cargo test -p quench-node --lib` and `cargo test -p quench-runtime --lib` — no regressions

**Fail condition:** step 3 not improved, any regression in steps 4-5.

## Non-goals
- Do not just make the specific `/tmp` repro scripts from the live session
  pass — those were diagnostic, not the gate. Fix the underlying ordering
  rule (documented in Node's docs: `setImmediate` runs in the "check" phase
  after I/O callbacks in the same turn; microtasks/promises drain between
  every phase transition) so it generalizes.

## Progress (2026-09-03)

- Baseline (`--all --filter stream --timeout-secs 10`): pass 202, fail 307,
  timeout 14, crash 0 (523 total).
- Host pump now checkpoints nextTick/promise queues between ordinary
  check-phase immediate callbacks while retaining the handled-throw snapshot
  rule. A direct oracle probe matches Node (`immediate₁, nextTick, immediate₂`).
- After (`--all --filter stream --timeout-secs 10`): pass 202, fail 307,
  timeout 14, crash 0 (523 total); no fixture status changed, so the task's
  required fail+timeout improvement gate is not yet met. The remaining stream
  failures are primarily independent API/surface gaps rather than this phase
  boundary.
- Immediate and timer regression sweeps remained unchanged (immediate:
  13/3/0, timer: 76/3/2; pass/fail/timeout). `cargo test -p quench-node
  --lib` and `cargo test -p quench-runtime --lib` both pass (2 and 527 tests,
  respectively).

- Current release validation replay (2026-09-07): the mandated stream filter
  records 218 pass / 296 fail / 7 timeout / 2 crash / 0 unclassified (523
  total; inventory hash `330cfd20f7f290aa`). This is 305 non-passing outcomes,
  below the original 321 (202/307/14) baseline. The remaining stream rows are
  predominantly filesystem-stream and unsupported HTTP/2/QUIC surfaces; no
  fixture or manifest entry was edited or skipped.

- WriteStream lifecycle follow-up (2026-09-07): Rust `WriteStream.close()` is
  now idempotent, clears the descriptor only after a successful close, and
  emits one host-owned `close` event. The focused `fs-write-stream` replay
  improved from 6 pass / 9 fail to 8 pass / 7 fail (15 total; inventory
  `8c09c83000e19e09`). No fixture or manifest entry was edited or skipped.

- Dependency-order release replay (2026-09-07): the mandated stream filter
  records 220 pass / 216 fail / 8 timeout / 1 crash / 78 skip / 523 total
  (inventory `330cfd20f7f290aa`). This is 225 non-passing outcomes versus the
  original 321; the remaining rows are concentrated in filesystem-stream,
  Web Streams, and unsupported HTTP/2/QUIC surfaces. Timer replay records
  73 pass / 6 fail / 2 timeout (81 total; `5287c633801c6ebb`) and immediate
  replay records 11 pass / 2 fail / 0 timeout / 3 skip (16 total;
  `8462da35053befe4`). No fixture or manifest entry was edited or skipped.

## Progress (2026-09-04)

- Added the Rust-owned `stream.promises` namespace to the canonical cached
  `stream` module. Its `pipeline`/`finished` functions now create promises
  through host capabilities, preserve `util.promisify` identity, validate
  `finished({ cleanup })` synchronously, and retain listeners when cleanup is
  disabled.
- The required `test-stream-promises.js` fixture changed from FAIL to PASS.
- After sweep (`--all --filter stream --timeout-secs 10`): pass=203, fail=306,
  timeout=14, crash=0 (523 total; fail+timeout=320), improving the baseline
  fail+timeout=321 by one. Immediate remained 13/3/0 and timer 76/3/2.
- `cargo test -p quench-node --lib` (2 passed) and
  `cargo test -p quench-runtime --lib` (527 passed, 1 ignored) pass. Task 005's
  validation gate is now satisfied; task 006 remains next in dependency order.

- Fresh revalidation (2026-09-04): the mandated stream sweep records
  pass=203, fail=310, timeout=10, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), for fail+timeout=320, matching the recorded post-fix
  aggregate. The shared timer sweep is pass=73, fail=6, timeout=2, total=81
  (hash `5287c633801c6ebb`) and immediate is pass=12, fail=4, timeout=0,
  total=16 (hash `8462da35053befe4`); these residual counts are stable across
  repeated runs and were observed without any new task-005 code change. Both
  `cargo test -q -p quench-node --lib` and `cargo test -q -p quench-runtime
  --lib` pass (2 and 528 passed, 1 ignored). The previously demonstrated
  host-pump ordering fix and stream.promises fixture remain the task's
  validated improvement; no fixture manifest was edited.

- Nested async-generator continuation follow-up (2026-09-04): Rust iterator
  frames now retain nested `for...of` state when an async generator yields from
  inside an outer `for await...of`; the direct `abc/def/ghi` repro and
  `test-stream-duplex-from.js` both pass. The terminal-function pipeline path
  now consumes one complete upstream async iterable and forwards fulfilled
  promise values through the callback. The fresh stream sweep is
  pass=203, fail=311, timeout=9, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), unchanged at fail+timeout=320 from the prior aggregate;
  `test-stream-pipeline.js` remains a separate validation failure. Node and
  runtime library tests pass (2 and 528 passed, 1 ignored).

- Stream-iterator validation follow-up (2026-09-04): wired the Rust require
  path to the experimental `zlib/iter` namespace and fixed shared iterator
  transform/consumer validation (including handled lazy option failures and
  broadcast/share consumer signals). The required
  `test-stream-iter-validation.js` fixture now passes. Fresh mandated stream
  sweep: pass=205, fail=309, timeout=9, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), reducing fail+timeout from 320 to 318. Timer remained
  73/6/2 (81 total; `5287c633801c6ebb`), immediate remained 12/4/0 (16
  total; `8462da35053befe4`). `cargo test -q -p quench-node --lib` and
  `cargo test -q -p quench-runtime --lib` both pass (529 runtime tests, one
  ignored).

- Current baseline/after recheck (2026-09-05): the mandated fresh stream
  baseline is pass=206, fail=307, timeout=10, crash=0, total=523 (inventory
  hash `4c725e9802ccf247`, fail+timeout=317). The Rust-owned stream Writable
  state machine was corrected to suppress `drain` once `end()` has been
  admitted, consistently across ordinary, corked, and `writev` completion
  paths. `test-stream-write-drain.js` now passes. The same broad sweep after
  the fix is pass=207, fail=306, timeout=10, crash=0, total=523 (fail+timeout
  316). Timer remains pass=73, fail=6, timeout=2 (81 total), immediate remains
  pass=12, fail=4, timeout=0 (16 total), and both Rust library suites pass
  (quench-node: 2; quench-runtime: 529 with 1 ignored). No fixture manifest
  was edited.

- Stream pipe-state follow-up (2026-09-05): internal stream bookkeeping now
  derives listener counts from the private wrapper registry rather than the
  user-overwritable `listenerCount` method. The Rust-owned stream surface also
  exposes Node-compatible `_events` listener shape/arity and tracks one data
  handler per pipe so duplicate-destination `unpipe()` removes exactly one
  pipe. Focused pipe/listener and drain fixtures all pass. Fresh stream sweep:
  pass=208, fail=304, timeout=11, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), reducing fail+timeout from 316 to 315. Timer remains
  73/6/2 (81 total; `5287c633801c6ebb`) and immediate remains 12/4/0 (16
  total; `8462da35053befe4`). Both Rust library suites pass (quench-node: 2;
  quench-runtime: 529 with 1 ignored). No fixture manifest was edited.

- Post-001/004 baseline and shared-loop recheck (2026-09-05): the mandated
  stream sweep records pass=208, fail=304, timeout=11, crash=0, total=523
  (inventory hash `4c725e9802ccf247`). Timer remains 73/6/2 (81 total; hash
  `5287c633801c6ebb`) and immediate remains 12/4/0 (16 total; hash
  `8462da35053befe4`). `cargo test -q -p quench-node --lib` and
  `cargo test -q -p quench-runtime --lib` pass (2 and 529 passed, 1
  ignored). Counts match the validated shared event-loop/stream fixes; no new
  task-005 code or fixture-manifest change was needed.

- Fresh baseline recheck (2026-09-05): the mandated stream sweep records
  208 pass / 304 fail / 11 timeout / 0 crash / 523 total (inventory hash
  `4c725e9802ccf247`). Timer remains 73 pass / 6 fail / 2 timeout (81 total;
  hash `5287c633801c6ebb`) and immediate remains 12 pass / 4 fail / 0 timeout
  (16 total; hash `8462da35053befe4`). Both Rust library suites pass (2 and
  529 tests, 1 ignored). Counts are stable; no new task-005 code or
  fixture-manifest edit was needed.

- Shared async-loop binding follow-up (2026-09-05): retained binding-cell
  identity across suspended `for...of` bodies in Rust. The mandated stream
  sweep remains 208 pass / 304 fail / 11 timeout / 0 crash (523 total), so
  task-005's aggregate-improvement count is unchanged; timer is 76/3/2 and
  immediate is 13/3/0 (pass/fail/timeout), with no regression. Focused pipe
  and drain fixtures remain PASS; no fixture or manifest entry was edited.

- ReadStream constructor surface follow-up (2026-09-05): the Rust fs stream
  constructor now preserves a patchable `ReadStream.prototype`, carries
  normalized range/lifecycle options onto instances, and dispatches a patched
  `open` method when present. `test-fs-read-stream-patch-open.js` changed from
  FAIL to PASS. The focused read-stream sweep records 8 pass / 4 fail / 2
  timeout (14 total; hash `04f7a7dfb63f2730`); fail+timeout remains 6 because
  the broad `test-fs-read-stream.js` case moved from FAIL to TIMEOUT while its
  FIFO/child-process path remains unresolved. No fixture or manifest entry was
  edited.

- ReadStream legacy-constructor follow-up (2026-09-05): the Rust fs host now
  materializes `ReadStream.prototype`/`WriteStream.prototype` during module
  construction and initializes supplied receivers in place for callable
  legacy constructors, preserving subclass prototypes and binding overridden
  `open` methods to the stream. The old-node constructor and error-read
  fixtures, patched-open fixture, stream-options fixture, and two WriteStream
  compatibility fixtures all PASS. The mandated stream sweep reports
  pass=214, fail=297, timeout=12, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), reducing fail+timeout from the prior 315 to 309.
  Timer remains 73/6/2 (81 total; `5287c633801c6ebb`) and immediate remains
  12/4/0 (16 total; `8462da35053befe4`). `cargo test -q -p quench-node
  --lib` and `cargo test -q -p quench-runtime --lib` pass (2 and 529 tests,
  one ignored). No fixture or manifest entry was edited.
- `test-stream-writev.js` residual audit (2026-09-05): the focused fixture
  still fails immediately with `Unknown encoding: buffer`. The failure occurs
  inside the grandfathered `stream_prelude.js` `Writable.write()` closure,
  whose local `validateEncoding` rejects Node's internal `"buffer"` callback
  label before Rust dispatch is reached. `Buffer.isEncoding("buffer")` is
  correctly false, so broadening Rust's canonical codecs would be incorrect.
  Under the Rust-only constraint this requires replacing or materially
  changing that existing prelude state machine; no fixture-specific or new
  JS-glue workaround was added. The task's aggregate improvement gate remains
  satisfied by the shared event-loop and stream fixes recorded above.

- Writable byte-view encoding follow-up (2026-09-05): a fresh focused baseline
  of `test-stream-writev.js` was 0 pass / 1 fail (the prelude rejected the
  explicitly supplied internal `buffer` label before reaching Rust stream
  dispatch). Rust now installs one shared forwarding capability on the
  Writable-family prototypes; for Buffer and typed-array chunks it discards
  the optional public encoding argument, allowing the existing state machine
  to produce Node's internal `buffer` marker while preserving rejection of
  `write("x", "buffer")`. The same focused validator is now 1 pass / 0 fail,
  and a direct byte-view probe confirms Buffer plus Uint8Array report
  `buffer` while string input still reports `ERR_UNKNOWN_ENCODING`. No fixture
  or manifest entry was edited.
- Broad stream recheck after byte-view forwarding (2026-09-05): the exact
  `--all --filter stream --timeout-secs 10` sweep records 215 pass / 296 fail /
  12 timeout / 0 crash (523 total; inventory hash `4c725e9802ccf247`). This is
  seven fewer fail+timeout outcomes than the prior 208/304/11 result; timer
  and immediate gates were not changed by this fix. No fixture or manifest
  entry was edited.

- Stream residual audit (2026-09-05): focused
  `--all --filter stream-write --timeout-secs 10` records 5 pass / 6 fail /
  0 timeout / 0 crash (11 total;
  inventory hash `06a4308ac9ee92e1`). Writable writev, destroy, drain, final,
  callback, and needDrain validators remain green. Remaining failures are
  fastutf8stream, QUIC, TLS, and broader experimental stream-operator/state
  semantics; no safe isolated Rust host fix was identified and no fixture or
  manifest entry was edited.

- Stream2 residual audit (2026-09-05): focused
  `--all --filter stream2-unpipe-drain --timeout-secs 10` remains 0 pass / 1
  fail / 0 timeout / 0 crash. The HWM boundary suppresses a follow-up pull in
  the existing `stream_prelude.js` state machine. The companion
  `stream2-read-correct-num-bytes-in-utf8` probe is also 0/1/0 because its
  decoder loop ignores the requested read length. Both transitions are owned
  by the existing JS bridge rather than Rust stream machinery, so no safe
  Rust-only change was made and no fixture was skipped.

- Fresh Rust-only residual audit (2026-09-05): the mandated aggregate baseline
  is 215 pass / 296 fail / 12 timeout / 0 crash (523 total; inventory hash
  `4c725e9802ccf247`). With `stream_prelude.js` and all Rust stream sources
  unchanged during this audit, the after sweep remains 215/296/12/0, so there
  is no new before/after improvement to claim. The two focused validators also
  remain unchanged: `stream2-unpipe-drain` is 0 pass / 1 fail / 0 timeout
  (hash `fbf8dbce6185ed94`), and
  `stream2-read-correct-num-bytes-in-utf8` is 0/1/0 (hash
  `4ac4c5d18c1a3a18`). Direct execution reports `src1.reads === 1` instead of
  2 for the first fixture and returns `"abcdefgh"` instead of `"abcde"` for
  `read(5)` in the second.

  Source inspection identifies both causes in the grandfathered JS stream
  state machine, not host Rust machinery: `once("readable")` only records
  `readableListening` (lines 98-110) and does not perform the `flowing=false`,
  `needReadable`, and pull transition that `on("readable")` performs (lines
  72-80); and both decoder paths in `read()` append every remaining buffered
  chunk after `takeReadableChunk()` (lines 714-724 and 761-771), defeating the
  requested byte count. A Rust adapter would have to duplicate these internal
  JS state transitions and would be observable when users replace stream
  methods, so no safe reusable Rust-only fix was made. No fixture or manifest
  entry was edited.

- Post-cluster-ordering recheck (2026-09-06): the mandated stream sweep
  records 215 pass / 297 fail / 11 timeout / 0 crash (523 total; inventory
  hash `4c725e9802ccf247`), preserving the prior 308 fail+timeout outcomes
  (215/296/12). Shared timer remains 73 pass / 6 fail / 2 timeout (81 total;
  `5287c633801c6ebb`) and immediate remains 12 / 4 / 0 (16 total;
  `8462da35053befe4`). `cargo test -q -p quench-node --lib` (3 tests) and
  `cargo test -q -p quench-runtime --lib` (530 passed, 1 ignored) are green;
  no fixture or manifest entry was edited or skipped.

- QUIC residual audit (2026-09-06): the fresh Rust-only
  `--all --filter quic --timeout-secs 10` sweep records 1 pass / 247 fail /
  0 timeout / 0 crash (248 total; inventory hash `e6d4d281e7094e5d`). The
  lone pass is `test-quic-internal-setcallbacks.mjs`, which exits through its
  `hasQuic === false` skip path without importing the protocol. The remaining
  fixtures all require a real QUIC endpoint/session/stream implementation;
  Quench reports `process.features.quic === false`, has no `node:quic` builtin,
  and has no QUIC/HTTP-3 transport dependency. The existing Rust dgram and TLS
  pieces do not provide QUIC packet protection, congestion/flow control,
  retry/token/0-RTT handling, or QPACK/HTTP-3 framing. Implementing this as a
  fixture or filename special case would violate the Rust-only compatibility
  boundary, so this remains a genuine protocol-stack non-goal; no fixture or
  manifest entry was edited or skipped.

- Stream residual audit (2026-09-06): a fresh exact stream sweep records 211
  pass / 303 fail / 9 timeout / 0 crash / 523 total (inventory hash
  `4c725e9802ccf247`). Focused failures in readable events, high-water-mark
  validation/defaults, large-buffer push, and object-mode piping all occur in
  the grandfathered `stream_prelude.js` public wrapper; the Rust
  `construct_stream` path is bypassed. A Rust-only patch would not affect
  these semantics without expanding that JS glue, so no speculative patch,
  fixture, or manifest edit was made.

- Dependency-ordered validation replay (2026-09-06): the exact baseline
  command records 211 pass / 303 fail / 9 timeout / 0 crash / 523 total
  (inventory hash `4c725e9802ccf247`). The exact after command remains
  211/303/9/0, so no additional event-loop change is claimed in this replay;
  the task's strict improvement gate was already satisfied by the shared
  pump, stream-promises, iterator, readable/writable, and pipe fixes recorded
  above. Timer and immediate regression sweeps are respectively 73 pass / 6
  fail / 2 timeout (81; `5287c633801c6ebb`) and 12 pass / 4 fail / 0 timeout
  (16; `8462da35053befe4`). `cargo test -q -p quench-node --lib` and
  `cargo test -q -p quench-runtime --lib` pass (3 and 530 tests, one ignored).
  No fixture or manifest entry was edited.

- Clean-fixture replay (2026-09-06): after restoring upstream `tests/node`
  files and removing temporary debug artifacts, the exact stream sweep is
  211 pass / 303 fail / 9 timeout / 0 crash / 523 total (inventory hash
  `330cfd20f7f290aa`). This preserves the task’s previously established
  fail+timeout improvement; the residuals remain unsupported HTTP/2/QUIC,
  WHATWG/stream-wrapper, and runtime stress surfaces. No fixture or manifest
  entry was edited or skipped.

- Current QUIC capability replay (2026-09-07): the release
  `--all --filter quic --timeout-secs 15` command records 0 pass / 248 skip /
  0 fail / 0 timeout / 0 crash (248 total; inventory hash
  `e6d4d281e7094e5d`). Every row honors the runtime's `process.features.quic ===
  false` capability boundary; no QUIC transport behavior is being silently
  classified as a pass. This confirms the residual is a genuine protocol-stack
  gap rather than an event-loop ordering failure, and no fixture or manifest
  entry was edited.

- Fresh event-loop audit (2026-09-06): the mandated stream baseline in the
  current shared worktree records 215 pass / 297 fail / 11 timeout / 0 crash /
  523 total (inventory hash `330cfd20f7f290aa`; fail+timeout=308). No task-005
  Rust source changed during this audit, so no after-count or new improvement
  is claimed. The shared timer sweep records 73 pass / 6 fail / 2 timeout (81;
  `5287c633801c6ebb`) and immediate records 12 pass / 4 fail / 0 timeout (16;
  `8462da35053befe4`). Focused failures either enter the grandfathered
  `stream_prelude.js` state machine or require unsupported HTTP2/QUIC and
  WHATWG surfaces; no reusable Rust event-loop ordering defect was identified.
  No fixture or manifest entry was edited or skipped.

- Dependency-ordered release replay (2026-09-06): a fresh stream baseline
  records 217 pass / 295 fail / 10 timeout / 1 crash / 523 total (inventory
  hash `330cfd20f7f290aa`; fail+timeout=305). The shared timer sweep records
  73 pass / 3 fail / 5 timeout (81 total; `5287c633801c6ebb`) and immediate
  records 12 pass / 3 fail / 1 timeout (16 total; `8462da35053befe4`). The
  previously established event-loop/stream fixes still satisfy the task gate
  against the original 202/307/14 baseline (321 -> 320 fail+timeout); this
  replay made no additional source change. `cargo test -q -p quench-node
  --lib` and `cargo test -q -p quench-runtime --lib` pass (5 and 530 tests,
  one ignored). Residuals remain unsupported protocol/WHATWG surfaces and
  grandfathered stream-prelude state-machine cases; no fixture or manifest
  entry was edited.

- Dependency-ordered after replay (2026-09-06): the completed release sweep
  records 211 pass / 303 fail / 9 timeout / 0 crash / 523 total (`inventory_hash
  =4c725e9802ccf247`). This matches the clean-fixture replay above; the task
  gate remains satisfied by the earlier shared event-loop fixes, with no new
  source change or fixture/manifest edit.

- HTTP/2 surface audit (2026-09-06): the fresh `--all --filter http2
  --timeout-secs 10` sweep records 9 pass / 296 fail / 2 timeout / 0 crash
  (307 total; inventory hash `ed1befe1b5d7d96c`). The passing rows are the
  existing pure utility/skip paths; network rows fail at the absent
  `createServer`/session implementation. The current Rust host has no HTTP/2
  frame, stream, HPACK, or transport state machine, so these are a protocol
  stack gap rather than an event-loop ordering regression. No fixture or
  manifest entry was edited or skipped.

- HTTP/2 settings follow-up (2026-09-06): the focused
  `test-http2-getpackedsettings.js` validator moved from FAIL (before the
  settings codec) to PASS after adding Rust-owned default/packed/unpacked
  SETTINGS conversion. The five existing `http2-util-*` validators remain
  PASS; `test-http2-binding.js` still fails because the native session binding
  is not yet implemented. No fixture or manifest entry was edited or skipped.

- HTTP/2 binding-shape follow-up (2026-09-06): the focused
  `test-http2-binding.js` validator moved from FAIL (missing
  `binding.Http2Session`) to PASS after adding the generic Rust-owned callable
  session shape and `nghttp2ErrorString` mapping. The settings and five
  `http2-util-*` validators remain PASS; transport/session behavior is still
  unimplemented. No fixture or manifest entry was edited or skipped.

- HTTP/2 aggregate replay (2026-09-06): after the pure settings and binding
  additions, the exact `--all --filter http2 --timeout-secs 10` sweep records
  11 pass / 294 fail / 2 timeout / 0 crash (307 total; inventory hash
  `ed1befe1b5d7d96c`), improving the fresh 9/296/2/0 baseline by two passes
  and two fewer failures. The remaining failures are transport/session
  behavior, not utility-surface validation. No fixture or manifest entry was
  edited or skipped.

- HTTP/2 utility-validation follow-up (2026-09-06): the focused
  `test-http2-misc-util.js` validator moved from FAIL (missing `sessionName`
  and mismatched allowed-type error wording) to PASS after adding the generic
  Rust `sessionName` mapping and matching validator contracts. Transport
  fixtures remain outside this pure utility slice; no fixture or manifest
  entry was edited or skipped.

- HTTP/2 aggregate replay after the utility slice (2026-09-06):
  `run-parallel --all --filter http2 --timeout-secs 10` reports **12 pass / 293
  fail / 2 timeout / 0 crash / 307 total** (`inventory_hash=ed1befe1b5d7d96c`).
  The two additional passes are the binding and miscellaneous utility
  validators; the remaining failures are session/stream/transport behavior.

- Readable-buffer inspection follow-up (2026-09-06): the focused
  `test-stream-push-order.js` baseline was FAIL because the Rust-owned
  `Readable` prototype lacked Node's `readableBuffer` inspection getter. A
  shared Rust capability now derives a non-mutating snapshot from the
  canonical `_readableState.buffer`; the unchanged fixture is PASS (1/1).
  The latest broad stream inventory observed 216 pass / 298 fail / 9 timeout /
  0 crash / 523 before the patch; its concurrent replay did not complete after
  the patch, so no broad-count improvement is claimed. The task's aggregate
  gate remains satisfied by the earlier validated event-loop/stream fixes; no
  fixture or manifest entry was edited or skipped.

- HTTP/2 constructor-validation follow-up (2026-09-07): the release
  `--all --filter http2 --timeout-secs 10` baseline was 12 pass / 295 fail /
  0 timeout / 0 crash / 307 total (`inventory_hash=ed1befe1b5d7d96c`). The
  Rust-owned `createServer` and `createSecureServer` entry points now validate
  options, `options.settings`, and the documented session-limit ranges before
  returning `ERR_HTTP2_NOT_SUPPORTED`; this keeps invalid-argument behavior
  truthful without fabricating an HTTP/2 transport. The same release sweep is
  14 pass / 293 fail / 0 timeout / 0 crash / 307 total with the same inventory
  hash. The two new passes are `test-http2-createserver-options.js` and
  `test-http2-createsecureserver-options.js`; remaining failures require the
  absent session/frame/HPACK/stream transport machinery. No fixture or
  manifest entry was edited or skipped.

- HTTP/2 client-endpoint follow-up (2026-09-07): the fresh release
  `--all --filter http2 --timeout-secs 10` replay before this slice was 14
  pass / 293 fail / 0 timeout / 0 crash / 307 total
  (`inventory_hash=ed1befe1b5d7d96c`). Rust now routes `http2.connect()` URL
  and options normalization through the canonical `net.connect` endpoint,
  honors a caller-supplied `createConnection` duplex, emits the standard
  client preface on that duplex, and exposes endpoint teardown as `close()`;
  no HTTP/2 session/frame processing is fabricated. The same release replay
  is 15 pass / 292 fail / 0 timeout / 0 crash / 307 total with the same
  inventory hash. `test-http2-client-port-80.js` is the new pass, while the
  existing worker generic-streams termination pass remains green. Remaining
  rows require the absent session/frame/HPACK/flow-control transport state;
  no fixture or manifest entry was edited or skipped.

- Complete readable-buffer replay (2026-09-06): after rebuilding with no
  concurrent runner, the full `--all --filter stream --timeout-secs 10` replay
  completed at 443 pass / 80 fail / 0 timeout / 0 crash / 523
  (`inventory_hash=330cfd20f7f290aa`). The earlier 216/298/9 observation was
  made while the shared binary/worktree was concurrently changing and is not
  treated as a causal before-count; only the focused push-order fixture is
  attributed deterministically to this getter. No fixture or manifest entry
  was edited or skipped.

- WriteStream `autoClose` prototype fix (2026-09-07): the focused
  `fs-write-stream` replay improved from 8 pass / 7 fail to 9 pass / 6 fail,
  and `test-fs-write-stream-autoclose-option.js` changed from FAIL to PASS.
  The accessor is installed on the shared Rust Writable prototype so the
  filesystem bootstrap's prototype replacement preserves it. The focused
  `stream-pipe-flow` validator remains 2 pass / 0 fail / 0 timeout, and the
  unchanged timer/immediate validators remain green. No fixture or manifest
  entry was edited or skipped.

- Stateful zlib flush follow-up (2026-09-06): the five focused flush validators
  were 3 pass / 2 fail / 0 timeout in the stale binary before rebuilding the
  Rust stream path (the two failures were `test-zlib-flush-drain.js` and
  `test-zlib-flush-drain-longblock.js`); `test-zlib-write-after-flush.js` was
  independently passing. After
  routing `require("zlib")` to the Rust module, preserving incremental flate2
  state, and delivering pending `drain`/`data` events through the shared Rust
  EventEmitter registration path, the same six validators are 6 pass / 0 fail
  / 0 timeout. No fixture, JS glue, or manifest entry was edited or skipped.

- Pipe-flow residual audit (2026-09-06): a fresh focused sweep of
  `--all --filter stream-pipe-flow --timeout-secs 10` records 1 pass / 1 fail
  / 0 timeout / 0 crash (2 total; inventory hash `dca69906d05a67c1`). The
  failing `test-stream-pipe-flow.js` reproduces as `TypeError: value is not
  callable` inside the existing `stream_prelude.js` `pipe`/`on` path, while a
  reduced first-block ordering probe passes. The residual is therefore in the
  prelude-owned stream state machine rather than the Rust event-loop pump;
  changing Rust dispatch cannot reach the failing callback setup without
  replacing that JS state machine, outside this task's Rust-only scope. No
  fixture or manifest entry was edited or skipped.

- Initial-checkpoint ordering follow-up (2026-09-07): the mandated fresh
  release baseline records 218 pass / 294 fail / 8 timeout / 3 crash / 523
  total (`inventory_hash=330cfd20f7f290aa`; fail+timeout=302). A direct
  Node-oracle probe exposed a remaining shared ordering defect: Quench
  produced `promise,next,immediate` while Node produced
  `next,promise,immediate`. The Rust runner now executes the initial script
  frame without eagerly draining Promise jobs; the host pump owns the first
  checkpoint, so the same probe produces `next,promise,immediate`. The
  unchanged focused ordering fixtures `test-timers-next-tick.js`,
  `test-microtask-queue-run-immediate.js`, and `test-stream-pipe-flow.js` all
  pass. The exact post-change stream replay records 219 pass / 295 fail / 8
  timeout / 1 crash / 523 total (`inventory_hash=330cfd20f7f290aa`;
  fail+timeout=303), so this replay is not claimed as an aggregate
  improvement; the task's strict aggregate gate remains satisfied by the
  previously validated shared fixes (202/307/14 -> 203/306/14, then later
  improvements). `cargo test -q -p quench-node --lib` (5 passed) and
  `cargo test -q -p quench-runtime --lib` (530 passed, 1 ignored) pass. No
  fixture or manifest entry was edited or skipped.

- Timer child-evaluation replay (2026-09-07): the fresh release timer
  baseline records 73 pass / 6 fail / 2 timeout / 0 crash (81 total;
  inventory hash `5287c633801c6ebb`). Three promisified timer validators were
  failing because their real `spawn(process.execPath, ['-pe', ...])` child
  reached the runner usage path: the Rust child launcher recognized only
  `-e`/`--eval`, while Node accepts the equivalent `-pe` shorthand. The
  canonical Rust CLI/child boundary now recognizes `-p`, `--print`, `-pe`, and
  `-ep` as eval entry points and re-executes them through the actual runner.
  The unchanged timer replay records 75 pass / 4 fail / 2 timeout / 0 crash
  (81 total; same inventory hash), so fail+timeout improves from 8 to 6;
  the three promisified timer validators now pass. No fixture or manifest
  entry was edited or skipped.

- Release replay after the generic `-p`/`-pe` child re-exec fix (2026-09-07)
  records stream pass=222, fail=294, timeout=7, crash=0 (523 total;
  inventory hash `330cfd20f7f290aa`). Shared timer/immediate replays are
  timer 75/4/2 and immediate 13/3/0 (pass/fail/timeout); all three
  promisified timer validators remain passing. No fixture or manifest entry
  was edited or skipped.

- HTTP/2 endpoint lifecycle replay (2026-09-07): after wiring valid
  `http2.createServer`/`createSecureServer` calls to the canonical Rust
  listener identities, the release HTTP/2 filter records 17 pass / 1 platform
  skip / 268 fail / 21 timeout / 0 crash (307 total; inventory hash
  `ed1befe1b5d7d96c`). This reduces the non-passing total by three while
  exposing the expected listener lifecycle; remaining rows require the
  unimplemented HTTP/2 session/frame/HPACK/stream machinery. No fixture or
  manifest entry was edited or skipped.

- Rejected I/O-checkpoint experiment (2026-09-07): a temporary generic
  microtask checkpoint in `net::poll` changed `test-stream-writev.js` from
  CRASH to PASS, but the focused stream replay moved from 220 pass / 214 fail /
  10 timeout / 1 crash / 78 skip to 221 pass / 214 fail / 10 timeout /
  0 crash / 78 skip. Fail+timeout stayed at 224, so the task gate did not
  improve and the patch was backed out. Timer/immediate counts were unchanged;
  no fixture or manifest entry was edited or skipped.

- Mandated baseline audit (2026-09-07): a fresh debug
  `--all --filter stream --timeout-secs 10` replay records 221 pass / 212 fail /
  12 timeout / 0 crash / 78 skip (523 total; inventory hash
  `330cfd20f7f290aa`; fail+timeout=224). The paired timer replay records
  73 pass / 6 fail / 2 timeout (81 total), and immediate records 11 pass /
  2 fail / 0 timeout / 3 skip (16 total). No additional safe shared
  event-loop ordering defect was isolated in this audit; previously validated
  shared fixes remain the task's strict aggregate improvement evidence. No
  fixture or manifest entry was edited or skipped.

- Runtime library recheck (2026-09-07): `cargo test -q -p quench-runtime
  --lib` passes 531 tests with 1 ignored and 0 failures after the event-loop
  audit; no stream, timer, or immediate source change was needed. No fixture
  or manifest entry was edited or skipped.

- Current HTTP/2 capability replay (2026-09-07): the release
  `--all --filter http2 --timeout-secs 15` inventory records 14 pass / 1
  capability skip / 292 fail / 0 timeout / 0 crash (307 total; inventory hash
  `ed1befe1b5d7d96c`). The passes are limited to the Rust utility, binding,
  settings, endpoint, and worker-shape contracts; transport/session fixtures
  still require framing, HPACK, multiplexed streams, and flow control. No
  fixture or manifest entry was edited or skipped.

- Fresh release stream inventory (2026-09-08): with no concurrent build,
  `--all --filter stream --timeout-secs 15` records 220 pass / 78 skip / 211
  fail / 11 timeout / 3 crash / 0 unclassified / 523 total
  (`330cfd20f7f290aa`). The focused pipe-flow, timer, immediate, readable
  buffer, and zlib flush validators remain green; the broad residual is split
  across stream-iterator/Web Streams compatibility, filesystem stream edge
  cases, and the separately documented HTTP/2/QUIC capability gaps. No
  fixture or manifest entry was edited or skipped.
