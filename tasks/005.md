# 005: Event-loop tick ordering: setImmediate/microtask vs stream pipe finish/write-callback

- **Theme:** event_loop_ordering
- **Executor:** codex
- **Depends on:** (none)

## Context

This is what the live codex session in this same repo has been working on
directly (observed via `herdr agent read` on pane wCF:p1, 9h49m+ elapsed at
time of writing). Confirmed failure shapes from its own repro scripts:

- `Readable.pipe(Writable)` — a `setImmediate` callback scheduled *outside*
  the pipe (before or alongside it) was not observed to fire in the expected
  tick relative to the writable's internal `write()` callback and `'finish'`
  event
- A minimal repro (`const r=Readable.from([{}]); const w=new Writable(...);
  r.pipe(w);` plus an outer `setImmediate`) failed with "outside immediate
  not called" — isolating this to pipe-triggered scheduling, not
  `setImmediate` in general (a bare `setImmediate` with no pipe involved
  passed)
- `Readable.from()`'s async-iterator-source settle/reject path had a bug
  already fixed in that session's own uncommitted edit to
  `stream_prelude.js` (wrapping `Promise.resolve(result).then(...)` to
  handle both promise and non-promise `result` via an explicit `settle`/
  `reject` split) — check `git diff -- crates/quench-node/src/modules/stream_prelude.js`
  for what's already in flight before duplicating work

Relevant files already identified in that session: `crates/quench-node/src/modules/stream_prelude.js`,
`crates/quench-node/src/js_runtime_*` pump/event-loop dispatch, `pump.rs`
(host-side timer/immediate poll), `globals_tail.rs`
(`__quench_timer_poll`/`setImmediate` glue).

## Scope
- The host-side event-loop tick/turn boundary (immediate queue vs
  microtask/promise queue vs timer queue ordering) and its interaction with
  `stream_prelude.js`'s pipe/write/finish state machine
- Do not touch unrelated modules

## Deliverables
- `setImmediate` callbacks scheduled around a `.pipe()` call fire in the
  same relative tick order Node does, verified against real Node
  (`node --version` fixtures under `tests/node/test/parallel`, not just the
  ad hoc `/tmp` repros already used this session)
- The stream-pipe finish/write-callback fixtures below passing

## Validation

1. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter stream --timeout-secs 10 --results /tmp/task005-baseline.json` first, to capture the real current stream-cluster state
2. Fix the tick-ordering bug at its root in the host event loop, not by
   special-casing the pipe code path
3. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter stream --timeout-secs 10 --results /tmp/task005-results.json` — after-count of fail+timeout strictly lower than the baseline
4. `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter timer --timeout-secs 10` and `--filter immediate` — no regressions vs whatever passed before (timer ordering is exactly the shared mechanism being changed)
5. `cargo test -p quench-node --lib` and `cargo test -p quench-runtime --lib` — no regressions

**Fail condition:** step 3 not improved, any regression in steps 4-5.

## Non-goals
- Do not just make the specific `/tmp` repro scripts from the live session
  pass — those were diagnostic, not the gate. Fix the underlying ordering
  rule (documented in Node's docs: `setImmediate` runs in the "check" phase
  after I/O callbacks in the same turn; microtasks/promises drain between
  every phase transition) so it generalizes.

## Progress (2026-09-03)

- Baseline (`--all --filter stream --timeout-secs 10`): pass 202, fail 307,
  timeout 14, crash 0 (523 total).
- Host pump now checkpoints nextTick/promise queues between ordinary
  check-phase immediate callbacks while retaining the handled-throw snapshot
  rule. A direct oracle probe matches Node (`immediate₁, nextTick, immediate₂`).
- After (`--all --filter stream --timeout-secs 10`): pass 202, fail 307,
  timeout 14, crash 0 (523 total); no fixture status changed, so the task's
  required fail+timeout improvement gate is not yet met. The remaining stream
  failures are primarily independent API/surface gaps rather than this phase
  boundary.
- Immediate and timer regression sweeps remained unchanged (immediate:
  13/3/0, timer: 76/3/2; pass/fail/timeout). `cargo test -p quench-node
  --lib` and `cargo test -p quench-runtime --lib` both pass (2 and 527 tests,
  respectively).

## Progress (2026-09-04)

- Added the Rust-owned `stream.promises` namespace to the canonical cached
  `stream` module. Its `pipeline`/`finished` functions now create promises
  through host capabilities, preserve `util.promisify` identity, validate
  `finished({ cleanup })` synchronously, and retain listeners when cleanup is
  disabled.
- The required `test-stream-promises.js` fixture changed from FAIL to PASS.
- After sweep (`--all --filter stream --timeout-secs 10`): pass=203, fail=306,
  timeout=14, crash=0 (523 total; fail+timeout=320), improving the baseline
  fail+timeout=321 by one. Immediate remained 13/3/0 and timer 76/3/2.
- `cargo test -p quench-node --lib` (2 passed) and
  `cargo test -p quench-runtime --lib` (527 passed, 1 ignored) pass. Task 005's
  validation gate is now satisfied; task 006 remains next in dependency order.

- Fresh revalidation (2026-09-04): the mandated stream sweep records
  pass=203, fail=310, timeout=10, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), for fail+timeout=320, matching the recorded post-fix
  aggregate. The shared timer sweep is pass=73, fail=6, timeout=2, total=81
  (hash `5287c633801c6ebb`) and immediate is pass=12, fail=4, timeout=0,
  total=16 (hash `8462da35053befe4`); these residual counts are stable across
  repeated runs and were observed without any new task-005 code change. Both
  `cargo test -q -p quench-node --lib` and `cargo test -q -p quench-runtime
  --lib` pass (2 and 528 passed, 1 ignored). The previously demonstrated
  host-pump ordering fix and stream.promises fixture remain the task's
  validated improvement; no fixture manifest was edited.

- Nested async-generator continuation follow-up (2026-09-04): Rust iterator
  frames now retain nested `for...of` state when an async generator yields from
  inside an outer `for await...of`; the direct `abc/def/ghi` repro and
  `test-stream-duplex-from.js` both pass. The terminal-function pipeline path
  now consumes one complete upstream async iterable and forwards fulfilled
  promise values through the callback. The fresh stream sweep is
  pass=203, fail=311, timeout=9, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), unchanged at fail+timeout=320 from the prior aggregate;
  `test-stream-pipeline.js` remains a separate validation failure. Node and
  runtime library tests pass (2 and 528 passed, 1 ignored).

- Stream-iterator validation follow-up (2026-09-04): wired the Rust require
  path to the experimental `zlib/iter` namespace and fixed shared iterator
  transform/consumer validation (including handled lazy option failures and
  broadcast/share consumer signals). The required
  `test-stream-iter-validation.js` fixture now passes. Fresh mandated stream
  sweep: pass=205, fail=309, timeout=9, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), reducing fail+timeout from 320 to 318. Timer remained
  73/6/2 (81 total; `5287c633801c6ebb`), immediate remained 12/4/0 (16
  total; `8462da35053befe4`). `cargo test -q -p quench-node --lib` and
  `cargo test -q -p quench-runtime --lib` both pass (529 runtime tests, one
  ignored).

- Current baseline/after recheck (2026-09-05): the mandated fresh stream
  baseline is pass=206, fail=307, timeout=10, crash=0, total=523 (inventory
  hash `4c725e9802ccf247`, fail+timeout=317). The Rust-owned stream Writable
  state machine was corrected to suppress `drain` once `end()` has been
  admitted, consistently across ordinary, corked, and `writev` completion
  paths. `test-stream-write-drain.js` now passes. The same broad sweep after
  the fix is pass=207, fail=306, timeout=10, crash=0, total=523 (fail+timeout
  316). Timer remains pass=73, fail=6, timeout=2 (81 total), immediate remains
  pass=12, fail=4, timeout=0 (16 total), and both Rust library suites pass
  (quench-node: 2; quench-runtime: 529 with 1 ignored). No fixture manifest
  was edited.

- Stream pipe-state follow-up (2026-09-05): internal stream bookkeeping now
  derives listener counts from the private wrapper registry rather than the
  user-overwritable `listenerCount` method. The Rust-owned stream surface also
  exposes Node-compatible `_events` listener shape/arity and tracks one data
  handler per pipe so duplicate-destination `unpipe()` removes exactly one
  pipe. Focused pipe/listener and drain fixtures all pass. Fresh stream sweep:
  pass=208, fail=304, timeout=11, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), reducing fail+timeout from 316 to 315. Timer remains
  73/6/2 (81 total; `5287c633801c6ebb`) and immediate remains 12/4/0 (16
  total; `8462da35053befe4`). Both Rust library suites pass (quench-node: 2;
  quench-runtime: 529 with 1 ignored). No fixture manifest was edited.

- Post-001/004 baseline and shared-loop recheck (2026-09-05): the mandated
  stream sweep records pass=208, fail=304, timeout=11, crash=0, total=523
  (inventory hash `4c725e9802ccf247`). Timer remains 73/6/2 (81 total; hash
  `5287c633801c6ebb`) and immediate remains 12/4/0 (16 total; hash
  `8462da35053befe4`). `cargo test -q -p quench-node --lib` and
  `cargo test -q -p quench-runtime --lib` pass (2 and 529 passed, 1
  ignored). Counts match the validated shared event-loop/stream fixes; no new
  task-005 code or fixture-manifest change was needed.

- Fresh baseline recheck (2026-09-05): the mandated stream sweep records
  208 pass / 304 fail / 11 timeout / 0 crash / 523 total (inventory hash
  `4c725e9802ccf247`). Timer remains 73 pass / 6 fail / 2 timeout (81 total;
  hash `5287c633801c6ebb`) and immediate remains 12 pass / 4 fail / 0 timeout
  (16 total; hash `8462da35053befe4`). Both Rust library suites pass (2 and
  529 tests, 1 ignored). Counts are stable; no new task-005 code or
  fixture-manifest edit was needed.

- Shared async-loop binding follow-up (2026-09-05): retained binding-cell
  identity across suspended `for...of` bodies in Rust. The mandated stream
  sweep remains 208 pass / 304 fail / 11 timeout / 0 crash (523 total), so
  task-005's aggregate-improvement count is unchanged; timer is 76/3/2 and
  immediate is 13/3/0 (pass/fail/timeout), with no regression. Focused pipe
  and drain fixtures remain PASS; no fixture or manifest entry was edited.

- ReadStream constructor surface follow-up (2026-09-05): the Rust fs stream
  constructor now preserves a patchable `ReadStream.prototype`, carries
  normalized range/lifecycle options onto instances, and dispatches a patched
  `open` method when present. `test-fs-read-stream-patch-open.js` changed from
  FAIL to PASS. The focused read-stream sweep records 8 pass / 4 fail / 2
  timeout (14 total; hash `04f7a7dfb63f2730`); fail+timeout remains 6 because
  the broad `test-fs-read-stream.js` case moved from FAIL to TIMEOUT while its
  FIFO/child-process path remains unresolved. No fixture or manifest entry was
  edited.

- ReadStream legacy-constructor follow-up (2026-09-05): the Rust fs host now
  materializes `ReadStream.prototype`/`WriteStream.prototype` during module
  construction and initializes supplied receivers in place for callable
  legacy constructors, preserving subclass prototypes and binding overridden
  `open` methods to the stream. The old-node constructor and error-read
  fixtures, patched-open fixture, stream-options fixture, and two WriteStream
  compatibility fixtures all PASS. The mandated stream sweep reports
  pass=214, fail=297, timeout=12, crash=0, total=523 (inventory hash
  `4c725e9802ccf247`), reducing fail+timeout from the prior 315 to 309.
  Timer remains 73/6/2 (81 total; `5287c633801c6ebb`) and immediate remains
  12/4/0 (16 total; `8462da35053befe4`). `cargo test -q -p quench-node
  --lib` and `cargo test -q -p quench-runtime --lib` pass (2 and 529 tests,
  one ignored). No fixture or manifest entry was edited.
- `test-stream-writev.js` residual audit (2026-09-05): the focused fixture
  still fails immediately with `Unknown encoding: buffer`. The failure occurs
  inside the grandfathered `stream_prelude.js` `Writable.write()` closure,
  whose local `validateEncoding` rejects Node's internal `"buffer"` callback
  label before Rust dispatch is reached. `Buffer.isEncoding("buffer")` is
  correctly false, so broadening Rust's canonical codecs would be incorrect.
  Under the Rust-only constraint this requires replacing or materially
  changing that existing prelude state machine; no fixture-specific or new
  JS-glue workaround was added. The task's aggregate improvement gate remains
  satisfied by the shared event-loop and stream fixes recorded above.

- Writable byte-view encoding follow-up (2026-09-05): a fresh focused baseline
  of `test-stream-writev.js` was 0 pass / 1 fail (the prelude rejected the
  explicitly supplied internal `buffer` label before reaching Rust stream
  dispatch). Rust now installs one shared forwarding capability on the
  Writable-family prototypes; for Buffer and typed-array chunks it discards
  the optional public encoding argument, allowing the existing state machine
  to produce Node's internal `buffer` marker while preserving rejection of
  `write("x", "buffer")`. The same focused validator is now 1 pass / 0 fail,
  and a direct byte-view probe confirms Buffer plus Uint8Array report
  `buffer` while string input still reports `ERR_UNKNOWN_ENCODING`. No fixture
  or manifest entry was edited.
- Broad stream recheck after byte-view forwarding (2026-09-05): the exact
  `--all --filter stream --timeout-secs 10` sweep records 215 pass / 296 fail /
  12 timeout / 0 crash (523 total; inventory hash `4c725e9802ccf247`). This is
  seven fewer fail+timeout outcomes than the prior 208/304/11 result; timer
  and immediate gates were not changed by this fix. No fixture or manifest
  entry was edited.

- Stream residual audit (2026-09-05): focused
  `--all --filter stream-write --timeout-secs 10` records 5 pass / 6 fail /
  0 timeout / 0 crash (11 total;
  inventory hash `06a4308ac9ee92e1`). Writable writev, destroy, drain, final,
  callback, and needDrain validators remain green. Remaining failures are
  fastutf8stream, QUIC, TLS, and broader experimental stream-operator/state
  semantics; no safe isolated Rust host fix was identified and no fixture or
  manifest entry was edited.

- Stream2 residual audit (2026-09-05): focused
  `--all --filter stream2-unpipe-drain --timeout-secs 10` remains 0 pass / 1
  fail / 0 timeout / 0 crash. The HWM boundary suppresses a follow-up pull in
  the existing `stream_prelude.js` state machine. The companion
  `stream2-read-correct-num-bytes-in-utf8` probe is also 0/1/0 because its
  decoder loop ignores the requested read length. Both transitions are owned
  by the existing JS bridge rather than Rust stream machinery, so no safe
  Rust-only change was made and no fixture was skipped.

- Fresh Rust-only residual audit (2026-09-05): the mandated aggregate baseline
  is 215 pass / 296 fail / 12 timeout / 0 crash (523 total; inventory hash
  `4c725e9802ccf247`). With `stream_prelude.js` and all Rust stream sources
  unchanged during this audit, the after sweep remains 215/296/12/0, so there
  is no new before/after improvement to claim. The two focused validators also
  remain unchanged: `stream2-unpipe-drain` is 0 pass / 1 fail / 0 timeout
  (hash `fbf8dbce6185ed94`), and
  `stream2-read-correct-num-bytes-in-utf8` is 0/1/0 (hash
  `4ac4c5d18c1a3a18`). Direct execution reports `src1.reads === 1` instead of
  2 for the first fixture and returns `"abcdefgh"` instead of `"abcde"` for
  `read(5)` in the second.

  Source inspection identifies both causes in the grandfathered JS stream
  state machine, not host Rust machinery: `once("readable")` only records
  `readableListening` (lines 98-110) and does not perform the `flowing=false`,
  `needReadable`, and pull transition that `on("readable")` performs (lines
  72-80); and both decoder paths in `read()` append every remaining buffered
  chunk after `takeReadableChunk()` (lines 714-724 and 761-771), defeating the
  requested byte count. A Rust adapter would have to duplicate these internal
  JS state transitions and would be observable when users replace stream
  methods, so no safe reusable Rust-only fix was made. No fixture or manifest
  entry was edited.
