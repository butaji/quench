# 075: Complete stencil infrastructure, core bodies and VM integration

Implement [the specification](../docs/stencil-jit-implementation-spec.md) at every
layer and interface. This replaces the historical stencil task chain; old
task closure does not certify the current gate.

- Preserve existing semantics and concurrent work; implement code, not another plan.
- Complete canonical generation, typed ABI routing, actual normal-source native
  loops, exact exits, rooting/safepoints and bounded executable ownership.
- Keep direct/runtime/host/oracle correctness testing active.
- Record the full completion matrix described in
  [the evidence checklist](../docs/architecture-evidence.md), including supported
  subsets, safe exclusions and actual executed tests.
- Do not run micros or unrelated benchmark-led tuning until every required row
  passes. A bridge, raw-byte test or one working loop is insufficient.
- Report genuine blockers rather than weakening requirements.

Evidence: the historical5b9e47088 matrix is not a current-suite certificate. See
`target/stencil-review-1788646448/native-evidence-audit.md`: newer default suite
770 pass/1 fail/1 ignored; loop-owner dispatch repair verified, code-store disposal
still fails. Generated build requires recheck; seeded fragments are not end-to-end proof.

## Implementation order and acceptance

1. **Close real-caller ownership and reentry.** Resolve the verified typed entry
   and acquire its retaining lease before native invocation. Release pool/cache
   `RefCell` borrows before calling it; wrapping a still-borrowed dispatch in a
   lease is insufficient. Exercise actual native -> Rust helper -> mutable pool
   access/retirement/admission -> return through the normal region caller. Verify
   roots, exact continuation, stale-entry rejection and no post-effect replay.
2. **Unify physical facts and generated artifacts.** One selected physical view
   owns the association between bytes/data, ABI, entry bounds, relocations,
   continuations and identity. Rust macros derive mechanical consequences from
   canonical declarations. Use rustc-built Rust leaves and minimal Rust-generated
   assembly for explicit continuation contracts; no C/Clang or runtime LLVM.
   Validate multi-hole identity/addends/ranges and fingerprint entry metadata; see
   `target/stencil-review-1788646448/extractor-contract-review.md`. Test real schemas.
3. **Compact admission state.** Replace parallel per-instruction native-plan
   arrays with a compact index and sparse, lazily allocated typed specializations.
   Preserve bounded legitimate alternatives and shared immutable descriptors.
   Remove superseded storage as consumers migrate; test cold/disabled footprint.
4. **Derive bounded regions from existing control flow.** Use canonical operand
   roles, predecessors, use/def, liveness and effect facts rather than adjacency
   and raw operand scans. Use bounded worklist analysis and a simple documented
   register/spill convention. Preserve aliases, loop-carried values, live-outs,
   exact PCs and interruptible backedges. No second semantic IR or optimizer VM.
   Implement the bounded stencil-selection pass specified in section 2: a
   disposable block-local value/use graph over canonical PCs, then known-fact
   specialization, constant propagation, effect-safe DCE/local value numbering,
   declared strength-reduced recipes, fixed register roles, costed fusion and
   hot/cold layout. A narrow verified post-relocation peephole may remove only
   declared identity operations and jump-to-next transfers. Tests must show both
   semantic equivalence and eliminated fragments/guards/moves; broken facts must
   select exact ordinary execution. No general SSA construction, legalization,
   global register allocation or general machine-code optimizer is in scope.
   **Immediate frame-sizing defect:** `Instruction::call_named(None)` encodes
   c=65535 as absent argument, but `register_count_for` treats every CallN c as a
   register. This saturates frame width to 65535 and `functions_arguments` fills
   about 512 KiB per invocation. Confirm with lowered-code width tests, then
   derive width from opcode/flags/metadata operand roles (including argument
   windows), never raw max(a,b,c) or a global sentinel exclusion. Distinguish
   nested callee-owned frames from structured fragments sharing the current
   frame; freeze the correct width once instead of recursively walking bodies
   at every call. Preserve all real slow-op temporaries and boundary register IDs.
   Test zero/one/many-argument calls, small caller with large nested callee,
   branches/loops/handlers, and initialized-word/release counts. Optimized Richards
   samples with tracing disabled show 1563 `RegisterFile::clear` leaf samples out
   of 2283 worker samples, plus 289 memset samples, in
   `target/stencil-review-1788646448/richards-trace-disabled.sample.txt`.
   This corroborates frame work, not an exact portable CPU-time percentage or
   proof that it explains the entire score gap. Fix sizing before changing release
   semantics, then verify time/RSS on the unchanged workloads.
   Counterfactual on production SHA256
   `04005dac8a01f5cd583247dffb91bae81d6af3f6bab9daa90d869bd07d70adc4`:
   `o={m:function(){return 1}}; function f(o){return o.m()}` called 5000 times
   takes 158/159/158 ms; changing only the call to `o.m(0)` takes 5/6/6 ms,
   alternating order across three process pairs, identical sum=5000. These
   coarse exploratory timings corroborate the sentinel path; they are not a
   general benchmark-win estimate or a substitute for frame-width unit tests.
5. **Make native execution remove meaningful work.** Compose supported hot
   interiors without per-operation/per-iteration Rust dispatch. Consume existing
   IC facts for native receiver/layout/dependency guards and property slot access,
   leaving complete miss/exotic semantics in shared helpers. Reduce avoidable
   boxing and retain/release through region liveness, with explicit root and
   ownership boundaries. Baseline and optimizing adapters share semantic facts.
   The guarded own-property get/set slice is the first known-fact specialization:
   machine code must consume the canonical layout/descriptor/slot proof and
   perform the access itself. Continue through actual CFG compositions rather
   than adding native calls that leave lookup, decoding and commit in Rust.
6. **Bound all physical resources.** Enforce per-owner and aggregate code, data,
   cache and metadata budgets across compiled owners. Retired-but-live allocations
   stay charged. Allow safe independent reclamation without reclaiming active
   code; verify actual slab packing, bounded churn and disposal accounting.
   Audit code ownership separately from JS object collection: the current
   `CodeStore.cold -> Op::MakeFunction*.body -> FunctionCode.store ->
   OnceLock<Rc<CodeStore>>` path appears to form a strong cycle. `OnceLock` being
   construction-only does not break ownership. First prove/refute with a weak
   store/drop test after all external program/function/plan handles are released;
   include nested branches/loops and repeated independent compilations. If
   confirmed, prefer non-owning same-store code IDs/ranges inside immutable code,
   with explicit strong owners on escaping function values, frames and native
   leases. A weak internal edge is an alternative, not permission to make escaping
   functions lose their code. Validate execution after the original program is
   dropped and eventual disposal after the last callable/lease dies. No global
   immortal registry or blanket Rc replacement. Rust documents that strong
   [Rc cycles are not reclaimed](https://doc.rust-lang.org/std/rc/index.html).
   Measure store/code/cache retained bytes before attributing RSS growth to GC.
   Independent existing-test execution (test-binary SHA256
   f1e3f057333f9d5a0bd203bd9595ad272c777166bd8a358f46904d4eb0cd846d):
   code_store_drops_after_nested_function_owner_is_released fails at the weak
   assertion; zero_argument_named_call_does_not_use_sentinel_as_register passes.
   Prefer store-internal fragment IDs resolved against an explicit borrowed
   owner, and external callable/frame/lease handles retaining that owner. A Weak
   upgrade cannot back a returned borrowed CodeView after its temporary Rc drops.
   Separate pending construction data from frozen fragment descriptors; retire
   source Op arrays only after all flattening/linking/fact consumers finish.
   Preserve JS function source text separately where observable. Include tier
   caches, retained source arrays, static blocks and instance-field initializers
   in ownership/drop tests; do not merely weaken the first discovered edge.

Use `lisp-mindset`: facts once, derived views, explicit state/effects and shallow
composition. Follow its size caps with cohesive modules, not giant macros or
arbitrary file chunks. Preserve passing slices; do not restart the architecture.

## Required tests

### Structured suspension: one authoritative continuation

Complete the in-progress async repair before certifying native exit equivalence.
Capture the actual code owner, resume PC, destination and enclosing structured
frames at the executed suspension; derive resume views from that state. Do not
recover execution history by selecting the first syntactic Await/Yield, defaulting
an unknown destination to register 0, or replacing an inner continuation with an
outer loop suffix. Nested loops and try/finally must retain every pending frame.
Audit `SuspendAt -> VmError::Suspended` conversions: discarding the point is safe
only after the authoritative machine state owns it, not while unwinding internal
fragments. Preserve await versus yield semantics even if storage is shared.
Run the conditional/multiple-await, nested-loop, finally and repeated-invocation
controls in 073, with side-effect logs proving exactly-once evaluation. Remove
temporary per-step environment checks and debug printing before acceptance.

Current repair review: `generator_loop::loop_frame_resume` discards `post_test`,
and `run_loop_after_yield` handles body/update/test but does not explicitly model
suspension in init, test or update. Represent the active loop phase as canonical
continuation data; preserve do/while ordering and per-iteration environments.
Test awaits in each legal loop position, nested depth >=3, sequential loops,
labelled break/continue, rejected awaits and return through finally, with exact
effect logs and captured let bindings. `SuspensionPoint::Nested` must materialize
every enclosing frame, not just one inner/outer pair. Audit repeated suspension
after resumption: install the new executed point before returning to the promise
driver, instead of reusing the previous frame's destination. Remove
`suspended_destination(...).unwrap_or(0)` and syntax-scanned recovery once the
authoritative path covers these cases; missing internal state is an internal
fault, not a guessed register or successful undefined completion. These are
now backed by `target/stencil-review-1788646448/continuation-recheck.md` (8/11 disagree).

Repair checkpoint is not closure: reserved/all-size smoke on production SHA256
64288e3be60344f2b8de2f9cdd24659567dff3ac8214441726f43dc4c9a5f6f8 completes
736/744 correctly (`micros-current-reserved.json` in the evidence directory).
All six iteration/close size/seed combinations return sum0 with closed1; expected
sums are528/32896/2098176 with closed1. Thus finally now executes in this artifact
but yielding/iteration is still wrong; do not retain only the older closed0
diagnosis. Both large await variants fail NotCallable. Plain nested two-by-two
await loops still return2 instead of4. A post-e8093c513 debug-artifact check
(SHA256974ba9a46cddeb15bec9612f62d9a8d10443a58e6dac425d468a292b0c7ce890,
unchanged before/after execution) reproduces nested2 and iterator `0 1`.
Recheck source/build identity when closing these rows; a commit is not a passing
regression matrix. Frame-sizing work does not discharge continuation correctness.

**Live binding loss across await (production64288e3b, repeatable).** In a
diagnostic copy of the large reserved await payload, split initial
`signature(await operation(state))` into an awaited temporary and then signature.
Before await, signature/encode/operation are all functions; afterward the awaited
value is the correct108825 but all three bindings are undefined. Node retains
all three and completes. Original unchanged payload also fails NotCallable.
Reproduction and exact original/diagnostic sources are in
`target/stencil-review-1788646448/await-binding-probe.mjs` and its JSON result.
Plain small IIFE controls, including32 calls x4096 awaits, pass: do not claim
every long await loop fails or that a specific GC threshold is already proven.
Instrument collection/clearing and suspended-frame ownership to locate the
mutation. `Environment::clear_cycle_edges` traverses shared slots, caller and
tdz_parent; function cycle_values flattens shared environment values into edges.
Audit actual Rc ownership versus flattened reachability and protection of pending
promise/continuation captures. Require forced-collection unit tests retaining
multiple closures that share cells, including a suspended consumer and a dead
sibling, plus released-all reclamation controls. Do not fix by disabling GC,
inflating its threshold or making every environment an immortal root. The
binding-loss result is verified; its collector attribution remains a hypothesis
until an exact clearing/ownership trace or unit reproduction proves it.

Review the new suspended-generator registry as a root bridge, not a second
liveness oracle. A successful Weak upgrade proves an Rc allocation still exists,
not that its only remaining owners are outside an unreachable JS cycle. Test
pending reachable await, fulfilled/rejected resume, abandoned generator/promise
cycles, dead sibling closures, root transfer during reentrant host callbacks and
all abrupt paths. No gap may exist between unregistering suspension and installing
the active frame's protection. Exercise collection with a pending exception,
receiver/arguments, handler values and unboxed native roots, not only registers.
Require eventual reclamation and registry return to baseline after the final
external owner disappears; never certify solely from the formerly failing async
payload passing. Derive membership from canonical continuation lifecycle with
bounded bookkeeping: avoid a full weak-list scan/upgrade on every await and
resume as concurrent pending counts grow. Preserve one semantic ownership graph;
if a conservative bridge cannot reclaim unreachable cycles, document and close
that limitation before claiming full lifetime readiness.

Regression-test quality check: the initial
`run::tests::suspended_shared_bindings_survive_collection_with_dead_siblings`
source (await0, then4096 dead object allocations, then signature(await operation))
also emits `function:function:42` on both known-broken production64288e3b and
preserved async-debug a82ffb10. Keep it as a control, not proof of this repair:
the allocations execute after the first resume and do not establish collection
while the relevant caller is suspended; it contains no dead sibling closure.
Require a failing-before/passing-after regression derived from the preserved
binding-loss case, and a deterministic unit test that asserts a collection
actually occurred with the consumer pending and its shared cells still needed.

Stronger causal control is now in `await-debugger-notes.md`: on preserved
async-debug a82ffb10, an LLDB observer breakpoint at Environment::clear_cycle_edges
still loses bindings; bypassing only that void function at its ARM64 entry
preserves all three and completes the correct108825 result. Collection otherwise
executes. Raw observer/bypass logs and commands are retained. Thus captured-
environment clearing is causally implicated; the exact missing root versus
incorrect owning-edge classification still needs a deterministic test/trace.
Do not ship this diagnostic bypass. Earlier LLDB thread-return attempts were
invalid, and the broader all-collection bypass was intentionally terminated as
redundant, not recorded as a pass or normal VM crash.

New copied debug artifact `quench-node-suspended-root-debug`, SHA256
25876abfa8b2cbdc226ed32702b20fe480c9a256a33fed36faacbc2ec076bf5c,
now independently passes both the unchanged large-await payload and the staged
binding-visibility probe: all three functions survive and result108825 completes.
This verifies repair of that reproduction, not the complete root/reclamation
contract. The same artifact still returns2 for nested awaits and `0 1` for the
generator-close sum control. Preserve these as separate open rows; debug results
are not production timing evidence.

Finite continuation controls in `continuation-controls.mjs/.json` further narrow
the open rows on debug25876abf (Node oracle, no benchmark detection):

- Nested await completes the first outer iteration, then logs before1:0 followed
  directly by end1. The second inner continuation is skipped, not just mis-added.
- Nested generator's first seven next() calls all yield0 with done=false; Node
  yields0,1,10,11 then completes. Array.from on it timed out10s, but finite next()
  observations—not that timeout—prove repeated values/progress failure.
- A for/yield loop inside try/finally yields0 once and then finalizes; Node
  yields0,1,2 before finally. A simple loop without try passes.
- `yield i; if(i===1) return99` inside the loop incorrectly continues to yield2;
  Node's next result is value99,done=true. In contrast, injected g.return(42)
  through finally passes. Test source Return and externally injected Return
  separately; one does not prove the other.

`run_loop_after_yield` currently groups Completion::Return with Normal. Audit
whether fragment result transport is conflated with actual JS return; represent
their provenance explicitly and propagate abrupt language completion through
the active handlers. Do not blindly reinterpret every Return or patch each
fixture. Derive resume dispatch from the innermost pending frame, preserve every
outer frame, and install each newly executed suspension before the next resume.

**The async-root bridge does not cover ordinary suspended generators.** New
`sync-generator-root-probe.mjs/.json` on debug25876abf: make() captures f()=>42
with two unused sibling closures, returns a generator yielding1 then returning
f(). Keep g reachable, call g.next(), allocate/mutate4096 ordinary objects, then
call g.next(). Quench yields1 then NotCallable; Node yields1 then returns42.
The64-allocation control passes;16384 fails again. No asynchronous functions,
benchmark fixture names or native execution are involved. Require this as a
failing-before/passing-after root regression too.

Extend the canonical ownership/trace contract to reachable generator and promise
containers and their frame/handler/capture edges, rather than multiplying ad-hoc
always-root registries. The new suspended_generator_root_transfers_and_reclaims_cycle
test manually unregisters before dropping the generator: it does not establish
reclamation of an abandoned pending cycle. Add an actual Generator -> environment
-> Generator (and Promise/continuation) cycle with all external handles dropped,
without manual release_suspended_generator or whole-runtime reset; prove weak
handles die and registry metadata returns to baseline. Pair with externally held
owners that must survive. Container identity, actual strong-edge multiplicity,
and active/suspended root transfer must agree before native safepoints inherit
the mechanism.

### Baseline correctness blockers found by the requested diagnostic review

**Line-oriented host output blocks lifecycle attribution.** Preserved production
04005dac emits all360 MICRO_PHASE markers plus MICRO_RESULT on one line in the
first lifetime run (`micros-linked-lifecycle.json`). It exits0 with a final result,
but the line-based parser cannot extract it or timestamp phases, so this is not
evidence the JS workload failed or never completed. Reproduce repeated
console.log/print calls through the same plain-file host entry; compare stdout
bytes against the applicable Node contract (console.log must append newline;
print is a Quench extension with an explicit contract). Normalize newline handling
once at the correct host/API boundary, preserving process.stdout.write's exact
bytes and avoiding double newlines. Test multiple writes, empty strings, embedded
newlines and both -e/file execution. Keep the frozen measurement parser strict;
rerun lifecycle after repair, retaining the invalid attempt and its raw RSS data.

**Array slice species ordering (confirmed on production 04005dac...).**
`arrays_slice::copy_slice` collects all source reads into a Vec before
`apply_slice` defines target properties. This reorders observable operations when
species returns a custom target. The final `builtins::set_property(length)` also
misses the proxy's set trap in the control below. Restore the ordinary
[Array.prototype.slice algorithm](https://tc39.es/ecma262/multipage/indexed-collections.html#sec-array.prototype.slice):
for each index, HasProperty/Get then CreateDataPropertyOrThrow before proceeding;
finally perform the throwing Set of length. Preserve early abrupt completion.

```js
var seen=0, a=[1,2];
Object.defineProperty(a,1,{get:function(){seen++;return 2}});
a.constructor={};
a.constructor[Symbol.species]=function(){
  return new Proxy({}, {defineProperty:function(){throw new Error("stop")}});
};
try { a.slice(); } catch(e) {}
console.log(seen); // Node: 0; preserved Quench: 1
```

With getters logging get0/get1 and species proxy logging define0/define1 and
setlength, Node emits get0,define0,get1,define1,setlength; Quench emits
get0,get1,define0,define1. Add both regressions, target nonwritable-length/false-set
tests, holes/inherited getters and species modifying source storage. A packed
copy may be selected only after observable argument/species work and valid
post-effect guards; no borrowed data pointer may survive user callbacks. Do not
apply the staged-Vec implementation to arbitrary targets as a fast-path fallback.

Before interpreting benchmark costs, reproduce and fix these ordinary execution
failures on the current revision. Snapshot `c1e8d875` plus the then-existing dirty
`cycle_collector.rs`, `environment.rs` and `execution_trace.rs` was built separately
with rustc 1.97.1 / LLVM 22.1.8, production and profiling+execution-trace profiles.
Evidence is under `target/stencil-review-1788646448/` (source hashes, complete
224-scenario small/legacy micros attempts, all-eight V8 attempts and isolation
probes). This is a pre-gate diagnostic baseline, not permission for benchmark-led
tuning. Recheck after concurrent changes; do not carry stale diagnoses forward.

- **Conditional returned function execution:**
  `function choose(b){if(b)return {f:function(){return 42}};return {f:function(){return 7}}}`
  followed by `var o=choose(true); console.log("before"); console.log(o.f()); console.log("after")`
  emits only `before` and exits 0 in the snapshot; Node/Bun must be the oracle.
  The micros clock-selection closure reduces to this failure. Investigate
  lowering/code identity, returned callable state and completion propagation;
  root cause is not yet established.
  Trace the concrete stale-source lead: `FunctionCode::rehome` links a clone in
  `append_tree` while retaining its original source; branch/loop flattening later
  consumes `source_ops()`, potentially copying unresolved nested function owners.
  Confirm the failing owner's store/range at execution. Link one canonical body
  before deriving either flat instructions or nested ranges; do not repair lookup
  failures with a second lazy code store. Include nested functions in both branch
  arms, loops, exception bodies and captures in the linking regression matrix.
  Do not broadly normalize internal
  `MissingReturn` into successful script completion or hide the missing tail.
  Cover both branches, nesting, direct/detached calls and unrelated returned
  functions with source-to-CLI regression tests, plus all applicable native modes.
  **Verified error-model flaw, independent of the remaining root-cause search:**
  `MissingReturn` also represents absent constants/metadata in `vm_runtime.rs`,
  unavailable function code in `vm_ops.rs`/`vm_execution.rs`, and malformed internal
  results in `run_simple_op.rs`; the host normalizer cannot distinguish these from
  legitimate script fallthrough. Separate internal execution faults from ordinary
  `Completion::Normal`/return/throw using a compact typed error, not another
  completion framework. Attach code identity/PC and a fixed reason on cold failure
  paths; do not allocate diagnostic strings per successful instruction. Validate
  linked nested bodies, ranges and required metadata at freeze/admission boundaries.
  Derive required metadata checks from opcode declarations. Test invalid metadata
  and unresolved code explicitly: they must never become exit-0 success, JS-catchable
  ordinary exceptions, or resumable guard misses. Native guard misses still use
  their existing exact-state fallback contract. This design distinction follows
  [ECMAScript completion semantics](https://tc39.es/ecma262/multipage/ecmascript-data-types-and-values.html#sec-completion-record-specification-type);
  it is a Quench engineering recommendation, not a prescribed spec representation.
- **Host output ownership:** Quench exposes callable global `print` but its output
  is discarded. `execute_print` uses `VmContext::emit_output`, while Node install
  attaches the sink to `NodeHost` and constructs a default context. Decide the
  Node-compatible global surface explicitly (local Node/Bun expose no `print`)
  and derive installed output capabilities from one host contract. Do not change
  frozen micros to hide this. Console-only isolation still fails on the closure
  issue above, so print is not the sole cause of missing benchmark results.
- **CLI event-loop completion:** a file scheduling `setTimeout(...,0)` and a
  resolved-Promise callback prints sync/microtask but drops the timer. Review
  `main::run_file -> eval_script` versus the existing host pump path; preserve
  entry-mode, argv, module, exception and exit semantics rather than substituting
  one execution mode for another blindly. Test real executable completion/order.

Every micros candidate attempt lacked a completed result; all eight V8 control
fixtures lacked a score although Node completed them. Profiling snapshots from
incomplete execution cannot explain hot workload performance. Fix shared
correctness mechanisms, then rerun unchanged suites before accepting speed/RSS
claims. Do not infer 224 independent defects from a common execution failure.

### Fact and pattern contracts

- Reuse existing facts/quickening/canonical lowered operations. A fact store is a
  shared interface to authoritative facts, not a new heap-wide mutable database
  or second semantic graph. Distinguish Proven, Guarded and Unknown from observed
  probabilities; field type and callee observations are not proofs.
- For supported loop admission, use a bounded affine range vocabulary: exact
  initial value, constant step, comparison limit and checked offset arithmetic.
  Eliminate a bounds check only when all accesses follow from the entry guard and
  induction proof and array storage/length facts remain valid. Unknown, overflow,
  mutation or coercion falls back; no general theorem prover or SSA optimizer.
- Declare each supported rewrite's semantic domain, required facts, effect kills
  and live-state obligations. Generic Add may coerce/call/throw; I32 inputs do not
  guarantee an I32 result. `0 + x -> x` is not valid generic JS, nor valid for all
  Numbers because of negative zero. Repeated property reads may be reused only
  for proven ordinary data access with no invalidating writes, aliases or calls.
- Guards imply only facts encoded by the actual shape/layout/dependency contract.
  Establish object identity/tag before dereferencing; shape does not automatically
  prove field representation, prototype-chain identity or immutable callee.
- Pattern selection is bounded and deterministic but not blindly longest-first:
  account for guards, spills, exits and emitted bytes. Preserve scalar/generic
  fallback and cap variant enumeration. No benchmark names or fixed benchmark
  object schemas in production declarations. Keep bounded local placement, not
  runtime instruction selection or a general register allocator.
- Dependency invalidation reuses existing identities and entry/lifetime machinery;
  test dependency mutation, stale-version/ABA handling, replacement and exact live
  state recovery. Every exit maps required values to slots/registers/constants
  without replaying effects. Not every guard belongs at entry: revalidate after
  effects that can invalidate it.

### Executed coverage

- Executable Cargo unit coverage for extractor, relocations, selection, ABI,
  publication, ownership and failure paths; build-script tests alone do not count.
- Every implemented core stencil: results, guard misses, numeric/property/binding
  edge cases and independent canonical ordinary-execution expectations.
- Ordinary source -> admission -> identified generated artifact -> native
  execution -> exact exit, including composition, helpers and reentry.
- Deterministic efficiency: stable hits reuse code without redundant rendering,
  patching or executable allocation; supported interiors avoid Rust dispatch;
  loop initialization occurs once; cold state is sparse; churn/retries and memory
  obey policy bounds; incompatible specialization facts never alias in caches.
- Current-revision runtime/host/trace and generated/non-generated regressions.
  Record actual nonzero counts and distinguish native execution from simulated
  callbacks or cross-compilation. No timing/RSS/comparator thresholds in unit tests.

## Research guidance and finite gate

### JS-pattern and compiler-contract coverage

Pair each stable fast-path case with a fact-breaking counterpart:

- Loop-carried arithmetic and ordered reductions: cancellation, signed zero,
  NaNs/infinities, nonzero initial state and zero/one/many iterations.
- Dense indexed updates: overlapping aliases, bounds, holes and inherited access.
- Repeated own/prototype property access: receiver, descriptor and chain mutation.
- Numeric/tagged alternation and mono/poly/mega transitions: bounded specialization
  with correct generic fallback, not repeated rendering under churn.
- Nullish/truthiness branches: distinguish null/undefined from false/zero/empty
  string; preserve evaluation order and short-circuit effects.
- Locals/captures across allocation, calls and exceptions: exact live state,
  ownership, roots and continuation. Existing B boundaries remain acceptable.

Trace compiler assumptions to Proven facts or dominating, still-valid guards.
Use checked Number arithmetic and canonical ToInt32 plus wrapping/masked bitwise
operations. Never manufacture alias guarantees with overlapping mutable Rust
references or retain invalidatable pointers across helper calls. Guard bounds,
alignment, lifetime and CPU features before execution. Fingerprint actual source,
ABI/layout and effective rustc/LLVM configuration; inspect representative IR and
object output without making exact instruction selection a portable test quota.

SIMD/fused recipes are optional physical choices. Preserve JS ordering and results;
do not enable fast-math to make floating reductions vectorize. LLVM's
[vectorization contracts](https://llvm.org/docs/Vectorizers.html) and Rust's
[assembly boundary](https://doc.rust-lang.org/reference/inline-assembly.html)
guide validity, not a promise that all compiler features improve this VM.

### Design references and completion

Apply mechanisms, not entire foreign VM architectures:

- [Deegen](https://arxiv.org/abs/2411.11469): generated contracts, quickening/ICs,
  register retention, check removal and hot/cold separation.
- [Sparkplug](https://v8.dev/blog/sparkplug): cheap compilation from existing
  bytecode, compatible frames and shared semantic helpers. Helpers are legitimate;
  dispatch-free requirements concern supported hot interiors, not all JavaScript.
- [JavaScriptCore](https://webkit.org/blog/10308/speculation-in-javascriptcore/):
  guarded property specialization and precise dependency invalidation. Reuse
  Quench's identities and mechanisms; do not create a parallel IC universe.

LLVM optimizes template bodies before extraction, not across runtime-patched
fragments. Preserve JS arithmetic/order, Rust alias/lifetime validity and guarded
CPU features. No speculative fast-math or unproven compiler assumptions.

Pass every mandatory infrastructure contract, section-3 N/G native family and B
boundary in the specification, with normal-runtime wiring and the tests above.
Optional exotic native bodies, SIMD or additional optimizing tiers are not new
prerequisites. Performance benefits remain hypotheses until measured in 073.

## Completion coherence review

Before closing this task, re-read `lisp-mindset` and review all retained `tasks/*`
against the actual codebase, not only recent stencil diffs. Record a compact
coverage map of reviewed subsystems and any unreviewed areas; do not imply a full
audit from sampling a few files. Trace syntax/lowering, canonical operations,
values/heap/ownership, objects/ICs, calls/frames, interpreter/native adapters,
host boundaries, generation, executable lifetime and tests end to end.

Check one fact/representation, derived state, shared semantics, explicit effects,
bounded physical specialization and cohesive module/function sizes. Identify
duplicate state, stale tasks, unreachable native paths, inconsistent assumptions
and claims without code/test evidence. Reconcile active task content/dependencies
with implementation; preserve deferred scope and historical evidence provenance.

Compare design decisions with primary Deegen/VM research and rustc/LLVM contracts:
explain which mechanisms fit Quench and which do not. Inspect representative hot
Rust-template IR and native code for dispatch, spills, redundant checks, branches,
memory access and feature/ABI assumptions, using realistic JS pattern families
and their fact-breaking counterparts. No unconditional O(1) claim for arbitrary JS.

Fix in-scope correctness, safety and mandatory contract discrepancies before
closing. Rank optional performance ideas for 073 with expected benefit, memory/
complexity costs and a falsifiable measurement; do not turn this review into an
unbounded rewrite or a claim of globally optimal performance. Store only the
current concise review in the existing evidence summary, with revision and tests.

After completion, automatically proceed to [073](073.md). Task state lives only in
[index.json](index.json); this file defines acceptance, not a progress diary.
