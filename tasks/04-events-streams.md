# Stage 04 — Events and streams

Finish `events`, Node readable/writable/duplex/transform state machines, async iteration, backpressure, pipeline/finished, web-stream adapters, consumers, and stream promises. Model lifecycle facts once and share them across HTTP, fs, and child-process adapters.

Run upstream stream/events fixtures and focused stages 371, 372, 550, 2253–2600. Acceptance: ordering, backpressure, abort, destroy, premature close, validation, and error propagation match Node under sync and async sources.
