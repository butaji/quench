# 23: Port Wasm and close the specification suite at 100 percent

## Outcome

Execute every directive in the pinned WebAssembly testsuite through the next shared runtime.

## Current gap

The Wasm frontend and interpreter use legacy runtime values, memories, exceptions, host calls, and lifecycle assumptions.

## Dependencies

See this task's `depends_on` in [index.json](index.json).

## Scope and implementation sequence

Execution lowering is task 37 (Wasm operators to the shared residual
bytecode, consuming validator-derived types per task 41). Do not port the
legacy `hir`/`wasm/emit` path onto the next engine: it is ~4k lines that task
37 would immediately replace. Keep `quench-wasm` ownership of binary/text
decoding and validation, then adapt instantiation, values/references, functions, tables, memories, globals, exceptions, SIMD, threads/atomics, GC/reference proposals, imports/exports, and host effects to new runtime handles and roots.

## Verification

Discover all pinned `.wast` files including proposals, execute every directive, and require correct parse/validation/runtime/trap classification. Add GC, shared-memory, re-entry, growth, aliasing, and JS/Wasm boundary controls.

## Definition of done

- Every discovered directive passes; there is no Quench skip list.
- Wasm and JavaScript share values/roots through one documented boundary, not duplicate heaps.
- Repeated runs have identical inventory and classification.

## Performance evidence

Report suite time, memory high-water, and representative execution overhead; no speed gate applies.
