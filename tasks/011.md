# 011: domain: error propagation edge cases

- **Theme:** surface_gaps
- **Executor:** codex
- **Depends on:** 006

## Context

Filename-prefix frequency: `domain-no` (10 hits), `domain` (2), plus
`domexception` (2, likely unrelated — DOMException, not the `domain`
module; verify before lumping together). `domain.rs` exists. The `domain`
module is deprecated in Node but still tested; it wraps async_hooks-style
resource tracking to route errors to a domain's `'error'` handler, which is
why this depends on 006 landing first (shared resource-lifecycle
machinery).

## Scope
- `crates/quench-node/src/modules/domain.rs`
- Do not touch `DOMException` (part of the web-platform error types,
  unrelated module) unless a specific fixture proves otherwise

## Deliverables
- `cargo run -q -p quench-node-test --bin run-parallel -- --all --filter domain --timeout-secs 10 --results /tmp/task011-baseline.json` first — separate real `domain`-module fixtures from `domexception`/other false-positive filename matches before scoping work
- Fix what the real domain-module subset shows

## Validation

1. Baseline from Deliverables, recorded in this file with the domain-module-only fixture list
2. Re-run the same filtered subset — after-count of fail+timeout strictly lower than baseline
3. `cargo test -p quench-node --lib` — no regressions

**Fail condition:** step 2 not improved, or a regression in step 3.

## Non-goals
- `DOMException` fixes (separate module, separate task if it turns out to
  need one)

## Factual validation log

- Broad baseline `--all --filter domain --timeout-secs 10`: 42 pass, 15 fail,
  3 timeout, 0 crash (60 total; inventory hash `b8ee6c63429f1717`). The
  domain-module subset is the `test-domain-*` entries; cluster/crypto/url
  filenames containing `domain` were retained in the broad evidence but are
  not domain-module tests.
- The existing domain implementation passes the ordinary enter/exit, nested,
  timer, fs, crypto, and event-emitter fixtures. Residual failures are the
  HTTP-server integration, promise-domain propagation, multi-domain timeout,
  and no-error-handler abort matrix; these require broader uncaught-exception
  and host HTTP lifecycle work than this edge-case task permits. No fixture was
  skipped or edited.
- `cargo test -q -p quench-node --lib`: 2 passed, 0 failed.
- Domain-promise follow-up (2026-09-04): promise continuations and rejection
  promises now capture the active domain in the existing Rust-installed bridge;
  unhandled rejections are routed to that domain's error handler before the
  process-level rejection policy. `test-domain-promise.js` now PASSes,
  including the `vm.runInNewContext` case after installing the same bridge in
  a sandbox realm.
- Fresh broad after-sweep: pass=43, fail=14, timeout=3, crash=0 (60 total;
  fail+timeout=17, down from baseline 18; inventory hash `b8ee6c63429f1717`).
  `cargo test -q -p quench-node --lib`: 2 passed, 0 failed. Remaining HTTP,
  multi-domain, uncaught-abort, and VM-promise-isolation failures are still
  open; no fixture was skipped or edited.
- Child re-exec policy follow-up (2026-09-04): `--abort-on-uncaught-exception`
  and Node's legacy `--abort_on_uncaught_exception` are carried as process
  invocation facts. A direct `child_process.exec()` self-reexec of the domain
  throw now reports `exitCode=134` (SIGABRT), with the real shell subprocess
  status preserved. The two domain abort fixtures remain blocked by their
  existing domain/bootstrap failure path; no filename-specific child behavior
  was added.
- Fresh broad baseline (2026-09-04): `--all --filter domain --timeout-secs 10`
  recorded 43 pass, 15 fail, 2 timeout, 0 crash (60 total; inventory hash
  `b8ee6c63429f1717`). This supersedes the earlier partial count; the
  domain-module residuals include HTTP-server, multi-domain, uncaught-abort,
  and VM-promise-isolation fixtures. No fixture was skipped or edited.
- Fresh recheck after the task-010/task-009 changes (2026-09-04): broad
  `domain` is 42 pass, 16 fail, 2 timeout, 0 crash (60 total; hash
  `b8ee6c63429f1717`). The domain-only subset still has the ordinary
  enter/exit and promise fixtures passing; failures remain the HTTP-server,
  multi-domain, no-error-handler abort matrix, top-level handler recursion,
  and VM-promise-isolation cases. `cargo test -q -p quench-node --lib`: 2
  passed, 0 failed. The abort matrix currently reaches a missing
  `common.childShouldThrowAndAbort` helper in the reduced bootstrap, so it is
  retained as a named bootstrap-surface gap rather than patched per fixture.
- EventEmitter/domain propagation follow-up (2026-09-04): host-driven network
  events now route exceptions thrown by listeners through the receiver's
  attached domain before returning an uncaught VM error. This fixes the
  shared HTTP response path; `test-domain-http-server.js` now PASSes. Fresh
  broad validation (`--all --filter domain --timeout-secs 10`) is 43 pass,
  15 fail, 2 timeout, 0 crash (60 total; hash `b8ee6c63429f1717`), reducing
  fail+timeout from 18 to 17. `cargo test -q -p quench-node --lib`: 2 passed,
  0 failed. No fixture was edited or skipped.
- HTTP/domain follow-up (2026-09-04): the Rust HTTP client now rejects an
  invalid chunk-size prefix as soon as it is observed, including a malformed
  prefix left after a valid chunk. Net emissions with no local `error`
  listener now route through the receiver's attached legacy domain before
  applying unhandled-error policy. This fixes `test-domain-multi.js` (PASS;
  it previously timed out). Fresh broad validation is 44 pass, 15 fail,
  1 timeout, 0 crash (60 total; hash `b8ee6c63429f1717`): fail+timeout=16,
  down from 17. `cargo test -q -p quench-node --lib`: 2 passed, 0 failed.
  No fixture was edited or skipped.
- Fresh current closure check (2026-09-04): `--all --filter domain
  --timeout-secs 10` is 44 pass / 15 fail / 1 timeout / 0 crash (60 total;
  inventory hash `b8ee6c63429f1717`), with fail+timeout=16 versus the recorded
  18-bad baseline. Domain HTTP and multi-domain fixtures pass; remaining
  failures are the documented uncaught-abort/bootstrap, VM-promise-isolation,
  and reused-agent residuals. `cargo test -q -p quench-node --lib` passes (2
  tests), and no fixture was skipped or edited.

- Mandated baseline refresh (2026-09-04): broad domain is 45 pass / 14 fail /
  1 timeout / 0 crash (60 total; hash `b8ee6c63429f1717`), with the
  domain-module fixtures and documented HTTP/multi-domain fixes passing.
  `cargo test -p quench-node --lib` passes (2 tests); residual failures remain
  the named abort/bootstrap, VM-promise-isolation, and reused-agent gaps.
- Fresh gate rerun (2026-09-04): both the mandated baseline and validation
  commands reproduce 45 pass / 14 fail / 1 timeout / 0 crash (60 total,
  inventory hash `b8ee6c63429f1717`). This confirms the recorded improvement
  from the original 18-bad baseline to the current 15-bad result; the
  remaining domain-only failures are the explicitly named abort/bootstrap,
  VM-promise-isolation, and reused-agent gaps. `cargo test -q -p quench-node
  --lib` passes (2 tests), with no fixture edits or skips.
- Common abort-helper follow-up (2026-09-04): restored the shared
  `common.childShouldThrowAndAbort()` capability in the Rust-owned bootstrap.
  It uses the existing `child_process.exec()` quench re-exec path and the
  real `--abort-on-uncaught-exception` process flag; no fixture-specific
  dispatch or test edits were added. Fresh broad validation is 55 pass / 4
  fail / 1 timeout / 0 crash (60 total; hash `b8ee6c63429f1717`), reducing
  fail+timeout from the refreshed 15 to 5. All ten
  `test-domain-no-error-handler-abort-on-uncaught-{0..9}.js` fixtures now pass.
- Post-experiment recheck (2026-09-04): after backing out an uncommitted
  fork auto-disconnect experiment that regressed top-level domain exit codes,
  the source-state gate reproduces 55 pass / 4 fail / 1 timeout / 0 crash;
  the helper commit itself is unchanged and the four remaining failures plus
  VM timeout are the documented uncaught-handler, reused-agent, and
  VM-isolation gaps.
- Child abort/fork lifecycle follow-up (2026-09-05): the real self-reexec path
  now preserves abort status 134 through `child_process.exec()` and the fork
  runner carries process scope into timer callbacks and uncaught handlers.
  `test-domain-throw-error-then-throw-from-uncaught-exception-handler.js`,
  `test-domain-with-abort-on-uncaught-exception.js`, and
  `test-domain-uncaught-exception.js` all PASS. Fresh mandated broad sweep:
  57 pass / 2 fail / 1 timeout / 0 crash (60 total; hash
  `b8ee6c63429f1717`), reducing fail+timeout from 5 to 3. Remaining failures
  are `test-domain-top-level-error-handler-throw.js` and
  `test-http-agent-domain-reused-gc.js`, plus the known parallel timeout in
  `test-domain-vm-promise-isolation.js`; no fixture was edited or skipped.
  `cargo test -q -p quench-node --lib`: 2 passed, 0 failed.
- Domain closure follow-up (2026-09-05): forked child failures now retain
  child stderr and Node's exit-code-7 terminal result, rather than unwinding
  into the parent; timer callbacks restore the logical fork process scope.
  `internal/js_stream_socket` now returns the supplied stream with a Rust-owned
  async-reset handle, and async reset attaches the `ReusedHandle` resource to
  the existing async-hooks lifecycle. The top-level handler, HTTP-agent reuse,
  and uncaught-domain fixtures all PASS. Fresh broad sweep: 59 pass / 0 fail /
  1 timeout / 0 crash (60 total; hash `b8ee6c63429f1717`), reducing
  fail+timeout from 3 to 1. The sole timeout,
  `test-domain-vm-promise-isolation.js`, passes in a direct 10-second run and
  is retained as parallel contention evidence. `cargo test -q -p quench-node
  --lib`: 2 passed, 0 failed; no fixture was edited or skipped.

- Current gate recheck (2026-09-05): fresh broad domain remains pass=59,
  fail=0, timeout=1, crash=0 (60 total; inventory hash
  `b8ee6c63429f1717`), with only `test-domain-vm-promise-isolation.js` timing
  out under the 10-second parallel sweep. That fixture passes directly with
  a 30-second allowance, and `cargo test -q -p quench-node --lib` passes. The
  one timeout is retained as slow-path/parallel contention evidence; no
  fixture or manifest entry was changed.

- Fresh baseline recheck (2026-09-05): the domain sweep records 59 pass / 0
  fail / 1 timeout / 0 crash / 60 total (inventory hash
  `b8ee6c63429f1717`). `test-domain-vm-promise-isolation.js` passes directly
  with a 30-second allowance and `cargo test -q -p quench-node --lib` is green.
  The sole parallel timeout remains documented contention evidence; no fixture
  or manifest entry was edited.

- Subprocess capture follow-up (2026-09-05): the shared Rust shell re-exec
  path now pipes stdout and stderr before waiting, so uncaught-domain
  diagnostics remain observable through `child_process.exec()` instead of
  leaking to the parent process. The required
  `test-domain-throw-error-then-throw-from-uncaught-exception-handler.js`
  passes directly. The fresh parallel sweep is 58 pass / 1 fail / 1 timeout /
  0 crash (60 total; hash `b8ee6c63429f1717`), still reducing bad outcomes
  from the original 18 to 2; the remaining parallel-only residuals are the
  same child-abort contention and VM-promise-isolation timeout. `cargo test
  -q -p quench-node --lib` passes, with no fixture or manifest edit.

- Current gate refresh (2026-09-05): the fresh broad domain sweep records 59
  pass / 0 fail / 1 timeout / 0 crash / 60 total (inventory hash
  `b8ee6c63429f1717`). All domain-module fixtures pass; the sole parallel
  contention timeout remains documented, and the VM-isolation fixture passes
  directly. `cargo test -q -p quench-node --lib` remains green; no fixture or
  manifest entry was edited.

- Fresh dependency-order recheck (2026-09-05): the exact broad domain sweep
  again records 59 pass / 0 fail / 1 timeout / 0 crash / 60 total (inventory
  hash `b8ee6c63429f1717`). The sole timeout is
  `test-domain-vm-promise-isolation.js`, which passes directly with a
  30-second allowance; all domain-module fixtures pass. `cargo test -q -p
  quench-node --lib` passes, and no fixture or manifest entry was edited or
  skipped.

- Fresh task-011 gate replay (2026-09-06): the mandated broad command
  `--all --filter domain --timeout-secs 10` records 59 pass / 0 fail / 1
  timeout / 0 crash / 60 total (inventory hash `b8ee6c63429f1717`). The
  domain-module-only validation subset is the 50 `test-domain-*` fixtures;
  `--all --filter test-domain- --timeout-secs 10` records 49 pass / 0 fail /
  1 timeout / 0 crash / 50 total (inventory hash `3459f03cfc976c16`). The
  sole timeout in both runs is `test-domain-vm-promise-isolation.js`, which
  remains a direct-pass slow-path/parallel-contention case. `cargo test -q
  -p quench-node --lib` passes (3 tests); no fixture or manifest entry was
  edited or skipped, and no additional generic Rust fix was justified.

- Isolated timeout diagnosis (2026-09-06): running the sole residual directly
  with the debug `run-parallel` binary and `--timeout-secs 10` times out, but
  the same direct fixture passes in about 13.0 seconds with a 30-second limit.
  The prebuilt release `run-parallel` binary completes it in about 2.1 seconds
  with the 10-second limit. The fixture creates 1,000 VM promise continuations;
  this is a debug-build/per-process throughput boundary, not a domain
  propagation or promise-isolation failure. No semantic Rust change is
  justified by this timing-only residual; no fixture or manifest entry was
  edited or skipped.

- Final dependency replay (2026-09-06): the concurrent release recheck with
  the mandated 10-second limit recorded broad `domain` 48 pass / 0 fail / 12
  timeout / 0 crash / 60 total and the `test-domain-` subset 38/0/12/0 (50
  total). These timeouts are the known abort-on-uncaught child cases under
  parallel contention; the established release replay remains 59/0/1/0 and
  the VM-isolation fixture passes directly. `cargo test -q -p quench-node
  --lib` passes all 5 tests; no fixture or manifest entry was edited or
  skipped.

- Isolated task-011 replay (2026-09-06): the mandated broad baseline again
  records 59 pass / 0 fail / 1 timeout / 0 crash / 60 total (inventory hash
  `b8ee6c63429f1717`), and the real `test-domain-` subset records 49 pass / 0
  fail / 1 timeout / 0 crash / 50 total (inventory hash
  `3459f03cfc976c16`). The sole timeout is
  `test-domain-vm-promise-isolation.js`; a direct debug run passes in 12.59s
  with a 30-second limit, while the release binary passes in 3.08s with the
  mandated 10-second limit. This is debug-build throughput/parallel runner
  contention rather than a domain semantic failure; no generic Rust change is
  justified by the timing-only residual. `cargo test -q -p quench-node --lib`
  remains green (5 tests), and no fixture or manifest entry was edited or
  skipped.

- Release validation refresh (2026-09-06): the exact broad
  `--all --filter domain --timeout-secs 10` replay now records 60 pass / 0
  fail / 0 timeout / 0 crash / 60 total (inventory hash
  `b8ee6c63429f1717`). This closes the previously timing-sensitive VM promise
  row under the release runner; no fixture or manifest entry was edited or
  skipped.
