# TLS and HTTPS baseline

Status: partial.

`createSecureContext`, TLS validation, and explicit unsupported transport errors are implemented; secure transport, HTTPS request/get, Agent pooling, and TLS-backed servers remain.

Acceptance: focused TLS/HTTPS fixtures, Node CLI behavior checked for errors/options, compatibility matrix updated, focused and overall compatibility runs pass.
