# 28: Finalize repository contracts

## Outcome

Leave a coherent, warning-free repository whose documentation, dependencies, queue, and commands describe only the final architecture.

## Current gap

Cutover can leave temporary binaries, duplicated parser versions, migration documentation, obsolete tools, and task state that no longer matches production.

## Dependencies

See this task's `depends_on` in [index.json](index.json).

## Scope and implementation sequence

Unify OXC on the final exact release, remove migration-only runners/dependencies/tools, update AGENTS/GOAL/CONTEXT/docs and public commands, validate all local links, and remove obsolete architecture vocabulary. Preserve completed task files, mark every item `done`, and set `next_task` to `null` only after final evidence passes.

## Verification

Run warning-denied formatting/check/test/clippy/lint/coherence from a clean checkout, all three 100% conformance gates, residual validation/phase checks, Node oracle controls, Wasm boundary tests, and final Score/RSS qualification. Confirm `cargo tree -d` has no duplicate OXC family caused by workspace code.

## Definition of done

- Documentation, manifests, binaries, and task state describe one interpreter-only architecture.
- The working tree has no broken link, stale migration authority, warning, or generated tracked artifact.
- All 28 tasks are retained as `done` and `next_task` is `null`.

## Performance evidence

Publish the final matched summary with raw artifacts under ignored `target/` storage and exact provenance.
