# 016: Outline cold ordinary-fallback bodies from guarded hot paths

- **Theme:** deegen_codegen
- **Executor:** codex
- **Depends on:** 006, 007

## Scope
- crates/quench-runtime/src/vm
- crates/quench-runtime/src/machine.rs

## Deliverables
- Mark complete ordinary-fallback handler bodies #[cold] and, where beneficial, move them into separate non-inlined functions so the guarded fast-path loop stays compact
- Verify no observable semantics move as part of outlining; this is a code-layout change only
- Measure instruction-cache pressure or dispatch-loop size before/after on the neutral microbenchmark corpus

## Acceptance
- Fallback bodies are reachable from every guard miss with unchanged behavior
- Neutral corpus geomean does not regress; dispatch-loop code size is reported as evidence

## Non-goals
- Removing or shortcutting any fallback semantics
