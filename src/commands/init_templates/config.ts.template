// runts.config.ts
import { defineConfig } from "runts";

export default defineConfig({
  server: {
    port: 8000,
    host: "127.0.0.1",
  },
  build: {
    optimization: {
      lto: true,
      opt_level: "z",
    },
  },
  islands: {
    hydration: "eager",
  },
  dev: {
    open: true,
    hmr: true,
  },
});
