# One Native | Fast | Dynamic ladder

Objects and execution use one Native | Fast | Dynamic ladder, not
per-language types. A guard or a box is the only layer transition; frontends
choose an entry layer from their proven facts.
