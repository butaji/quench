# ADR 0002: Superseded — staged compact IR interpreter

- Status: superseded by ADR 0005.

The former AST-to-owned-IR migration is not a target architecture. OXC syntax
and semantics are queried as data, then reduced directly to residual ops.
