# Archived benchmark evidence

These files are historical records from the August 2026 benchmark pass. They
are not active gates, current performance claims, or inputs to production
behavior.

- `benchmark-history-2026-08.json` preserves the former `bench/index.json`
  ledger, including its original `/tmp/nextjs/my-app/bench.cjs` source path.
- `perf-budget-2026-08.json` preserves the former `bench/perf-budget.json`
  thresholds used during that pass.

The active benchmark and performance contracts live under `quench-bench/` and
`tools/`. Re-run measurements before treating these archived values as current.
