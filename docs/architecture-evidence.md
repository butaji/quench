# Task 075 — current stencil infrastructure matrix

Status: **gate CLOSED / NOT COMPLETE**. Current default/generated/host evidence
is bound to clean implementation commit `a49b438f0d`; the last full trace suite remains
bound to `7841a3f2b`. Micros
and task 073 remain deferred until every required row passes with normal-source
and non-vacuous native evidence.

| Area / required family | Implementation and normal wiring | Executed evidence at this revision | Support and remaining gap |
| --- | --- | --- | --- |
| Canonical declarations | `build_stencil_contract.rs` is the shared build-time representation of `RegionDeclaration`, `DeclAbi` and the macro-derived Rust leaf recipes; both `build.rs` and the executable extractor test consume it. Each Rust leaf recipe declares its exact region name, ABI, residual shape, signature, result type and body; selection rejects any mismatch instead of inferring compatibility from opcode strings. `PhysicalStencilView` couples one canonical record to exact selected bytes, ABI, entries, target and identity. Region IDs are stable name-derived declaration facts with build-time collision rejection. Build provenance hashes every declaration input; artifact identity additionally hashes extracted bytes, fallthrough and typed relocations. | At `a49b438f0d`, default runtime 817/817 + extractor 4/4 + routing 1/1; generated runtime 823/823 + extractor 4/4 + routing 2/2; Node host 14/14. Raw logs: `/tmp/quench-runtime-typed-recipes-default-final.log`, `/tmp/quench-runtime-typed-recipes-enabled.log`, `/tmp/quench-node-typed-recipes.log`. | Generated views reject incompatible layouts before entry. `build.rs` and the effectful extractor remain above the size cap and need cohesive catalog/compiler/parser splits. |
| Typed capability publication/lifetime | `stencil_arena.rs` keeps non-owning `EntryToken` private and constructs it only through ABI-specific macro-generated constructors after the typed accessor verifies publication. A retaining `AllocationLease` rechecks owner/generation/ABI/address and now records the exact owning slab. Reentrant invocation releases pool borrows before machine entry; per-owner lease counts prevent reclamation of the executing allocation without pinning unrelated idle slabs. Cache-aware eviction removes only unleased owners and their derived rows. `machine.rs::PhysicalState` composes cache disposal with the existing lifecycle for LoadConst, Move, Property, Nullish, Unary, Truthiness, Binary, AddChain, NativeDispatch and NativeRegion plans; installed alternatives remain ABI-specific. | At `a1a3ada2bd`, focused lease filter 11/11 passes, including actual generated dispatch -> Rust helper -> nested typed native entry, mutable pool admission, exact single helper effect, and return. At `e7ae504417`, a final-external-owner test proves the lease retains executable capacity and executes after the last outside owner drops, then releases the pool 1/1. Current full-suite results are recorded in Verification below. | Active allocation isolation, stale-address rejection, independent idle reclamation, final-owner release and per-pool/global code caps are covered. `stencil_cache.rs` now owns process-wide cache metadata charging. Cohesive extraction from the oversized arena module remains. |
| Residual CFG/admission | `BaselinePlan` stores one compact `AdmissionSpan` per PC and sparse typed alternatives in one shared array; the optimizing view reuses it. `stencil_cache.rs` is the single render-memo representation: cold caches reserve zero bytes, a warm cache is bounded to 16 rows, all caches share a 4 MiB process ceiling, and clear/final-owner removal/drop release their reservation. The shared executable pool owns the canonical render memo; per-plan caches are derived views, so equivalent physical keys/signatures reuse one allocation across sites while distinct patch signatures remain separate. Canonical `register_flow`, out-of-line call operand windows, control successors and a bounded backward worklist derive frame widths and live-outs. Local-slot identifiers for `LoadLocal`, `LoadLocalChecked`, and flagged local `Move` are excluded from VM-register uses; nonconsecutive call arguments remain live and size the frozen frame even when their IDs exceed the compact destination. `region_admission_matches` validates declared operations, operands, legal entries and successors before publication. `stencil_plan.rs` derives Add→Add bindings and one bounded local-numeric input representation: two distinct local slots, one repeated slot, or one local slot plus a right-hand Number constant. Required dead-register clears are part of the selection and cost. | At `734f10e29b`, focused operand-window sizing/liveness and sentinel controls pass; clean full default runtime passes 815/815 + extractor 3/3 + routing 1/1; generated runtime passes 821/821 + extractor 3/3 + routing 2/2; Node host passes 14/14. Each runtime configuration has one intentional ignore; doc tests pass 2/2. | Constant propagation and value numbering are restricted to proven effect-free numeric producer boundaries; effect-killed general LVN/DCE, strength-reduced recipes, hot/cold layout, verified peepholes and reusable multi-block planning remain. |
| Region ABI and exits | `stencil_select.rs::RegionAbi/RegionContract`, `vm_runtime.rs` typed status/fault-PC paths, and `native_region_bridge` provide bounded scalar/word/array contracts. Structured suspension retains the original lowered phase range plus its exact resume PC, so compact branch targets do not change meaning; installed outer frames remain authoritative across nested re-suspension. The VM result boundary maps only canonical `Completion::Normal` to `undefined`; Node no longer normalizes the internal `MissingReturn` fault into exit-zero success. | On the current `cacf262397` implementation, all 11 finite sources retained in `target/stencil-review-1788646448/continuation-recheck.json` match local Node exactly: nested await/generator progress, simple and try/finally generators, source/injected return, await in init/test/update/do-while, and captured-root survival under collection churn. The result-boundary test passes; Node host tests pass 14/14. | Internal failure sites still need richer fixed reason/code/PC data. Full native root maps, helper/reentry/safepoint contracts and all post-effect outer-driver cases remain. |
| Actual native N/G bodies | Rustc-generated whole leaves now cover f64 arithmetic, ordered/equality comparisons, tagged identity, exact i32 bitwise/shift operations, negate, increment and numeric truthiness; labeled Rust `global_asm!` objects cover ARM property get/set/prototype guards and the numeric loop. `NativeAddChainPlan` composes a small numeric chain using admission-time fixed bindings. The ARM numeric-array region executes condition, dense load/add/store, induction, interrupt poll and backedge in one machine-code entry. | At `a49b438f0d`, generated actual-byte tests pass for NaN/ordered comparisons, tagged identity, signed bitwise results, masked shifts/unsigned representation, numeric truthiness and increment. Ordinary-source property tests prove exact generated artifact identity, semantic values and guarded rejection. Full default/generated suites pass as recorded above. | x86 retains its existing verified native property bodies and safely rejects the prototype-chain body. Tagged local/constant/nullish/pointer-truthiness and dense-array templates still need migration or explicit generated-contract coverage. Admission remains a bounded declared loop shape rather than reusable multi-block composition; most bounded optimizer stages remain unimplemented. |
| Relocations/publication | `stencil_arena.rs` validates mappings, W^X publication and cache ownership; `validate_physical_template` checks declaration effects. Rust object extraction uses the `object` parser with symbol-relative bounds and an explicit relocation allowlist. `PhysicalRelocation` now retains offset, kind, target **and addend** through generated selection; the supported Branch26 contract requires zero addends and rejects others before publication. | At `e0cf84111`, unsupported-addend rejection executes 1/1; generated filter executes 24/24. Earlier Branch26 table executes 6/6 and transactional multi-hole patching is covered. | Branch26 fragment composition is supported; ARM page-pair/literal relocation families and full publication-failure cleanup remain safe rejections needing direct coverage before broader generated bodies. |
| Guards/fallback | Admission checks declared ABI/opcode/effects; Unknown/unsupported cases use canonical handlers. Physical, semantic and committed statuses preserve fault PCs. Raw AArch64 streams fail closed on unknown instructions before entry, and all hole-free ARM templates pass the same vocabulary validator. Direct-call detection treats only BL/BLR as helper calls; BR transfers are validated through the declared bridge ABI and root/effect contract. The x86 scanner conservatively recognizes direct E8 and FF /2 indirect CALL while excluding FF /4 JMP; plausible calls reject rather than silently under-declare effects. | Runtime/trace suites pass at the current source; `region_verifier_rejects_physical_call_for_raw_abi` — 1 passed with direct/offset/indirect-call and JMP-negative cases. | Full instruction decoding for x86 prefixes/data boundaries remains; alias/live-out proof, helper throws/reentry, GC roots, interruptible backedges and finally/suspension boundaries need outer-driver tests. |
| B families | Existing VM machinery remains semantic owner; native plans reject unsupported interiors and return to ordinary execution. The cycle collector models generators as weakly registered graph nodes instead of relying only on the async suspended-root bridge; generator edges derive from the canonical activation fields. Array slice executes observable Has/Get/CreateDataProperty steps in index order and performs the final throwing length Set. | Current CLI/Node differential controls pass 11/11 for structured suspension/rooting and 3/3 for slice getter/define/set ordering, early throw, and non-writable length. Reachable synchronous generator captures survive collection churn and abandoned generator/function/environment cycles reclaim in focused tests. | Dedicated native allocating/reentrant/rooting/exception boundary coverage is still incomplete. Additional slice holes/inherited-getter/species-mutation tests remain required before its boundary row is exhaustive. |
| Diagnostics/budgets | `execution_trace.rs` has bounded stencil identities/rejection/outcome counters; slab/cache paths expose usage. Executable slabs have per-pool/process code bounds; `stencil_cache.rs` separately accounts bounded cache metadata process-wide. | At implementation revision `38bb9aabd7`, execution-trace runtime passes 823/823 with one intentional ignore, plus extractor 3/3, routing 1/1 and docs 2/2 (`target/stencil-fusion-root-runtime-trace.log`). Current untraced resource tests are recorded in the admission row. | Native-vs-modeled witnesses, render/lease bytes and aggregate variant/eviction reporting need integration; final-revision trace evidence remains required. |
| Verification | `machine_tests.rs`, `stencil_plan.rs`, `stencil_fusion_tests.rs`, `stencil_cache.rs` and `stencil_arena.rs` cover emitted arithmetic, selection bindings, owner eviction/rebuild, ABI-checked dispatch, status transport, property guards, array execution and ordinary-source admission/execution. `CodeArena::freeze` replaces nested same-store strong links with deferred weak resolution while external owners remain explicit. | At `a49b438f0d`: default runtime 817, generated runtime 823 and Node host 14 pass; each runtime configuration has one intentionally ignored test. Extractor passes 4 in both configurations, routing passes 1 default/2 generated, and doc tests pass 2. Trace runtime 823 + extractor 3 + routing 1 remains bound to `38bb9aabd7`. | Native hostile exits beyond the now-tested pure/property boundaries, remaining bounded optimizer stages, and complete required N/G body reachability remain before closing gate; final-revision trace suites must still be repeated after later gate changes. |

Safe exclusions remain explicit: allocating/reentrant helpers, proxies/accessors,
sparse/typed-memory arrays, suspension and exotic coercions stay on complete
ordinary semantics until their root, invalidation and exact-continuation
contracts are implemented. Production ARM admission remains conservative; these
correctness tests make no performance claim.
