# Task 075 — current stencil infrastructure matrix

Status: **gate CLOSED / NOT COMPLETE**. Source evidence is bound to commit
`e6b91b8fb` (documentation/task cleanup remains user-owned and dirty). Micros
and task 073 remain deferred until every required row passes with normal-source
and non-vacuous native evidence.

| Area / required family | Implementation and normal wiring | Executed evidence at this revision | Support and remaining gap |
| --- | --- | --- | --- |
| Canonical declarations | `build.rs::REGION_DECLARATIONS` generates `region_abi_catalog!`; `stencil_select.rs` derives ABI contracts, selection and validation. | `cargo test -p quench-runtime native_ -- --nocapture` — 45 passed. | Generated catalog is active; build-time object extraction is not yet complete. |
| Typed capability publication/lifetime | `stencil_arena.rs` keeps `OwnedEntry` private, records the generated `RegionAbi` at publication, and rejects wrong-ABI accessors. `StencilLifecycle` and owner checks reject stale entries. Constant, Move, Unary, Nullish, Truthiness, AddChain, Property and Binary use one installed enum (Binary alternatives remain explicitly typed). | Wrong-ABI emitted-address test passes (`/tmp/quench-typed-abi-mismatch-20260905.log`); native owner suite: 45 passed (`/tmp/quench-native-typed-abi-20260905.log`); full runtime 737, trace 752, host 4 (`/tmp/quench-runtime-full-20260905-typed-abi-v2.log`, `/tmp/quench-runtime-trace-20260905-typed-abi-v2.log`, `/tmp/quench-node-full-20260905-typed-abi-v2.log`). | Shared-slab eviction tests pass for migrated plans; aggregate active-lease accounting remains. |
| Residual CFG/admission | `machine.rs::BaselinePlan` and `vm_runtime.rs` route lowered residual operations and existing region records; ordinary-source tests exercise admission. | Existing ordinary-source admission/native tests are included in the 45-test native run. | General build-time CFG region admission with legal entries, liveness and reusable loop dataflow is incomplete. |
| Region ABI and exits | `stencil_select.rs::RegionAbi/RegionContract`, `vm_runtime.rs` typed status/fault-PC paths, and `native_region_bridge` provide bounded scalar/word/array contracts. | Native run includes status, fault-PC and array native-path tests. | Full root maps, helper/reentry/safepoint contracts and all post-effect outer-driver cases remain. |
| Actual native N/G bodies | Emitted ARM64/x86 leaves cover constants, moves, f64 arithmetic, comparisons, bitwise, truthiness/nullish and dense-array paths; `NativeAddChainPlan` composes a small numeric chain. | Native suite includes emitted-byte witnesses and ordinary-source loop/backedge tests; modeled callbacks remain labeled as modeled. | Required reusable loop with condition/load/arithmetic/store/induction/backedge and broader property/control families is not complete. |
| Relocations/publication | `stencil_arena.rs` validates mappings, W^X publication and cache ownership; `validate_physical_template` checks declaration effects. | Prior arena/publication suite: 32 passed (`/tmp/quench-stencil-arena-tests-20260905.log`). | Build-time assembler/object extraction, complete relocation range/alignment matrix and publication-failure cleanup need direct coverage. |
| Guards/fallback | Admission checks declared ABI/opcode/effects; Unknown/unsupported cases use canonical handlers. Physical, semantic and committed statuses preserve fault PCs. | Runtime/trace suites above pass; prior post-entry no-replay bridge test is `/tmp/quench-array-fault-pc-bridge-20260905.log`. | Alias/live-out proof, helper throws/reentry, GC roots, interruptible backedges and finally/suspension boundaries need outer-driver tests. |
| B families | Existing VM machinery remains semantic owner; native plans reject unsupported interiors and return to ordinary execution. | Full runtime and host suites pass at this revision. | Dedicated allocating/reentrant/rooting/exception boundary matrix is still missing. |
| Diagnostics/budgets | `execution_trace.rs` has bounded stencil identities/rejection/outcome counters; slab/cache paths expose usage. | Trace runtime: 751 passed, 0 failed, 1 ignored. | Native-vs-modeled witnesses, render/lease bytes and global variant/eviction budgets need integrated reporting. |
| Verification | `machine_tests.rs` covers emitted arithmetic, owner eviction/rebuild, status transport, array execution and ordinary-source admission. | 45 focused native tests; full runtime 736, trace 751, host 4, all zero failures. | Add wrong-ABI rejection, stale-generation reuse, native loop zero/one/many hostile exits, root survival and resource-lifetime integration before closing gate. |

Safe exclusions remain explicit: allocating/reentrant helpers, proxies/accessors,
sparse/typed-memory arrays, suspension and exotic coercions stay on complete
ordinary semantics until their root, invalidation and exact-continuation
contracts are implemented. Production ARM admission remains conservative; these
correctness tests make no performance claim.
