# Task 075 infrastructure evidence

This is the single current matrix for `docs/stencil-jit-implementation-spec.md`.
Evidence below is bound to source revision `e9c78e4d7` (cleanup files remain
dirty by user authorization). The gate is CLOSED / NOT COMPLETE: rows marked partial or missing block
micros/task 073. This matrix reflects the current working revision; the
documentation/task cleanup edits remain user-owned and are not included in
functional commits.

| Layer / family | Code and normal wiring | Executed evidence | Supported / safe boundary | Status and remaining gap |
| --- | --- | --- | --- | --- |
| Canonical declarations | `build.rs::REGION_DECLARATIONS` generates bytes, holes, op sequences, declaration names, ABI family (including typed `TaggedWord`, `ConstantWord`, `ScalarBool`, `ScalarWordBool`, `ScalarWordPairBool`, `ScalarI32`/`ScalarU32`), keys and accessors; generated `canonical_abi_contract` supplies context arguments, clobbers, live-outs, roots and interruptibility to the small `region_abi_catalog!` enum; the catalog derives `RegionAbi::accepts_region_context` and `RegionAbi::priority`, so BaselinePlan and NativeRegionPlan cannot drift into separate scalar/raw ABI allowlists or ranking tables; generated declaration names now also feed storage/diagnostic attribution; literal constants use generated `Literal64` holes while bridge targets use `Ptr64`; scalar Add maps to the terminating `loop` declaration, while non-returning `fallthrough` remains composition-only; `stencil_patch.rs::typed_writer!` derives relocation writers; `NativeRegionContext::new_with_abi` carries the selected declaration into the bridge; `machine::validate_physical_template` is shared by region validation and scalar/tagged plan construction; array-kernel dispatch now matches the generated record's ABI and operation sequence through `region_matches_generated_array_decl` | `generated_region_*` tests plus ABI/physical-template tests; raw-array target contract **1 passed** (`/tmp/quench-array-target-contract-20260905.log`); generated physical-shape classification **1 passed** (`/tmp/quench-array-abi-shape-20260905.log`); declaration-routed indexed-increment **1 passed** (`/tmp/quench-generated-array-dispatch-20260905.log`); fused-update declaration route **1 passed** (`/tmp/quench-generated-array-update-20260905.log`); scalar/array ABI routing **1 passed** (`/tmp/quench-generated-abi-routing-20260905.log`); generated priority/routing also passes in the full runtime (`/tmp/quench-runtime-full-20260905-generated-abi-priority.log`); generated declaration names and storage attribution compile and pass with the latest full runtime (`/tmp/quench-runtime-full-20260905-generated-names-fixed.log`); AddConst emitted-byte/literal-domain and tagged-pair routing tests remain bound to their listed revisions | Unknown declarations reject before publication; scalar/tagged/raw entries cannot carry bridge pointer relocations; embedded literals are not misclassified as context pointers; physical helper calls and register-transfer opcodes are rejected unless the declared ABI permits them; preserving leaves declare no hardware clobbers and helper bridges require root materialization | Partial: operand/liveness/effect metadata is still derived partly in runtime validators; hardware masks are declaration contracts but per-instruction clobber extraction and live-root maps remain incomplete; source files exceed the requested size limits |
| Helper/reentry boundary validation | `machine::validate_physical_template` now requires any detected physical call to satisfy the declared helper ABI, root-materialization bit, and at least one canonical semantic boundary effect (heap read/write, control, throw/observable/allocation); this is checked independently of the enum’s `may_call_helper` flag; `cycle_collector::protect_frame` roots all live register words plus the current environment around helper-capable bridge execution | `physical_helper_call_requires_semantic_boundary_and_roots` **1 passed** (`/tmp/quench-helper-boundary-20260905.log`); `protected_frame_roots_live_register_words_until_bridge_exit` **1 passed** (`/tmp/quench-frame-root-20260905.log`); generated declarations still pass `physical_templates_match_declared_abi_before_entry` | Pure-operation bridge declarations with call-shaped bytes reject before publication; helper-capable canonical bridges retain complete ordinary semantics at their declared boundary; frame roots survive collection only for the synchronous bridge lifetime | Partial: direct helper exception/reentry/GC root maps and finally-resume cases remain to be implemented and tested |
| Frame roots across fallback | `execute_region_fallback` now installs the same `cycle_collector::protect_frame` guard for any residual span whose canonical effects may throw, covering direct guarded-region misses as well as C-bridge fallback; raw helper-free entries remain unrooted and pointer-only | `native_bridge_preserves_fault_pc_after_prior_effect` **1 passed** (`/tmp/quench-rooted-fault-20260905.log`); full runtime after this change **685 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-rooted-fallback.log`) | Root guard is scoped to validation-success through exact transition/error return and is dropped before retry/outer dispatch; unsupported helper/reentry interiors still use complete canonical handlers | Partial: collector still stores cloned `Value` roots rather than direct frame maps; suspended/host-held roots and helper reentry matrices remain incomplete |
| Live frame root registration | `cycle_collector::protect_frame` now registers a stable `RegisterFile` pointer plus Rc-owned environment in a bounded TLS root list; the collector scans packed words only at collection time, and `RootGuard` truncates the list on every exit | `protected_frame_roots_live_register_words_until_bridge_exit` **1 passed** with explicit registration/cleanup witness (`/tmp/quench-frame-root-live-20260905-witness.log`); full runtime after this change **685 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-frame-live.log`) | Bridge entry/exit avoids per-register `Value` decoding; frame owner remains live for the synchronous call and raw pointer is never retained after guard drop | Partial: suspended/host-held frames need explicit registration and collector scan tests; direct hardware root maps remain incomplete |
| Bounded frame root registry | `cycle_collector::FRAME_ROOTS` is capped at 64 active frames; pathological nesting falls back to the existing owned-word root snapshot rather than growing the registry or dropping roots | `frame_root_registry_is_bounded_with_complete_overflow_fallback` **1 passed** (`/tmp/quench-frame-root-cap-20260905.log`); full runtime after this change **686 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-frame-cap.log`) | Normal bridge entry remains pointer-only/O(1); overflow preserves complete rooting and RootGuard cleanup | Partial: cap is a safety bound, not a complete suspended/host-held frame map |
| Baseline bridge roots | `native_dispatch_bridge` now roots the live `RegisterFile` words and current environment before invoking the canonical handler, matching the composed fallback contract; the guard spans handler completion/error decoding | `native_dispatch_finish_rejects_post_entry_malformed_status` **1 passed** (`/tmp/quench-dispatch-root-20260905.log`); full runtime after this change **685 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-dispatch-root.log`) | Single-op bridge allocation/throw paths retain complete semantics with no retry; direct scalar/raw leaves remain helper-free and do not enter this bridge | Partial: direct frame maps and suspended/host-held root integration remain incomplete |
| Residual control flow/admission | `ir::Opcode::{control_operands,operands_are_canonical}`, `machine::{region_admission_matches,validate_region_window}`, baseline plan construction; ordinary `reduce_source` reaches nested loop bodies | At `3348c65a2` (clean source commit; user cleanup files remain dirty), `region_admission_rejects_noncanonical_operands_before_publication` **1 passed** (`/tmp/quench-region-static-contract-20260904.log`); `ordinary_source_lowering_emits_numeric_backedge_and_executes` remains the normal lowering/backedge check | Generated ABI/entry/operand/control contracts reject malformed windows before rendering; structured/unsupported loops remain ordinary residual execution | Partial: general multi-block region builder/liveness admission is not complete; current native loop shape is bounded and declared |
| Loop alias admission | `execute_composed_array_numeric_loop` now applies a conservative role-disjointness proof to independent loop-carried/index/array/result registers after checking canonical forwarding edges; any destructive alias rejects before raw entry | `numeric_loop_alias_roles_fail_closed` **1 passed** (`/tmp/quench-loop-alias-contract-20260905.log`); ordinary-source numeric loop still reaches and executes the ARM path (`/tmp/quench-arm-loop-alias-20260905.log`); full runtime **687 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-alias-contract.log`) | Compiler-generated distinct roles admit; hostile aliasing takes complete ordinary semantics with no effects from the native body | Partial: this covers the bounded numeric-loop declaration; general CFG liveness/alias analysis remains incomplete |
| Region ABI/state/roots | `stencil_select::AbiContract` now carries declaration-derived context arguments, separate SIMD and integer hardware clobber masks, live-out mask, root-materialization requirement and interruptibility; declaration-carried `NativeRegionContext::abi`, distinct `TaggedWord`/scalar/raw-array/bridge entry families, `NativeArray{Kernel,Loop}Context (repr(C))`, `RegisterFile::visit_values`, `VmContext::interrupt_flag`; raw array entries now validate register/context/data/index/end/alignment/interrupt pointers before marking native entry; `machine::physical_template_{clobber,gpr_clobber}_mask` derives ARM64 SIMD/GPR destinations from emitted words and rejects undeclared scratch registers before publication | raw context layout plus invalid/null/unaligned-context tests (1 pass, `/tmp/quench-array-context-safety-20260905.log`); undeclared SIMD and GPR clobber tests **1 each** (`/tmp/quench-clobber-undeclared-20260905.log`, `/tmp/quench-gpr-clobber-reject-20260905.log`); full generated-template verifier **1 passed** (`/tmp/quench-gpr-clobber-contract-20260905.log`); `rendered_numeric_array_loop_poll_exits_after_committed_iteration`; `ordinary_source_lowering_emits_numeric_backedge_and_executes` now drives a requested interrupt through the normal resume path; ABI clobber/live-out/root contract assertions (`/tmp/quench-abi-clobber-20260905.log`); tagged-word property/move native tests (1/1 each) | Scalar, tagged-word, raw-array and bridge ABIs are distinct at construction and dispatch; malformed raw contexts reject before entry; preserving leaves declare no hardware clobbers; helper bridges require roots; raw numeric contexts exclude helper/reentrant operations; raw ARM templates cannot write SIMD or integer registers outside their declaration masks | Partial: declaration masks and ARM64 physical destinations are now checked before entry, but live-root maps and complete helper/GC safepoint contracts are not complete for every ABI |
| Native numeric/control bodies | `NativeBinaryPlan` f64 arithmetic/comparison, guarded `IncI` increment/decrement using a generated literal-patched AddConst template, typed `ScalarI32`/`ScalarU32` bitwise/shift bodies, ARM64 dense indexed-load/store/loop bytes, and the declaration-driven `array_numeric_update` fused load/add/store body; strict identity equality/inequality use generated `ScalarWordPairBool` tagged-word entries; `NativeLoadConstPlan`, `NativeTruthinessPlan`, `NativeNullishPlan`, `NativeUnaryPlan`, `NativeAddChainPlan` | Direct ARM64 `AGetI` emitted-byte execution **1 passed** (`/tmp/quench-array-get-native-20260905.log`) and baseline typed-ABI routing **1 passed** (`/tmp/quench-array-get-routing-20260905.log`); direct normal-driver dense `ASetI` execution plus out-of-bounds fallback **1 passed** (`/tmp/quench-array-set-native-20260905.log`); fused `AGetI→Add→ASetI` native execution plus destructive-alias canonical fallback **1 passed** (`/tmp/quench-array-update-native-20260905.log`); ordinary-source numeric comparison with native witness and string conversion fallback **1 passed** (`/tmp/quench-comparison-native-20260905.log`); ordinary-source bitwise admission **1 passed** (`/tmp/quench-bitwise-admission-20260905.log`) and separate execution/hostile conversion fallback **1 passed** (`/tmp/quench-bitwise-execution-20260905.log`); full runtime at `4b4713155` **718 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-bitwise-split.log`) | f64 arithmetic, guarded Number `++/--`, exact tagged primitive/pointer truthiness, strict identity words, primitive constants, ordered comparisons, exact Number-to-Int32 bitwise/shift, unary `~`, dense numeric indexed load/store, fused indexed numeric update, and bounded numeric loop; strings/heap payloads/coercive cases use canonical fallback | Partial: broader comparison/control composition and multi-op loop admission remain ordinary fallback |
| ARM composition/control | `AARCH64_ARRAY_LOOP_BYTES` has condition, indexed load/add/store, induction update, interrupt poll and native backedge; `NativeRegionPlan::execute` is reached from baseline/optimizing plans; `validate_region_window` and `NativeRegionPlan::new_inner` independently verify physical templates before entry; `execute_composed_array_numeric_loop` now also verifies its operation sequence against the generated `array_numeric_loop` declaration before reading operands | At current source `b38c6a72a`, `ordinary_source_lowering_emits_numeric_backedge_and_executes` **1 passed** (`/tmp/quench-arm-loop-20260905-validation.log`) plus rendered loop tests covering zero/one/many/nonzero result and committed interrupt exit; generated-declaration loop admission/execution **2 passed** (`/tmp/quench-loop-generated-admission-20260905.log`); checkpoint verifier 1/1 at `381bbd026` (`/tmp/quench-interrupt-checkpoint-contract.log`) | ARM production policy remains conservative; unsupported ABI/target rejects before entry | Partial: the bounded loop has a physical checkpoint and exact committed handoff, but broader branch/live-out composition and runtime interrupt-request plumbing remain incomplete |
| Relocations/publication | `stencil_patch::apply_holes` derives writers for `Literal64`, `Ptr64`, immediates and branches; `Stencil::validate`, `StencilArena::make_executable` (W^X + I-cache flush), declaration build checks; `build.rs::aarch64_b_imm26/aarch64_b_cond` derive branch words; ARM bitwise/shift/unary-not words now come from parameterized build-time encoders (`aarch64_bitop_w`/`aarch64_shift_w`/`aarch64_mvn_w`) rather than duplicated hex literals | relocation range/alignment tests; generated catalog tests; AddConst literal patch test (`/tmp/quench-addconst-literal-20260905.log`); direct emitted ARM bitwise/shift execution **7 passed** (`/tmp/quench-encoder-bodies-20260905.log`); unary-not emitted execution **1 passed** (`/tmp/quench-mvn-encoder-20260905.log`); optional assembler verification passed with `QUENCH_VERIFY_STENCIL_ENCODINGS=1` (`/tmp/quench-stencil-assembler-branch-helpers.log`) | Out-of-range, overlap, alignment, pointer-vs-literal mismatches and unsupported target fall back | Partial: branch displacements and integer op encodings derive from named build-time helpers and assembler labels verify the loop layout, but compiler/assembler-generated relocation sources do not yet replace all handwritten constants |
| Guards/exits/no replay | `execute_region_fallback` and `native_region_bridge` share `validate_residual_window` two-pass validation of opcode, canonical operands and legal control successors; `NativeDispatchError::{Physical,Committed,SemanticAt}`; `NativeRegionContext::finish`; array alias/bounds/representation guards; `stencil_contains_call` physical-template verifier | committed-status/no-retry and post-entry fallback tests; legal effect-then-final-return no-replay test (`/tmp/quench-no-replay-test-20260905.log`); `fallback_reports_exact_fault_pc_after_prior_effect`; `native_bridge_preserves_fault_pc_after_prior_effect`; residual operand/control validation exercised by ARM loop and fallback tests (`/tmp/quench-region-validation-20260905.log`, `/tmp/quench-arm-loop-validation-20260905.log`); `region_verifier_rejects_physical_call_for_raw_abi`; hostile-region tests in full suite | Effectful/allocating/reentrant interiors exit to canonical machinery; changed operands/edges and early returns reject before any handler; raw ABIs reject physical call encodings and pointer relocations | Partial: helper/reentry fault matrices and exact finally/GC cases need broader direct coverage |
| Executable ownership/lifetime | shared `SharedStencilSlab` for composed regions, baseline numeric scalar plans, `NativeAddChainPlan`, `NativeMovePlan`, `NativePropertyPlan` and `NativeDispatchPlan`; typed entry validation (`f64_entry`/`bool_entry`/`i32_entry`/`u32_entry`/`f64x3_entry`/tagged-word); owner IDs in caches; bounded slab capacities and active-dispatch accounting; `stencil_arena::OwnedEntry` now pairs each cached typed pointer with its publishing owner token; `with_owned` validates generation before RAII-protected invocation; scalar plans rebuild after eviction | owner-token mismatch rejection and live invocation (`owned_entry_rejects_generation_mismatch_before_invocation`, 1 passed, `/tmp/quench-owned-generation-20260905.log`); full runtime **726 passed, 0 failed, 1 ignored** at source `e61b50bae` (`/tmp/quench-runtime-full-20260905-owner-final.log`); host check passes (`/tmp/quench-node-check-20260905-owned-entry.log`) | Shared plans retain the slab owner, validate owner identity before every cached scalar call, and increment active-use accounting through return/unwind; stale addresses fail closed even if virtual memory is recycled; composed entries render per call and remain owner-checked; allocation pressure remains globally bounded; failed publication clears per-plan cache | Partial: owner retirement is synchronous and not yet coordinated with helper/reentry lifetimes; broader active-code accounting remains incomplete |
| Diagnostics | `execution_trace::stencils`, `stencil_iterations`, `stencil_storage`, `NativeRegionPlan::last_native_execution`; test-only invocation-local `NativeBinaryPlan::native_entry_count` witness; `Counters::default` now explicitly initializes opcode arrays on the stable toolchain | ordinary-source bitwise admission and execution are separate non-vacuous tests (`/tmp/quench-bitwise-admission-20260905.log`, `/tmp/quench-bitwise-execution-20260905.log`); trace-enabled indexed-increment native-driver test **1 passed** (`/tmp/quench-get-inc-trace-20260905.log`); trace-enabled lane-profile tests **11 passed** (`/tmp/quench-execution-trace-default-20260905.log`); `cargo check -p quench-runtime --features execution-trace` passes | Modeled callbacks are not counted as native witnesses; shared leaf pointers are not retained across evictable owners | Partial: rejection/exit reason and per-region resource facts need broader end-to-end emission |
| Values/locals/frames | `FunctionCode::required_register_count`, frame construction and reusable register windows; `NativeMovePlan` serves generated `LoadLocal` and guarded `StoreLocal` `TaggedWord` declarations: `Environment::proven_word_ptr` and `can_store_proven_tagged_bits` reject cells/invalid bindings before bytes, while `store_proven_tagged_bits` performs the ownership-preserving commit; baseline and optimizing drivers route typed transfers before canonical local handling; `NativeLoadConstPlan` covers primitive tagged constants | `native_load_local_uses_declared_tagged_word_entry` and `baseline_load_local_uses_native_tagged_word_for_proven_slot` **2 passed** (`/tmp/quench-load-local-20260905.log`); `native_store_local_uses_declared_tagged_word_entry` and `baseline_store_local_commits_native_tagged_word_once` **2 passed** (`/tmp/quench-store-local-20260905.log`); immutable target remains at native-entry count 1 in the latter test; `proven_word_pointer_rejects_cells_and_tdz` **1 passed** (`/tmp/quench-local-pointer-safety-20260905.log`); full runtime at `5d91eacae` **696 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-store-local-fixed.log`) | TDZ/deleted/cell/missing/immutable/heap-owning locals stay on canonical handlers; only initialized non-cell loads and mutable non-cell stores use typed entries; register/slot ownership is retained/released by canonical word writers; primitive Number/Boolean/Null/Undefined words use `ConstantWord`; B-class calls retain complete ordinary boundaries | Partial: canonical capture/frame root layout remains incomplete; direct call-entry templates are optional and not claimed |
| Properties/prototypes/arrays | property get, declaration-driven `store_property` tagged-word body, generated `array_get_number`/`array_set_number` and `array_numeric_update` `ArrayKernel` declarations in `vm_runtime`; `BaselinePlan` ranks generated raw-array ABI over older bridges for equal opcode spans; raw-array rows are generated executable only on ARM64, with other targets explicitly rejecting to bridge/fallback | Named property get/store and hostile source/target tests above; direct ARM64 dense indexed load/routing **2 passes** (`/tmp/quench-array-get-native-20260905.log`, `/tmp/quench-array-get-routing-20260905.log`); dense indexed store with Infinity payload/out-of-bounds fallback **1 pass** (`/tmp/quench-array-set-native-20260905.log`); fused indexed update and alias fallback **1 pass** (`/tmp/quench-array-update-native-20260905.log`); full runtime **715 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-array-update.log`) | Plain dense numeric indexed loads/stores, guarded own-data reads/writes, and a fused indexed numeric update; getters/proxies/holes/transitions/cells/indirect sources/mutations and destructive operand aliases fall back; bridge and raw-array ABIs are distinct and selected by declaration contract; non-ARM raw templates are non-executable | Partial: broader indexed/property bundles, incremental widening and optional mutation tokens remain unimplemented |
| Prototype property guarded body | `run_baseline_completion_step_from_with_hook` reuses `NativePropertyPlan` for a bounded four-link chain via `quickened_prototype_slot_data`; each owner rejects replacement/dictionary/accessor/shadowing before returning a `SlotWord` pointer, and the existing tagged-word ABI performs the load | `ordinary_residual_prototype_get_executes_guarded_property_stencil` **1 passed** with native-entry witness (`/tmp/quench-prototype-native-20260905.log`); own-property regression also **1 passed** in the same log; full runtime **701 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-prototype.log`) | Immediate plain data prototypes only; mutation, accessors, proxies, sparse/exotic chains and chains deeper than four owners use complete canonical lookup; no stale pointer retained across calls | Partial: dependency-token caching is not needed for correctness but chain traversal remains per hit; native set/prototype bundles are not implemented |
| Calls/construct/objects/async/iteration/strings/BigInt/RegExp | Existing canonical VM handlers and bridge plans | full runtime/host tests | Complete ordinary semantics remain authoritative; no unsupported native interior is admitted | Boundary integration only; no claim of native bodies for these families |
| Verification | Runtime crate full lib suite, emitted-byte tests, ordinary residual admission and normal-driver comparison/bitwise/unary/property/indexed-load/store/fused-update/primitive-constant/truthiness/nullish/local transfer/identity execution tests; host tests | Latest source `e9c78e4d7`: full runtime **726 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-unary-encoder.log`); raw GPR clobber rejection **1 passed** (`/tmp/quench-gpr-clobber-reject-20260905.log`); generated-template verifier **1 passed** (`/tmp/quench-gpr-clobber-contract-20260905.log`); owner-generation test **1 passed** (`/tmp/quench-owned-generation-20260905.log`); host library **4 passed, 0 failed** (`/tmp/quench-node-full-20260905-gpr-clobber.log`); prior generated declaration-routed array, loop, fused-update, ARM byte and hostile fallback tests remain listed above | No fixture/source recognition; no micros run | Gate remains **NOT COMPLETE** until partial rows have concrete implementation/tests |

No micros command has been run for this matrix. Task 073 remains pending until
all mandatory infrastructure rows and cross-layer contracts are complete.
The current source revision `47fb4b639` also has an invocation-level
interrupt-status transport test (`/tmp/quench-interrupt-status-20260905.log`):
an interrupt after entry is classified as committed and cannot become an
entry retry.
The same revision's full runtime regression is **719 passed, 0 failed, 1
ignored** (`/tmp/quench-runtime-full-20260905-interrupt-status.log`).
Scalar baseline owner eviction/rebuild now has an actual plan-driver test
(1 passed, `/tmp/quench-scalar-owner-eviction-20260905.log`), and the full
runtime at `615f20ef2` is **720 passed, 0 failed, 1 ignored**
(`/tmp/quench-runtime-full-20260905-owner-eviction.log`).
The composed indexed-update plan also evicts and rebuilds its shared owner in
the normal-driver test (1 passed, `/tmp/quench-composed-owner-eviction-20260905.log`);
its full runtime remains **720 passed, 0 failed, 1 ignored**
(`/tmp/quench-runtime-full-20260905-composed-owner.log`).
The fused indexed-update declaration also matches ordinary frontend lowering:
`ordinary_source_lowering_executes_fused_indexed_numeric_update` passed at
`375445281` (`/tmp/quench-array-update-source-20260905.log`), while its
destructive-alias sibling remains canonical fallback.
The host/API boundary also compiles against this source (`cargo check -p
quench-node --no-default-features`, `/tmp/quench-node-check-20260905-fused-update.log`).
The same host check was repeated after the constant-update admission
(`/tmp/quench-node-check-20260905-fused-const-update.log`).
The indexed-increment body is exercised through ordinary baseline routing with
numeric output and string-index fallback (`/tmp/quench-get-inc-native-20260905.log`).
Its destructive destination/index alias is now rejected before raw entry and
the canonical path preserves the loaded value (`/tmp/quench-get-inc-alias-20260905.log`;
focused 1 passed, full runtime 722 passed, 0 failed, 1 ignored at `ca616f99a`).
Unsigned-right-shift native execution now records the first rendered integer
entry and preserves the uint32-as-Number result, including masked fractional
counts (`/tmp/quench-uint32-shift-20260905.log`; focused 1 passed, full runtime
723 passed, 0 failed, 1 ignored at `9d2c80e8c`).
The ordinary-source ARM loop driver now resumes at the exact region PC after
an interruptible native iteration (rather than `pc + 1`), clears the request,
and completes the remaining iterations without replay; focused execution and
the full runtime both pass (`/tmp/quench-normal-loop-interrupt-20260905.log`,
`/tmp/quench-runtime-full-20260905-normal-loop-interrupt.log`; 1 and 723 passed).
Both baseline and composed finish paths now decode the C status through the
same typed `NativeStatus` state machine; transport tests pass for committed,
interrupt and malformed statuses (`/tmp/quench-native-status-typed-20260905.log`,
`/tmp/quench-native-status-dispatch-20260905.log`). Full runtime at
`fb7a3924d`: **720 passed, 0 failed, 1 ignored**
(`/tmp/quench-runtime-full-20260905-typed-status.log`).
The decoder now lives in the cohesive `vm_native_status.rs` boundary module;
the source split itself is covered by the same passing suite.
The ARM loop template now derives its B/B.cond/CBNZ displacements from named
build-time labels; emitted-loop tests and `QUENCH_VERIFY_STENCIL_ENCODINGS=1`
both pass (`/tmp/quench-loop-label-encoding-20260905.log`,
`/tmp/quench-loop-label-assembler-20260905.log`). Full runtime at
`899e1f043`: **720 passed, 0 failed, 1 ignored**
(`/tmp/quench-runtime-full-20260905-loop-labels.log`).
Unknown-status entry-boundary coverage is **1 passed**
(`/tmp/quench-native-status-unknown-20260905.log`); full runtime at
`809c861af` is **721 passed, 0 failed, 1 ignored**
(`/tmp/quench-runtime-full-20260905-status-boundary.log`).
`NativeRegionPlan` now retires cached physical bytes on both physical and
post-entry committed failures; direct retirement coverage passes
(`/tmp/quench-composed-retire-20260905.log`) and full runtime at
`1212ea6a6` is **722 passed, 0 failed, 1 ignored**
(`/tmp/quench-runtime-full-20260905-composed-retire.log`).
The same retirement test now confirms ordinary semantic errors preserve valid
physical code; focused coverage is **1 passed**
(`/tmp/quench-region-retire-distinction-20260905.log`) and full runtime at
`69f34f47e` is **722 passed, 0 failed, 1 ignored**
(`/tmp/quench-runtime-full-20260905-retire-distinction.log`).
The host library regression suite also passes at this implementation state:
**4 passed, 0 failed** (`/tmp/quench-node-full-20260905-generated-abi.log`).
Latest runtime validation after raw-clobber fail-closed handling is **724
passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-clobber-latest.log`).
