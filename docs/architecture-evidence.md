# Task 075 — current stencil infrastructure matrix

Status: **gate CLOSED / NOT COMPLETE**. Current default, generated-object,
execution-trace and host evidence is bound to implementation commit
`b98047a282` for the full default and generated-object runtime runs. Execution-trace,
extractor and host evidence remains at `cd66fb57ff`. Micros and
task 073 remain deferred until every required row passes with normal-source and
non-vacuous native evidence.

| Area / required family | Implementation and normal wiring | Executed evidence at this revision | Support and remaining gap |
| --- | --- | --- | --- |
| Canonical declarations | `build_stencil_contract.rs` is the shared build-time representation of `RegionDeclaration`, `DeclAbi` and macro-derived Rust leaf/assembly recipes; both `build.rs` and the executable extractor test consume it. Every generated recipe declares its exact region name, ABI, residual shape and ARM hole layout; leaf recipes also own signature/result/body. Selection rejects any mismatch instead of inferring compatibility from names or opcode strings. `PhysicalStencilView` couples one canonical record to exact selected bytes, ABI, entries, target and identity. Region IDs are stable name-derived declaration facts with build-time collision rejection. Build provenance hashes every declaration and exact rendered Rust source; artifact identity additionally hashes extracted bytes, verified holes, fallthrough and typed relocations. The 25-line build facade composes cohesive Rust-only encoder, declaration, verifier and catalog-render units; every handwritten build unit is under 500 lines. Three declaration slices contain every region fact once, and one flattened build-time view feeds artifact extraction, ABI generation, keys, rows, accessors and validation in original order. Cargo watches every included source explicitly. | At `4a610b1a4c`, default runtime 826/826 + extractor 6/6 + routing 1/1 + docs 2/2; generated runtime 832/832 + extractor 6/6 + routing 2/2 + docs 2/2; Node host 14/14. Logs: `/tmp/quench-runtime-build-catalog-default.log`, `/tmp/quench-runtime-build-catalog-generated.log`, `/tmp/quench-node-build-catalog.log`. | Generated views reject incompatible layouts before entry. Several legacy encoder/verifier functions remain above the 40-line function cap and need cohesive decomposition; this row is not complete from file splitting alone. |
| Typed capability publication/lifetime | `stencil_arena.rs` keeps non-owning `EntryToken` private and constructs it only through ABI-specific macro-generated constructors after the typed accessor verifies publication. A retaining `AllocationLease` rechecks owner/generation/ABI/address and records the exact owning slab. Reentrant invocation releases pool borrows before machine entry; per-owner lease counts prevent reclamation of the executing allocation without pinning unrelated idle slabs. `LeaseState` is the single retirement fact for a published allocation generation: it rejects new tokens/leases, excludes retired slabs from rendering, leaves an active lease callable and charged, permits fresh publication elsewhere, and reclaims the retired mapping on final release. `machine.rs::PhysicalState::apply_dispatch_outcome` wires committed Dispatch/Region outcomes to physical retirement before clearing logical state. | At `e66d55fdd2`, `retired_generation_stays_charged_until_final_lease_release` passes 1/1 with real rendered Add bytes, stale-token rejection, 4 KiB retired-live charging, an independent 4 KiB replacement, execution through the old active lease, and exact per-pool reclamation. All arena tests pass 52/52; default/generated runtime results are in Verification. At `798ac3f0ff`, normal-driver Bridge success/throw/reuse passes 2/2 with nested native execution and GC root survival. | Active allocation isolation, stale-address rejection, independent idle reclamation, semantic reuse, committed retirement, final-owner release and per-pool/global code caps are covered. `stencil_cache.rs` owns process-wide cache metadata charging. A compiled-owner aggregate for all admission metadata and cohesive extraction from the oversized arena module remain. |
| Residual CFG/admission | `BaselinePlan` owns an allocation-free `None` for cold/disabled admission or one `Rc<AdmissionStorage>` containing a compact `AdmissionSpan` per PC and sparse typed alternatives. The optimizing view borrows entries and shares that single owner instead of cloning an admission `Rc` into every PC. Admission metadata has a conservative 256 KiB owner charge and 16 MiB process charge; base and per-entry charges are reserved before builder retention, exhaustion is monotonic, and the final owner releases the charge. When policy exposes no physical admission, construction returns before operand-window collection, CFG/liveness analysis, admission spans or specialization allocation; the retained empty slab owns no executable mapping. `stencil_cfg.rs::ControlFlowFacts` is the single immutable bounded derivation of fixed two-edge successor sets, predecessor edges, legal region entries and register liveness from canonical `BaselineEntry` control and operand-window facts. It is computed once per baseline plan and shared by every admission candidate and liveness round; malformed edges reject fail-closed without allocating temporary successor vectors. Its worklist budget fails closed to all observed register uses rather than returning a partial live set. The cohesive 429-line `stencil_value_graph.rs` owns the disposable eight-node value/use graph, versioned `ValueId`s, deterministic source/expression numbering, exact constant propagation, local value numbering and backward marking; the 351-line `stencil_plan.rs` owns physical recipe bindings and cost decisions. The graph preserves a nonconstant ordered `(a+b)+c` tree across intervening lowered local loads and selects the existing three-input physical recipe without reassociation. Admission ranks candidates by declared removed-work/transfer cost and span; exhausting bounded lookahead preserves the best prior legal selection. A comparison plan derives one bounded compare-to-`JumpIfFalse` transition: it crosses only complete, pure definitions proven dead by liveness, rejects malformed targets and illegal external entries, and supplies both successors to the typed physical body. | At `b98047a282`, default runtime passes 883/1 ignored and generated-object runtime 889/1 ignored (`target/task075/runtime-native-compare-family-{default,generated}.log`). The focused comparison contract passes 8/8 in both configurations. The independent optimizer matrix remains 26/26 for folding, value numbering, effect/control rejection, liveness, ordering and bounded selection. | All six Number comparison predicates now perform ordered comparison and successor selection in emitted ARM64 code. General reusable multi-block fragment planning remains; effect-scoped guard reuse, strength-reduced recipes, hot/cold layout and verified peepholes remain bounded follow-on work. |
| Region ABI and exits | `stencil_select.rs::RegionAbi/RegionContract`, `vm_runtime.rs` typed status/fault-PC paths, and `native_region_bridge` provide bounded scalar/word/array contracts. Helper-capable Bridge regions execute only the canonical operation and return a `CallContinuation`; the retaining native lease is gone before the outer driver performs host/JS reentry. Structured suspension retains the original lowered phase range plus its exact resume PC, so compact branch targets do not change meaning; installed outer frames remain authoritative across nested re-suspension. The VM result boundary maps only canonical `Completion::Normal` to `undefined`; Node no longer normalizes the internal `MissingReturn` fault into exit-zero success. | At `a35ecd4187`, rebuilt debug CLI SHA-256 `a6625bec2d4c77373581259e919151abf8adda7f802cdd3b016393258cbc7d05` matches Node on all 11 finite continuation cases: nested await/generator, try/finally yield, source/injected return, await in while-test/for-init/for-update/do-while, and suspended capture rooting. The two normal-driver Bridge tests continue to cover actual physical entry, borrow-free reentry, exact Return/error propagation, caller roots, exactly-once effects and reuse after throw. | Internal failure sites still need richer fixed reason/code/PC data. Additional allocating-helper/finally boundaries, native backedge root maps, abandoned-cycle reclamation and all post-effect outer-driver cases remain. |
| Actual native N/G bodies | Rustc-generated whole leaves cover f64 arithmetic, ordered/equality comparisons, tagged identity, exact i32 bitwise/shift operations, negate, increment and numeric truthiness. Typed Rust `global_asm!` recipes cover ARM move/load-local/store-local, patched primitive constants, tagged nullish/truthiness, pointer truthiness, own/prototype property guards, dense numeric get/set/get+increment/update kernels, numeric loops, and every Number comparison plus branch selection. `NativeCompareBranchContext` has compile-time size/alignment/offset assertions; its closed ABI entry publishes the Boolean and exactly one declared residual PC while a retaining allocation lease remains active. The templates route unordered values explicitly, so equality/order preserve NaN and signed-zero behavior. | At `b98047a282`, ordinary source reaches each exact `compare_{equal,not_equal,less,less_equal,greater,greater_equal}_branch` physical identity in default and rustc-generated configurations. Eight tests cover every predicate, both successors, NaN, signed zero, string guard fallback, Boolean live-out, ABI mismatch rejection, dead/effectful/side-entry exclusion and 32-hit allocation reuse. Full runtime totals are in the admission row. | x86 safely retains the earlier compare leaf plus Rust successor selection because this ABI is ARM64-only. Multi-property guard reuse and general multi-block composition remain unsupported. Local-source extraction currently occurs at the one-entry boundary; only supported interiors retain values natively. |
| Relocations/publication | `stencil_arena.rs` validates mappings, W^X publication and cache ownership; `validate_physical_template` checks declaration effects. Rust object extraction uses the `object` parser with symbol-relative bounds and an explicit relocation allowlist. One generic relocation record carries section, offset, width, kind, target and addend through declared, observed and verified views. `stencil_layout.rs` is the bounded DynAsm-style derived layout: up to eight labeled fragments and sixteen typed fixups resolve into scratch bytes, then `StencilArena` copies the finished layout into one aligned allocation and publishes it once. Duplicate/undefined labels, bad fragments, overlap, alignment/range and byte budgets reject transactionally before allocation; publication failure removes ABI/cache state and restores the cursor. x86 Rel32 and ARM Branch26/conditional Branch19 fixups preserve their verified opcode fields and enforce exact target ranges; fallthrough publication uses the same symbolic path instead of manual arena-address arithmetic. | At `0a71b12599`, layout tests pass 8/8, patch tests 12/12, default runtime passes 869/1 ignored and generated-object runtime passes 875/1 ignored (`target/task075/runtime-cond-layout-{default,generated}.log`). The conditional fixup targets a symbolic cold block and rejects wrong opcodes, misalignment and both out-of-range limits. At `0fe7cb3fcb`, a real ARM64 test executes three fragments through two patched native successors; extractor, trace and host results remain recorded at their stated earlier revisions. | ARM page-pair/literal relocation families remain safe rejections. The reusable planner still needs to feed arbitrary admitted multi-block fragments into this bounded layout; current normal wiring uses the declared two-piece fallthrough and whole-loop bodies. Direct injected `mprotect` failure is not portable, though every layout/patch failure is proven pre-publication and cache-empty. |
| Guards/fallback | Admission checks declared ABI/opcode/effects; Unknown/unsupported cases use canonical handlers. Physical, semantic and committed statuses preserve fault PCs. Raw AArch64 streams fail closed on unknown instructions before entry, and all hole-free ARM templates pass the same vocabulary validator. Direct-call detection treats only BL/BLR as helper calls; BR transfers are validated through the declared bridge ABI and root/effect contract. The x86 scanner conservatively recognizes direct E8 and FF /2 indirect CALL while excluding FF /4 JMP; plausible calls reject rather than silently under-declare effects. | Runtime/trace suites pass at the current source; `region_verifier_rejects_physical_call_for_raw_abi` — 1 passed with direct/offset/indirect-call and JMP-negative cases. | Full instruction decoding for x86 prefixes/data boundaries remains; alias/live-out proof, helper throws/reentry, GC roots, interruptible backedges and finally/suspension boundaries need outer-driver tests. |
| B families | Existing VM machinery remains semantic owner; native plans reject unsupported interiors and return to ordinary execution. A supported Bridge call returns its canonical continuation before host/JS execution, keeping generated frames and pool borrows out of reentrant semantics. The cycle collector models generators as weakly registered graph nodes instead of relying only on the async suspended-root bridge; generator edges derive from canonical activation fields. Array slice executes observable Has/Get/CreateDataProperty steps in index order and performs the final throwing length Set. | At `798ac3f0ff`, normal-driver Bridge success/throw/reuse passes 2/2 with nested native admission, GC root survival and exact effects. CLI/Node differential controls pass 11/11 for structured suspension/rooting and 3/3 for slice ordering and throws; Node host passes 14/14. | Additional allocating helpers, finally/rejected-call paths and unsupported boundary families still need explicit direct coverage. Additional slice holes/inherited-getter/species-mutation tests remain required before its boundary row is exhaustive. |
| Diagnostics/budgets | `execution_trace.rs` has bounded stencil identities/rejection/outcome counters; slab/cache paths expose usage. Executable slabs have per-pool/process code bounds; `stencil_cache.rs` separately accounts bounded cache metadata process-wide. Admission storage exposes its conservative retained-metadata charge without conflating executable ownership. Invocation-local test witnesses distinguish physical Bridge entry from modeled callbacks. | At `cd66fb57ff`, execution-trace runtime passes 895 with one intentional ignore (`target/task075/runtime-cold-admission-trace.log`). Admission budget arithmetic/release tests and normal plan ownership tests are recorded in the admission row. | Render/lease attribution, forced process-admission rejection and aggregate variant/eviction reporting need integration; final-gate trace evidence must be rerun after later changes. |
| Verification | `machine_tests.rs`, bridge/planner/fusion/property/compare-branch tests, `stencil_cfg.rs`, `stencil_value_graph.rs`, `stencil_cache.rs`, `stencil_admission.rs`, `stencil_layout.rs` and `stencil_arena.rs` cover emitted arithmetic/control, selection bindings, CFG/liveness, sparse owner bounds, owner eviction/rebuild, typed physical entry, reentry/root boundaries, status transport, property guards, array execution and ordinary-source admission/execution. `CodeArena::freeze` replaces nested same-store strong links with deferred weak resolution while external owners remain explicit. | At `b98047a282`, complete default runtime passes 883/1 ignored plus extractor 10/10, generation 1/1 and docs 2/2; generated-object runtime passes 889/1 ignored plus extractor 10/10, generation 2/2 and docs 2/2. At `cd66fb57ff`, execution-trace runtime passes 895/1 ignored and Node host 14/14. Logs are under `target/task075/`. The finite continuation oracle remains 11/11 against Node. | Native hostile exits beyond tested Bridge/property boundaries, reusable multi-block CFG composition, remaining required N/G normal-driver coverage and further cohesive source-size extraction remain before closing the gate. Final same-revision trace/extractor/host reruns are required after later changes. |

Safe exclusions remain explicit: allocating/reentrant helpers, proxies/accessors,
sparse/typed-memory arrays, suspension and exotic coercions stay on complete
ordinary semantics until their root, invalidation and exact-continuation
contracts are implemented. Production ARM admission remains conservative; these
correctness tests make no performance claim.
