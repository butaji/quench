# Task 075 — current stencil infrastructure matrix

Status: **gate CLOSED / NOT COMPLETE**. Source evidence is bound to commit
`3a0241fc3` (documentation/task cleanup remains user-owned and dirty). Micros
and task 073 remain deferred until every required row passes with normal-source
and non-vacuous native evidence.

| Area / required family | Implementation and normal wiring | Executed evidence at this revision | Support and remaining gap |
| --- | --- | --- | --- |
| Canonical declarations | `build.rs::REGION_DECLARATIONS` generates `region_abi_catalog!`; `stencil_select.rs` derives ABI contracts, selection and validation. `build_stencil_artifacts.rs` derives Rust templates, rustc object extraction, fingerprints and immutable artifact tables; `select_stencil` consumes matching extracted bytes and rejects cross-target artifacts. Each generated byte slice is named once and shared by artifact metadata and its `Stencil` view. The same recipe declaration now emits scalar leaves and the fused `add_chain` artifact; normal selection is asserted to choose that generated artifact. Helper-boundary classification is derived from the declaration ABI rather than branch-byte heuristics. | `QUENCH_VERIFY_STENCIL_ENCODINGS=1 cargo check -p quench-runtime` passed (`/tmp/quench-verify-symbols-unique.log`); generated artifact consistency and selection (including `add_chain`) — 1 passed (`/tmp/quench-rust-object-selection.log`); generated-object native chain tests — 2 passed (`/tmp/quench-native-chain-generated.log`). | Build-time extraction is implemented for hole-free Rust arithmetic leaves/chains with one shared recipe mapping; unsupported toolchains and relocatable/fallthrough templates reject or use canonical bytes. |
| Typed capability publication/lifetime | `stencil_arena.rs` keeps non-owning `EntryToken` private and constructs it only through ABI-specific macro-generated constructors after the typed accessor verifies publication. The token records the generated `RegionAbi`, and `with_owned` rechecks owner/generation/ABI while holding an active-use lease. Shared plan call sites no longer extract callable pointers independently; local plans retain their arena. Constant, Move, Unary, Nullish, Truthiness, AddChain, Property and Binary use one installed enum (Binary alternatives remain explicitly typed). | Wrong-ABI, stale-generation, dispatch ABI mismatch/interior-address and owner eviction tests pass; focused native chain 2 and full runtime 740 (`/tmp/quench-runtime-full-20260905-abideclared.log`). | Shared-slab active accounting is covered; scalar-plan lifecycle consolidation and concurrent owner reclamation remain. |
| Residual CFG/admission | `machine.rs::BaselinePlan` and `vm_runtime.rs` route lowered residual operations and existing region records; ordinary-source tests exercise admission. | Existing ordinary-source admission/native tests are included in the 45-test native run. | General build-time CFG region admission with legal entries, liveness and reusable loop dataflow is incomplete. |
| Region ABI and exits | `stencil_select.rs::RegionAbi/RegionContract`, `vm_runtime.rs` typed status/fault-PC paths, and `native_region_bridge` provide bounded scalar/word/array contracts. | Native run includes status, fault-PC and array native-path tests. | Full root maps, helper/reentry/safepoint contracts and all post-effect outer-driver cases remain. |
| Actual native N/G bodies | Emitted ARM64/x86 leaves cover constants, moves, f64 arithmetic, comparisons, bitwise, truthiness/nullish and dense-array paths; `NativeAddChainPlan` composes a small numeric chain. | Native suite includes emitted-byte witnesses and ordinary-source loop/backedge tests; modeled callbacks remain labeled as modeled. | Required reusable loop with condition/load/arithmetic/store/induction/backedge and broader property/control families is not complete. |
| Relocations/publication | `stencil_arena.rs` validates mappings, W^X publication and cache ownership; `validate_physical_template` checks declaration effects. Rust object extraction uses the `object` parser with section-relative symbol bounds, rejects multiple/ambiguous executable text sections, validates exact isolated bounds/alignment and rejects undefined/undeclared relocations; Rust `global_asm!` verification uses the same rustc parser path and checks labeled entry symbols/ranges. `write_branch26` now validates the AArch64 `B` opcode before changing its immediate. | `cargo test -p quench-runtime aarch64_branch26 -- --nocapture` — 2 passed (`/tmp/quench-aarch64-branch-reloc-20260905.log`); generated and disabled selection tests each pass 1 (`/tmp/quench-rust-object-selection.log`, `/tmp/quench-rust-object-disabled.log`). | Generated leaf relocation-free objects are covered; composable named holes, local relocations, complete range/alignment matrix and publication-failure cleanup need direct coverage. |
| Guards/fallback | Admission checks declared ABI/opcode/effects; Unknown/unsupported cases use canonical handlers. Physical, semantic and committed statuses preserve fault PCs. Raw AArch64 streams now fail closed on unknown instructions before entry, and all hole-free ARM templates pass the same vocabulary validator. | Runtime/trace suites pass at the current source; `cargo test -p quench-runtime raw_template_rejects -- --nocapture` — 3 passed (`/tmp/quench-native-validator.log`); `physical_templates_match_declared_abi_before_entry` — 1 passed (`/tmp/quench-aarch64-stream-validator7.log`); ordinary-source backedge test — 1 passed (`/tmp/quench-loop-validator.log`); prior post-entry no-replay bridge test is `/tmp/quench-array-fault-pc-bridge-20260905.log`. | Alias/live-out proof, helper throws/reentry, GC roots, interruptible backedges and finally/suspension boundaries need outer-driver tests. |
| B families | Existing VM machinery remains semantic owner; native plans reject unsupported interiors and return to ordinary execution. | Full runtime and host suites pass at this revision. | Dedicated allocating/reentrant/rooting/exception boundary matrix is still missing. |
| Diagnostics/budgets | `execution_trace.rs` has bounded stencil identities/rejection/outcome counters; slab/cache paths expose usage. | Trace runtime: 754 passed, 0 failed, 1 ignored (`/tmp/quench-runtime-trace-20260905-entrytoken.log`). | Native-vs-modeled witnesses, render/lease bytes and global variant/eviction budgets need integrated reporting. |
| Verification | `machine_tests.rs` and `stencil_arena.rs` cover emitted arithmetic, owner eviction/rebuild, ABI-checked dispatch, status transport, array execution and ordinary-source admission. | Focused generated native 2; AArch64 relocation validation 2; full generated runtime 741 and trace 756 with zero failures at the latest source (`/tmp/quench-runtime-full-20260905-rustonly-final.log`, `/tmp/quench-runtime-trace-20260905-rustonly-final.log`, `/tmp/quench-aarch64-branch-reloc-20260905.log`). | Native loop zero/one/many hostile exits, root survival, and resource-lifetime integration remain before closing gate. |

Safe exclusions remain explicit: allocating/reentrant helpers, proxies/accessors,
sparse/typed-memory arrays, suspension and exotic coercions stay on complete
ordinary semantics until their root, invalidation and exact-continuation
contracts are implemented. Production ARM admission remains conservative; these
correctness tests make no performance claim.
