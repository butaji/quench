# Task 075 — current stencil infrastructure matrix

Status: **gate CLOSED / NOT COMPLETE**. Current default, generated-object,
execution-trace and host evidence is bound to implementation commit
`44eb32cf2a` plus the preserved pre-existing dirty tree. Focused CFG/publication
evidence is bound to `894defa900`. Current default,
generated-object, execution-trace and host runs are identified below. Micros and
task 073 remain deferred until every required row passes with normal-source and
non-vacuous native evidence.

| Area / required family | Implementation and normal wiring | Executed evidence at this revision | Support and remaining gap |
| --- | --- | --- | --- |
| Canonical declarations | `build_stencil_contract.rs` is the shared build-time representation of `RegionDeclaration`, `DeclAbi`, `RecipeComposition` and macro-derived Rust leaf/assembly recipes; both `build.rs` and the executable extractor test consume it. Every generated recipe declares its exact region name, ABI, residual shape, composition kind and ARM hole layout; leaf recipes also own signature/result/body. Assembly recipes declare physical operand equalities, region-boundary targets, alias exclusions and exit values once; catalog generation emits the runtime binding/output tables and build/artifact fingerprints cover those facts. `build_stencil_outputs.rs` and `build_stencil_catalog/catalog_physical.rs` isolate the output facts and their mechanical renderer. `stencil_select.rs` now contains only shared physical types and generated catalog inclusion; `stencil_physical_select.rs`, `stencil_select_optimizer.rs`, and three focused test modules derive selection, bounded fact reduction, and verification from those same facts. Every selector unit is below 500 lines. The build validator treats those production modules as one declared input set and emits rerun dependencies for each rather than depending on monolithic file layout. The scalar physical ABI is split into `ScalarF64Binary`, `ScalarF64Unary` and `ScalarF64x3`, so semantic arity cannot accidentally authorize an incompatible machine entry; `IncI` explicitly declares its two-register AddConst physical convention. Add-chain composition is one assembly-recipe fact; generation, legacy catalog tail selection, relocation validation and publication derive from it instead of matching the region name. Selection rejects any mismatch instead of inferring compatibility from names or opcode strings. `PhysicalStencilView` couples one canonical record to exact selected bytes, ABI, entries, target and identity. Region IDs are stable name-derived declaration facts with build-time collision rejection. Three declaration slices contain every region fact once, and one flattened build-time view feeds artifact extraction, ABI generation, keys, rows, accessors and validation in original order. Cargo watches every included source explicitly. | At `405efb4347`, selector, artifact, and admission contract filters pass 17/4/5. Complete default/generated/trace runtime passes 930/936/945 with one intentional ignore each; Node host passes 14/14 (`target/task075/*selector-split*.log`). Earlier executable extractor, typed-constructor, relocation-drift, and ordinary-source native-loop coverage remains. | Generated views reject incompatible layouts, operand wiring, exit materialization and scalar signatures before entry. Several legacy encoder/verifier functions remain above the 40-line function cap and need cohesive decomposition; this row is not complete from file splitting alone. |
| Typed capability publication/lifetime | `stencil_arena.rs` keeps non-owning `EntryToken` private and constructs it only through ABI-specific macro-generated constructors after the typed accessor verifies publication. A retaining `AllocationLease` rechecks owner/generation/ABI/address and records the exact owning slab. Reentrant invocation releases pool borrows before machine entry; per-owner lease counts prevent reclamation of the executing allocation without pinning unrelated idle slabs. `LeaseState` is the single retirement fact for a published allocation generation: it rejects new tokens/leases, excludes retired slabs from rendering, leaves an active lease callable and charged, permits fresh publication elsewhere, and reclaims the retired mapping on final release. `machine.rs::PhysicalStorage` is the single local-or-shared storage authority for scalar, fused, property and dispatch plans; region plans now require their shared executable owner rather than representing an impossible owner-less state. `PhysicalState::apply_dispatch_outcome` wires committed Dispatch/Region outcomes to physical retirement before clearing logical state. | At `9c86bbd0c3` plus the preserved dirty tree, focused owner/admission/native-loop checks pass and complete default/generated runtime runs pass 917/923 with one intentional ignore (`target/task075/runtime-region-owner-{default,generated}.log`). Earlier active-lease retirement and normal-driver Bridge reentry tests remain unchanged. | Active allocation isolation, stale-address rejection, independent idle reclamation, semantic reuse, committed retirement, final-owner release and per-pool/global code caps are covered. `stencil_cache.rs` owns process-wide cache metadata charging. Compiled-owner aggregate/cohesive arena extraction remains open. |
| Residual CFG/admission | `BaselinePlan` owns an allocation-free `None` for cold/disabled admission or one `Rc<AdmissionStorage>` containing a compact `AdmissionSpan` per PC and sparse typed alternatives. The optimizing view borrows entries and shares that single owner instead of cloning an admission `Rc` into every PC. Admission metadata has a conservative 256 KiB owner charge and 16 MiB process charge; base and per-entry charges are reserved before builder retention, exhaustion is monotonic, and the final owner releases the charge. When policy exposes no physical admission, construction returns before operand-window collection, CFG/liveness analysis, admission spans or specialization allocation; the retained empty slab owns no executable mapping. `stencil_cfg.rs::ControlFlowFacts` is the single immutable bounded derivation of successors, predecessors, legal entries, canonical operands and register liveness. Its `RegionControlPlan` derives up to eight block entries and sixteen explicit internal, backedge or external-exit edges from that same data; it preserves two logical conditional exits even when both target the same PC while exposing a deduplicated successor view to liveness. `RegionControlPlan::matches_operations` verifies each retained edge against canonical `Opcode::control_flow()` before entry; external entries, malformed edges, loops outside the supported physical contract and budget overflow reject. `RegionControlPlan::permits_transfer` is now the single query used by physical layout to authorize a sequential edge, branch, backedge or exact external exit. `stencil_region_layout::FragmentPlacement` maps each physical fragment label to one operation or external-exit `RegionPoint`; duplicate/missing placements and every fixup that is not a real CFG transfer reject before composition. Catalog regions, compare branches, Add chains, generic region selection and disposable value windows consume this plan in normal admission instead of repeating range booleans. Add-chain, comparison and `NativeRegionPlan` retain their derived plan through typed selection and entry/publication verification; `CompareBranch` no longer stores parallel span/false-target facts. Generic region ranking now returns `(record, control)` as one admitted value, and the ordinary-source indexed-update witness checks that exact retained range. Test-only bridge constructors remain explicitly control-less and are not normal-admission evidence. An embedded Add-chain is explicitly a closed two-operation physical subrecipe rather than a false outer-CFG claim. `stencil_binding.rs` resolves generated operand contracts against baseline/live instruction windows, rejects Unknown operands, checks boundary targets and aliases, and proves every nonterminal live definition has a declared physical output. The numeric-loop executor no longer owns parallel positional or live-out assignment forests. `stencil_value_graph.rs` owns the disposable eight-node value/use graph, exact constant propagation, local value numbering and backward marking; `stencil_plan.rs` owns physical recipe bindings and cost decisions. Fixed-arity method and direct calls use one declared operand-window fact for every supported arity. Every frontend reducer now retains its monotonic register high-watermark through linking: top-level, function, arrow, dynamic-function, eval and multi-script/module-sequence programs all consume the lowering-owned frame fact rather than reconstructing it from cold indices. | At `182d064162`, selected-layout tests pass 6/6, CFG tests 14/14 and Add-chain integration tests 10/10 (`target/task075/{stencil-region-controlled-publication,stencil-cfg-cohesive-liveness,add-chain-controlled-publication}-current.log`). They include a legal internal branch/backedge pair, exact external exit PCs, transactional rejection, and local/shared Add-chain publication through the retained plan. Earlier frontend/frame and ordinary-source native-loop evidence remains. | The bounded multi-block control plan is retained by normal generic admission and now reaches actual Add-chain publication, but the generic fragment layout still consumes only existing declared compositions. Selecting typed artifact fragments and external exit stubs for arbitrary admitted block plans remains. Intentionally manual residual constructors conservatively derive widths. Effect-scoped guard reuse, declared strength-reduced recipes and profile-backed hot/cold layout remain bounded follow-on work. |
| Region ABI and exits | `stencil_select.rs::RegionAbi/RegionContract`, `vm_native_outcome.rs`, and `native_region_bridge` provide bounded scalar/word/array contracts. Both bridges store exactly one typed `Transition`, `Throw { pc, error }`, or `InternalFailure { pc, message }`; the numeric C status validates that outcome and cannot independently select stale result/error/location fields. Semantic and committed failures therefore retain their exact residual PC through recording, ABI decoding, context finish and every outer native driver. Composed array failure sites derive the unique committed operation or exit from the admitted operation contract instead of duplicating positional offsets. A malformed pair is retryable only before entry and non-retryable afterward. Helper-capable Bridge regions execute only the canonical operation and return a `CallContinuation`; the retaining native lease is gone before the outer driver performs host/JS reentry. Structured suspension retains the original lowered phase range plus its exact resume PC, so compact branch targets do not change meaning; installed outer frames remain authoritative across nested re-suspension. Promise rejection observation is one lifecycle transition shared by `await` and `.then(_, onRejected)`; it removes a synchronously queued unhandled entry before catch/finally continuation. The VM result boundary maps only canonical `Completion::Normal` to `undefined`; Node no longer normalizes the internal `MissingReturn` fault into exit-zero success. `exceptions::finish_try_completion` is the single post-body transition used by the production try driver and native-boundary tests, so materialized native live-outs, semantic throws and finalizer ordering cannot diverge. | At `b55fd4e483`, continuation contracts pass 4/4, Promise lifecycle tests pass 4/4, and a plain-source Node differential emits the same `before|catch:x|finally` sequence. Complete default/generated/trace runtime runs pass 930/936/945 with one intentional ignore each; Node host passes 14/14 (`target/task075/*await-rejection*.log`). At `f4e3b9e467`, two native exception-boundary cases pass in default and generated configurations: actual native Add materializes its live-out before Throw and one finalizer execution, while a string/Number guard miss performs canonical concatenation before the same boundary (`target/task075/stencil-exception-boundaries-current{,-generated}.log`). Earlier focused status/fault/no-replay tests execute 11 passing cases and retain the exact operation PC. | Allocating/reentrant native interiors remain explicit pre-entry exits. Native pending-exception root transfer and additional post-effect outer-driver failures remain. |
| Actual native N/G bodies | Rustc-generated whole leaves cover f64 arithmetic, ordered/equality comparisons, tagged identity, exact i32 bitwise/shift operations, negate, increment and numeric truthiness. Typed Rust `global_asm!` recipes cover ARM move/load-local/store-local, patched primitive constants, tagged nullish/truthiness, pointer truthiness, own/prototype property guards, dense numeric get/set/get+increment/update kernels, numeric loops, and every Number comparison plus branch selection. Add→Add is now two independently compiled ARM fragments: the head retains the first result in `d0`, a checked Branch26 successor links to the tail, and only the tail returns. Its normal admission carries the canonical residual span into selection and both local/shared publication paths, so byte compatibility alone cannot authorize the entry. `LocalResultBinding` is now the single result/register/optional-local-store fact for numeric, exact bitwise/shift, and guarded-property selections; a small macro derives the identical span/cost transition for each selection family. Entry guards prove numeric sources and a non-cell mutable target before physical execution. The native integer ABI performs canonical ToInt32 conversion, including NaN/infinity/truncation and masked shift counts, while unsigned right shift retains its uint32 Number result. The ABI-specific commit edge stores the tagged result while skipping the scalar store dispatch. Local predicate admission derives `LoadLocal`, optional nullish classification, intervening dead pure definitions, `JumpIfFalse`, both successors, live source and discarded registers from canonical CFG/use-def facts. One bounded typed enum selects the existing truthiness or nullish physical ABI rather than maintaining parallel fusion mechanisms. The commit jumps directly to the selected successor, preserves a live coalescing source and clears skipped definitions so stale owners are not retained. Unsupported truthiness tags reject before entry. Compare-and-branch derives both continuation PCs and its eliminated-dead-operation span from the same CFG control plan and revalidates that plan against the typed physical view before publication. `NativeCompareBranchContext` has compile-time size/alignment/offset assertions; its closed ABI entry publishes the Boolean and exactly one declared residual PC while a retaining allocation lease remains active. The templates route unordered values explicitly, so equality/order preserve NaN and signed-zero behavior. | At `44eb32cf2a`, four predicate-fusion tests cover algorithmic liveness, ordinary-source truthiness and nullish/coalescing execution, numeric/tagged/pointer/string values, both successors, live-source materialization, scalar-leaf elimination, unsupported truthiness fallback and stale-root release. Complete same-tree runtime runs pass 937 default, 943 generated-object and 952 execution-trace with one intentional ignore each; Node host passes 14/14 (`target/task075/*predicate-current*.log`). Earlier planner/numeric/property-store, Add-chain, comparison, bitwise and generated-artifact identity evidence remains. | x86 safely retains the earlier compare and Add-chain encodings where the Rust fragment generator is ARM64-only. Multi-property guard reuse and general multi-block composition remain unsupported. Local-source extraction currently occurs at the one-entry boundary; only supported interiors retain values natively. `InitLocal` is intentionally not folded into this mutable-store contract because TDZ/immutable initialization has a distinct semantic edge. |
| Relocations/publication | `stencil_arena.rs` validates mappings, W^X publication and cache ownership; `validate_physical_template` checks declaration effects. Rust object extraction uses the `object` parser with symbol-relative bounds and an explicit relocation allowlist. One generic relocation record carries section, offset, width, kind, target and addend through declared, observed and verified views. `stencil_layout.rs` is the bounded DynAsm-style derived layout: a `StencilFragment` list retains each selected stencil's declared holes and its own typed patch bindings while up to eight symbolic labels, sixteen typed fixups, thirty-two holes and 4096 bytes are checked before copying. `stencil_region_layout.rs` now derives fragment edges from the selected artifact's exact relocation/target facts; the arena no longer rediscovers branch kind or successor sites. Every relative hole must have exactly one compatible fixup and every fixup must name a declared hole; duplicate/undefined labels and missing, duplicate, mismatched or undeclared bindings reject before fragment patch buffers are created. Per-fragment non-relative values and internal edges then resolve in scratch bytes, and `StencilArena` copies the finished layout into one aligned allocation and publishes it once. x86 Rel32 and ARM Branch26/conditional Branch19 holes and fixups preserve their verified opcode fields and enforce exact target ranges. `HoleKind::CondBranch19` now travels through the same patch-value, stencil-validation and transactional composition path rather than existing as a layout-only special case. After validation, the narrow local peephole replaces only an ARM64 unconditional branch to the immediately following instruction with an architectural NOP, preserving every layout offset while removing the taken transfer; conditional and non-adjacent edges remain patched branches. | At `5c6a11a488`, default arena tests pass 55/55; selected-layout tests pass 2/2; generated-object selected-layout and fallthrough tests pass 2/2 each; actual generated Add execution passes 1/1. The generated view's two typed edge sites name one canonical successor, while mixed/wrong target labels, substituted layouts and unknown identities reject transactionally before allocation/publication. At `894defa900`, patch tests pass 12/12, layout tests 17/17, default/generated controlled-layout tests pass 7/7 each, and complete default/generated runtime suites pass 941/947 with one intentional ignore each (`target/task075/{stencil-cond-branch-*,runtime-all-cond-branch-current-*}.log`). Coverage includes an actual two-exit conditional fragment composition and pre-patch duplicate/undefined-label rejection. At clean `581dc60426`, generated Add-chain extraction preserves target `q_add_chain_tail`, zero addend and Branch26 offset 4; actual execution witnesses head+tail publication rather than a whole-function substitute. | ARM page-pair/literal relocation families remain safe rejections. The reusable CFG planner still needs to feed arbitrary admitted multi-block fragments into this bounded selected-view layout; current normal wiring uses declared two-piece fallthrough/Add-chain and whole-loop bodies. Direct injected `mprotect` failure is not portable, though every layout/patch failure is proven pre-publication and cache-empty. |
| Guards/fallback | Admission checks declared ABI/opcode/effects; Unknown/unsupported cases use canonical handlers. Pre-entry rejection is the only retryable physical result. Semantic throws and committed internal failures each preserve a contract-derived fault PC; post-entry physical faults are converted once into typed non-retryable outcomes. Raw AArch64 streams fail closed on unknown instructions before entry, and all hole-free ARM templates pass the same vocabulary validator. Direct-call detection treats only BL/BLR as helper calls; BR transfers are validated through the declared bridge ABI and root/effect contract. The x86 scanner conservatively recognizes direct E8 and FF /2 indirect CALL while excluding FF /4 JMP; plausible calls reject rather than silently under-declare effects. | Runtime suites pass at `a6954f4999`; `region_verifier_rejects_physical_call_for_raw_abi` previously passed 1/1 with direct/offset/indirect-call and JMP-negative cases. | Full instruction decoding for x86 prefixes/data boundaries remains; helper throws/reentry, GC roots, interruptible backedges and finally/suspension boundaries need outer-driver tests. |
| B families | Existing VM machinery remains semantic owner; native plans reject unsupported interiors and return to ordinary execution. A supported compact Bridge call returns its canonical continuation before host/JS execution, keeping generated frames and pool borrows out of reentrant semantics. Calls with receiver/argument metadata remain explicit ordinary boundaries; their continuation owns those values while a reentrant host allocation/collection runs. `PromiseData::allocate` is the only Promise construction boundary in runtime and host code. Promise continuations, generator machine/register state, pending completions and async queues now form one collector-visible ownership graph; the parallel suspended-generator registry and its manual release protocol are removed. Array slice executes observable Has/Get/CreateDataProperty steps in index order and performs the final throwing length Set. Rejected awaits preserve captured bindings and execute catch/finally effects exactly once while suppressing the now-handled source rejection. A supported native arithmetic operation may complete before an ordinary Throw/finally boundary; its live-out remains materialized and the finalizer executes once. A broken numeric fact rejects before entry and uses complete string-coercing Add semantics. | At `c7a1e9aa9c`, three Bridge/boundary tests pass: actual compact native Bridge reentry, exact-once semantic throw/reuse, and explicit unsupported receiver+argument call rooting across collection (`target/task075/stencil-bridge-boundaries-current.log`). At `44eb32cf2a`, continuation contracts pass 4/4 and the preserved eleven-case plain-source runner matches local Node (`target/stencil-review-1788646448/continuation-recheck-current-44eb32cf.json`). At `f4e3b9e467`, the two native Throw/finally boundary controls pass under default and generated-object builds. Complete default/generated/trace and host totals are recorded below. | Rejected helper calls and pending-exception native-root transfer still need explicit direct coverage; allocating/reentrant interiors remain safe ordinary boundaries. |
| Diagnostics/budgets | `execution_trace.rs` has bounded stencil identities/rejection/outcome counters; slab/cache paths expose usage. Executable slabs have per-pool/process code bounds; `stencil_cache.rs` separately accounts bounded cache metadata process-wide. Admission storage exposes its conservative retained-metadata charge without conflating executable ownership. Invocation-local test witnesses distinguish physical Bridge entry from modeled callbacks. | At `cd66fb57ff`, execution-trace runtime passes 895 with one intentional ignore (`target/task075/runtime-cold-admission-trace.log`). Admission budget arithmetic/release tests and normal plan ownership tests are recorded in the admission row. | Render/lease attribution, forced process-admission rejection and aggregate variant/eviction reporting need integration; final-gate trace evidence must be rerun after later changes. |
| Verification | `machine_tests.rs`, bridge/planner/fusion/property/compare-branch/predicate tests, `stencil_cfg.rs`, `stencil_binding.rs`, `stencil_value_graph.rs`, `stencil_cache.rs`, `stencil_admission.rs`, `stencil_layout.rs`, `stencil_arena.rs`, `continuation_contract_tests.rs` and the collector graph tests cover emitted arithmetic/control, selection bindings/outputs, CFG/liveness/control budgets, sparse owner bounds, owner eviction/rebuild, typed physical entry, reentry/root boundaries, status/outcome/PC transport, property guards, array execution, ordinary-source admission/execution, structured continuation behavior and Promise/Generator graph reclamation. `CodeArena::freeze` replaces nested same-store strong links with deferred weak resolution while external owners remain explicit. | At `f4e3b9e467` plus the preserved dirty tree, complete default runtime passes 943/1 ignored and generated-object runtime passes 949/1 ignored (`target/task075/runtime-all-exception-boundary-current-{default,generated}.log`). The latest execution-trace runtime 952/1 ignored and host 14/14 evidence remains bound to `44eb32cf2a` in `target/task075/runtime-all-predicate-current-trace.log` and `target/task075/node-all-predicate-current.log`. | Native hostile exits beyond tested Bridge/property boundaries, mapping the bounded CFG plan into generic fragment composition, remaining required N/G normal-driver coverage and further cohesive source-size extraction remain before closing the gate. Final same-revision extractor and supported-target reruns are required after later changes. |

Safe exclusions remain explicit: allocating/reentrant helpers, proxies/accessors,
sparse/typed-memory arrays, suspension and exotic coercions stay on complete
ordinary semantics until their root, invalidation and exact-continuation
contracts are implemented. Production ARM admission remains conservative; these
correctness tests make no performance claim.
