# Task 075 infrastructure evidence

This is the single current matrix for `docs/stencil-jit-implementation-spec.md`.
The gate is still open: rows marked partial or missing block micros/task 073.
Functional changes through commit `551bc13b4` are pushed to `origin/main`; the
working tree also contains the user's documentation/task cleanup edits.

| Layer / family | Code and normal wiring | Executed evidence | Supported / safe boundary | Status and remaining gap |
| --- | --- | --- | --- | --- |
| Canonical declarations | `build.rs::REGION_DECLARATIONS` generates bytes, holes, op sequences, ABI family, keys, rows and accessors; `stencil_select.rs::region_abi_catalog!` derives ABI contracts; `stencil_patch.rs::typed_writer!` derives relocation writers | `generated_region_*` (4 tests), `cargo check -p quench-runtime` | Unknown declarations reject before publication | Partial: operand/liveness/effect metadata is still derived partly in runtime validators; source files exceed the requested size limits |
| Residual control flow/admission | `ir::Opcode::{control_operands,operands_are_canonical}`, `machine::validate_region_window`, baseline plan construction; ordinary `reduce_source` reaches nested loop bodies | `ordinary_source_lowering_emits_numeric_backedge_and_executes` | Structured/unsupported loops remain ordinary residual execution | Partial: general multi-block region builder/liveness admission is not complete; current native loop shape is bounded and declared |
| Region ABI/state/roots | `stencil_select::AbiContract`, `NativeRegionContext`, `NativeArray{Kernel,Loop}Context (repr(C))`, `RegisterFile::visit_values` | raw context layout tests; full runtime suite (657 passed) | Raw numeric contexts exclude helper/reentrant operations; helpers use canonical bridge | Partial: hardware clobber/root maps and interrupt polls are not fully represented for every ABI |
| Native numeric bodies | `NativeBinaryPlan` arithmetic leaves plus strict-equality bytes; `NativeAddChainPlan`; ARM64 `AARCH64_ARRAY_KERNEL_BYTES` and `AARCH64_ARRAY_LOOP_BYTES` | `executable_equality_region_matches_numeric_semantics`; `native_strict_numeric_equality_uses_typed_scalar_entry`; `rendered_numeric_array_loop_executes_native_backedge`; `rendered_array_kernel_executes_machine_bytes` | f64 arithmetic and guarded dense numeric array loop; NaN/equality tested | Partial: comparison family is only strict numeric equality; bitwise/ordering native bodies remain ordinary fallback |
| ARM composition/control | `AARCH64_ARRAY_LOOP_BYTES` has condition, indexed load/add/store, induction update and native backedge; `NativeRegionPlan::execute` is reached from baseline/optimizing plans | `ordinary_source_lowering_emits_numeric_backedge_and_executes` (ordinary lowering + ARM opt-in witness); rendered loop test covers zero/one/many/nonzero result | ARM production policy remains conservative; unsupported ABI/target rejects | Partial: bounded loop has no machine-level interrupt checkpoint; broader branches/live-outs are not composed |
| Relocations/publication | `stencil_patch::apply_holes`, `Stencil::validate`, `StencilArena::make_executable` (W^X + I-cache flush), declaration build checks | relocation range/alignment tests; generated catalog tests; ARM byte execution | Out-of-range, overlap, alignment and unsupported target fall back | Partial: compiler/assembler-generated relocation source is not yet replacing all handwritten constants |
| Guards/exits/no replay | `execute_region_fallback` two-pass validation; `NativeDispatchError::{Physical,Committed,SemanticAt}`; `NativeRegionContext::finish`; array alias/bounds/representation guards | committed-status/no-retry and post-entry fallback tests; hostile-region tests in full suite | Effectful/allocating/reentrant interiors exit to canonical machinery | Partial: helper/reentry fault matrices and exact finally/GC cases need broader direct coverage |
| Executable ownership/lifetime | shared `SharedStencilSlab` for composed regions; owner IDs in caches; bounded slab capacities and active-dispatch accounting; scalar plans retain private arenas | shared-slab cap/active-dispatch tests; full runtime suite | Active composed entries retain slab; failed publication clears cache | Partial: scalar plans still use per-plan arenas; eviction/disposal tests do not cover every scalar entry owner |
| Diagnostics | `execution_trace::stencils`, `stencil_iterations`, `stencil_storage`, `NativeRegionPlan::last_native_execution` | trace-enabled runtime tests/builds | Modeled callbacks are not counted as native witnesses | Partial: rejection/exit reason and per-region resource facts need broader end-to-end emission |
| Values/locals/frames | `FunctionCode::required_register_count`, frame construction and reusable register windows; native Move/scalar leaves | frame/register tests in full suite | TDZ/unknown locals use canonical handlers | Partial: canonical capture/frame root layout and direct call-entry templates remain incomplete |
| Properties/prototypes/arrays | property leaf and array guards in `vm_runtime`; array facts in `value_array_data` | property/array hostile tests in full suite | getters/proxies/holes/mutations fall back | Partial: prototype dependency tokens and incremental widening are not complete |
| Calls/construct/objects/async/iteration/strings/BigInt/RegExp | Existing canonical VM handlers and bridge plans | full runtime/host tests | Complete ordinary semantics remain authoritative; no unsupported native interior is admitted | Boundary integration only; no claim of native bodies for these families |
| Verification | Runtime crate full lib suite, emitted-byte tests, ordinary lowering test; host tests previously passing | `cargo test -p quench-runtime --lib -- --test-threads=2`: **657 passed, 0 failed, 1 ignored**; focused tests above; `cargo test -p quench-node --lib`: 4 passed previously | No fixture/source recognition; no micros run | Gate remains **NOT COMPLETE** until partial rows have concrete implementation/tests |

No micros command has been run for this matrix. Task 073 remains pending until
all mandatory infrastructure rows and cross-layer contracts are complete.
