# Task 075 — current completion matrix

Status: **gate CLOSED / NOT COMPLETE**. Evidence below is current through the
`a7a656ce7d` runtime checkpoint plus explicitly identified preserved evidence.
Task 073 and micros remain deferred.

| Contract | Current implementation and normal wiring | Current executed evidence | Remaining mandatory gap |
| --- | --- | --- | --- |
| Canonical declarations | `rust_leaf_catalog!` and `rust_assembly_catalog!` each emit the `RegionDeclaration`, physical recipe and full-contract association from one datum. Generated selection compares complete bytes/holes/entry/ABI metadata rather than rediscovering identity from names and opcodes. Bridge-only declarations stay separate because they have no native recipe. `PhysicalStencilView` derives one immutable `RegionImageIdentity`; `VerifiedRegionImage` carries that identity and finalized bytes through publication, eliminating separate key/ABI/byte/cache inputs. Cache identity now derives from that same complete view: target/compiler, ABI, entries, code/data, holes, relocations, links, continuation and helper/effect boundary all participate, with sequence lengths and optional fallthrough state delimited. The internal continuation ABI, successor roles and generated-only patch holes derive from the assembly recipe. Legacy and generated layouts therefore cannot borrow each other's offsets. `stencil_region_links.rs` resolves declared roles to symbolic region points before layout. `TruthyBoolBranch`, `WordConstFragment` and `ReturnWord` declare one shared `WordX0` continuation; rustc extraction, selection and composition consume that contract. Generated-only fragments are not callable through empty legacy views. This is the useful AsmJit `CodeHolder` shape—one target-bound code/data/relocation/identity value—without importing its assembler, compiler, allocator or runtime. | At `88a9baadef`, default runtime passes 977/1 ignored and generated runtime passes 988/1 ignored; extractor passes 15/15, generated routing 4/4 and control composition 2/2. At `244e5f250e`, focused physical-contract mutation tests prove entry, ABI and target differences cannot share a cache identity. Two raw ARM64 control tests and one ordinary-source test execute actual generated bytes, including independently patched true/false literal arms. | Verify remaining required family consumers use only canonical associations. |
| Typed capability and ABI | Private `EntryToken`s are created by ABI-specific accessors after publication checks. Each exact entry address has one immutable publication record containing key, physical signature, ABI and byte length; disposable cache rows are checked against it and cannot relabel code. `AllocationLease` retains the exact generation during calls; bridge entry resolution releases slab `RefCell` borrows before reentry. Retirement rejects new leases while active leases retain the exact allocation generation. | At `a7a656ce7d`, the complete default/generated/trace runtime configurations pass 978/989/1005 tests respectively, each with one intentional ignore, plus extractor 15/15, routing 1/4 and control composition 2/2 in each applicable mode. These runs include cache-relabel rejection, ABI separation, reentry, retirement and aggregate bounds. | No capability-lifetime design gap is known for the supported entry ABIs; repeat only if later code changes this boundary. |
| Residual CFG and admission | `ControlFlowFacts`, `RegionControlPlan`, `stencil_binding.rs` and the bounded value graph derive successors, use/def, liveness, aliases, legal entries and exact exits from existing residual operations. The disposable graph performs exact folding, local value numbering, dead-pure marking and costed fusion. `PlannedFragment`/`PlannedTransfer` keep semantic region points until layout derives labels, fragment indices and fixups. `stencil_region_builder` consumes a bounded sequence of complete physical views and derives its semantic opcode sequence, every successor transfer and combined physical cache identity from that value. `RegionControlPlan::from_relative_edges` records only verified physical placement; source admission separately proves the real branch target and each constant/return arm. `NativeWordConstantBranchPlan` therefore handles non-contiguous lowered arms without demanding a source-shaped opcode adjacency pattern. Boolean values execute one generated branch/arm image; broken Boolean facts use the existing complete path. Nested `TraceSite` operations are consumed by the same source-metadata transition as top-level sites, so diagnostic builds no longer inject semantic `Slow` operations or change region admission. | At `15a094895f`, generated execution-trace runtime passes 1003/1 ignored, extractor 15/15, routing 4/4 and control composition 2/2. Ordinary source executes both Boolean arms through the generated image in trace and non-trace builds; a Number guard miss remains ordinary and does not increment the composed witness. A focused nested-branch test proves trace sites preserve source metadata without residual `Slow` instructions. | Complete effect-scoped guard reuse and remaining required source witnesses. |
| Frame/layout contract | Lowering retains a frame high-watermark; `Instruction::register_flow` and operand windows distinguish register operands from metadata such as absent `CallN` arguments. Same-frame fragments contribute width; nested functions do not. | Zero-argument sentinel and owner-retirement focused tests pass on the preserved tree. | Bind a clean revision to the full zero/one/many-argument and structured-fragment matrix, then remeasure only in task 073. |
| Region ABI, roots and exits | Native results use explicit transition, throw-with-PC, committed failure and pre-entry rejection outcomes. Helper-capable bridge calls return a canonical continuation before host/JS reentry. Promise/generator machine state participates in collector ownership. | Native exception/finalizer boundaries 3/3 in both artifact modes; bridge/root boundary 3/3; exact status/fault suite 11/11. | Add remaining hostile post-effect outer-driver, helper rejection, active-root and interrupt/safepoint cases. Allocating/reentrant native interiors remain pre-entry exclusions. |
| Structured suspension boundary | `SuspensionPoint` is the authoritative phase/destination state. Linked direct yield/await sites retain exact `CodeRange`; structured try frames derive by range containment, not first-yield scans. Nested body suspension derives its destination from the inner point, never register 0. Try continuations resume at the exact instruction after their container, preserving intervening compact operations. | At `26bc4d27d0`, continuation contracts pass 6/6; the 11-case plain-source runner matches Node 11/11. Default/generated runtime suites pass 951/957 with one intentional ignore each. | Raw-slice generator execution retains an explicit legacy no-owner form; finish abrupt active-root/reentry coverage before certifying native exits. |
| Required native N/G bodies | Actual Rust/rustc artifacts cover Number arithmetic, comparisons, exact bitwise/shift, truthiness/nullish, move/local/constant, guarded property slots, dense numeric elements, Add-chain fragments and an ARM numeric loop/backedge. Normal Move execution preserves owned identity; Add-chain links independently emitted fragments. | Current default/generated focused runs execute ordinary-source constant/local/move/load/store bodies. At `4b730c0c58`, local store matches an independently disabled canonical plan, preserves an owned tagged value and rejects an immutable binding before native entry in both artifact modes. Same-site property execution proves native stable mono/poly alternatives, megamorphic retirement to bounded fallback and accessor mutation fallback 1/1 in each artifact mode. Strict writable and non-writable stores remain in the complete ordinary boundary and never enter a non-strict native leaf, 1/1 in each mode. Arithmetic, property and indexed hostile controls from `cac53a7a78` remain passing. | Complete final all-family same-revision gate runs. Catalog or bridge-only rows do not count; general multi-property/CFG composition remains unsupported. |
| Relocation and publication | Rust object parsing preserves symbol-relative ranges and declared relocations. `stencil_layout.rs` resolves typed labels/fixups transactionally; ARM Branch26/Branch19 and x86 Rel32 are range checked before one W^X publication and cache flush. | Extractor 13/13; region edge coverage 8/8; controlled publication 4/4 in both modes. | Page-pair/literal relocations remain safe rejection. Add final malformed-object/target and failed-publication coverage from the specification. |
| B-family boundaries | Calls, proxies/accessors, sparse arrays, exotic coercions, suspension and allocation remain owned by canonical VM helpers. Array slice uses ordered Has/Get/CreateDataProperty and a throwing final length Set. Native arithmetic can exit before ordinary throw/finally without replay. | Bridge boundary 3/3; slice ordering and Promise rejection/try-finally controls pass. | Complete explicit B-family helper/root/error/catch-finally tests; no new native exotic bodies are required. |
| Resource and diagnostics | `bounded_resource.rs` owns process reservations. `stencil_arena/resource.rs` derives resident, used, retired-live, owner-cache, active-lease and process-executable facts from the slab/lease authority; trace storage records that one snapshot. Pools receive the same explicit budget authority used by production, making aggregate-owner exhaustion deterministic to test. Optional events distinguish real entry, bridge, exit and rejection. External plan-cache metadata is charged separately and intentionally excluded from the slab's `owner_cache_rows` population. | At `6db3e9ee7f`, eviction 8/8, retirement 3/3 and retired-generation 1/1 pass. Aggregate independent-owner exhaustion/release passes 1/1 at `87033116d7`; default/generated runtime suites pass 952/958 with one intentional ignore each at `1deaabeafe`. | Re-run the resource/trace matrix at the final revision; no mandatory supported-owner disposal case is currently missing. |
| Verification and size discipline | Cohesive continuation derivation is in `generator_try_path.rs` (211 lines); `generator_machine.rs`, `loops_run.rs` and `vm_generator_step.rs` are each under 500 lines. Catalog key/link rendering is split by responsibility; artifact compiler/rendering remain under 500 lines. Linear numeric composition is isolated in `stencil_region_builder.rs` (336 lines); word layout/publication is in `stencil_word_composition.rs` (366 lines); local predicate semantics/admission are in `stencil_predicate_fusion.rs` (221 lines), leaving `stencil_fusion.rs` at 363 lines. Tests use ordinary JS and independent Node oracle results. | At source revision `a7a656ce7d`, default runtime is 978 passed/1 ignored, generated runtime is 989 passed/1 ignored and generated execution-trace runtime is 1005 passed/1 ignored. Auxiliary extractor/generation/control suites pass 15/15, 1-or-4/4 and 2/2 as applicable. `quench-node` passes 16/16 with one ignored doc-test. | Final coherence review and any tests required by remaining semantic-family gaps remain mandatory. |

Safe exclusions are explicit: unsupported target/ABI combinations, allocating or
reentrant native interiors, proxies/accessors, sparse or typed-memory arrays and
exotic coercions take complete ordinary semantics. Production ARM admission stays
conservative until post-gate measurements; these tests are not speedup evidence.
