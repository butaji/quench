# Task 075 — current completion matrix

Status: **gate CLOSED / NOT COMPLETE**. Evidence below is current through
`87033116d7` plus the preserved dirty tree. Task 073 and micros remain deferred.

| Contract | Current implementation and normal wiring | Current executed evidence | Remaining mandatory gap |
| --- | --- | --- | --- |
| Canonical declarations | `build_stencil_contract.rs` and declaration macros derive IDs, ABI kinds, recipe bindings, target views and generated artifact associations. `PhysicalStencilView` carries one selected identity, bytes, entries, holes, effects and target contract through publication. | Selector/artifact/admission filters 17/4/5; Rust extractor 13/13. | Finish replacing legacy encoder duplication and verify every required family consumes the canonical declaration. |
| Typed capability and ABI | Private `EntryToken`s are created by ABI-specific accessors after publication checks. `AllocationLease` retains the exact generation during calls; bridge entry resolution releases slab `RefCell` borrows before reentry. | Owner/reentry, stale-generation and active-retirement focused tests pass in both artifact modes. | Finish aggregate retired-live accounting and remaining scalar/composed owner disposal cases. |
| Residual CFG and admission | `ControlFlowFacts`, `RegionControlPlan`, `stencil_binding.rs` and the bounded value graph derive successors, use/def, liveness, aliases, legal entries and exact exits from existing residual operations. Cold/disabled plans allocate no admission slab. | CFG 14/14; selected layout 6/6; Add-chain controlled publication 10/10. | Feed arbitrary admitted bounded block plans into selected fragment composition; complete effect-scoped guard reuse and remaining normal-source witnesses. |
| Frame/layout contract | Lowering retains a frame high-watermark; `Instruction::register_flow` and operand windows distinguish register operands from metadata such as absent `CallN` arguments. Same-frame fragments contribute width; nested functions do not. | Zero-argument sentinel and owner-retirement focused tests pass on the preserved tree. | Bind a clean revision to the full zero/one/many-argument and structured-fragment matrix, then remeasure only in task 073. |
| Region ABI, roots and exits | Native results use explicit transition, throw-with-PC, committed failure and pre-entry rejection outcomes. Helper-capable bridge calls return a canonical continuation before host/JS reentry. Promise/generator machine state participates in collector ownership. | Native exception/finalizer boundaries 3/3 in both artifact modes; bridge/root boundary 3/3; exact status/fault suite 11/11. | Add remaining hostile post-effect outer-driver, helper rejection, active-root and interrupt/safepoint cases. Allocating/reentrant native interiors remain pre-entry exclusions. |
| Structured suspension boundary | `SuspensionPoint` is the authoritative phase/destination state. Linked direct yield/await sites retain exact `CodeRange`; structured try frames derive by range containment, not first-yield scans. Nested body suspension derives its destination from the inner point, never register 0. Try continuations resume at the exact instruction after their container, preserving intervening compact operations. | At `26bc4d27d0`, continuation contracts pass 6/6; the 11-case plain-source runner matches Node 11/11. Default/generated runtime suites pass 951/957 with one intentional ignore each. | Raw-slice generator execution retains an explicit legacy no-owner form; finish abrupt active-root/reentry coverage before certifying native exits. |
| Required native N/G bodies | Actual Rust/rustc artifacts cover Number arithmetic, comparisons, exact bitwise/shift, truthiness/nullish, move/local/constant, guarded property slots, dense numeric elements, Add-chain fragments and an ARM numeric loop/backedge. Normal Move execution preserves owned identity; Add-chain links independently emitted fragments. | Ordinary-source Move 1/1 in both modes; generated AddConst/AddChain filters 24/24; ARM loop/kernel and comparison/bitwise actual-byte tests pass. | Complete the specification family audit with actual-body plus guard-miss and ordinary-driver witnesses. Catalog or bridge-only rows do not count; general multi-property/CFG composition remains unsupported. |
| Relocation and publication | Rust object parsing preserves symbol-relative ranges and declared relocations. `stencil_layout.rs` resolves typed labels/fixups transactionally; ARM Branch26/Branch19 and x86 Rel32 are range checked before one W^X publication and cache flush. | Extractor 13/13; region edge coverage 8/8; controlled publication 4/4 in both modes. | Page-pair/literal relocations remain safe rejection. Add final malformed-object/target and failed-publication coverage from the specification. |
| B-family boundaries | Calls, proxies/accessors, sparse arrays, exotic coercions, suspension and allocation remain owned by canonical VM helpers. Array slice uses ordered Has/Get/CreateDataProperty and a throwing final length Set. Native arithmetic can exit before ordinary throw/finally without replay. | Bridge boundary 3/3; slice ordering and Promise rejection/try-finally controls pass. | Complete explicit B-family helper/root/error/catch-finally tests; no new native exotic bodies are required. |
| Resource and diagnostics | `bounded_resource.rs` owns process reservations. `stencil_arena/resource.rs` derives resident, used, retired-live, owner-cache, active-lease and process-executable facts from the slab/lease authority; trace storage records that one snapshot. Pools receive the same explicit budget authority used by production, making aggregate-owner exhaustion deterministic to test. Optional events distinguish real entry, bridge, exit and rejection. | At `87033116d7`, aggregate independent-owner exhaustion/release passes 1/1. At `0502c99be5`, retirement lifecycle and trace-schema contracts pass 1/1 each. At `1deaabeafe`, default/generated runtime suites pass 952/958 with one intentional ignore each. | Add remaining scalar/composed disposal cases. External plan-cache metadata is a distinct population and is not included in `owner_cache_rows`. |
| Verification and size discipline | Cohesive continuation derivation is in `generator_try_path.rs` (211 lines); `generator_machine.rs`, `loops_run.rs` and `vm_generator_step.rs` are each under 500 lines. Tests use ordinary JS and independent Node oracle results. | Default runtime 952 passed/1 ignored; generated runtime 958 passed/1 ignored at `1deaabeafe`; continuation oracle 11/11. Logs: `target/task075/runtime-full-{default,generated}-exact-continuation.log` and `target/stencil-review-1788646448/continuation-recheck-exact-site-fixed.json`. | Final same-revision trace, host, extractor, generated-selection, actual-native, hostile safety, efficiency and lifetime runs remain mandatory after code gaps close. |

Safe exclusions are explicit: unsupported target/ABI combinations, allocating or
reentrant native interiors, proxies/accessors, sparse or typed-memory arrays and
exotic coercions take complete ordinary semantics. Production ARM admission stays
conservative until post-gate measurements; these tests are not speedup evidence.
