# Task 075 infrastructure evidence

This is the single current matrix for `docs/stencil-jit-implementation-spec.md`.
The gate is CLOSED / NOT COMPLETE: rows marked partial or missing block
micros/task 073. This matrix reflects the current working revision; the
documentation/task cleanup edits remain user-owned and are not included in
functional commits.

| Layer / family | Code and normal wiring | Executed evidence | Supported / safe boundary | Status and remaining gap |
| --- | --- | --- | --- | --- |
| Canonical declarations | `build.rs::REGION_DECLARATIONS` generates bytes, holes, op sequences, ABI family (including typed `ScalarI32`/`ScalarU32`), keys, rows and accessors; `stencil_select.rs::region_abi_catalog!` derives ABI contracts (including the loop interruptible flag); `stencil_patch.rs::typed_writer!` derives relocation writers | `generated_region_*` (4 tests), `cargo check -p quench-runtime` | Unknown declarations reject before publication | Partial: operand/liveness/effect metadata is still derived partly in runtime validators; source files exceed the requested size limits |
| Residual control flow/admission | `ir::Opcode::{control_operands,operands_are_canonical}`, `machine::validate_region_window`, baseline plan construction; ordinary `reduce_source` reaches nested loop bodies | `ordinary_source_lowering_emits_numeric_backedge_and_executes` | Structured/unsupported loops remain ordinary residual execution | Partial: general multi-block region builder/liveness admission is not complete; current native loop shape is bounded and declared |
| Region ABI/state/roots | `stencil_select::AbiContract`, `NativeRegionContext`, `NativeArray{Kernel,Loop}Context (repr(C))`, `RegisterFile::visit_values`, `VmContext::interrupt_flag` | raw context layout tests; `rendered_numeric_array_loop_poll_exits_after_committed_iteration`; `interrupt_request_is_stable_across_context_clones`; full runtime at `87529b263` (665 passed) | Raw numeric contexts exclude helper/reentrant operations; helpers use canonical bridge | Partial: the array-loop ABI and per-VM interrupt flag are tested, but hardware clobber/root maps and complete helper/GC safepoint contracts are not complete for every ABI |
| Native numeric bodies | `NativeBinaryPlan` f64 arithmetic/comparison and typed `ScalarI32`/`ScalarU32` bitwise/shift bodies; `NativeAddChainPlan`; ARM64 `AARCH64_ARRAY_KERNEL_BYTES` and `AARCH64_ARRAY_LOOP_BYTES` | `executable_equality_region_matches_numeric_semantics`; `executable_ordered_regions_reject_unordered_nan`; `executable_i32_bitwise_regions_match_signed_results`; `executable_shift_regions_mask_counts_and_preserve_unsigned_result`; `native_strict_numeric_equality_uses_typed_scalar_entry`; `native_bitwise_i32_regions_guard_number_conversion`; `ordinary_source_lowering_admits_guarded_bitwise_region`; rendered loop/kernel tests | f64 arithmetic, ordered comparisons with NaN exclusion, exact JS Number-to-Int32 conversion (including truncation, NaN/infinity and modulo-2^32) feeding native `&/|/^` and masked `<< / >> / >>>`; guarded dense numeric array loop | Partial: bitwise-not and broader comparison/control composition remain ordinary fallback |
| ARM composition/control | `AARCH64_ARRAY_LOOP_BYTES` has condition, indexed load/add/store, induction update, interrupt poll and native backedge; `NativeRegionPlan::execute` is reached from baseline/optimizing plans | `ordinary_source_lowering_emits_numeric_backedge_and_executes` (ordinary lowering + ARM opt-in witness); rendered loop tests cover zero/one/many/nonzero result and committed interrupt exit | ARM production policy remains conservative; unsupported ABI/target rejects | Partial: the bounded loop has a physical checkpoint and exact committed handoff, but broader branch/live-out composition and runtime interrupt-request plumbing remain incomplete |
| Relocations/publication | `stencil_patch::apply_holes`, `Stencil::validate`, `StencilArena::make_executable` (W^X + I-cache flush), declaration build checks; `build.rs::aarch64_b_imm26/aarch64_b_cond` derive branch words | relocation range/alignment tests; generated catalog tests; ARM byte execution; optional assembler verification passed with `QUENCH_VERIFY_STENCIL_ENCODINGS=1` (`/tmp/quench-stencil-assembler-branch-helpers.log`) | Out-of-range, overlap, alignment and unsupported target fall back | Partial: branch displacements now derive from named encoders and assembler labels verify the loop layout, but compiler/assembler-generated relocation sources do not yet replace all handwritten constants |
| Guards/exits/no replay | `execute_region_fallback` two-pass validation; `NativeDispatchError::{Physical,Committed,SemanticAt}`; `NativeRegionContext::finish`; array alias/bounds/representation guards; `stencil_contains_call` physical-template verifier | committed-status/no-retry and post-entry fallback tests; `fallback_reports_exact_fault_pc_after_prior_effect`; `native_bridge_preserves_fault_pc_after_prior_effect`; `region_verifier_rejects_physical_call_for_raw_abi`; hostile-region tests in full suite | Effectful/allocating/reentrant interiors exit to canonical machinery; raw ABIs reject physical call encodings and pointer relocations | Partial: helper/reentry fault matrices and exact finally/GC cases need broader direct coverage |
| Executable ownership/lifetime | shared `SharedStencilSlab` for composed regions, baseline numeric scalar plans, `NativeAddChainPlan`, `NativeMovePlan`, `NativePropertyPlan` and `NativeDispatchPlan`; typed entry validation (`f64_entry`/`bool_entry`/`i32_entry`/`u32_entry`/`f64x3_entry`/tagged-word); owner IDs in caches; bounded slab capacities and active-dispatch accounting | shared-slab cap/active-dispatch tests; idle-generation eviction/cache-owner test (`/tmp/quench-shared-eviction-test-final.log`); global-cap reclamation plus active-dispatch refusal (`/tmp/quench-shared-global-cap-active.log`); shared typed scalar/loop/chain/Move/property focused tests; full runtime suite | Shared plans resolve typed pointers while borrowing their owner, so no evictable slab address is retained; allocation pressure reclaims oldest idle generations only below the global cap; failed publication clears per-plan cache; dispatch execution uses the shared slab owner | Partial: owner retirement is synchronous and not yet coordinated with helper/reentry lifetimes; those contracts remain incomplete |
| Diagnostics | `execution_trace::stencils`, `stencil_iterations`, `stencil_storage`, `NativeRegionPlan::last_native_execution`; test-only invocation-local `NativeBinaryPlan::native_entry_count` witness | ordinary-source bitwise admission/driver test now asserts a native invocation (`/tmp/quench-bitwise-native-witness.log`); trace-enabled runtime tests/builds | Modeled callbacks are not counted as native witnesses; shared leaf pointers are not retained across evictable owners | Partial: rejection/exit reason and per-region resource facts need broader end-to-end emission |
| Values/locals/frames | `FunctionCode::required_register_count`, frame construction and reusable register windows; native Move/scalar leaves | frame/register tests in full suite | TDZ/unknown locals use canonical handlers; B-class calls retain complete ordinary boundaries | Partial: canonical capture/frame root layout remains incomplete; direct call-entry templates are optional and not claimed |
| Properties/prototypes/arrays | property leaf and array guards in `vm_runtime`; array facts in `value_array_data` | property/array hostile tests in full suite | getters/proxies/holes/mutations fall back; chain guards provide the required complete boundary | Partial: optional mutation dependency tokens and incremental widening are not implemented |
| Calls/construct/objects/async/iteration/strings/BigInt/RegExp | Existing canonical VM handlers and bridge plans | full runtime/host tests | Complete ordinary semantics remain authoritative; no unsupported native interior is admitted | Boundary integration only; no claim of native bodies for these families |
| Verification | Runtime crate full lib suite, emitted-byte tests, ordinary lowering/admission and normal-driver bitwise/shift execution tests; host tests | Revision `cc631cfd1`: full runtime **667 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260904-global-cap.log`); focused native witness **1 passed, 0 failed** (`/tmp/quench-bitwise-native-witness.log`); idle eviction **1 passed, 0 failed** (`/tmp/quench-shared-eviction-test-final.log`); global-cap reclamation **1 passed, 0 failed** (`/tmp/quench-shared-global-cap.log`); assembler verification `cargo check` passed (`/tmp/quench-stencil-assembler-final2.log`); host **4 passed, 0 failed** (`/tmp/quench-node-lib-20260904-global-cap.log`) | No fixture/source recognition; no micros run | Gate remains **NOT COMPLETE** until partial rows have concrete implementation/tests |

No micros command has been run for this matrix. Task 073 remains pending until
all mandatory infrastructure rows and cross-layer contracts are complete.
