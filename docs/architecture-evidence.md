# Task 075 — current stencil infrastructure matrix

Status: **gate CLOSED / NOT COMPLETE**. Current default/generated/host evidence
is bound to clean implementation commit `a1a3ada2bd`; the exact generated-loop witness remains
bound to `48bd26aa2`, and the last full trace suite remains bound to `7841a3f2b`. Micros
and task 073 remain deferred until every required row passes with normal-source
and non-vacuous native evidence.

| Area / required family | Implementation and normal wiring | Executed evidence at this revision | Support and remaining gap |
| --- | --- | --- | --- |
| Canonical declarations | `build_stencil_contract.rs` is the shared build-time representation of `RegionDeclaration`, `DeclAbi` and the macro-derived Rust leaf recipes; both `build.rs` and the executable extractor test consume it. `stencil_select.rs` derives runtime ABI contracts and validation. `PhysicalStencilView` couples one canonical record to exact selected bytes, ABI, entries, target and identity. Build provenance hashes every declaration input; artifact identity additionally hashes extracted bytes, fallthrough and typed relocations. | At `bd96a7843`, default runtime 787/787 + extractor 3/3 + routing 1/1; generated runtime 793/793 + extractor 3/3 + routing 2/2; Node host 14/14. Raw logs: `/tmp/quench-runtime-bd96-default.log`, `/tmp/quench-runtime-bd96-generated.log`, `/tmp/quench-node-bd96.log`. | The duplicate extractor declaration/recipe schema is removed. Generated views reject incompatible layouts before entry. `build.rs` and the effectful extractor remain above the size cap and need cohesive catalog/compiler/parser splits. |
| Typed capability publication/lifetime | `stencil_arena.rs` keeps non-owning `EntryToken` private and constructs it only through ABI-specific macro-generated constructors after the typed accessor verifies publication. A retaining `AllocationLease` rechecks owner/generation/ABI/address and now records the exact owning slab. Reentrant invocation releases pool borrows before machine entry; per-owner lease counts prevent reclamation of the executing allocation without pinning unrelated idle slabs. Cache-aware eviction removes only unleased owners and their derived rows. `machine.rs::PhysicalState` composes cache disposal with the existing lifecycle for LoadConst, Move, Property, Nullish, Unary, Truthiness, Binary, AddChain, NativeDispatch and NativeRegion plans; installed alternatives remain ABI-specific. | At `a1a3ada2bd`, focused lease filter 11/11 passes, including actual generated dispatch -> Rust helper -> nested typed native entry, mutable pool admission, exact single helper effect, and return. Default runtime 798/798 + extractor 3/3 + routing 1/1; generated runtime 804/804 + extractor 3/3 + routing 2/2; Node host 14/14. Logs: `target/stencil-lease-runtime-default.log`, `target/stencil-lease-runtime-generated.log`, `target/stencil-lease-node.log`. | Active allocation isolation, stale-address rejection, independent idle reclamation and per-pool caps are covered. A direct final-external-owner/retired-live aggregate accounting test and cohesive extraction from the oversized arena module remain. |
| Residual CFG/admission | `BaselinePlan` stores one compact `AdmissionSpan` per PC and sparse typed alternatives in one shared array; the optimizing view reuses it. Canonical `register_flow`, control successors and a bounded backward worklist derive live-outs. `region_admission_matches` validates declared operations, operands, legal entries and successors before publication. `stencil_plan.rs` is the first disposable selection consumer: it derives Add→Add fixed F64x3 bindings, dead-intermediate and alias checks, and a bounded benefit from canonical operations/liveness once at admission. | At `6a6725c831`, 3 planner tests and 4 fused integration tests pass, including nontrivial register bindings, live-intermediate rejection and unsafe-forwarding rejection. The full default runtime passes 797/797, generated runtime 803/803 and Node host 14/14, with one intentional runtime ignore. Logs: `target/stencil-plan-unit.log`, `target/stencil-plan-fused.log`, `target/stencil-plan-runtime-default.log`, `target/stencil-plan-runtime-generated.log`, `target/stencil-plan-node.log`. | General constant propagation, effect-killed value numbering/DCE, strength-reduced recipe selection, hot/cold layout and verified peepholes remain. Alias proofs and reusable multi-block loop planning beyond the declared bounded catalog remain incomplete. |
| Region ABI and exits | `stencil_select.rs::RegionAbi/RegionContract`, `vm_runtime.rs` typed status/fault-PC paths, and `native_region_bridge` provide bounded scalar/word/array contracts. Structured suspension now retains the original lowered phase range plus its exact resume PC, so compact branch targets do not change meaning; installed outer frames remain authoritative across nested re-suspension. | Ten finite await/yield/return/finally controls match local Node, including nested loops and await in init/test/update/do-while. Node host tests pass 14/14. | Full native root maps, helper/reentry/safepoint contracts and all post-effect outer-driver cases remain. |
| Actual native N/G bodies | Emitted ARM64/x86 leaves cover constants, moves, f64 arithmetic, comparisons, bitwise, truthiness/nullish and dense-array paths; `NativeAddChainPlan` composes a small numeric chain using admission-time fixed bindings rather than per-hit shape matching. The ARM numeric-array region executes condition, dense load/add/store, induction, interrupt poll and backedge in one machine-code entry. Distinct `PropertyGuard` and `PropertyWriteGuard` context ABIs consume the canonical `(layout, property, slot, descriptor/deletion state)` proof. Native code rechecks live guards and performs the slot load or non-owning slot commit itself; ownership-sensitive words reject before entry. | Current full-suite evidence is recorded in the admission row. At `b97d6af290`, focused actual-byte and normal-driver property tests cover read/write ABI separation, native commit, stable reuse, eviction/rebuild, stale-layout rejection, and owning source/destination fallback. The exact generated-loop witness remains 1/1 at `48bd26aa2`. | Prototype chain traversal is still guarded in Rust before the typed owner-slot entry. Admission remains a bounded declared loop shape rather than reusable multi-block composition. Property target bodies are verified canonical templates, not yet rustc-extracted generated artifacts. Most bounded optimizer stages remain unimplemented. |
| Relocations/publication | `stencil_arena.rs` validates mappings, W^X publication and cache ownership; `validate_physical_template` checks declaration effects. Rust object extraction uses the `object` parser with symbol-relative bounds and an explicit relocation allowlist. `PhysicalRelocation` now retains offset, kind, target **and addend** through generated selection; the supported Branch26 contract requires zero addends and rejects others before publication. | At `e0cf84111`, unsupported-addend rejection executes 1/1; generated filter executes 24/24. Earlier Branch26 table executes 6/6 and transactional multi-hole patching is covered. | Branch26 fragment composition is supported; ARM page-pair/literal relocation families and full publication-failure cleanup remain safe rejections needing direct coverage before broader generated bodies. |
| Guards/fallback | Admission checks declared ABI/opcode/effects; Unknown/unsupported cases use canonical handlers. Physical, semantic and committed statuses preserve fault PCs. Raw AArch64 streams fail closed on unknown instructions before entry, and all hole-free ARM templates pass the same vocabulary validator. Direct-call detection treats only BL/BLR as helper calls; BR transfers are validated through the declared bridge ABI and root/effect contract. The x86 scanner conservatively recognizes direct E8 and FF /2 indirect CALL while excluding FF /4 JMP; plausible calls reject rather than silently under-declare effects. | Runtime/trace suites pass at the current source; `region_verifier_rejects_physical_call_for_raw_abi` — 1 passed with direct/offset/indirect-call and JMP-negative cases. | Full instruction decoding for x86 prefixes/data boundaries remains; alias/live-out proof, helper throws/reentry, GC roots, interruptible backedges and finally/suspension boundaries need outer-driver tests. |
| B families | Existing VM machinery remains semantic owner; native plans reject unsupported interiors and return to ordinary execution. The cycle collector now models generators as weakly registered graph nodes instead of relying only on the async suspended-root bridge; generator edges derive from the canonical activation fields. | Reachable synchronous generator captures survive collection churn and abandoned generator/function/environment cycles reclaim in focused tests. Default/generated/trace runtime and 14 host tests pass at this revision. | Dedicated native allocating/reentrant/rooting/exception boundary coverage is still incomplete. |
| Diagnostics/budgets | `execution_trace.rs` has bounded stencil identities/rejection/outcome counters; slab/cache paths expose usage. | Trace runtime: 800 passed, 0 failed, 1 ignored (`/tmp/quench-runtime-trace-7841a3f2b.log`). | Native-vs-modeled witnesses, render/lease bytes and global variant/eviction budgets need integrated reporting. |
| Verification | `machine_tests.rs`, `stencil_plan.rs` and `stencil_arena.rs` cover emitted arithmetic, selection bindings, owner eviction/rebuild, ABI-checked dispatch, status transport, property guards, array execution and ordinary-source admission. `CodeArena::freeze` replaces nested same-store strong links with deferred weak resolution while external owners remain explicit. | At `a1a3ada2bd`: default runtime 798, generated runtime 804 and Node host 14; all pass with one intentionally ignored runtime test per runtime configuration. Extractor passes 3 in both configurations; routing passes 1 default and 2 generated. At `6b6dcaf99a`, the nested CodeStore weak-drop regression passes 1/1 (`target/code-store-drop-current.log`). Trace remains 800 + extractor 2 + routing 1 at `7841a3f2b`. | Native hostile exits, root survival, final-owner aggregate accounting, remaining bounded optimizer stages, and complete required N/G body reachability remain before closing gate; trace must be rerun at the final revision. |

Safe exclusions remain explicit: allocating/reentrant helpers, proxies/accessors,
sparse/typed-memory arrays, suspension and exotic coercions stay on complete
ordinary semantics until their root, invalidation and exact-continuation
contracts are implemented. Production ARM admission remains conservative; these
correctness tests make no performance claim.
