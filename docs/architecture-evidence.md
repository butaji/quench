# Task 075 — current stencil infrastructure matrix

Status: **gate CLOSED / NOT COMPLETE**. Source evidence is bound to clean commit
`7841a3f2b`. Micros
and task 073 remain deferred until every required row passes with normal-source
and non-vacuous native evidence.

| Area / required family | Implementation and normal wiring | Executed evidence at this revision | Support and remaining gap |
| --- | --- | --- | --- |
| Canonical declarations | `build.rs::REGION_DECLARATIONS` generates `region_abi_catalog!`; `stencil_select.rs` derives ABI contracts, selection and validation. `build_stencil_artifacts.rs` derives Rust templates, rustc object extraction, fingerprints and immutable artifact tables; `PhysicalStencilView` now couples one canonical record to the exact selected bytes, key, ABI, entry set, target and generated/legacy identity, and `render_selected_*` consumes that view. Each generated byte slice is named once and shared by artifact metadata and its `Stencil` view. The same typed recipe catalog emits scalar leaves, `AddConst`, and the fused `add_chain` artifact. Numeric region-key accessors are derived from ABI/operation/return facts rather than a name allowlist. Helper-boundary classification is derived from declaration ABI. | Enabled generation/selection and actual generated AddConst and AddChain execution with invocation-local generated-identity witnesses — 1 pass each (`/tmp/quench-typed-catalog-generated-20260905.log`); ordinary frontend/lowering to generated AddConst execution — 1 pass in the same run; disabled selection passes 1 (`/tmp/quench-object-section-disabled-20260905.log`); metadata mismatch rejection passes 1. | Rust object extraction checks target architecture **and object format** (Mach-O/ELF/COFF), selects the entry symbol's validated text section (including ELF `.text.<name>`), uses one relocation-validation boundary for all sections, and rejects undeclared relocations. Generated views reject incompatible legacy fallthrough layouts before entry. Exact isolated bounds reject additional global text symbols; effective encoded Rust flags are applied and fingerprinted. Unsupported targets use canonical bytes; relocatable/fallthrough templates remain explicit exclusions. |
| Typed capability publication/lifetime | `stencil_arena.rs` keeps non-owning `EntryToken` private and constructs it only through ABI-specific macro-generated constructors after the typed accessor verifies publication. The token records the generated `RegionAbi` and verified function-pointer address; `with_owned` rechecks owner/generation/ABI/address while holding an active-use lease. `machine.rs::PhysicalState` composes cache disposal with the existing lifecycle for LoadConst, Move, Property, Nullish, Unary, Truthiness, Binary, AddChain, NativeDispatch and NativeRegion plans; installed alternatives remain ABI-specific. Cache-aware eviction and global-cap reclamation now remove all derived rows for retired owners. Committed physical failures call the lifecycle's permanent `retire`, while pre-entry rejection uses rebuildable `reset`; the repeated reset mechanics are generated by one local Rust macro. | Default runtime 785 passed; generated-object runtime 791 passed; execution-trace runtime 800 passed. All had 0 failures and 1 ignored test. | Shared-slab active accounting and stale-address rejection are covered; concurrent owner reclamation and aggregate retired-live budgeting remain. |
| Residual CFG/admission | `BaselinePlan` stores one compact `AdmissionSpan` per PC and sparse typed alternatives in one shared array; the optimizing view reuses it. Canonical `register_flow`, control successors and a bounded backward worklist derive live-outs. `region_admission_matches` validates declared operations, operands, legal entries and successors before publication. | Sparse/disabled-policy/optimizing-storage tests pass. Ordinary-source numeric backedge execution passes. External forward and backward entries into a region interior reject; the new backward-entry regression passes 1/1. | Alias proofs and reusable multi-block loop planning beyond the declared bounded catalog remain incomplete. |
| Region ABI and exits | `stencil_select.rs::RegionAbi/RegionContract`, `vm_runtime.rs` typed status/fault-PC paths, and `native_region_bridge` provide bounded scalar/word/array contracts. Structured suspension now retains the original lowered phase range plus its exact resume PC, so compact branch targets do not change meaning; installed outer frames remain authoritative across nested re-suspension. | Ten finite await/yield/return/finally controls match local Node, including nested loops and await in init/test/update/do-while. Node host tests pass 14/14. | Full native root maps, helper/reentry/safepoint contracts and all post-effect outer-driver cases remain. |
| Actual native N/G bodies | Emitted ARM64/x86 leaves cover constants, moves, f64 arithmetic, comparisons, bitwise, truthiness/nullish and dense-array paths; `NativeAddChainPlan` composes a small numeric chain. | Native suite includes emitted-byte witnesses and ordinary-source loop/backedge tests; modeled callbacks remain labeled as modeled. | Required reusable loop with condition/load/arithmetic/store/induction/backedge and broader property/control families is not complete. |
| Relocations/publication | `stencil_arena.rs` validates mappings, W^X publication and cache ownership; `validate_physical_template` checks declaration effects. Rust object extraction uses the `object` parser with symbol-section-relative bounds, rejects hidden unwind/TLS sections, validates isolated bounds and target alignment, and rejects undefined/undeclared relocations; Rust `global_asm!` verification uses the same rustc parser path and checks labeled entry symbols/ranges. `write_branch26` validates the AArch64 `B` opcode before changing its immediate. | `cargo test -p quench-runtime branch26 -- --nocapture` — 6 passed, covering exact signed limits, just-outside limits, alignment, opcode masks, short buffers and overflow (`/tmp/quench-branch-table-20260905.log`); generated and disabled selection tests each pass 1 (`/tmp/quench-object-section-current-20260905.log`, `/tmp/quench-object-section-disabled-20260905.log`). | Generated leaf relocation-free objects are covered; composable named holes, local relocs, complete pair/range matrix and publication-failure cleanup need direct coverage. |
| Guards/fallback | Admission checks declared ABI/opcode/effects; Unknown/unsupported cases use canonical handlers. Physical, semantic and committed statuses preserve fault PCs. Raw AArch64 streams fail closed on unknown instructions before entry, and all hole-free ARM templates pass the same vocabulary validator. Direct-call detection treats only BL/BLR as helper calls; BR transfers are validated through the declared bridge ABI and root/effect contract. The x86 scanner conservatively recognizes direct E8 and FF /2 indirect CALL while excluding FF /4 JMP; plausible calls reject rather than silently under-declare effects. | Runtime/trace suites pass at the current source; `region_verifier_rejects_physical_call_for_raw_abi` — 1 passed with direct/offset/indirect-call and JMP-negative cases. | Full instruction decoding for x86 prefixes/data boundaries remains; alias/live-out proof, helper throws/reentry, GC roots, interruptible backedges and finally/suspension boundaries need outer-driver tests. |
| B families | Existing VM machinery remains semantic owner; native plans reject unsupported interiors and return to ordinary execution. The cycle collector now models generators as weakly registered graph nodes instead of relying only on the async suspended-root bridge; generator edges derive from the canonical activation fields. | Reachable synchronous generator captures survive collection churn and abandoned generator/function/environment cycles reclaim in focused tests. Default/generated/trace runtime and 14 host tests pass at this revision. | Dedicated native allocating/reentrant/rooting/exception boundary coverage is still incomplete. |
| Diagnostics/budgets | `execution_trace.rs` has bounded stencil identities/rejection/outcome counters; slab/cache paths expose usage. | Trace runtime: 800 passed, 0 failed, 1 ignored (`/tmp/quench-runtime-trace-7841a3f2b.log`). | Native-vs-modeled witnesses, render/lease bytes and global variant/eviction budgets need integrated reporting. |
| Verification | `machine_tests.rs` and `stencil_arena.rs` cover emitted arithmetic, owner eviction/rebuild, ABI-checked dispatch, status transport, array execution and ordinary-source admission. | At `7841a3f2b`: default runtime 785 + extractor 2 + routing 1; generated runtime 791 + extractor 2 + routing 2; trace runtime 800 + extractor 2 + routing 1; Node host 14. All executed counts are nonzero and all failures are zero. | Native hostile exits, root survival, resource-lifetime integration and complete required N/G body reachability remain before closing gate. |

Safe exclusions remain explicit: allocating/reentrant helpers, proxies/accessors,
sparse/typed-memory arrays, suspension and exotic coercions stay on complete
ordinary semantics until their root, invalidation and exact-continuation
contracts are implemented. Production ARM admission remains conservative; these
correctness tests make no performance claim.
