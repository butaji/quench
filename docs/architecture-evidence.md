# Task 075 infrastructure evidence

This is the single current matrix for `docs/stencil-jit-implementation-spec.md`.
The gate is CLOSED / NOT COMPLETE: rows marked partial or missing block
micros/task 073. This matrix reflects the current working revision; the
documentation/task cleanup edits remain user-owned and are not included in
functional commits.

| Layer / family | Code and normal wiring | Executed evidence | Supported / safe boundary | Status and remaining gap |
| --- | --- | --- | --- | --- |
| Canonical declarations | `build.rs::REGION_DECLARATIONS` generates bytes, holes, op sequences, ABI family (including typed `TaggedWord`, `ScalarI32`/`ScalarU32`), keys and accessors; literal constants use generated `Literal64` holes while bridge targets use `Ptr64`; `stencil_select.rs::region_abi_catalog!` derives ABI contracts including hardware clobber masks, live-out masks, root-materialization and interruptibility; `stencil_patch.rs::typed_writer!` derives relocation writers; `NativeRegionContext::new_with_abi` carries the selected declaration into the bridge; `machine::validate_physical_template` is shared by region validation and scalar/tagged plan construction | `generated_region_*` tests plus `physical_templates_match_declared_abi_before_entry` and `physical_helper_call_is_rejected_for_scalar_abi` (1/1 each, `/tmp/quench-abi-template-20260905.log`, `/tmp/quench-abi-call-20260905.log`); ABI clobber/live-out/root assertions pass (`/tmp/quench-abi-clobber-20260905.log`); AddConst emitted-byte and literal-domain tests pass (`/tmp/quench-addconst-literal-20260905.log`, `/tmp/quench-literal-domain-20260905.log`); full runtime at `79952f837` 677/0/1 (`/tmp/quench-runtime-full-20260905-abi-clobber.log`) | Unknown declarations reject before publication; scalar/tagged/raw entries cannot carry bridge pointer relocations; embedded literals are not misclassified as context pointers; physical helper calls and register-transfer opcodes are rejected unless the declared ABI permits them; preserving leaves declare no hardware clobbers and helper bridges require root materialization | Partial: operand/liveness/effect metadata is still derived partly in runtime validators; hardware masks are declaration contracts but per-instruction clobber extraction and live-root maps remain incomplete; source files exceed the requested size limits |
| Residual control flow/admission | `ir::Opcode::{control_operands,operands_are_canonical}`, `machine::{region_admission_matches,validate_region_window}`, baseline plan construction; ordinary `reduce_source` reaches nested loop bodies | At `3348c65a2` (clean source commit; user cleanup files remain dirty), `region_admission_rejects_noncanonical_operands_before_publication` **1 passed** (`/tmp/quench-region-static-contract-20260904.log`); `ordinary_source_lowering_emits_numeric_backedge_and_executes` remains the normal lowering/backedge check | Generated ABI/entry/operand/control contracts reject malformed windows before rendering; structured/unsupported loops remain ordinary residual execution | Partial: general multi-block region builder/liveness admission is not complete; current native loop shape is bounded and declared |
| Region ABI/state/roots | `stencil_select::AbiContract` now carries declaration-derived context arguments, hardware clobber mask, live-out mask, root-materialization requirement and interruptibility; declaration-carried `NativeRegionContext::abi`, distinct `TaggedWord`/scalar/raw-array/bridge entry families, `NativeArray{Kernel,Loop}Context (repr(C))`, `RegisterFile::visit_values`, `VmContext::interrupt_flag` | raw context layout tests; `native_bridge_preserves_fault_pc_after_prior_effect` (1 pass); `rendered_numeric_array_loop_poll_exits_after_committed_iteration`; `interrupt_request_is_stable_across_context_clones`; ABI clobber/live-out/root contract assertions (`/tmp/quench-abi-clobber-20260905.log`); tagged-word property/move native tests (1/1 each) | Scalar, tagged-word, raw-array and bridge ABIs are distinct at construction and dispatch; preserving leaves declare no hardware clobbers; helper bridges require roots; raw numeric contexts exclude helper/reentrant operations | Partial: declaration masks are now enforced before entry, but per-instruction hardware clobber extraction, live-root maps and complete helper/GC safepoint contracts are not complete for every ABI |
| Native numeric bodies | `NativeBinaryPlan` f64 arithmetic/comparison and typed `ScalarI32`/`ScalarU32` bitwise/shift bodies; canonical `Opcode::Unary` + `NativeUnaryPlan` for `BitwiseNot`; `NativeAddChainPlan`; ARM64 `AARCH64_ARRAY_KERNEL_BYTES` and `AARCH64_ARRAY_LOOP_BYTES` | At current source `cde700d73`, direct `native_bitwise_not_i32_entry_preserves_to_int32_rules` **1 passed** (`/tmp/quench-bitwise-not-direct.log`) and ordinary-source `ordinary_source_lowering_executes_bitwise_not_and_falls_back_for_string` **1 passed** (`/tmp/quench-bitwise-not-normal-20260904.log`); comparison normal-driver **1 passed** (`/tmp/quench-comparison-normal-driver-20260904.log`) with invocation-local witnesses; emitted-byte comparison/bitwise/shift and rendered loop/kernel tests remain passing | f64 arithmetic, ordered comparisons with NaN exclusion, exact JS Number-to-Int32 conversion (including truncation, NaN/infinity and modulo-2^32) feeding native `&/|/^`, masked `<< / >> / >>>`, and unary `~`; guarded dense numeric array loop; unsupported targets reject typed unary entry before execution | Partial: broader comparison/control composition remains ordinary fallback |
| ARM composition/control | `AARCH64_ARRAY_LOOP_BYTES` has condition, indexed load/add/store, induction update, interrupt poll and native backedge; `NativeRegionPlan::execute` is reached from baseline/optimizing plans; `validate_region_window` and `NativeRegionPlan::new_inner` independently verify physical templates before entry | At current source `b38c6a72a`, `ordinary_source_lowering_emits_numeric_backedge_and_executes` **1 passed** (`/tmp/quench-arm-loop-20260905-validation.log`) plus rendered loop tests covering zero/one/many/nonzero result and committed interrupt exit; checkpoint verifier 1/1 at `381bbd026` (`/tmp/quench-interrupt-checkpoint-contract.log`) | ARM production policy remains conservative; unsupported ABI/target rejects before entry | Partial: the bounded loop has a physical checkpoint and exact committed handoff, but broader branch/live-out composition and runtime interrupt-request plumbing remain incomplete |
| Relocations/publication | `stencil_patch::apply_holes` derives writers for `Literal64`, `Ptr64`, immediates and branches; `Stencil::validate`, `StencilArena::make_executable` (W^X + I-cache flush), declaration build checks; `build.rs::aarch64_b_imm26/aarch64_b_cond` derive branch words | relocation range/alignment tests; generated catalog tests; AddConst literal patch test (`/tmp/quench-addconst-literal-20260905.log`); ARM byte execution; optional assembler verification passed with `QUENCH_VERIFY_STENCIL_ENCODINGS=1` (`/tmp/quench-stencil-assembler-branch-helpers.log`) | Out-of-range, overlap, alignment, pointer-vs-literal mismatches and unsupported target fall back | Partial: branch displacements now derive from named encoders and assembler labels verify the loop layout, but compiler/assembler-generated relocation sources do not yet replace all handwritten constants |
| Guards/exits/no replay | `execute_region_fallback` and `native_region_bridge` share `validate_residual_window` two-pass validation of opcode, canonical operands and legal control successors; `NativeDispatchError::{Physical,Committed,SemanticAt}`; `NativeRegionContext::finish`; array alias/bounds/representation guards; `stencil_contains_call` physical-template verifier | committed-status/no-retry and post-entry fallback tests; legal effect-then-final-return no-replay test (`/tmp/quench-no-replay-test-20260905.log`); `fallback_reports_exact_fault_pc_after_prior_effect`; `native_bridge_preserves_fault_pc_after_prior_effect`; residual operand/control validation exercised by ARM loop and fallback tests (`/tmp/quench-region-validation-20260905.log`, `/tmp/quench-arm-loop-validation-20260905.log`); `region_verifier_rejects_physical_call_for_raw_abi`; hostile-region tests in full suite | Effectful/allocating/reentrant interiors exit to canonical machinery; changed operands/edges and early returns reject before any handler; raw ABIs reject physical call encodings and pointer relocations | Partial: helper/reentry fault matrices and exact finally/GC cases need broader direct coverage |
| Executable ownership/lifetime | shared `SharedStencilSlab` for composed regions, baseline numeric scalar plans, `NativeAddChainPlan`, `NativeMovePlan`, `NativePropertyPlan` and `NativeDispatchPlan`; typed entry validation (`f64_entry`/`bool_entry`/`i32_entry`/`u32_entry`/`f64x3_entry`/tagged-word); owner IDs in caches; bounded slab capacities and active-dispatch accounting; `SharedStencilSlab::with_active` now guards typed calls from lookup through return with RAII release | At `3912172df` (source; docs commit `d3d0ee6e2`), native/shared regressions **23 passed** and slab tests **6 passed** (`/tmp/quench-native-regressions-20260904-active-guard.log`, `/tmp/quench-shared-raii-20260904.log`); `shared_slab_typed_entry_guard_blocks_eviction_during_call` proves active count remains held across invocation and unwinds (`/tmp/quench-shared-unwind-20260904.log`); full runtime **672 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260904-raii.log`); production check passed (`/tmp/quench-production-check-20260904-raii.log`) | Shared plans resolve typed pointers while borrowing their owner and increment active-use accounting through the call, so idle eviction cannot reclaim the allocation between lookup and invocation; RAII releases accounting on unwind; allocation pressure remains globally bounded; failed publication clears per-plan cache | Partial: owner retirement is synchronous and not yet coordinated with helper/reentry lifetimes; those contracts remain incomplete |
| Diagnostics | `execution_trace::stencils`, `stencil_iterations`, `stencil_storage`, `NativeRegionPlan::last_native_execution`; test-only invocation-local `NativeBinaryPlan::native_entry_count` witness | ordinary-source bitwise admission/driver test now asserts a native invocation (`/tmp/quench-bitwise-native-witness.log`); trace-enabled runtime tests/builds | Modeled callbacks are not counted as native witnesses; shared leaf pointers are not retained across evictable owners | Partial: rejection/exit reason and per-region resource facts need broader end-to-end emission |
| Values/locals/frames | `FunctionCode::required_register_count`, frame construction and reusable register windows; native Move/scalar leaves | frame/register tests in full suite | TDZ/unknown locals use canonical handlers; B-class calls retain complete ordinary boundaries | Partial: canonical capture/frame root layout remains incomplete; direct call-entry templates are optional and not claimed |
| Properties/prototypes/arrays | property leaf and array guards in `vm_runtime`; `NativePropertyPlan` executes the declaration's tagged-word stencil after the shape/descriptor guard; array facts in `value_array_data` | `ordinary_residual_named_get_executes_guarded_property_stencil` and `native_property_uses_rendered_address_without_remapping` (2 focused passes, source patch committed as `12e9d02c7`; mutation-hostile extension committed as `a1c98a5f8`, `/tmp/quench-property-stencil-tests.log`, `/tmp/quench-property-normal-driver-witness-summary.log`, `/tmp/quench-property-normal-driver-hostile.log`); property/array hostile tests in full suite | guarded plain own-data reads execute native bytes through the normal baseline driver; getters/proxies/holes/mutations fall back; chain guards provide the required complete boundary | Partial: native set bundles, optional mutation dependency tokens and incremental widening are not implemented |
| Calls/construct/objects/async/iteration/strings/BigInt/RegExp | Existing canonical VM handlers and bridge plans | full runtime/host tests | Complete ordinary semantics remain authoritative; no unsupported native interior is admitted | Boundary integration only; no claim of native bodies for these families |
| Verification | Runtime crate full lib suite, emitted-byte tests, ordinary residual admission and normal-driver comparison/bitwise/unary/property execution tests; host tests | Full runtime at source `cda071d37` **677 passed, 0 failed, 1 ignored** (`/tmp/quench-runtime-full-20260905-residual-validation-fixed.log`); residual fault/operand/no-replay tests **1 passed each** (`/tmp/quench-region-validation-20260905.log`, `/tmp/quench-arm-loop-validation-20260905.log`, `/tmp/quench-no-replay-test-20260905.log`); ABI/template/transfer tests **1 passed each** (`/tmp/quench-abi-template-20260905.log`, `/tmp/quench-abi-call-20260905.log`, `/tmp/quench-abi-transfer-20260905.log`); AddConst literal test **1 passed** (`/tmp/quench-addconst-literal-20260905.log`); current normal-source bitwise test **1 passed** (`/tmp/quench-bitwise-normal-20260905.log`); host boundary at `a4ff4c31a` **4 passed, 0 failed** (`/tmp/quench-node-host-20260905-abi-clobber.log`); cross-target x86 check remains unavailable because target toolchain is not installed (`/tmp/quench-cross-x86-unary-20260904.log`) | No fixture/source recognition; no micros run | Gate remains **NOT COMPLETE** until partial rows have concrete implementation/tests |

No micros command has been run for this matrix. Task 073 remains pending until
all mandatory infrastructure rows and cross-layer contracts are complete.
