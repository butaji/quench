# Task 075 — current stencil infrastructure matrix

Status: **gate CLOSED / NOT COMPLETE**. Current default and generated-object
runtime evidence is bound to implementation commit `de39b57e72`; current
execution-trace and host evidence remains bound to `798ac3f0ff`. Micros and task
073 remain deferred until every required row passes with normal-source and
non-vacuous native evidence.

| Area / required family | Implementation and normal wiring | Executed evidence at this revision | Support and remaining gap |
| --- | --- | --- | --- |
| Canonical declarations | `build_stencil_contract.rs` is the shared build-time representation of `RegionDeclaration`, `DeclAbi` and macro-derived Rust leaf/assembly recipes; both `build.rs` and the executable extractor test consume it. Every generated recipe declares its exact region name, ABI, residual shape and ARM hole layout; leaf recipes also own signature/result/body. Selection rejects any mismatch instead of inferring compatibility from names or opcode strings. `PhysicalStencilView` couples one canonical record to exact selected bytes, ABI, entries, target and identity. Region IDs are stable name-derived declaration facts with build-time collision rejection. Build provenance hashes every declaration and exact rendered Rust source; artifact identity additionally hashes extracted bytes, verified holes, fallthrough and typed relocations. The 25-line build facade composes cohesive Rust-only encoder, declaration, verifier and catalog-render units; every handwritten build unit is under 500 lines. Three declaration slices contain every region fact once, and one flattened build-time view feeds artifact extraction, ABI generation, keys, rows, accessors and validation in original order. Cargo watches every included source explicitly. | At `4a610b1a4c`, default runtime 826/826 + extractor 6/6 + routing 1/1 + docs 2/2; generated runtime 832/832 + extractor 6/6 + routing 2/2 + docs 2/2; Node host 14/14. Logs: `/tmp/quench-runtime-build-catalog-default.log`, `/tmp/quench-runtime-build-catalog-generated.log`, `/tmp/quench-node-build-catalog.log`. | Generated views reject incompatible layouts before entry. Several legacy encoder/verifier functions remain above the 40-line function cap and need cohesive decomposition; this row is not complete from file splitting alone. |
| Typed capability publication/lifetime | `stencil_arena.rs` keeps non-owning `EntryToken` private and constructs it only through ABI-specific macro-generated constructors after the typed accessor verifies publication. A retaining `AllocationLease` rechecks owner/generation/ABI/address and records the exact owning slab. Reentrant invocation releases pool borrows before machine entry; per-owner lease counts prevent reclamation of the executing allocation without pinning unrelated idle slabs. `LeaseState` is the single retirement fact for a published allocation generation: it rejects new tokens/leases, excludes retired slabs from rendering, leaves an active lease callable and charged, permits fresh publication elsewhere, and reclaims the retired mapping on final release. `machine.rs::PhysicalState::apply_dispatch_outcome` wires committed Dispatch/Region outcomes to physical retirement before clearing logical state. | At `e66d55fdd2`, `retired_generation_stays_charged_until_final_lease_release` passes 1/1 with real rendered Add bytes, stale-token rejection, 4 KiB retired-live charging, an independent 4 KiB replacement, execution through the old active lease, and exact per-pool reclamation. All arena tests pass 52/52; default/generated runtime results are in Verification. At `798ac3f0ff`, normal-driver Bridge success/throw/reuse passes 2/2 with nested native execution and GC root survival. | Active allocation isolation, stale-address rejection, independent idle reclamation, semantic reuse, committed retirement, final-owner release and per-pool/global code caps are covered. `stencil_cache.rs` owns process-wide cache metadata charging. A compiled-owner aggregate for all admission metadata and cohesive extraction from the oversized arena module remain. |
| Residual CFG/admission | `BaselinePlan` stores one compact `AdmissionSpan` per PC and sparse typed alternatives in one shared array; the optimizing view reuses it. `stencil_cache.rs` is the single render-memo representation: cold caches reserve zero bytes, a warm cache is bounded to 16 rows, all caches share a 4 MiB process ceiling, and clear/final-owner removal/drop release their reservation. The shared executable pool owns the canonical render memo; per-plan caches are derived views, so equivalent physical keys/signatures reuse one allocation across sites while distinct patch signatures remain separate. Canonical `register_flow`, out-of-line call operand windows, control successors and a bounded backward worklist derive frame widths and live-outs. Local-slot identifiers for `LoadLocal`, `LoadLocalChecked`, and flagged local `Move` are excluded from VM-register uses; nonconsecutive call arguments remain live and size the frozen frame even when their IDs exceed the compact destination. `region_admission_matches` validates declared operations, operands, legal entries and successors before publication. `stencil_plan::BlockValueGraph` is a disposable fixed-capacity view of at most eight canonical pure definitions; it derives `LoadLocal`/numeric `LoadConst`/unflagged `Move` producers from opcode effect and register-flow facts, propagates constants and aliases, reuses repeated local values, rejects duplicate definitions, and removes producer materialization only when liveness proves every skipped definition dead. It selects existing typed numeric or guarded property specializations rather than owning semantics or machine recipes. Constant-only numeric windows commit folded bits without rendering, while local/move property windows reuse the existing IC and descriptor/prototype guards. | At `de39b57e72`, planner, property and ordinary-source focused filters pass, including warm fused GetN, hostile non-object fallback and deleted captured-binding rejection before native entry. Full default runtime passes 843/843 and generated-object runtime passes 849/849, each with one intentional ignore. No new benchmark-identity source matches were introduced. | The bounded graph stops at effectful/control operations. Effect-scoped guard reuse across multiple property operations, additional strength-reduced recipes, hot/cold layout, verified peepholes and reusable multi-block planning remain. |
| Region ABI and exits | `stencil_select.rs::RegionAbi/RegionContract`, `vm_runtime.rs` typed status/fault-PC paths, and `native_region_bridge` provide bounded scalar/word/array contracts. Helper-capable Bridge regions execute only the canonical operation and return a `CallContinuation`; the retaining native lease is gone before the outer driver performs host/JS reentry. Structured suspension retains the original lowered phase range plus its exact resume PC, so compact branch targets do not change meaning; installed outer frames remain authoritative across nested re-suspension. The VM result boundary maps only canonical `Completion::Normal` to `undefined`; Node no longer normalizes the internal `MissingReturn` fault into exit-zero success. | At `798ac3f0ff`, the two normal-driver Bridge tests prove ABI selection, actual physical entry, borrow-free reentry, exact Return and semantic-error propagation, caller-root survival, exactly-once effects, and reuse after throw. The retained 11-source continuation matrix and Node host suite pass 14/14. | Internal failure sites still need richer fixed reason/code/PC data. Additional finally/exception and allocating-helper boundaries, native backedge root maps, and all post-effect outer-driver cases remain. |
| Actual native N/G bodies | Rustc-generated whole leaves cover f64 arithmetic, ordered/equality comparisons, tagged identity, exact i32 bitwise/shift operations, negate, increment and numeric truthiness. Typed Rust `global_asm!` recipes cover ARM move/load-local/store-local, patched primitive constants, tagged nullish/truthiness, pointer truthiness, own/prototype property guards, dense numeric get/set/get+increment/update kernels and the numeric loop. Labeled literal holes are verified against section-relative object symbols before publication. `NativeAddChainPlan` composes a small numeric chain using admission-time fixed bindings. The ARM numeric-array region executes condition, dense load/add/store, induction, interrupt poll and backedge in one machine-code entry. `NativeLocalPropertyPlan` now fuses bounded pure local/move producers into the existing guarded property body, retaining the receiver value and committing live-out state only after a successful native read. | At `de39b57e72`, ordinary source lowering reaches the fused local-property admission. Its cold execution installs the IC; the warm execution invokes one guarded native property entry without invoking the scalar LoadLocal leaf. String receivers and deleted captured bindings reject before entry and take exact ordinary semantics; the deleted-binding case throws and does not increment the native witness. Prototype-chain mutation, shadow and accessor controls remain green. Full default runtime passes 843/843 and generated-object runtime passes 849/849, each with one intentional ignore. | x86 retains its existing verified native bodies and safely rejects ARM-only bodies. Multi-property guard reuse and general multi-block composition remain unsupported. |
| Relocations/publication | `stencil_arena.rs` validates mappings, W^X publication and cache ownership; `validate_physical_template` checks declaration effects. Rust object extraction uses the `object` parser with symbol-relative bounds and an explicit relocation allowlist. One generic relocation record carries section, offset, width, kind, target and addend through declared, observed and verified views. Format adapters produce observations; one pure transactional matcher uniquely consumes declarations independent of object-record order and rejects unknown, duplicate, missing, width/addend drift, range overflow and overlap before returning anything publishable. The Mach-O adapter uses the normalized object relocation stream so paired `ARM64_RELOC_ADDEND` records are not mistaken for independent symbols, while exact Branch26 flags and zero encoded immediates remain checked. The opt-in encoder verifier writes one immutable Rust `global_asm!` source, compiles it with configured rustc, and validates exact object words/symbols plus loop entry/backedge words. | At `586b32c160`, default runtime passes 826/826 plus extractor 10/10, routing 1/1 and docs 2/2 (`/tmp/quench-runtime-macho-fixture-default.log`); generated runtime passes 832/832 plus extractor 10/10, routing 2/2 and docs 2/2 (`/tmp/quench-runtime-macho-fixture-generated.log`). The extractor target uses a Rust-created Mach-O object to prove a paired nonzero ARM64 addend is preserved, plus reordered two-hole success and exact typed failures for missing, duplicate, unknown, width, addend and overlap. At `506dd973b6`, `QUENCH_VERIFY_STENCIL_ENCODINGS=1 cargo check -p quench-runtime` passes on `aarch64-apple-darwin`; the runtime Branch26 boundary/transaction filter passes 6/6. | Branch26 fragment composition is supported. ARM page-pair/literal relocation families and full publication-failure cleanup remain safe rejections needing direct coverage before broader generated bodies. |
| Guards/fallback | Admission checks declared ABI/opcode/effects; Unknown/unsupported cases use canonical handlers. Physical, semantic and committed statuses preserve fault PCs. Raw AArch64 streams fail closed on unknown instructions before entry, and all hole-free ARM templates pass the same vocabulary validator. Direct-call detection treats only BL/BLR as helper calls; BR transfers are validated through the declared bridge ABI and root/effect contract. The x86 scanner conservatively recognizes direct E8 and FF /2 indirect CALL while excluding FF /4 JMP; plausible calls reject rather than silently under-declare effects. | Runtime/trace suites pass at the current source; `region_verifier_rejects_physical_call_for_raw_abi` — 1 passed with direct/offset/indirect-call and JMP-negative cases. | Full instruction decoding for x86 prefixes/data boundaries remains; alias/live-out proof, helper throws/reentry, GC roots, interruptible backedges and finally/suspension boundaries need outer-driver tests. |
| B families | Existing VM machinery remains semantic owner; native plans reject unsupported interiors and return to ordinary execution. A supported Bridge call returns its canonical continuation before host/JS execution, keeping generated frames and pool borrows out of reentrant semantics. The cycle collector models generators as weakly registered graph nodes instead of relying only on the async suspended-root bridge; generator edges derive from canonical activation fields. Array slice executes observable Has/Get/CreateDataProperty steps in index order and performs the final throwing length Set. | At `798ac3f0ff`, normal-driver Bridge success/throw/reuse passes 2/2 with nested native admission, GC root survival and exact effects. CLI/Node differential controls pass 11/11 for structured suspension/rooting and 3/3 for slice ordering and throws; Node host passes 14/14. | Additional allocating helpers, finally/rejected-call paths and unsupported boundary families still need explicit direct coverage. Additional slice holes/inherited-getter/species-mutation tests remain required before its boundary row is exhaustive. |
| Diagnostics/budgets | `execution_trace.rs` has bounded stencil identities/rejection/outcome counters; slab/cache paths expose usage. Executable slabs have per-pool/process code bounds; `stencil_cache.rs` separately accounts bounded cache metadata process-wide. Invocation-local test witnesses distinguish physical Bridge entry from modeled callbacks. | At `798ac3f0ff`, execution-trace runtime passes 846/846 with one intentional ignore. Current untraced resource tests are recorded in the admission and lifetime rows. | Render/lease byte attribution and aggregate variant/eviction reporting need integration; final-gate trace evidence must be rerun after later changes. |
| Verification | `machine_tests.rs`, `stencil_bridge_tests.rs`, `stencil_plan_tests.rs`, `stencil_fusion_tests.rs`, `stencil_property_integration_tests.rs`, `stencil_cache.rs` and `stencil_arena.rs` cover emitted arithmetic, selection bindings, owner eviction/rebuild, ABI-checked physical entry, reentry/root boundaries, status transport, property guards, array execution and ordinary-source admission/execution. `CodeArena::freeze` replaces nested same-store strong links with deferred weak resolution while external owners remain explicit. | At `de39b57e72`, default runtime passes 843 and generated-object runtime 849; each has one intentional ignore. Focused fused-property deletion passes 1/1 in addition to the prior ordinary-source filters. At `798ac3f0ff`, execution-trace runtime passes 846 with one intentional ignore and Node host passes 14/14. | Native hostile exits beyond the tested Bridge/property boundaries, compiled-owner admission-metadata budgeting, remaining bounded optimizer stages, reusable CFG composition, and final current-revision host/trace/extractor/routing runs remain before closing the gate. |

Safe exclusions remain explicit: allocating/reentrant helpers, proxies/accessors,
sparse/typed-memory arrays, suspension and exotic coercions stay on complete
ordinary semantics until their root, invalidation and exact-continuation
contracts are implemented. Production ARM admission remains conservative; these
correctness tests make no performance claim.
