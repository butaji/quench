[build]
# Enforce clippy rules from .clippy.toml — never allow warnings.
rustflags = ["-D", "warnings"]

[profile.dev]
opt-level = 0

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"